/**
 * `src/lib/mock/fixtures/` 배럴 — **17일차(2026-08-12), Task 007 계속분**
 *
 * 소비 계층(18일차 `MockDataSource`, 4팀 `/sample` 쇼케이스 등)은 이 배럴 하나만
 * import하면 된다. 개별 파일 헤더에 각자의 스코프(전량/부분/미커버 목록)가 있으니
 * 실제 사용 전에 `./screens.ts` 헤더의 "스코프" 절을 먼저 읽을 것.
 */

export * from './schedule';
export * from './states';
export * from './screens';
