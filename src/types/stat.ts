/**
 * 통계 도메인 타입 — **골격만** (3일차 2026-07-23)
 *
 * 채울 범위: **E-19 PlayerMatchStat / E-20 PlayerSeasonStat / E-21 PlayerCareerStat /
 * E-22 TeamSeasonStat / E-23 Standing** — **5일차(07-27)**
 *
 * 착수 전 확인할 원칙 (`docs/devStep/02.타입스키마설계원칙.md`):
 * - **T16 (D-19)**: `goals`(정규+연장)와 **승부차기 기록을 별도 필드/별도 타입으로 분리**한다.
 * - **T17 (D-19)**: 득점왕·시즌/통산 득점 집계 타입은 승부차기 값을 **입력으로 받을 수 없게**
 *   설계한다(브랜드 타입 또는 별도 집계 타입으로 오용 차단).
 * - **C-23 ④**: 파생 지표(선수 평점·xG·점유율)는 **노출된 이벤트에서 재계산한 값**만 담는다.
 * - **미결 I-19**: 승부차기 브랜드 생성 책임 확정 — **5일차 판단 지점**.
 */

export {};
