/**
 * 경기 도메인 타입 — **골격만** (3일차 2026-07-23)
 *
 * 채울 범위: **E-15 Fixture / E-16 MatchEvent / E-17 MatchLineup / E-18 Weather** — **5일차(07-27)**
 *
 * 착수 전 확인할 원칙 (`docs/devStep/02.타입스키마설계원칙.md`):
 * - **T18 (D-19)**: 경기 결과 타입은 승패 판정용 **승부차기 스코어를 별도 필드**로 보유한다.
 *   정규+연장 득점과 나란히 두어 합산 가능한 형태로 만들지 않는다.
 * - **T13 (D-18)**: 표시 서식이 아닌 원시값(타임스탬프·숫자)만 담는다.
 * - **C-23 (NFR-SEC-004)**: 종료 전 결과 필드는 **null 허용**이어야 서버가 미래 정보를
 *   비노출로 응답할 수 있다. 타입이 non-null을 강제하면 컷오프 구현이 불가능해진다.
 * - **미결 I-19**: 승부차기 브랜드 생성 책임 확정 — **5일차 판단**(`stat.ts`와 함께).
 */

export {};
