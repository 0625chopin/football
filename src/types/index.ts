/**
 * 도메인 타입 공개 진입점 — 1팀 코어·품질팀 소유 (`src/types/**`, 타 팀 읽기 전용)
 *
 * 전 팀은 도메인 타입을 **`@/types`에서만** import 한다. 각 팀 코드에서 로컬 재정의를 두지
 * 않는다(코드 리뷰 체크리스트 C-5·C-6).
 *
 * 진행 상태 (Task 002):
 * | 일차 | 범위 | 상태 |
 * |---|---|---|
 * | 3일차 07-23 | 파일 분할 + **E-01~E-08** (`world.ts`, `person.ts` 초안) | ✅ |
 * | 4일차 07-24 | E-09~E-20 + 34속성 → `person.ts` 완성, `match.ts`·`stat.ts` 완성(E-15~20), `economy.ts`에 E-12~14 추가 | ✅ |
 * | 5일차 07-27 | E-21~E-32 + E-33~E-40 선정의 (`stat`/`ops`/`economy`/`betting`), I-19·I-31·I-32 반영 | ✅ |
 * | 6일차 07-28 | enum성 값 단일 선언 (`enums.ts` 전량) + I-33(`SeasonPhase.TIEBREAK`)·I-37(`MatchEvent.relatedEventSequence`) 반영 | ✅ |
 * | 7일차 07-29 | 브랜드 타입·포인트 정수·시드 계층·상수 스냅샷 (`brand.ts`, `config.ts`) + E-41~43 공통코드 타입·T12 규약 | ✅ |
 * | 8일차 07-30 | E-45~47(`ops.ts`, 1차 범위 잔여분) 보완 + `Manager.isActing`(D-23, I-49) + 이벤트 생성 규칙 3건(I-53~55) + 타입↔엔티티 매핑표(`README.md`) + 타입 레벨 테스트(`*.type-test.ts`) + **SP-1 전 6팀 리뷰 반영** → **H-01 동결** | ✅ |
 *
 * ## ⚑ H-01 — 8일차(2026-07-30) 도메인 타입 동결 선언
 *
 * `src/types/**`이 E-01~E-47 전 엔티티(2차 릴리스 선정의 E-33~E-40 포함)를 담아 **동결**됐다.
 *
 * **SP-1 동결 리뷰 (전 6팀 참여) 결과**: 2·3·4·5·6팀이 각자 담당 Task를 역산해 타입 갭을
 * 검토한 결과 **"동결 전 반영 필요" 5건**(1팀 자체 발견 E-45~47 포함 1건 + 3팀 제기 1건 +
 * 팀장 확정 이벤트 규칙 3건, `docs/ISSUES.md` I-45·I-49·I-53~55) 외에는 **전원 0건**이었다.
 * 나머지 지적(브래킷 슬롯·H-15 반환구조·DC-08 퍼센트 범주·stale 주석 등)은 파생 가능/타입
 * 대상 아님/비차단 후속 반영으로 판정해 `docs/ISSUES.md` I-46·I-48·I-50~52에 근거와 함께
 * 기록했다. **T5(능력치 브랜드화, I-47)는 미도입으로 최종 결정.**
 *
 * **검증**: `npx tsc --noEmit` 오류 0 / `npm run lint` 오류 0 / `npm test` 6 files·120 tests
 * 통과 / 중복 enum 선언 0건(C-6) / 서브경로 import(`@/types/xxx`) 위반 0건(C-5) / 브랜드 ID
 * 32종 전량 상호 배타 확인.
 *
 * **동결 후 2차 검증(같은 날)**: 4팀이 `CronRun.errorCode`/`errorMessage`에 번역 대상 여부
 * 주석이 없다고 지적(I-56) — 구조 변경이 아닌 주석 보강이라 아래 절차 2번 규칙에 따라
 * 배치를 기다리지 않고 당일 반영했다. 이 규칙이 실제로 처음 쓰인 사례다.
 *
 * **이후 절차 (C-7, 8일차 2차 검증에서 구체화)**:
 *
 * 1. **동결 대상 = 타입 구조만.** 필드 추가/삭제/타입 변경, enum 멤버 추가/삭제, 시그니처
 *    변경은 전부 아래 배치 절차를 거친다.
 * 2. **동결 대상 아님 = 주석/문서.** 오타·stale 설명·번역 대상 여부 명시 같은 **주석 정정은
 *    구조 변경이 아니므로 배치를 기다리지 않고 발견 즉시 처리한다.** 근거: 8일차 하루에만
 *    stale 주석이 3건 나왔고(7일차 이월분 2건 + `PointTransactionReasonCode` 1건), 그중
 *    하나는 방치됐다면 32일차 DB `CHECK` 제약 위반으로 실제 장애가 났을 사안이었다
 *    (`docs/ISSUES.md` I-48). 주석 정정에도 배치를 강제하면 이런 사고가 다음 배치까지
 *    방치된다 — 구조 변경과 주석 정정은 리스크 성격이 달라 같은 절차를 적용하지 않는다.
 * 3. **구조 변경 요청 절차**: 발견한 팀이 **직접 수정하지 않고** `docs/ISSUES.md`에 **OPEN
 *    상태로** 이슈 등록한다(근거 FR/E/D ID 명시, 값을 지어내지 않는다 — I-41 원칙). 등록되지
 *    않은 구두 요청은 접수하지 않는다. 1팀이 **11일차부터 5영업일 주기(11·16·21·26·31·…
 *    일차)에 그날 마감 시각까지 등록된 OPEN 건을 일괄 검토**해 반영 또는 기각한다 —
 *    **기각 시 사유를 이슈 본문에 기록**해 같은 요청이 다음 창구에 재등장하지 않게 한다.
 *    마감 후 등록분은 다음 창구로 자동 이월된다.
 *
 *    **⚠️ 창구 일정 변경 이력(근거 명시 — I-16류 표기 사고 재발 방지)**: 1팀 8일차 초안은
 *    11일차 기점을 제안했다. **팀장이 1차로 "4·9·14·19·24·29일차(9일차 첫 창구)"로 승인**
 *    했으나, **팀장이 재검토 후 최종적으로 11일차 기점을 확정**했다 — 사유 ① 동결
 *    직후(9일차)에 창구가 열리면 "동결이 사실상의 마지막 반영 기회"라는 오늘 SP-1의
 *    메시지가 하루 만에 희석된다(최소 간격 확보 필요) ② 9일차는 Task 004(1팀)·003(3팀)·
 *    005(4팀)·009(6팀)·023(2팀 계속)이 동시 착수되는 날이라 그날 1팀에 배치 심사까지
 *    얹는 것은 부하 설계상 부적절하다. **최종: 11일차부터 5영업일 주기**(9일차 첫 창구
 *    안은 폐기).
 * 4. **긴급 예외**: "있으면 좋겠다"가 아니라 **실제로 담당 Task 구현이 막힌 경우에만**,
 *    담당 팀이 팀장에게 직접 알려 팀장 승인으로 1팀이 다음 창구를 기다리지 않고 즉시
 *    판정한다(오늘 3팀의 `Manager.isActing` 요청이 이 기준으로 동결 전 반영까지 격상된
 *    선례, I-49 참조). "급하다"의 판정 기준은 오늘 하루 종일 쓴 것과 동일 — **파생 계산으로
 *    대체 불가 + 요구사항 근거 확정** 두 가지가 겹쳐야 한다. "있으면 좋겠다"는 예외가 아니다.
 *
 * **11일차 첫 창구 사전 점검(8일차 시점 판정)**: 현재 OPEN인 이슈(I-29·I-30·I-34·I-36·
 * I-38·I-40·I-41·I-46·I-51) 중 **`src/types/**` 구조 변경이 필요한 것은 0건**이다 — 전부
 * ① 물리 스키마/툴체인/ESLint 등 이 파일들 밖의 작업(I-29·I-30·I-36) ② 값 taxonomy 자체가
 * 아직 없어 판정 불가(I-41, 2팀 Task 023/024 착수 후에야 값이 나옴) ③ 다른 팀 소유
 * 문서화 책임(I-51, 2팀 026 계약 문서) ④ Task 004(9일차, `src/lib/data/**`) 설계 산출물로
 * 직접 나오는 판정이라 C-7 배치 큐를 거치지 않고 9일차 Task 004 작업 중 자연히 결정됨
 * (I-34·I-38) 중 하나다. 9~10일차 사이 새로 등록되는 이슈가 있으면 11일차 창구 전 재확인한다.
 *
 * 동결 전 SP-1 리뷰에서 나온 지적사항도 전부 이 경로(이슈 등록 → 판정 → 반영/유예)로
 * 처리됐다(`docs/ISSUES.md` I-45~I-56).
 *
 * 타입↔엔티티 상세 추적표는 `src/types/README.md`.
 */

export * from './brand';
export * from './enums';
export * from './world';
export * from './person';
export * from './match';
export * from './stat';
export * from './economy';
export * from './betting';
export * from './config';
export * from './ops';
