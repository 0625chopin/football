/**
 * 배팅 / 사용자 도메인 타입 — **골격만** (3일차 2026-07-23)
 *
 * 채울 범위: **E-33~E-40 선정의**(BetMarket / BetSelection / Odds / Bet / BetLeg /
 * User / Wallet / WalletTransaction) — **5일차(07-27)**
 *
 * 주의: 배팅(5종) + 사용자(3종)는 **2차 릴리스** 범위다. 1차에서는 타입만 선정의하고
 * 화면·엔진이 소비하지 않는다. WalletTransaction 충전 관련은 3차.
 *
 * 착수 전 확인할 원칙:
 * - **T13**: 배당은 숫자, 포인트는 정수(`Points`)로 두고 서식은 UI 책임.
 * - 마켓 상태는 enum성 값이므로 `enums.ts`에 단일 선언한다(**6일차**, T8).
 * - **C-23 / 041**: 마감·정산 시각 판정은 서버 권한이며 클라이언트 시계를 신뢰하지 않는다.
 */

export {};
