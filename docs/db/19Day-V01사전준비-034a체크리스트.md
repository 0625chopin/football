# 19일차 — V-01 사전 준비 · 034a 착수 체크리스트

**작성**: 6팀(DB인프라) · **일차**: 19일차(2026-08-14) · **성격**: 대기 일차 문서 산출물(코드·마이그레이션 변경 없음)

## 1. V-01 실측 사전 준비

V-01 = Edge 런타임에서 30경기 처리 CPU 시간이 2초/호출 한도 이내인가(본작업은 26~37일차, `docs/team-schedule/06-DB인프라팀.md` §4).

- **오늘 범위**: 실측 하네스 "설계"만. 실제 하네스 코드는 2팀 엔진 성능 벤치 기준(30경기 ≤ 1.5초, 25일차 수신 예정)이 도착한 뒤에 작성한다 — 벤치 대상 없이 하네스만 먼저 만들면 재작성 위험.
- **필요 입력**: 2팀 엔진 벤치 기준(25일차) — 아직 미수신.
- **준비 항목**:
  1. 하네스 배치 위치 계획 — `supabase/functions/tick/` 내부 또는 별도 벤치 스크립트. 실제 파일 생성은 25일차 이후.
  2. 측정 항목 정의 — 호출당 CPU 시간, 30경기 배치 처리 시간, 2초 한도 대비 마진.
  3. 실패 판정 시 대응 경로 사전 확인 — 외부 크론(GitHub Actions) 또는 상주 프로세스 전환, 처리 상한 하향(I-09). 실패 시 033 착수 보류 + 즉시 보고(§6 제약).
  4. 37일차 SP-3 공유 대상 확인 — 결과는 전 팀 대상 공유, 이후 026 도착(39일차)까지 38~39일차는 자체 추가 작업 없이 대기.

## 2. 034a 착수 사전 체크리스트 (3팀 H-07 수신 후 착수)

수락 기준: "동일 인터페이스 구현 대조" — Mock 어댑터와 Supabase 어댑터가 `DataSource` 계약의 전 메서드에 대해 동일 시그니처로 구현되었는지 대조.

| 인계물 | 소유 | 상태(19일차 확인) | 034a에서 쓰는 곳 |
|---|---|---|---|
| H-02 `DataSource` 계약 | 1팀 | 수신 완료(13일차) — `src/lib/data/DataSource.ts`, 9개 그룹 62개 메서드, 전부 도메인 타입(`@/types` 배럴)만 참조 | 구현 대조 기준 |
| H-07 Mock 어댑터 구현체 | 3팀 | `src/lib/data/mock/MockDataSource.ts`(849줄) 존재 확인. **배럴(`index.ts`) 미존재** — 오늘 종료 시점 산출물에 배럴이 포함되는지 재확인 필요 | 034a 구현 대조 기준(비교 대상) |
| H-06 ESLint 가드레일 | 1팀 | 20일차 수신 예정, 아직 없음 | 034a 이후 25일차 검증에 사용 |
| 기존 자산 | 6팀 | `src/lib/data/supabase/mapper.ts`(1058줄) 이미 존재 — 034a는 이 매퍼를 재사용/확장하고 새로 만들지 않는다 | 034a 구현 기반 |

**인터페이스 기대치(H-07에서 받아야 할 형태)**:
- `DataSource` 인터페이스를 그대로 구현한 클래스/객체 export(현재 `MockDataSource.ts` 구조와 동일 패턴 유지 확인).
- 반환 타입은 `@/types` 배럴 기반 도메인 타입 또는 `DataSource.ts` 로컬 합성 DTO만 사용 — 별도 타입 재선언 없어야 함(C-5·C-6).
- Result 래핑 여부(`factory.ts`/`result.ts`와의 관계) — 034a 착수 시 Mock 쪽 래핑 패턴을 그대로 따라가야 하므로 20일차 착수 전 확인 필요.

## 3. 오늘(19일차) 변경 사항

실제 코드·마이그레이션·스키마 변경 없음. 본 문서만 추가.
