# 2팀 — 시뮬레이션 엔진팀 — 산출물 기록

> 위치: `docs/team-schedule/outputs/02-시뮬레이션엔진팀.md` / 본체: [`02-시뮬레이션엔진팀.md`](../02-시뮬레이션엔진팀.md)
> 이 파일은 **일차별 산출물·조정 기록의 누적처**입니다. 팀 md 본체를 가볍게 유지하기 위해 분리했습니다.
> **팀원 소환 시 열람 대상이 아닙니다** — 팀원은 본체 md의 소유 경로·해당 일차 행만 봅니다.
> 새 기록은 이 파일 끝에 append 하세요.

---

## 1. 요약 (1~13일차)

| 항목 | 값 |
|---|---|
| 참여 일차 | 1~6일차, 9~13일차 (7~8일차는 대기 구간이나 대체 작업 수행) |
| 담당 Task | Task 006(종료), Task 023(진행 중) |
| 주요 산출물 | `src/lib/sim/rng/{prng,derive,precision,sort,hash}.ts` 및 각 `.test.ts`, `bench.test.ts` / `src/lib/sim/match/{tick,events,stats,substitution,penalty,gk-fallback}.ts` 및 각 `.test.ts` / `src/lib/sim/match/TIER_B_RESIM_DESIGN.md`, `tier-b-resim-contract.ts` / `docs/devStep/04.틱엔진설계메모_7일차.md`, `05.SP1사전검토_2팀_7일차.md`, `06.SP1동결리뷰_2팀_8일차.md` |
| 종료된 Task | Task 006 (시드 PRNG·결정론 유틸) — 6일차 100만 회 바이트 동일성 벤치로 수락, H-04 인계 |
| 진행 중 | Task 023 (코어 틱 엔진) — 13일차까지 tick/events/stats/substitution/penalty/gk-fallback 완료, Tier B 26필드 재시뮬레이션(신설 31/32일차)·Vitest 시드 스냅샷·성능 벤치는 이후 일차 예정 |
| 미해소 이월 | I-40(assertIndex 상한 없음, 비차단) · I-41(CardReason enum 부재, 비차단) · I-50(Fixture.bracketSlot, 비차단) · I-51(H-15 반환 계약 문서화 책임) · I-71(JSON 공통코드 4종 폴백, 36일차 이월) · I-83(엔진의 상수 조회 경로, 17일차 전 사용자 판정 대기) · I-91·I-92(참고용/상시 재확인) |

## 2. 일차별 기록

### 1일차 (2026-07-21, 화) — Task 006
- **한 일**: `src/lib/sim/rng/prng.ts`에 xoshiro128\*\* PRNG를 순수 함수로 구현(상태 `{state, value}` 명시 반환).
- **산출물**: `src/lib/sim/rng/prng.ts` (신규, 225줄)
- **판정**: 완료
- **특기**: I-16 해소(시드 계층 타입 소유 경계 — 1팀 A-01·2팀 I-a 병합, `worldSeed` 32비트 잠정 확정). `PrngState`/`PrngResult<T>` 소유는 2팀 유지, `src/types/`로 승격하지 않기로 확정.
- 출처: `docs/dailyWorkLog/1Day.md`

### 2일차 (2026-07-22, 수) — Task 006
- **한 일**: 시드 계층 파생 `world_seed → season_seed → match_seed → event_seed` 구현. 배당 프리시뮬을 위한 독립 네임스페이스를 초기 PRNG 상태 서로소까지 포함해 3중으로 설계.
- **산출물**: `src/lib/sim/rng/derive.ts` (신규, 268줄)
- **판정**: 완료
- **특기**: `grep -n "xg" ROADMAP.md` 0건을 근거로 5팀 xG 지표를 "요구사항 밖일 가능성"으로 제기했으나(I-22) 팀장 원문 확인 결과 **기각** — xG는 화면 지표가 아니라 엔진 득점 모델의 중간량(요구사항 5곳 근거)으로 재확인, Task 023·024 공수에 +0.5×2 반영. 매핑표 오류 2건을 2팀이 자체 발견(I-20 Task 030 담당팀 오기 3팀 단독 정정, I-21 D-25 엔진 상수 수정 주체=2팀 명시). I-23·I-24 해소.
- 출처: `docs/dailyWorkLog/2Day.md`

### 3일차 (2026-07-23, 목) — Task 006
- **한 일**: 고정 정밀도 확률 비교 헬퍼 구현 — 확률을 6자리 정수(마이크로 단위)로 정규화 후 정수 비교(부동소수 이행성 문제 회피).
- **산출물**: `src/lib/sim/rng/precision.ts` (신규)
- **판정**: 완료
- **특기**: H-24(월드시간↔실시간 환산) 소비자 관점에서 `World` 타입을 선제 검토. `derive.ts` 상위 2비트 네임스페이스 태그(MAIN/ODDS_PRESIM) 규약을 명문화. `bigint` 도입은 2팀이 근거 3건(32비트 워드 연산·JSON 직렬화 불가·틱 핫패스 성능)으로 기각. `SeasonPhase` 5종 표기에 6번째가 빠졌다는 의문을 제기해 I-33(TIEBREAK 페이즈) 발견의 단서가 됨. 1팀 → 2팀 점검 부적합 0건.
- 출처: `docs/dailyWorkLog/3Day.md`

### 4일차 (2026-07-24, 금) — Task 006
- **한 일**: 안정 정렬 헬퍼(명시적 tiebreak 키 타입 강제, `findTiedRuns()`) 및 상태 해시(순수 TS SHA-256 + `canonicalize()`) 구현.
- **산출물**: `src/lib/sim/rng/sort.ts`, `src/lib/sim/rng/hash.ts` (신규)
- **판정**: 완료
- **특기**: `crypto.subtle`은 `digest()`가 항상 Promise라 동기 순수 함수 체인을 깨뜨리므로 기각, 순수 TS 구현 채택. 승부차기 `detail` jsonb 설계를 팀장이 재질의(변경요청 F와 같은 취약성인지) → 2팀이 `Fixture`가 FR-ST-005 재계산 대상 목록에 없음을 근거로 "동일 취약 아님" 판정. FR-MT-002 이벤트 23종 목록을 원문에서 추출해 제출, 6일차 enums.ts 확정에 투입. 소유 경로 침범 0건.
- 출처: `docs/dailyWorkLog/4Day.md`

### 5일차 (2026-07-27, 월) — Task 006
- **한 일**: Vitest 최초 도입(`npm install vitest`, `npm run test` = `vitest run`). 시드 재현성·분포 균등성·입력 순서 셔플 불변성 3종 테스트 작성.
- **산출물**: `package.json`/`package-lock.json`(vitest 4.1.10), `prng.test.ts`, `sort.test.ts`, `derive.test.ts`, `precision.test.ts`, `hash.test.ts` (전부 신규)
- **판정**: 완료
- **특기**: **I-39 발견(OPEN)** — 팀장 2차 검증에서 먼저 포착 후 2팀에 실측 확인 요청: D-28(`worldSeed` 32→53비트 개정)이 `src/lib/sim/rng/**` 구현에 미반영(`derive.ts` 헤더 주석이 "32비트로 확정"이라 남아 있음). 6일차 H-04 인계 전 최우선 수정 대상으로 지정됨. `vitest.config.ts`는 1팀 Task 008(12일차) 소관이라 2팀이 의도적으로 만들지 않음(상대경로 import만 사용 중이라 문제 없음).
- 출처: `docs/dailyWorkLog/5Day.md`

### 6일차 (2026-07-28, 화) — Task 006
- **한 일**: I-39 최우선 수정(`createState`가 53비트 시드 전량 소비, `derive.ts` payload 30→51비트 재설계, `assertUint32`→`assertSafeSeed`) 후 Task 006 수락 기준인 동일 시드 100만 회 바이트 단위 동일성 벤치 수행.
- **산출물**: `src/lib/sim/rng/prng.ts`·`derive.ts` (수정), `bench.test.ts` (신규), `prng.test.ts`·`derive.test.ts`에 53비트 경계 회귀 12건 추가
- **판정**: 완료 (I-39 재작업 후 Task 006 수락)
- **특기**: **Task 006 종료, H-04 인계 확정**(3팀 Mock 팩토리 007·배당 프리시뮬 035, 소비 시작 13일차). 벤치 결과: 100만 회 추출 1회차 49.33ms/2회차 43.95ms, value·최종 state `toEqual` 완전 일치 — 재현 ≤100ms 기준 대비 5만 회 3.02ms로 30배 여유. I-39 해소를 1팀이 검증 통과, 부수로 **I-40 신규 등재**(`assertIndex` 2³² 상한 검증 없음, 비차단, 2팀 여유 시 처리). I-33(TIEBREAK 6번째 페이즈) 파급 3건 중 2팀 Task 025는 ROADMAP 갱신으로 전달.
- 출처: `docs/dailyWorkLog/6Day.md`

### 7일차 (2026-07-29, 수) — 대기 구간 대체 작업
- **한 일**: Task 023 착수를 위한 도메인 타입(H-01, 8일차 동결) 대기 중 대체 작업 3건 수행 — ① PRNG 1,000만 회 확장 벤치(일반 시드/53비트 상한 부근) ② 90(+연장30)틱 상태 전이도·이벤트 23종 분기 트리 설계 메모(확률 수치는 전부 TBD) ③ SP-1(8일차 타입 동결 리뷰) 사전 검토 — 엔진 관점 타입 갭 5건 식별.
- **산출물**: `src/lib/sim/rng/bench.test.ts` (확장), `docs/devStep/04.틱엔진설계메모_7일차.md` (신규), `docs/devStep/05.SP1사전검토_2팀_7일차.md` (신규)
- **판정**: 완료
- **특기**: shrimp-task-manager로 세운 계획 3건이 동시 작업 중이던 다른 팀 계획에 덮어써져 소실(shrimp가 프로젝트 단위 단일 인스턴스라 병렬 팀이 태스크 공간을 공유). 이후 팀 md 해당 일차 섹션을 단일 소스로 삼기로 함. `detail` jsonb 인코딩 규약 4건을 제기했으나 타입 추가 대상 아님으로 판정(I-42). I-41(CardReason enum 부재) 신규 — 동결 블로커 아님, 2팀 023/024 설계 후 1팀이 배치 반영.
- 출처: `docs/dailyWorkLog/7Day.md`

### 8일차 (2026-07-30, 목) — SP-1 동결 리뷰
- **한 일**: 8일차 타입 동결(H-01) 직전 SP-1 리뷰 — Task 023~028 완료 판정을 역산해 14항목 대조.
- **산출물**: `docs/devStep/06.SP1동결리뷰_2팀_8일차.md` (신규)
- **판정**: 완료 (0건 — 전부 "타입 충분, 알고리즘/`detail`/공통코드로 흡수")
- **특기**: 5팀의 자책골 관련 가정을 엔진 관점에서 검증, `teamId`=득점 귀속팀이어야 자책골이 상대팀 `goalsFor`로 집계된다는 더 강한 근거 제시(I-53). Task 023 착수(`tick.ts` 골격) 시작.
- 출처: `docs/dailyWorkLog/8Day.md`

### 9일차 (2026-07-31, 금) — Task 023
- **한 일**: 90(+30연장)틱 순회 엔진 골격 구현, 120틱까지 순회 확인.
- **산출물**: `src/lib/sim/match/tick.ts`, `tick.test.ts` (신규, 신규 12케이스 — 120틱 도달·정규틱 120개·`minute` 단조증가·연장 구간·결정론 재현·스토피지 범위 20시드 샘플·틱 인덱스 연속성)
- **판정**: 완료
- **특기**: **I-60 확정** — AS-10("사후 임의 배분 금지")의 실제 적용 범위를 2팀이 판정: Tier B 30개 필드는 직접 카운터로 유지하고(`MatchEventType` 23종 불변), 결정론 SSOT는 이벤트 로그가 아니라 `matchSeed` 재시뮬레이션(NFR-QA-003)임을 명확화 — 1팀이 2팀 제안보다 더 보수적인 문구로 확정. `stats.ts`(11일차) 착수 전 56필드 Tier A/B 매핑표 작성을 결정. `derive.ts`↔`schema-design.md` CHECK 제약 시드 4계열 53비트 정합을 팀장과 독립 검증(완전 일치). 04번 설계 메모의 `stoppage`→`addedTime` 오기를 스스로 발견해 정정.
- 출처: `docs/dailyWorkLog/9Day.md`

### 10일차 (2026-08-03, 월) — Task 023
- **한 일**: 이벤트 23종 생성 및 시간순 정렬 구현(동기 함수, `detail` 최소화, 표시 문자열 0건을 `assertNoDisplayText`로 런타임 강제).
- **산출물**: `src/lib/sim/match/events.ts` (신규)
- **판정**: 완료 — 단, **`events.ts`에는 co-located 테스트 파일이 만들어지지 않음**(13일차 I-90에서 커버리지 0건으로 드러남, §4 참조)
- **특기**: 착수 전 `tick.ts`(9일차)와의 시드 충돌을 스스로 발견해 회피(경계분 45/90에서 `eventIndex=0` 재사용 시 스토피지 추첨과 동일 시드 → `eventIndex`를 1·2로 이동, 9일차 산출물은 건드리지 않음). 1·3팀의 데이터 접근 계약과 2팀 엔진이 처음 같은 날 마주쳐 접합면 이슈 3건 발생 — `events.ts`↔`match_event` 테이블 10필드 1:1 완전 정합 확인, 4팀 `enums.ts`(19일차)와의 리터럴 23종 정합 확인, 3팀 공통코드 36그룹에 날씨·계수 관련 대응 그룹이 없음을 3팀과 독립적으로 확인(Task 024 착수 전 협의 예정). **I-65 자체 발견** — 메타 이벤트(`KICKOFF`/`HALF_TIME`/`FULL_TIME`/`EXTRA_TIME_START`)를 골·파울과 동일하게 매 틱 가중 추첨 대상으로 취급해 `FINISHED` 경기에도 이벤트 0건이 가능한 결함, 1팀 `result.ts`를 읽다가 스스로 발견해 보고(11~16일차 `stats.ts`에서 해소 예정).
- 출처: `docs/dailyWorkLog/10Day.md`

### 11일차 (2026-08-04, 화) — Task 023
- **한 일**: Tier A 16필드 이벤트 폴드 스탯 누적 구현. 동시에 Tier B(직접 카운터·재시뮬레이션 대상) 설계 메모와 계약 초안 작성.
- **산출물**: `src/lib/sim/match/stats.ts`, `stats.test.ts` (신규), `src/lib/sim/match/TIER_B_RESIM_DESIGN.md`, `tier-b-resim-contract.ts` (신규)
- **판정**: 완료 — 단, **Tier B 필드는 이 일차 산출물 범위 밖으로 확인되며 별도 일정 신설로 이어짐(§4 참조)**
- **특기**: 6팀 스키마(공유 56컬럼 전부 NOT NULL)와 2팀 `accumulatePlayerMatchStats()`(Tier A 16필드만 산출) 사이의 간극을 6팀 11일차 교차 점검이 발견 → **사용자 판정으로 Tier B 재시뮬레이션 컴포넌트를 2팀 소유로 배정**(D-31). 후속 설계에서 재시뮬레이션 대상이 40개가 아니라 26개(`NO_EVENT_TYPE`)임을 규명, "matchSeed 재시뮬레이션은 tick 엔진 재실행이 아니라 별도 시드 통계 모델"임을 명확화. 이 결정으로 **舊 31~79일차가 전부 +2 재번호**되고 H-15·H-16 인계일이 각 2일 순연됨(§2.1). 2팀 미확정 가정 3건에 대해 1팀이 SSOT 판정(I-68~I-70). I-71(JSON 공통코드 4종 폴백 빈 객체) 신규 — 36일차 이월, 2팀은 20일차 `tactics.ts`에서 방어 필요.
- 출처: `docs/dailyWorkLog/11Day.md`

### 12일차 (2026-08-05, 수) — Task 023
- **한 일**: 교체 로직 구현(최대 5명·3창, 부상 발생 시 즉시 교체 판단).
- **산출물**: `src/lib/sim/match/substitution.ts`, `.test.ts` (신규, 12케이스)
- **판정**: 완료
- **특기**: **I-83 자체 발견** — H-05(3팀 공통코드 카탈로그) 인계를 받으며 엔진이 상수를 어느 경로로 읽을지(직접 호출형 vs 스냅샷 주입형)가 미확정임을 발견, 결정론이 깨질 수 있는 구조적 갭으로 등재. 팀장 잠정 판단은 스냅샷 주입 유력이나 **사용자 확정 대기**(17일차 Task 024 착수 전까지 코드 변경 불필요). **I-86** — 하프타임 교체가 3창 예외인지 전수 조사해 FR-MT-012 원문에 예외 정의가 없음을 확인, 12일차 구현(모든 창 동일 취급)이 원문에 충실함을 재확인(비차단). 팀장 2차 검증에서 교체창 식별 키의 `teamId` 누락을 지적받아 `(minute, addedTime)` → **`(teamId, minute, addedTime)` 3튜플**로 정정, `substitution.ts` 헤더 JSDoc에 사후 소비처(6팀 스탯 재계산·5팀 중계 UI) 명시.
- 출처: `docs/dailyWorkLog/12Day.md`

### 13일차 (2026-08-06, 목) — Task 023
- **한 일**: 승부차기(5+서든데스) 구현. PK 골은 `player_match_stat.goals`에 미포함, `pk_home`/`pk_away` 분리 기록(D-19).
- **산출물**: `src/lib/sim/match/penalty.ts`, `penalty.test.ts` (신규)
- **판정**: 완료 (10/10 통과, 기존 파일 0건 수정)
- **특기**: **H-04 인계 첫 소비 사례** — 3팀 `generate.ts`(선수명 생성, Task 007)가 2팀 PRNG를 `nextIntBelow`로 `{state, value}` 스레딩해 처음 실사용. 2팀이 모듈 소유자로서 3팀의 블랙리스트 재추첨 루프에 state 재사용 버그가 없는지 팀장·3팀과 3중 검증(무한 동일값 루프 없음 확인). **I-90 발견 계기 제공** — 1팀이 2팀 `penalty.ts` 커버리지 확인 중 `coverage-final.json`에 `events.ts`(305줄, 10일차 2팀 산출물)가 완전 부재함을 발견(테스트 없는 파일이 분모에서 빠지는 vitest.config 구조 문제) — `vitest.config.ts` 자체 수정은 14일차로 유보, 오늘은 기록만. `match/stats.ts` branch 커버리지 66.67%(44/66)로 70% 미달인 것도 함께 확인. **I-91**(`nextIntBelow(state,1)`이 state를 소비하지 않음, 참고용, 현재 미발동) · **I-92**(NFR-DT-001 자동 차단 ESLint 룰 부재, 매 교차 점검마다 수동 재확인 필요) 2팀 제기. `__suites__` 위치 판정에서 2팀 회신(`accounting`은 Task 029/3팀 소관, sim 전용 스위트 아님)이 1팀 판정을 뒤집는 근거가 됨. Task 023의 GK 퇴장 + 교체 소진 시 필드플레이어 GK 배치(`gk-fallback.ts`, D-22)도 이 일차에 완료.
- 출처: `docs/dailyWorkLog/13Day.md`

## 3. 인계물 (H-*)

| 인계 | 방향 | 일차 | 상태 |
|---|---|---|---|
| H-04 (시드 PRNG·결정론 유틸 `src/lib/sim/rng/**`) | 2팀 → 3팀 (Mock 팩토리 007, 배당 프리시뮬 035) | 확정 6일차 · 소비 시작 13일차 | ✅ 확정 및 첫 소비 개시 |
| H-01 (도메인 타입 47종, 동결) | 1팀 → 2팀 (Task 023 착수 근거) | 수신 9일차 | ✅ 수신, Task 023에 사용 중 |
| H-05 (공통코드 36그룹 카탈로그 + `loadConstants(group)`) | 3팀 → 2팀 (Task 024·025·027 예정) | 확정 12일차 · 소비 시작 13일차 | ✅ 확정, 소비는 17일차(Task 024) 이후 |

## 4. 이 팀이 관여한 이슈·결정

| ID | 내용 | 일차 | 상태 |
|---|---|---|---|
| I-16 | 시드 계층 타입 소유 경계 + `worldSeed` 32비트(1팀 A-01·2팀 I-a 병합) | 1일차 | ✅ 해소 |
| I-20 | Task 030 담당팀 오기("2팀"→3팀 단독), 2팀이 자기 작업표 부재로 발견 | 2일차 | ✅ 해소 |
| I-21 | D-25 엔진 상수 수정 주체(2팀) 누락 — 실행 불가능한 배분이었음, 2팀 발견 | 2일차 | ✅ 해소 |
| I-22 | xG 근거 부재 주장(2팀 제기) — 원문 재확인 결과 정반대(요구사항 5곳 근거) | 2일차 | ❌ 기각 (재론 금지) |
| I-33 | `SeasonPhase` 6번째 페이즈 `TIEBREAK` — 2팀이 "5종 표기인데 왜 6종이냐" 질문한 것이 발견 단서 | 3일차 제기 · 6일차 1팀/28일차 2팀 반영 | ✅ 승인 완료 |
| I-39 | D-28(`worldSeed` 32→53비트) `src/lib/sim/rng/**` 미반영 — 팀장 2차검증 포착, 2팀 실측 확인 후 수정 | 5일차 발견 · 6일차 해소 | ✅ 해소 |
| I-40 | `derive.ts` `assertIndex`에 2³² 상한 검증 없음 (방어 계층 공백, 비차단) | 6일차 | ⏳ OPEN (2팀 여유 시) |
| I-41 | `CardReason` enum 부재 (동결 블로커 아님, 2팀 023/024 설계 후 반영) | 7일차 | ⏳ OPEN |
| I-42 | `detail` jsonb 인코딩 규약 4건(2팀 제기) | 7일차 | ✅ 타입 추가 대상 아님으로 판정 |
| I-53 | 자책골 `teamId`=득점 귀속팀 근거 — 2팀이 엔진 스탯 정합성 근거 제시 | 8일차 | ✅ 확정 |
| I-60 | AS-10 적용 범위 — Tier B 직접 카운터 유지, SSOT는 `matchSeed` 재시뮬레이션 | 9일차 | ✅ 확정 |
| I-65 | 메타 이벤트(`KICKOFF` 등) 결정론 미보장 — 2팀 자체 발견 | 10일차 | ⏳ 11~16일차 `stats.ts`에서 해소 예정 |
| I-68~I-70 | 2팀 미확정 가정 3건(Tier B 설계 관련) — 1팀 SSOT 판정 | 11일차 | ✅ 판정 완료 |
| I-71 | JSON 공통코드 4종 폴백이 빈 객체 (NFR-CFG-005③ 미충족) | 11일차 | ⏳ 36일차 이월 (2팀은 20일차 `tactics.ts` 방어 필요) |
| I-83 | 엔진의 공통코드 조회 경로(직접 호출 vs 스냅샷 주입) 미확정 — 2팀 자체 발견 | 12일차 | ⏳ 사용자 판정 대기 (17일차 전) |
| I-86 | 하프타임 교체 3창 예외 미정의 — 2팀 전수 조사로 "원문에 예외 없음" 확인 | 12일차 | ✅ 비차단으로 확인 (결함 아님) |
| I-90 | 커버리지 설정이 테스트 없는 파일을 분모에서 제외 — `events.ts`(2팀 10일차 산출물) 커버리지 0건 노출 | 13일차 | ⏳ 기록만, 설정 수정은 14일차 |
| I-91 | `nextIntBelow(state,1)`이 state를 소비하지 않음(2팀 제기, 참고용) | 13일차 | ⏳ OPEN (현재 미발동) |
| I-92 | NFR-DT-001 자동 차단 ESLint 룰 부재 — 2팀 제기, 수동 grep이 유일한 방어선 | 13일차 | ⏳ OPEN (매 교차 점검 재확인) |
| D-25 | 엔진 상수 수정 주체 = 2팀 단독(`src/lib/sim/**` 소유) | 2일차 | ✅ 확정 |
| D-28 | `worldSeed` 32→53비트 개정 | 5~6일차 반영 | ✅ 확정 |
| D-31 | Tier B 재시뮬레이션 컴포넌트 2팀 소유 배정 + 신 31/32일차 신설(舊 31~79일차 +2 재번호) | 11일차 | ✅ 사용자 승인 |

## 5. 확인 못 한 것

- I-40·I-41·I-50·I-51·I-71·I-83·I-91·I-92의 13일차 이후 최종 해소 여부(해당 일차 로그가 아직 없어 미확인).
- H-04·H-05 인계 문서 상 "소비 시작 13일차"가 실제 코드 변경으로 나타났는지는 3팀 쪽 파일(`generate.ts`)에서만 확인, 2팀 산출물 자체 변경은 없었음(13일차 로그 "기존 파일 0건 수정"으로 명시됨).
- Task 006 공수 추정 대비 실제 소요(계획 6일 vs 실제 1~6일차, I-39 재작업 포함) 편차의 정량적 집계는 팀 md §5에 사후 반영됐는지 별도 확인 필요.

---

## 15일차 (2026-08-10) — Task 023 시드 스냅샷

**산출물**: `src/lib/sim/match/snapshot-pipeline.ts`·`match-snapshot.test.ts`·`events.test.ts`(신규), `__snapshots__/match-snapshot.test.ts.snap`(806줄), `stats.ts`(v8 ignore 추가)

- **NFR-QA-003 충족** — 100경기 시드 스냅샷 diff 0(2회 연속 실행), 이벤트↔스탯 재계산 일치.
- **다이제스트 스냅샷 채택** — 원본 이벤트 배열을 그대로 저장하면 수만 줄로 부풀어 회귀 diff를 사람이 읽을 수 없다. 기존 `rng/hash.ts`의 `hashState()`(SHA-256, NFR-DT-003 도구 재사용)로 경기당 이벤트·스탯을 64자리 hex 2개로 축약. vitest `toMatchSnapshot()`이 자동 생성해 별도 스크립트 불필요.
- **커버리지 실질 해소** — `events.ts` 0%→**100%**, `stats.ts` 라인 97.61%→100% / 브랜치 66.66%→**76.92%**. 이 결과가 1팀의 perFile 채택(I-94) 차단 사유를 없앴다.
- **도달불가 분기만 제외** — `default: exhaustiveCheck`(`MatchEventType` 23종 소진 후 구조적 도달불가)를 `/* v8 ignore start/stop */`으로 감쌈. `next 2`로는 switch의 case-선택 브랜치가 남아 `default:` 라벨째 감싸는 방식으로 교체. **테스트 가능한 15개 분기(참가자 미배정 스킵 경로)는 덮지 않고 남김** — I-103으로 이월(16일차 이후).

**팀장 검증**: 결함 없음. ignore 범위가 `src/lib/**` 전체에서 이 1건뿐임을 grep 실증.

---

## 16일차 (2026-08-11) — Task 023 성능 벤치 + I-103 종결

**산출물**: `src/lib/sim/match/perf-bench.test.ts`(신규), `stats.test.ts`(describe 추가).

**벤치 설계**: 경기 1건을 `buildTickSequence`→`generateMatchEvents`→`linkPenaltyOutcomes`→`accumulatePlayerMatchStats` **전 파이프라인 그대로** 측정 구간에 넣었다(부분 측정 아님). 200건 표본, 5건마다 연장전 포함. 15일차 `snapshot-pipeline.ts`는 "테스트 전용 고정 픽스처"라 재사용하지 않고 벤치 전용 참가자 배정기를 별도로 뒀다 — OWN_GOAL 수혜팀 귀속 규약(I-53)을 명시 재현해야 스코어 정합 검증이 의미를 갖기 때문.

**실측**: **p95 0.203ms / p99 0.430ms** (수락 기준 50ms / 120ms). 스코어 정합 불일치 **0건**(골 532건/200경기 = 2.66골/경기).

**import 제약 자동화**: `src/lib/sim/**` 전 파일을 스캔해 `react`/`@supabase/*` 0건을 검증하는 테스트 추가 — 주석 선언이 아니라 테스트로 고정했다.

**I-103 종결**: 참가자 미배정 15개 분기 전량 테스트 커버(기존 내용 무변경).

**팀장 검증: 결함 0건** — 측정 구간이 전 파이프라인을 덮는지 코드로 확인, 벤치 재실행 통과. `performance.now()`는 측정 전용이라 NFR-DT-001 무관하며 1팀 16일차 lint 룰과도 충돌하지 않음을 확인.

## 17일차 (2026-08-12) — Task 024 착수, 계수 함수 9종 골격

`src/lib/sim/ability/modifiers.ts`(179줄) + `modifiers.test.ts`(36케이스) 신규.

**구조**: 개별 계수 8종(condition/fitness/injury/familiarity/home/weather/manager/position) + 합성 `combineAbilityModifiers`(계수 체인 곱 후 재클램프) = 9개. 각 함수는 전용 입력 인터페이스를 갖는다. **`clampAbilityModifier`가 계수 체인의 유일한 클램프 진입점**이며 9개 전부가 이를 경유한다 — 유한수 아님/`min > max`는 `RangeError`.

**클램프 [0.35, 1.35]**: 하한 0.35는 `match/gk-fallback.ts`의 D-22 GK 교차 배율과 같은 값인데, 우연이 아니라 "유효 능력이 최저치에서도 0이 되지 않는다"는 동일한 설계 의도다. 이 경계가 시즌별 밸런싱 파라미터(NFR-CFG-001)인지 알고리즘 불변식인지 미결이라 **`gk-fallback.ts`(I-83)와 동일한 "안전 기본값 export + options override" 패턴**을 그대로 따랐다 — `SimConstantSnapshot`(E-44) 주입 시 코드 변경 없이 갈아끼운다.

**precision.ts 대상이 아님**(파일 헤더에 근거 명시): 이 클램프는 `[0.35,1.35]` 범위의 단순 배율이고 `Math.min`/`Math.max`(둘 다 IEEE-754 표준 연산)만으로 플랫폼·시점 무관하게 결정론적이다. 성공/실패 판정을 다루는 **확률 비교는 여전히 `rng/precision.ts`를 경유**한다.

**의도적 공백**: 개별 8종은 현재 중립값 1.0을 클램프해 반환하는 자리표시자다. 실공식은 담당 일차에 채운다(각 함수 `// TODO(N일차)`) — 컨디션·피로·캐미 **18일차**, 포지션 숙련도 **19일차**, 날씨·감독 상성 **20일차**. 오늘 채우면 그날 담당의 판단(특히 `tactics.ts`/`position.ts` 분리 여부)을 앞지른다.

테스트: 하한·상한·경계값·override·`min>max`·NaN 전건 통과. `npm run test` 504→전체 그린, `tsc --noEmit`·`eslint` 0.

**팀장 검증: 결함 0건.** 1팀 17일차 신규 sim import 룰 적용 하에서도 lint clean.

## 18일차 (2026-08-13) — Task 024

`modifiers.ts`의 자리표시자 3종을 실공식으로 교체(17일차 `clampAbilityModifier` 단일 진입점 재사용, **신규 클램프 없음**).

- `conditionModifier`: `M = 0.70 + 0.30×(C−1)/9`
- `fitnessModifier`: `M = 0.75 + 0.25×(fitness/100)`
- `familiarityModifier`: `M = min(1.0 + 0.01×familiaritySeasons, 1.06)`

**판단 지점**: 캐미는 과제 행에 **상한(+6%)만 명시**돼 있고 증가 곡선(선형 vs 체감)·연 증가율이 미정이라 **선형 1%p/시즌**으로 채택하고 근거를 파일 상단 주석에 남겼다(6시즌차부터 상한 고정). 밸런싱 재조정 시 이 지점이 후보.

테스트: `modifiers.test.ts` 36→39케이스, **수락 기준 경계값 전건 통과**(C=1→0.70, C=10→1.00, fitness=0/100, seasons=0/3/6/20). `tsc --noEmit` 0, `npm run test` 542 통과.

**이월(19~20일차)**: 잔여 자리표시자 5종(부상·홈·날씨·감독·포지션). 날씨·포지션 로직의 `modifiers.ts` 잔류 vs `tactics.ts`/`position.ts` 분리는 해당 일차 판단으로 유지.

**팀장 검증: 결함 0건.** 공식 3종을 코드와 직접 대조해 일치 확인.

## 19일차 (2026-08-14) — Task 024 포지션 계수

- `src/lib/sim/ability/position.ts` (신규) — 11노드/15엣지 인접 그래프, 모듈 로드 시 전 쌍 BFS 테이블 사전 계산. 보유 숙련도 5단계(1.00/0.95/0.88/0.75/0.60), 미보유 `max(0.45, 0.88 − 0.11×dist)`, **GK 교차 0.35를 보유 단계표보다 우선** 적용(FR-PL-006 하드 고정 + 수용 기준 ④).
- `src/lib/sim/ability/modifiers.ts` — `positionModifier`/`PositionModifierInput` 스텁 제거·이관.
- `position.test.ts` (신규 60건), `modifiers.test.ts`(포지션 케이스 이관).
- GK 교차 배율은 재선언 없이 `match/gk-fallback.ts`의 `GK_CROSS_POSITION_MODIFIER_DEFAULT`를 import — 같은 공통코드 값의 중복 선언 회피.

**파일 분리 판단(17일차 인계 해소)**: `modifiers.ts` 잔류가 아닌 **분리 확정**. 근거 ⓐ 단일 수식인 컨디션/피로/캐미와 달리 그래프+BFS+3단 분기로 복잡도 층위가 다름 ⓑ 팀 일정표 산출물 필드가 이미 `position.ts` 지정 ⓒ `match/gk-fallback.ts`(14일차) 선례. **20일차 날씨·감독은 각각 단일 매트릭스 룩업이라 이 판단이 자동 승계되지 않음 — 그날 별도 판단.**

**팀장 검증: 결함 0건.** GK 예외가 보유 단계표보다 선행 배치됨을 코드에서 직접 확인. `npx vitest run src/lib/sim` 17파일 299케이스 전건 통과.

## 20일차 (2026-08-17) — Task 024 (날씨·감독)

- `src/lib/sim/ability/tactics.ts` / `tactics.test.ts` 신규, `modifiers.ts` 스텁 이관·제거 + `NEUTRAL_MODIFIER` export, `modifiers.test.ts` 정리.
- 날씨 `M_weather`(FR-MT-006) · 감독 `M_manager`(FR-MT-009 6×6) — `loadConstants` 경유, **숫자 리터럴 0건**(check-literals 대상 11파일 포함, 후보 0).
- **파일 분리 확정** — 19일차 포지션 판단(BFS 복잡도) 미승계, "공통코드 로더 의존"이라는 별도 축으로 재판단. `fallback.ts`에 구체 숫자·JSON 구조가 없어(36일차 소관) 안전 기본값 선언 불가 → 로더 강제. `options?.table ?? loadConstants(group)`로 override 시 순수성 유지, `ConstantSourceUnavailableError`는 전파(부트스트랩 누락 은폐 방지).
- 테스트 65케이스 통과. 제보: **I-118**(`ABILITY_MULT` 키 정렬), **I-119**(xG 배율 Task 행 누락).

## 21일차 (2026-08-18) — Task 024 (라인업 자동 선정)

- `src/lib/sim/lineup/select.ts` / `select.test.ts` 신규.
- `selectLineup()` — 가용성(부상·정지) 배제 → 컨디션×피로×포지션 합성 점수 → 선발 11(그리디 슬롯) + 벤치 7(GK≥1 우선 확정). `modifiers.ts`/`position.ts` 계수는 **호출만, 재구현 없음**.
- 정지: 기존 `PlayerState.suspensionRemainingLeague/Cup` 사용. `CompetitionType` 4종→2분류 매핑은 025/026 소관이라 추측 없이 입력으로 위임.
- **로테이션 부분 충족** — `PlayerState`에 출전 이력 필드 부재(8일차 동결). `fitnessModifier` 자연 유도 + `playerId` 오름차순 결정론까지만 → **I-123**(팀장: 멈춘 것이 옳다).
- 테스트 12/12, `src/lib/sim/` 316/316. `check:literals` 신규 1건(`LINEUP_STARTER_COUNT=11`)은 구조 상수로 팀장 확인 → 조치 불요. 이슈: I-123, I-124.

## 22일차 (2026-08-19) — Task 024 카드·정지 + D-23 감독 폴백

- `src/lib/sim/discipline/suspension.ts` / `.test.ts` 신규(단독 21케이스). 카드 누적 5장 정지·퇴장 정지를 **리그/컵 축으로 완전 분리** — 상호 미간섭을 테스트로 직접 고정(수락 기준 충족).
- 21일차 `select.ts`의 `SuspensionCompetition`, `stats.ts`의 `PlayerMatchStatTierAFold`를 **재구현 없이 소비**.
- **퇴장 정지 경기 수(1~3)를 확정하지 않았다** — CardReason taxonomy가 I-41로 보류 중이라 호출자 명시(`dismissalSuspensionGames`) + 범위 검증(fail-fast)로 남김. 21일차 로테이션 판단(타입에 없는 필드를 만들지 않고 멈춤)과 동일 원칙.
- D-23 `resolveManagerStyle`(공석 → BALANCED) 동일 파일 배치를 팀장에 질의 → **승인·유지**(순수 함수 1개라 이관 비용 ≈ 0, 분리 여부는 025/026 착수 시점 재판단).
- `tsc` 0 / `eslint` 0 / 전체 test 회귀 0.

## 23일차 (2026-08-20) — Task 024 (24일차까지 계속)

- `src/lib/sim/ability/modifier-chain.test.ts`(신규) — 8개 계수 함수를 **중립 입력으로 실제 호출** 후 `combineAbilityModifiers` 합성 결과가 base(1.0)와 일치함을 고정. 기존 테스트는 리터럴 배열만 검증해 개별 함수 → 합성 경로가 비어 있었다.
- ability 4파일 75케이스, Stmt/Branch/Func/Line **전부 100%**(NFR-QA-002 충족).
- 이슈 후보 0건.

## 24일차 (2026-08-21) — Task 024 **종료**

- `modifier-chain.test.ts` — "실 공통코드 로더 경로 통합 검증" 추가. 23일차 테스트가 weather/manager에 **테스트 전용 리터럴 테이블을 주입**해 실제 로더를 타지 않았음을 자체 진단하고, `installHardcodedFallback()` 후 오버라이드 없이 빈 `WEATHER_EFFECT`/`MANAGER_MATCHUP`에서 `NEUTRAL_MODIFIER` 폴백 → base 1.0 유지 + `ConstantSourceUnavailableError` 미발생을 고정.
- `src/lib/sim/ability/README.md`(신규) — 9개 계수 표(입력/공식/반환타입/클램프), 소비자별 부분집합 조립, 미배선 지점(`match/**` 미연결, `penalty.ts`가 유일 연결점), **H-14 3팀 인계 절**(OWN_GOAL teamId 귀속 / xg 승격 필드 / PK 골 미포함 D-19 / `relatedEventSequence` 표시 전용).
- ⚠️ 36일차(031a)에 계수 실값이 들어오면 README·통합테스트의 "1.0 유지" 전제 갱신 필요(README에 명시).
- 이슈 후보 0건.

## 25일차 (2026-08-24) — Task 025 Berger 더블 라운드로빈

- `src/lib/sim/schedule/berger.ts`(신규) — `generateBergerDoubleRoundRobin(teamIds)`. 서클법으로 팀0 고정·나머지 회전해 단일 라운드로빈을 만들고, 2차전은 1차전 각 라운드의 홈/원정을 반전해 이어 붙인다. 라운드 홀/짝으로 페어링을 뒤집어 고정팀이 매 라운드 홈이 되는 편향을 막는다.
- **NFR-SC-003 준수 — `teamCount`를 순수 함수 파라미터로만 받아 리터럴 24/20/16을 갖지 않는다.** 팀 수는 전부 공통코드(`LEAGUE_TEAM_COUNT`)에서 주입된다. 난수 미사용(NFR-DT-001).
- 홀수·2 미만 팀 수는 예외 — 바이(bye) 처리는 이 알고리즘의 전제 밖이라 조용히 처리하지 않고 명시적으로 던진다.
- `berger.test.ts`(신규 12케이스) — 24/20/16팀 → **552/380/240경기 · 46/38/30라운드**, 팀당 라운드 1회 등장, 전 쌍 홈1·원정1, 결정론, 2차전 반전, 예외 2종.
- **팀장 지시로 홈/원정 균형 단언 추가** — 3개 티어 모두 팀당 홈=원정=`teamCount−1` 정확히 일치. 균형은 구조적으로 항상 성립한다(2차전이 1차전 전체를 반전해 이어 붙이므로 `round%2` 반전 트릭과 무관).
- 최장 연속 동일 홈/원정 스트릭 실측: 24/20/16팀 전부 **2** — 3연속 이상 없음(수락 기준의 "3연속 방지 경고 로그" 경로는 사용되지 않는다).
- 이슈 후보 0건.

---

## 27일차 (2026-08-26) — Task 025 킥오프 산출

- `src/lib/sim/schedule/kickoff.ts`(신규) · `kickoff.test.ts`(신규, 10 tests). `berger.ts` 라운드 번호에 킥오프 시각을 붙이는 순수 함수 계층.
- 간격(75/90/115분)·REGULAR 페이즈 길이(3,450분) **전부 파라미터 주입** — 숫자 리터럴 0건(NFR-CFG-001).
- **최종 라운드 T+3,450분은 강제 정렬이 필요했다**: 자연 계산이 어긋난다(46R × 75분 = 3,375분). 라운드 간격을 선형 스케일링해 1라운드=오프셋, 마지막 라운드=정확히 T+`regularPhaseDurationMin`에 오도록 맞춘다.
- **I-12 적용** — `computeLeagueRoundOffsetsMin`이 최소 간격 ÷ 리그 수로 1라운드를 어긋나게 배치. 리그 수를 입력 배열 길이에서 도출해 4리그 확장에도 하드코딩 없이 동작.
- **I-146 해소** — `detectVenueStreaks` JSDoc에 "연속 라운드 기준 / 결번은 스트릭 단절 / 결번 대진 재사용 시 연속 경기 해석 별도 필요(I-107 병행)" 명시. 로직 변경 없음.
  - ⚠️ **1차 보고에서 "대상 함수가 아직 존재하지 않음(컵/브래킷 미착수)"으로 잘못 보류했다가 팀장 반려로 처리했다.** 이슈 대상은 컵 함수가 아니라 이미 있는 `berger.ts:126`이었다. **교훈: 이슈 대상 식별을 추측하지 말 것.**
- **I-145는 정당 보류** — 이슈 자체가 "소비 계층 생성 시점에 닫는다"를 종료 조건으로 명시. 엔진은 계속 반환값으로만 경고를 전달한다(`gk-fallback.ts` 14일차 결정과 동일 근거).
- 테스트: `tsc` 0 / `lint` 0 / 전체 992 passed, 회귀 0건. 이슈 후보 0건.

## 28일차 (2026-08-27) — Task 025 시즌 페이즈 상태머신

- `src/lib/sim/season/phase.ts`(신규), `phase.test.ts`(신규 12건). 게이트 그린(tsc 0 / lint 0 / test 1025 passed).
- 이벤트 → `{from,to}` 전이 테이블 기반 순수 함수 `transitionSeasonPhase(phase, event)`. 8개 이벤트로 `REGULAR ⇄ CUP_SLOT → PLAYOFF → (TIEBREAK?) → SETTLEMENT → PRESEASON → REGULAR`를 덮는다.
- **멱등은 "목표 페이즈면 no-op"**: 이미 `to`면 그대로 반환, `from`도 `to`도 아니면 예외. 수락 기준 "동일 전이 2회 호출 시 1회 효과" 충족.
- **동률 판정을 인자 주입이 아니라 이벤트 선택으로 분리**했다 — 호출자가 `ENTER_TIEBREAK`(경유)와 `COMPLETE_PLAYOFF`(직행) 중 고른다. Task 026(동률 판정)이 나와도 이 모듈은 불변.
- `SeasonPhase`(6종, D-27/I-33)는 `src/types/enums.ts`에 이미 있어 재선언 없이 배럴 import. `Math.random`·`Date.now`·`react`·`@supabase` 미사용(NFR-DT-001).
- 잔여: 배속(0.25×~20×) 비례 재계산 및 정지/재개 오프셋(~30일차).

## 29일차 (2026-08-28) — Task 025

- `src/lib/sim/schedule/speed.ts`(신규), `speed.test.ts`(신규 10건). 게이트 그린(tsc 0 / lint 0 / test 1067 passed).
- **배속 변경은 비례(곱셈), 정지/재개는 오프셋(덧셈)으로 분리**했다 — `rescaleKickoffsForSpeedChange`(0.25×~20×) / `rescheduleKickoffsForPauseResume`. 두 함수 모두 "기준 시각 이전(이미 지난) 킥오프는 불변" 규칙을 공유한다.
- **AS-16(동시 종료 정렬)은 리그 일괄 적용으로 구조적으로 보존**된다 — `rescaleLeagueKickoffsForSpeedChange`/`rescheduleLeagueKickoffsForPauseResume`가 여러 리그에 동일 context/window를 적용하므로 `kickoff.ts`가 만든 정렬이 재계산 후에도 유지된다(리그를 따로 재계산하면 깨진다).
- **월드시계 실시간↔월드분 적분 일반식(H-24)은 의도적으로 구현하지 않았다** — 이 모듈은 스케줄 재계산만 좁게 담당. 30일차 소관.
- 기존 `kickoff.ts` 무수정, 신규 파일만 추가. `Math.random`·`Date.now`·`react`·`@supabase` 0건(NFR-DT-001).
- 잔여(30일차, Task 025 마감): H-24 적분 일반식. I-119도 Task 026 내 처리.

## 30일차 (2026-08-31) — Task 025 종료 + I-119

- `src/lib/sim/schedule/worldclock.ts` (+test) — **H-24 월드시간↔실시간 환산 계약**. ⓐ `worldMinutesAt`/`matchElapsedMinutesAt`(순수, `now` 호출자 주입 — `Date.now()` 0건) ⓑ `clockRevision` 기반 `shouldResyncWorldClock`/`classifyWorldClockTransition` ⓒ `applyPause`/`applyResume`(정지 구간 동결, `pausedTotalMinutes` 가산)
- `src/lib/sim/schedule/four-league-scale.test.ts` — 4리그 확장(12팀·130분 합성 리그, NFR-SC-003)
- `src/lib/sim/schedule/speed.ts` — `assertValidSpeedMultiplier`·MIN/MAX export
- `src/lib/sim/match/xg-manager-tendency.ts` (+test) — I-119. 성향별 own/conceded xG 배율 × 숙련도 실현율(`0.6 + 0.4×skill/30`)
- `docs/handoff/H-24-worldclock-realtime-contract.md` — 5팀 인계(35일차 Task 015 소비)

**수락 기준 실측**: 최종 라운드 킥오프 세 리그 전부 `2026-08-28T09:30:00.000Z` — **드리프트 0분**(기준 ≤ 30분). 4리그에서도 0분. 재생성 100% 동일.

**팀장 반려 1건 → 시정 완료**: `xg-manager-tendency.ts`가 `MANAGER_TENDENCY_XG_DEFAULT_TABLE`을 sim 모듈 export 리터럴로 두고 `options?.table ?? DEFAULT`로 폴백 — **I-83(14일차 사용자 승인 확정) 위반**. `gk-fallback.ts` 헤더에 동일 시도가 "오판이었다"로 기록돼 있다. `table`을 필수 필드로 승격하고 성향 키 누락 시 예외로 시정. **반려 시점에 게이트 3종은 전부 통과 상태였다.**

이슈: **I-160**(`MANAGER_STYLE_XG` 공통코드 그룹 부재 — 3팀 소관, 미신설 시 호출 불가).

## 31일차 (2026-09-01) — Task 023 (신설분)

- `src/lib/sim/match/tier-b-resim.ts` (+`.test.ts`, 신규) — `deriveTierBMatchStats`. Tier B 26필드(`NO_EVENT_TYPE`: 패스 8·드리블 4·수비 11·GK 3) `matchSeed` 재시뮬레이션. 11일차 계약 `tier-b-resim-contract.ts`는 미수정.
  - 기댓값 테이블 전량 주입(NFR-CFG-001). `attempted`만 지터, `completed = round(attempted × rate)`로 파생해 `completed ≤ attempted` 구조 보장.
  - 26필드 드리프트 가드 테스트가 계약의 `TIER_B_RESIM_FIELD_NAMES`와 일치 검증(독립 12 + 쌍 14 = 26).
- `src/lib/sim/match/events.ts` (+test) — `ensureStructuralMarkers` 추가(I-65). 4종 결정론적 보정, `occursProbability=0` 최악 케이스 검증.
- 게이트: tsc 0 / eslint 0 / `src/lib/sim/match/` 11 files·167 tests 통과.
- **잔여(I-164)**: 두 함수 모두 소비처 0건. 배선은 오케스트레이션 계층(H-15 이후) 몫이며, `ensureStructuralMarkers`는 `linkPenaltyOutcomes`보다 **앞에서** 호출해야 한다(순서 위반 테스트 동반 필요).

## 32일차 (2026-09-02) — Task 023

- `src/lib/sim/match/events.test.ts` (+66줄, 3 tests) — 구조 마커 삽입 회귀 3케이스
  - HALF_TIME이 FIRST_HALF_STOPPAGE 마지막 틱(45분+추가시간)에 위치
  - 연장 포함 시 FULL_TIME이 EXTRA_SECOND 마지막 틱(120분)에 위치
  - 정위치 아닌 동일 타입 기존 이벤트를 마커로 오인하지 않고 별도 삽입(I-65 재발 방지)
- 32일차 나머지 3항목(결정론 재현·그룹 불변식·26필드 커버리지)은 31일차 `tier-b-resim.test.ts`에 이미 구현돼 있어 재확인만 — 팀장이 describe/it 전수 대조로 확인
- 테스트: 3 files / 58 tests 통과, `npx tsc --noEmit` 0건
- I-164 미착수(오케스트레이션 계층 몫) — 배선 전제 테스트도 작성하지 않음
