# 5팀 — 화면·배팅 UX팀 — 산출물 기록

> 위치: `docs/team-schedule/outputs/05-화면배팅UX팀.md` / 본체: [`05-화면배팅UX팀.md`](../05-화면배팅UX팀.md)
> 이 파일은 **일차별 산출물·조정 기록의 누적처**입니다. 팀 md 본체를 가볍게 유지하기 위해 분리했습니다.
> **팀원 소환 시 열람 대상이 아닙니다** — 팀원은 본체 md의 소유 경로·해당 일차 행만 봅니다.
> 새 기록은 이 파일 끝에 append 하세요.

---

## 1. 요약 (1~13일차)

| 항목 | 값 |
|---|---|
| 참여 일차 | 1~4·7~9·11·13일차 (실작업 9일) — 5·6·10·12일차는 업무 없음(대기 구간) |
| 담당 Task | 정규 Task 없음 — 013B·015~021·039·040은 모두 33일차 이후 착수 예정. 1~13일차는 전부 팀 md §4 **대체작업**과 2건의 한시 소환 후속조치 |
| 주요 산출물 | `docs/wireframe/00-공통규약.md` ~ `10-어드민공통코드-폼스펙.md` (총 11개 파일) |
| 종료된 Task | 없음 |
| 진행 중 | §4 대체작업 11~14일차 구간(어드민 콘솔 폼 스펙, 36그룹) — 13일차 완료(36/36). 15일차부터 배팅 UX 사전설계 구간으로 전환 예정 |
| 미해소 이월 | W-02(`MatchCard` 승격 검토) → 27일차 SP-2 정식 안건으로 이월 |

## 2. 일차별 기록

### 1일차 (2026-07-21, 화) — §4 대체작업
- **한 일**: 화면 8종 ↔ 라우트/UC/FR/Task/구현일차 매핑표, 전 화면 공통 9절 포맷 규약 확정. 와이어프레임 00(공통규약)·01(홈-라이브센터)·02(리그 순위표) 작성. 시각 토큰은 4팀 H-11(27일차) 이후로 명시 유보
- **산출물**: `docs/wireframe/00-공통규약.md`(208행), `01-홈-라이브센터.md`(172행), `02-리그-순위표.md`(171행) (신규)
- **판정**: 완료
- **특기**: 확인요청 18건 제기(해소 4 / 27일차 SP-2 이월 1(W-02) / 통상 12 / 긴급 0). `OddsButton` 컴포넌트 표 분류 오류를 자체 발견해 미등록 처리로 직접 정정. 경과분 배속 환산 문제를 짚어 **H-24(월드시간↔실시간 환산 계약) 인계물 신설의 계기**를 마련(이날 교차 점검 최대 성과) — 다만 "매초 tick" 자체는 버그가 아니었고 실제 결함은 경과분 출처 미명시·재동기화 누락 2건이었음을 5팀 스스로 정정 보고. `H-24 ③`→`②` 항 번호 오참조 1건을 자체 대조로 잡음
- 출처: `docs/dailyWorkLog/1Day.md`

### 2일차 (2026-07-22, 수) — §4 대체작업
- **한 일**: 와이어프레임 03(일정·결과)·04(경기상세·라이브중계) 작성, 체크리스트를 자기 화면에 적용해 결함 점검
- **산출물**: `docs/wireframe/03-일정-결과.md`(167행), `04-경기상세-라이브중계.md`(294행) (신규)
- **판정**: 완료
- **특기**: 체크리스트 자체 적용 중 **NFR-SEC-004 축 전체 부재**(→ C-23 신설, I-26), **C-2(`Math.random`/`Date.now` 금지) 적용 경로 미명기**(→ `src/lib/sim/` 한정으로 명확화, I-27), **D-19(승부차기) 매핑에 UI Task 016·017 누락**(I-28) 3건을 발견·제보해 당일 해소. 팀장 결정 **D2-01**(세그먼트 `layout.tsx` → 5팀 소유), **D2-02**(`match.ts`·`bet.ts` 골격은 4팀·문구 값은 5팀)이 이날 5팀 관련으로 확정. `GOAL`/`ASSIST` 깜빡임 실패 모드(도움 없는 골 표시) 지적. 자체 이슈번호(I-20~22)로 제안했으나 2팀 제안분과 겹쳐 **I-26~28로 재채번**(5팀 문서 내 인용 없어 정정 대상 없음)
- 출처: `docs/dailyWorkLog/2Day.md`

### 3일차 (2026-07-23, 목) — §4 대체작업
- **한 일**: 와이어프레임 05(선수 상세, 9섹션)·06(클럽 상세, 8섹션) 작성. 대기 구간 원칙상 정지 대상이나 1·2일차 연속성을 위해 진행 확인받음
- **산출물**: `docs/wireframe/05-선수상세.md`, `06-클럽상세.md` (신규)
- **판정**: 완료
- **특기**: 04에서 이미 제기한 **결과 역산 위험(S-4, 선수 평점 누출)과 동일한 위험이 05·06에도 존재**함을 지적(W-34). **I-35**(와이어프레임 01~04 엔티티 ID가 정본과 불일치) 발견 — 4일차로 정정 이월
- 출처: `docs/dailyWorkLog/3Day.md`

### 4일차 (2026-07-24, 금) — §4 대체작업
- **한 일**: 와이어프레임 07(어드민 운영콘솔)·08(어드민 공통코드·스케줄러) 작성(어드민 3종, 영역 G1~G6/H1~H5/J1~J4) + **I-35 정정**(01~04 엔티티 ID를 정본에 맞춤)
- **산출물**: `docs/wireframe/07-어드민-운영콘솔.md`, `08-어드민-공통코드-스케줄러.md` (신규), `01~04` 정정
- **판정**: 완료 (I-35 당일 해소)
- **특기**: **이날 최대 발견** — I-35 정정 작업 중 **1팀 코드 주석의 E-번호 드리프트 5건**을 발견(5팀→1팀). "아직 주석 단계라 지금 고치면 재작업 비용 0"이라는 타이밍 판단이 정확해 하루 늦었으면 실제 인터페이스가 잘못된 번호로 생성될 뻔했음. 04 D5(팀 스탯 비교) 정정 중 **팀×경기 축 엔티티 부재**(W-38)를 발견해 9일차 Task 004 착수 항목으로 고정시킴. 07/08 분할 시 팀장이 예시로 제시한 매핑(07=콘솔+scheduler, 08=config)이 1일차 확정 규약(07=`/admin`, 08=config+scheduler)과 달랐는데, **5팀이 팀장 지시 대신 기존 규약을 지킨 것이 옳았음**(팀장 판단 오류 §7.2로 기록). shrimp `plan_task` 누락 지적받아 5일차부터 선행 적용
- 출처: `docs/dailyWorkLog/4Day.md`

### 5일차 (2026-07-27, 월) — 업무 없음 (대기 구간, 1~27일차)
- 출처: `docs/dailyWorkLog/5Day.md`

### 6일차 (2026-07-28, 화) — 업무 없음 (팀장 호출 누락)
- **특기**: 팀장이 이날 5팀을 "유휴 구간"으로 오판해 미호출. 실제로는 §4 대체작업이 `1~5`/`6~10`/`11~14`… **구간 단위 배정**이라 6일차부터 문구 작업이 시작됐어야 했음(3·4·6팀의 통짜 대기 구간과 혼동). 7일차에 팀장이 이 오류를 자체 확인, 7~10일차를 6/6/6/5건 분할로 재계획해 10일차까지 이벤트 23종 완료 목표로 흡수(5팀 자체 27일 유휴 여유로 일정 영향 없음)
- 출처: `docs/dailyWorkLog/6Day.md`, `docs/dailyWorkLog/7Day.md` §4.2

### 7일차 (2026-07-29, 수) — §4 대체작업
- **한 일**: 이벤트 23종 중계 문구 템플릿 초안 착수(#1~6: KICKOFF·SHOT_ON·SHOT_OFF·SHOT_BLOCKED·GOAL·ASSIST, ko/en). 원래 6일차 배정분이나 팀장 호출 누락으로 7일차 시작
- **산출물**: `docs/wireframe/09-이벤트중계문구.md` (신규 — 문구 작성 규약 + #1~6 템플릿)
- **판정**: 완료
- **특기**: D1 스코어보드가 `GOAL`/`OWN_GOAL`/`PENALTY_SCORED` 3계열을 **fold**해 스코어를 산출하는 구조를 근거로, PK 득점 시 `GOAL`·`PENALTY_SCORED` 이중 발생하면 "1골이 2골로 집계"되는 결함을 지적하고 우선순위 격상 요청 → 2팀이 5팀 로직을 역산해 "PK 득점은 `PENALTY_SCORED` 단독이어야 한다"를 도출 → 팀장이 **I-43**으로 확정(세 팀이 서로 다른 경로로 같은 결함에 도달한 이날의 최대 성과). 승부차기는 킥마다 별도 `MatchEvent`(**I-44**)로 확정되어 문구를 "단일 요약문"이 아닌 "킥 단위 반복 문구"로 사전 조정. `CardReason` enum 신설을 제기했으나 요구사항에 사유 taxonomy 근거가 없어 **신설 보류**(I-41 OPEN) — TBD 슬롯으로 진행(자유문자열 임시 가정 금지)
- 출처: `docs/dailyWorkLog/7Day.md`

### 8일차 (2026-07-30, 목) — §4 대체작업 + SP-1
- **한 일**: 이벤트 문구 #7~12 계속 + **SP-1(타입 동결 전 전 팀 참여 리뷰)** — 015~018·021·039·040·어드민 화면 기준으로 `src/types/**` 역산 대조
- **산출물**: `docs/wireframe/09-이벤트중계문구.md`(#7~12 + SP-1 §11 + 확정 4~6 §9.1)
- **판정**: 완료
- **특기**: SP-1 반영요청 **0건**(5개 팀 전원 공통). 1팀이 발견한 **E-45~47**(`CronRun`/`CronGap`/`AuditLog`) 정의 누락에 대해 5팀도 "본체가 비어있어 Task 058 `/admin/scheduler`가 막힐 뻔했다"고 **독립적으로 같은 결론**에 도달. 문구 #7~12 작성 중 만든 가정 3건을 2팀이 검증 — **I-53**(자책골도 `GOAL` 타입 유지·`teamId`=득점귀속팀, 5팀 근거는 채택되고 2팀이 엔진 스탯 정합성이라는 더 강한 근거를 보강) 확정, **I-54**(`PENALTY_AWARDED` 참조 방향을 결과→선언으로 정정, **5팀 가정 중 유일하게 틀렸던 건**) 확정. 팀장이 예정 이슈번호(I-47~49)를 잘못 사전 전달해 5팀 문서에 오기가 남았다가 1팀 교차 점검에서 정정됨
- 출처: `docs/dailyWorkLog/8Day.md`

### 9일차 (2026-07-31, 금) — §4 대체작업
- **한 일**: 이벤트 문구 #13~18(ko/en) 작성. `FOUL` 작성 시 `PENALTY_AWARDED.secondaryPlayerId` 해석과 겹치는지 자체 확인
- **산출물**: `docs/wireframe/09-이벤트중계문구.md` 외 정정 3종(01·03·04)
- **판정**: 재작업 1회 후 완료
- **특기**: **결함 — 와이어프레임 필드명 오표기 `stoppage` → `addedTime`.** 2팀 `tick.ts`와 대조하다 2일차 원문 오류를 자체 발견해 04번 §E-3만 1차 정정했으나, **팀장 2차 검증에서 "부분 정정"임을 재적발** — 같은 파일 2곳 + 이를 인용하는 01·03번 문서 2곳이 안 고쳐진 채 남아 오히려 문서 간 불일치를 새로 만든 상태였음 → 4곳 전량 정정 완료(`grep` 0건 확인). **I-59** 제기(`FOUL`/`PENALTY_AWARDED` 스탯 이중집계 우려) → **I-60**으로 이관·해소. `CronRunMetrics` ↔ `/admin/scheduler` J3 4필드 1:1 일치 확인. 어드민 쓰기 조작 4건(배속 변경/정지·재개/월드 리셋/공통코드 저장)이 `DataSource`에 없어 별도 경로가 필요함을 지적(Task 021 인계 사항)
- 출처: `docs/dailyWorkLog/9Day.md`

### 10일차 (2026-08-03) — 업무 없음 (대기 구간, 1~27일차)
- 출처: `docs/dailyWorkLog/10Day.md`

### 11일차 — 후속조치 한시 소환 (정규 업무 없음)
- **한 일**: 정규 업무는 없으나 후속조치 2건으로 한시 소환 — ① **C-03a 해소**: `UI_PARAM` 키를 UPPER_SNAKE(`POLL_INTERVAL_MS`/`POLL_LIVE_MS`/`LEADERBOARD_MIN_APPEARANCE_PCT`)로 확정, dot-notation 폐기(I-76 반영) ② **H-18(6팀 인증·지갑·RLS) 순연 판정** 대응
- **산출물**: `docs/wireframe/00-공통규약.md`(C-03a 갱신)
- **판정**: 완료
- **특기**: 2팀 Tier B 재시뮬레이션 신설(D-30·D-31)로 하류 6팀 037·038이 2일 순연 → **5팀 H-18 수신이 60일차→62일차로 밀림**, 039=62~69·040=70~75일차로 **1:1 그대로 전이**(5팀은 slack 0인 크리티컬 패스라 자체 버퍼로 흡수 불가) → **프로젝트 종료일 79→81일차로 전체 슬립**. 5팀 039/040 스코프 축소 회수안은 데이터 정합성·기능 범위 우선 원칙으로 **기각**. L-02 규제 게이트-H-18 사이 완충일 0 문제는 제안만 하고 미적용으로 남김. `00-공통규약.md` 소유권이 4팀으로 오배정돼 있던 것을 4팀이 편집 전 확인 요청해 잡았고(작업물 무오염), 5팀 소유로 바로잡음. shrimp `clearAllTasks` 공유 인스턴스 충돌 위험은 사전 금지 지시로 회피
- 출처: `docs/dailyWorkLog/11Day.md`

### 12일차 — 업무 없음
- 출처: `docs/dailyWorkLog/12Day.md`

### 13일차 (2026-08-06) — §4 대체작업 (11~14일차 구간)
- **한 일**: 어드민 콘솔 폼 스펙 작성 — 36개 공통코드 그룹의 편집 UI 유형(숫자/불리언/JSON/범위) 매핑, 3팀 H-05 카탈로그(당일 수신 예정일) 구조를 참고해 확정
- **산출물**: `docs/wireframe/10-어드민공통코드-폼스펙.md` (신규, 36그룹 전량 — 커버리지 36/36)
- **판정**: 재작업(당일 정정) 후 완료
- **특기**: **결함 — `CUP_PARAM` 분기 신호 부재(폼 스펙 근거 오류).** 5팀이 "코드별 `valueNum`/`valueJson` null 여부로 위젯 분기"라고 적었으나, **3팀이 `catalog.ts:484-495`와 E-42 CHECK 제약(그룹 전체 `valueType` 기준)으로 반증** — `CUP_PARAM` 그룹은 `valueType`이 `'JSON'` 단일이라 그룹 내 모든 코드가 `value_json` NOT NULL / `value_num` 항상 NULL이며, **분기 신호 자체가 애초에 존재하지 않음**(실제 가능한 분기는 파싱된 JSON의 shape 판정뿐, 3팀 `fallback.ts:258-261`이 이미 그렇게 처리). 교차 점검이 없었으면 Task 021(56일차)에서 동작하지 않는 스펙을 그대로 구현할 뻔했음. 5팀이 `fallback.ts`를 직접 열어 재확인 후 §4.2·§5·§7 정정 완료. min/max 활성화 시점도 "11일차+37일차"가 아니라 **37일차 단독**임을 함께 정정. 팀장이 5팀 문서의 갱신 이력 날짜 오기(14일차→13일차, 본문 3곳 포함)를 정정
- 출처: `docs/dailyWorkLog/13Day.md`

## 3. 인계물 (H-*)

| 인계 | 방향 | 일차 | 상태 |
|---|---|---|---|
| H-24 신설 계기(경과분 산출·배속 재동기화 계약 필요성 지적) | 5팀 발견 → 2팀 제공(예정) | 1일차 발견, 2팀 30일차 제공 예정 | 1~13일차 구간은 발견·등록만, 미수신 |
| H-05(공통코드 로더 — 폴링 주기·배속 범위·36그룹 스키마) | 3팀 → 5팀 | 13일차 예정 수신일 | 13일차 폼 스펙 작업은 정식 수신 전 카탈로그 구조 참고로 선행 착수, 이후 3팀과의 반증 대조(§13일차 CUP_PARAM 건) 발생 |
| H-18(인증·지갑(037)+RLS(038)) | 6팀 → 5팀 | 원 60일차 → 62일차로 2일 순연(D-31) | 11일차엔 순연 판정 대응만, 실수신 아님 |

(H-13·H-20·H-23은 5팀이 **제공**하는 인계물이나 발생 시점이 33일차 이후라 1~13일차 구간 해당 없음)

## 4. 이 팀이 관여한 이슈·결정

| ID | 내용 | 일차 | 상태 |
|---|---|---|---|
| I-26 | NFR-SEC-004 축 전체 부재 → C-23 신설 | 2일차 | 해소 |
| I-27 | C-2(`Math.random`/`Date.now` 금지) 적용 경로 미명기 — `src/lib/sim/` 한정 명확화 | 2일차 | 해소 |
| I-28 | D-19(승부차기) 매핑에 UI Task 016·017 누락 | 2일차 | 해소 |
| I-19 | PK(승부차기 등) 브랜드 ID 생성 책임 소재 — 5팀 R-13(와이어프레임 규약) 실효성 직결 | 2일차 언급 (1팀 5일차 처리 예정) | OPEN(2일차 기준) |
| I-35 | 와이어프레임 01~04 엔티티 ID가 정본과 불일치 | 3일차 제기 → 4일차 해소 | 해소 |
| (번호 미부여) E-번호 드리프트 5건 | 1팀 코드 주석 E-번호가 정본과 어긋남 — 5팀이 자기 작업 중 발견 | 4일차 | 해소(당일) |
| I-43 | PK 득점 시 `GOAL`·`PENALTY_SCORED` 이중발생 여부 — 5팀 fold 로직 지적으로 우선순위 격상, `PENALTY_SCORED` 단독 발생 확정 | 7일차 | 확정 |
| I-44 | 승부차기는 킥마다 별도 `MatchEvent`(반복 발생) | 7일차 | 확정 |
| I-41 | `CardReason` enum 신설 제기 | 7일차 | 보류(OPEN) — 요구사항 근거 부족 |
| I-53 | 자책골도 `GOAL` 타입 유지, `teamId`=득점귀속팀 (5팀 fold 일관성 근거 채택 + 2팀 엔진정합성 근거 보강) | 8일차 | 확정 |
| I-54 | `PENALTY_AWARDED` 참조 방향(결과→선언) 정정 — 5팀 가정 3건 중 유일하게 틀렸던 건 | 8일차 | 확정 |
| I-59 → I-60 | `FOUL`/`PENALTY_AWARDED` 스탯 이중집계 제기 → I-60으로 이관 | 9일차 | 해소 |
| (번호 미부여) `stoppage`→`addedTime` 오표기 | 와이어프레임 필드명 오표기 — 1차 정정 미완(부분정정)을 팀장이 재적발, 4곳 전량 정정 | 9일차 | 해소(재작업) |
| I-76 | `UI_PARAM` 키 UPPER_SNAKE 확정(dot-notation 폐기) — 5팀이 `00-공통규약.md` C-03a 갱신 | 11일차 | 해소 |
| (번호 미부여) `CUP_PARAM` 분기 신호 부재 | 5팀 자기 문서 제기(폼 스펙 근거 오류) → 3팀이 `catalog.ts`·E-42 CHECK로 반증, 5팀이 재확인 후 정정 | 13일차 | 해소(재작업) |

---

## 5. 확인 못 한 것

- 없음. 1~13일차 작업 로그 원문과 대조해 전 항목을 확인했습니다.

---

## 27일차 (2026-08-26) — SP-2 분할 합의 (판단만, 구현 없음)

- **SP-2 배분은 4팀 초안과 완전 일치해 이견 없이 확정.** 소속이 §1 소유 경로(`domain/`·`state/`=4팀 / `composite/`=5팀)로 이미 고정돼 있어 재배분 협상이 아니라 **대조 확인** 성격이라는 점을 먼저 정리한 것이 합의를 짧게 끝냈다.
- **W-02 `MatchCard` 승격 동의(21→22)** — 홈 A2-c·클럽상세 F8·일정결과 C2-r 3곳이 "팀A vs 팀B + 스코어/시간 + 상태" 동일 모델이라 `density:"card"|"row"` 단일 prop으로 감당 가능. 단 **LIVE 배지·경과분(H-24)은 카드형 전용**이라 조건부 렌더 분기 필요. 공수는 4.25인일에 흡수하지 말고 **+0.6~0.7인일 증분**(2모드 × 3소비처 동기화 비용).
- **W-31 F4-p 차트는 화면 로컬 유지 동의** — 클럽상세 1곳 전용, 카탈로그 등재 시 013B 공수만 늘어난다.
- **차트 라이브러리(→ I-152)**: 신규 의존성 도입을 보류하고 **1차 자체 SVG**로 시도하기를 희망. 두 차트 모두 단순 라인/막대라 손코딩 범위이고, 013B 28~33일차 6일 일정에 설치·번들·React Compiler 호환 검증 리스크를 얹고 싶지 않다. 부족하면 **31일차에 recharts 폴백** 판정.
- **H-12는 013B가 아니라 015(34일차) 착수 조건**임을 지적해 28일차 인계물에서 제외시켰다 — 잘못 기다렸으면 착수가 지연될 뻔했다.
- 28일차 013B 착수 조건(H-11) 수령 완료: 프리미티브 8종·토큰·`cn()`. 미도입 7종은 shadcn MCP로 자율 추가 가능(기록만).

## 28일차 (2026-08-27) — Task 013B 복합 2종 (착수, 실구현 첫 일차)

- `src/components/composite/{EventTimelineItem,NewsItem}.tsx` + `types.ts` — 전부 신규. `src/i18n/messages/{ko,en}/match.ts`에 `timeline`·`news` 그룹 추가. 최종 게이트 그린(tsc 0 / lint 0 / test 1025 passed).
- `EventTimelineItem`은 `MatchEventType` **23종 전수**를 배지 variant에 매핑하고 `satisfies Record<MatchEventType, …>`로 강제 — 이벤트가 늘면 컴파일이 깨진다.
- 팀·선수를 ID로만 들고 있는 `MatchEvent` 특성상 **이름 해석과 `relatedEventSequence`(ASSIST→GOAL 연결)는 리스트 컨테이너 책임**으로 두고 표시명을 prop으로 받는다 — 단일 아이템 범위 밖이라는 판단, 팀장 수용.
- 뉴스 도메인 타입이 없어 `NewsItemData` 로컬 선언, `news` 번역 그룹은 `match.ts`에 임시 거주(사유 주석 기재) → **I-154**.
- **팀장 검증에서 2건 재수정**했다(둘 다 게이트로는 안 잡히는 팀 간 규약 발산):
  - ① 4상태를 펼친 props + `"success"`로 갔던 것을 4팀 013A와 동일하게 **`state: CompositeViewState<T>` 단일 prop + `"ready"`** 로 정렬. `domain/types.ts`를 import하지 않고 동형 선언 — H-12가 34일차/015 인계라 인계 전 크로스 의존을 만들지 않기 위함(I-156).
  - ② 인터랙션이 0건인데 붙어 있던 **`"use client"` 제거 → `t(locale, key)` 직접 호출**. 011 규약상 `useTranslation()`은 클라이언트 트리 전용이고, 방치하면 소비처가 자동으로 클라이언트 경계에 들어가 메시지 카탈로그가 번들에 딸려 올라간다.
  - **교훈(정정 기록)**: ①의 완료 보고에 ②·③ 처리 결과가 없어 팀장이 파일을 확인하고 재요청했는데, 5팀은 그때 이미 ②·③ 작업 중이었고 **메시지가 교차**한 것이었다(5팀 정정 제기 → 팀장 확인 후 수용, 재요청은 불필요했다). 남는 규칙은 **피드백이 여러 항목일 때 중간 보고에도 항목별 상태(완료/진행 중/미착수)를 적는 것** — 팀장 쪽도 미반영을 곧장 "누락"으로 단정하지 않기로 했다.
- **잔여 6종**(PitchLineup, BracketTree, TrophyCase, GrowthChart, InjuryTimeline + MatchCard)은 33일차까지. **새 컴포넌트는 처음부터 서버 컴포넌트 + `t(locale, …)` + `state` 단일 prop**으로 시작할 것.

## 29일차 (2026-08-28) — Task 013B

- `composite/PitchLineup.tsx`(신규), `PitchLineup.test.ts`(신규 12건). i18n: `messages/{ko,en}/match.ts`에 `lineup` 그룹(6키, ko/en 대칭).
- **7종 포메이션 렌더**: 4-4-2 / 4-3-3 / 4-2-3-1 / 3-5-2 / 3-4-3 / 4-5-1 / 5-3-2. 각각 GK 포함 11슬롯·유효 좌표·유효 `Position`을 테스트가 단언.
- **`Formation`(src/types)이 값 미확정 `string`이라 도메인 타입을 재선언하지 않고 로컬 UI 상수 `FORMATION_LAYOUTS`로 처리**했다(C-5/C-6 위반 회피). 팀장 판정: 타당. 목록 밖 문자열은 방어 상태(`unsupportedFormation`)로 렌더.
- 렌더 로직을 순수 함수 `resolvePitchSlots`로 분리 — `.tsx` 렌더 테스트가 불가한 상황(I-151)에서의 커버리지 확보 수단.
- **28일차 지적(서버 컴포넌트 · `t(locale, …)` · `state` 단일 prop)이 1차 산출물부터 지켜졌다** — 같은 지적 재발 0건.
- 게이트 그린(tsc 0 / lint 0 / test 1067 passed).
- 신규 이슈: **I-158**(`Position`에 측면MF·윙백 코드 부재 — 자진 보고, 1팀 판정), **I-157**(피치 좌표계 발산 — 4팀과 공동, 015/34일차). I-158 판정 전까지 현 `LW`/`LB` 재사용 유지.
- 잔여(~33일차): BracketTree, TrophyCase, GrowthChart, InjuryTimeline, MatchCard. 차트류는 자체 SVG 1차(I-152, 31일차 판정).

## 30일차 (2026-08-31) — Task 013B BracketTree

- `src/components/composite/BracketTree.tsx` (+test) — 플레이오프·컵 공용 넉아웃 트리(4팀 020이 소비). 013B **4/7종**
- **가변 라운드 수 대응** — 라운드 배열 길이로 컬럼 수 결정. 부전승(라운드 간 매치 수 불일치) 케이스 테스트 커버
- **D-19 준수** — 정규 스코어/승부차기 스코어 **분리 필드**, 합산 없이 별도 표시. 승자 판정은 순수 함수 `resolveBracketWinnerSide`(명시 winner → 정규 → PSO 우선순위)
- 차트 라이브러리 미도입(I-152, 31일차 판정)이라 자체 DOM/CSS 커넥터로 1차 구현
- 도메인에 브래킷 타입이 없어 `TeamId`/`FixtureId`만 재사용, 트리 구조는 로컬 타입(타입 재선언 아님)
- `src/i18n/messages/{ko,en}/match.ts`에 `bracket` 그룹 추가 — 28일차 `timeline`·`news`와 동일 선례(파일 헤더 기재). 전용 네임스페이스 분리는 **I-161**(020 시점, 4팀 주도)
- I-157(좌표계)·I-158(`Position`) 비관여 — 판정 전까지 현 코드 재사용 유지

잔여 4종: `TrophyCase`, `GrowthChart`(**I-152 판정 선행 필요**), `InjuryTimeline`, `MatchCard`.

## 31일차 (2026-09-01) — Task 013B (6/7종)

- `src/components/composite/GrowthChart.tsx` (+`.test.ts`, 신규) — `PlayerAttributeHistory` 시계열 OVR 라인. **FR-PL-004 준수** — 읽는 필드가 `seasonNumber`/`ovr`뿐이라 `pa` 구조적 미노출.
- `src/components/composite/InjuryTimeline.tsx` (+`.test.ts`, 신규) — `Injury` 라운드 축 구간 막대, severity/status Badge 병기.
- 둘 다 **자체 SVG**, 좌표 계산은 순수 함수로 분리(BracketTree·PitchLineup 선례), 4상태(loading/empty/error/ready) 전부 구현.
- `src/i18n/messages/{ko,en}/player.ts` — `growthChart`·`injuryTimeline` 그룹 추가.
- **I-152 판정 확정: ⓐ 자체 SVG 충분 — recharts 미도입**(런타임 의존성 8개 유지). 근거: 단일 시리즈/구간 막대라 폴리라인·rect 좌표 계산으로 완결, 줌·범례·다중시리즈 요구 없음. 27일차 방침의 ⓑ 폴백 미발동.
- 게이트: tsc 0 / 신규 23건 통과 / 전체 1179 passed(당시 시점, 회귀 0) / lint 0.
- **잔여(I-165)**: 부상 상태 표시명이 `player.injuryTimeline.status*` 로컬 키에 있음. 당일 3팀이 정본 `enums.injuryStatus`를 신설했으므로 32일차에 소비처 교체 + 로컬 키 제거.
- 013B 진척: **6/7종** — `TrophyCase`만 잔여.

## 32일차 (2026-09-02) — Task 013B 종결 + I-165

- `src/components/composite/TrophyCase.tsx`(신규) + `TrophyCase.test.ts`(신규) — **013B 복합 7종 종결**
  - `Trophy`(필수) + `Award`(optional) 동시 수용 → 클럽 상세(F7)·선수 상세(E9) 분기 없이 커버
  - `TrophyType`별 그룹핑(count + 시즌 라벨)은 순수 함수로 분리해 테스트
- `src/components/composite/InjuryTimeline.tsx` — **I-165 해소**: `enums.injuryStatus.*` 직접 경유로 교체
- `src/i18n/messages/{ko,en}/player.ts` — `injuryTimeline.status{Active,Recovered}` 로컬 키 제거
- `src/i18n/messages/{ko,en}/team.ts` — `trophy` 그룹 추가
- 013B 7종 규약 감사: 위반 0건
- 테스트: 10 files / 92 tests 통과, `npx tsc --noEmit` 0건, `npm run lint` 0건
- 이슈: **I-166** — `TrophyType` 정본 카탈로그 부재로 `team.trophy.type.*` 로컬 키 우회 중(3팀 신설 대기)

## 33일차 (2026-09-03) — 013B 규약 리팩터·수락 실증 (013B 종결)

- 7종 전수 확인 → **리팩터 코드 변경 0건**(사전 충족): `useMemo`/`useCallback` 0건, 4상태 7/7, 하드코딩 0건
- `overflow-x-auto`는 유일 대상 `BracketTree`에 이미 적용(로딩 skeleton·ready 양쪽). 나머지 6종은 svg viewBox·flex-wrap로 자체 스케일
- 하드코딩 실측: JSX 텍스트노드 + `title`/`aria-label` 속성 전수 grep 0건
- **I-166 소비 전환**(팀장 추가 지시, 3팀 카탈로그 납품 후): `TrophyCase.tsx:158` → `enums.trophyType.${type}`, 23~27행 주석 갱신
- **소유 경계 준수**: 사장된 `team.trophy.type.*`가 4팀 소유 `team.ts`에 있어 **직접 삭제하지 않고 판단만 회신** → 4팀이 제거. 절차상 정확한 처리
- **H-13 인계**(→4팀 `/sample` 등록, 전부 `src/components/composite/` named export): `BracketTree` · `EventTimelineItem` · `GrowthChart` · `InjuryTimeline` · `NewsItem` · `PitchLineup` · `TrophyCase`

## 34일차 (2026-09-04) — Task 015 홈 라이브 카드 그리드 (34일차분)

- `src/components/composite/MatchCard.tsx` / `.test.ts` (신규 — **013B 잔여 1종 종결, 22종 전량 완료**)
- `src/app/[lang]/page.tsx`(자리표시자 → 카드 그리드) / `src/i18n/messages/{ko,en}/match.ts`(`card` 그룹, `live.label` 재사용)
- `MatchCard` — named export, 서버 컴포넌트, **I-156 단일 prop 규약 준수**(`state: CompositeViewState<MatchCardData>`, 리터럴 4상태). `density:"card"|"row"`로 목록형 재사용 여지 확보(오늘은 card만)
- 데이터는 `getDataSource()`(`getLiveFixtures`/`getTeamsByIds`/`getLeagues`)만 경유 — Mock 팩토리 직접 호출 0건
- 실렌더 확인(팀장, 재기동 후): `/ko` `/en` 200, 카드 3장, 스코어 `0-1`/`3-0`/`3-0`, 라이브 배지 정상
- **이탈 1건(팀장 지지)** — 경과분 미표시(`elapsedMinutes: null`): H-24 계약상 UI가 `(now − kickoff)`를 계산하지 않는데 `now` 출처가 없음. ⓐ `DataSource`에 시각 메서드 부재 ⓑ `MOCK_NOW` 직접 import는 `eslint.config.mjs:82` 위반(21일차 결함 A) ⓒ `Date.now()`는 Mock 세계 시각과 어긋나 음수. **가드레일을 뚫는 대신 기능 축소** — `computeElapsedMinutes` 순수 함수는 테스트까지 완비 → I-169
- **I-157(피치 좌표계)**: 015 범위에 피치가 없어 미착수 유지 — 017 라인업 뷰 시점으로 이월

## 36일차 (2026-09-08) — Task 015 폴링 + I-182 해소

- `src/app/api/live/matches/route.ts`·`types.ts`(신규), `LiveMatchGrid.tsx` fetcher를 `fetch()` 경유로 교체
- 팀장 실측: API 200 / 클라 번들에 `bootstrap`·`factory`·`mock`·`supabase` 0건 / 탭 활성 40s 1건·비활성 40s 0건
- 실운영 5초 주기는 6팀이 031a 시드 적재 후(현재 안전망 30초)
- 임시 타입 `LiveMatchesApiResponse` — 1팀 정식 계약 확정 시 교체(파일 헤더 명시)
- 디자인(Task 013C) 미변경 유지 확인

## 37일차 (2026-09-09) — Task 015 4상태 + DC-07

- `MatchCard` — 로딩 스켈레톤을 실제 카드 구조(헤더/팀2행/푸터)로 재구성해 CLS 축소, Empty에 `emptyNextKickoffAt`(DC-07 `formatKickoff` 변환), Error에 `onRetry` + 재시도 버튼
- `LiveMatchGrid` — `initialCards` 부재 시 6장 스켈레톤 그리드, `nextKickoffAt` prop 신설. 재시도는 **1팀 소유 폴링 훅을 건드리지 않고 `key` 리마운트**로 구현
- i18n `card.emptyNextKickoff` 신규, 재시도 라벨은 신설 없이 `error.generic.retryLabel` 재사용
- **지시 밖에서 발견·수정한 결함 1건** — 기존 empty/error가 board 표면 위에서 `text-muted-foreground` 등 페이지 토큰을 써 대비가 역전(Floodlit 규약 위반). board 전용 톤으로 교체, 팀장이 diff 전수 검색해 잔존 위반 0건 확인
- **36일차 관찰 "다음 킥오프 전부 오전 12:00" 원인 확정 — mock 데이터 문제, DC-07 무죄.** `world.ts:182` `MOCK_EPOCH_NOW`가 KST 정각 00:00이고 `progress.ts:917` 등 오프셋이 1440분 배수라 time-of-day 불변. **3팀 소유 파일이라 수정하지 않고 판단만 회신**, 팀장이 직접 재현해 확정 → I-195
- 테스트: typecheck/lint 통과, `npm run test` 1374 passed. dev 서버 curl로 `/ko`·`/en` 200 + `data-status="ready"` 8장 확인
- 이슈: I-195

## 38일차 (2026-09-10) — Task 015 검증 (모바일 세로 우선 · 6뷰포트 × 2로케일)

- **코드 변경 0건** — 화면은 34~37일차에 이미 완성돼 오늘은 순수 검증만 수행
- **320px 가로 스크롤 0** — 6뷰포트 전부 `scrollWidth === clientWidth`, ko/en 공통 → 통과
- **LCP** 전 12케이스 최대 **268ms**(기준 2.5s) → 통과. SSR `initialCards`가 즉시 라이브 카드를 그려 PS-1(3초 내 파악)도 사실상 즉시 충족
- **폴링·탭 비활성 중단** — 마운트 시 `/api/live/matches` 1회(≈0.6s) → hidden 32초간 추가 0건 → visible 복귀 즉시 1건. `usePollingList`(1팀 H-02) 계약대로 → 통과
- **CLS만 미달** — `ko` 320px에서 0.1642~0.50 반복 재현(기준 0.1), `en`·나머지 5뷰포트는 통과. 시프트 소스가 `LiveMatchGrid` 그리드·A1 세계시계·`H1`·`MatchCard` scoreboard에 동시 발생하는 전형적 웹폰트 스왑 패턴 → **원인이 루트 레이아웃 폰트 선언(4팀 소유)이라 직접 고치지 않고 판단만 회신**, 팀장이 ko 0.1735 / en 0으로 재현 확정 → **I-200**
- 부수 관찰 — 콘솔에 `UI_PARAM 공통코드 조회 실패 → 30000ms 폴백` 경고 반복. `polling.ts` 헤더(I-77)에 의도된 안전망으로 문서화돼 신규 결함은 아니나, **정상 폴링 5초가 여전히 미적용**이라는 뜻(6팀 시드 적재 선행)
- 스냅샷 12장: `/tmp/task015-snapshots/{ko,en}-{320,375,768,1024,1440,1920}.png`
- 테스트: `src/app/[lang]` 3 passed · 팀장 전체 스위트 1407 passed · typecheck·lint 클린
- 이슈: I-200

## 39일차 (2026-09-11) — Task 016 순위표

- `src/app/[lang]/leagues/[leagueId]/page.tsx` — `params` JSON 자리표시자 → 실화면. B1 간이헤더 + B2 존범례 + B3 순위테이블
- 신규(5팀 소유 `composite/**`): `StandingsTable.tsx` · `ZoneLegend.tsx` · `standings-zone.ts`(+`.test.ts`). `i18n/messages/{ko,en}/league.ts`에 header·zone·table 키 추가
- **순위·승점·득실을 재계산하지 않는다** — 2팀 `getStandings()` 산출물을 그대로 표시만 한다. 순위 규칙이 여기서 다시 구현되면 두 곳의 판정이 어긋난다(와이어프레임 I-7: "정렬 없음. FR-LG-005 순위 규칙이 유일한 정렬")
- **존은 파생값이다** — 승격/플레이오프/강등은 `Standing`에 필드가 없고 rank + 리그 슬롯 설정에서 매 렌더 파생된다. `resolveStandingZone`(`standings-zone.ts`)이 그 공식의 **유일한 소유자**이고 `StandingsTable`·`ZoneLegend` 둘 다 이 한 곳만 참조한다
- 색 + 아이콘(▲◆▼) + 라벨 **3중 표기**로 NFR-A11Y-002(색상 단독 금지) 충족. 아이콘은 `aria-hidden`, 범례는 `<ul aria-label>`
- `FormStrip`·`TeamBadge`(4팀 기존 컴포넌트) **재사용** — 신규는 5팀 소유 경로에만 만들었다
- **오늘 스코프 밖(40일차 이후)**: B1-t 탭 · B4 타이브레이커 적용 단계 주석 · B5 리그3 리빌드 제재 · 시즌 선택기. **Task 016 완전 종료는 이후로 남긴다**
- 검증: typecheck 0 · lint 0 · `npm run test` 통과. Playwright로 3리그(tier1/2/3) × ko/en 육안 확인 — 리그1(승격 없음)·리그2(승격+플레이오프 중첩)·리그3(강등 없음) 존 규칙 각각 확인, 375px 가로 스크롤 0. 팀장 재확인: 6경로 200, `data-slot="table-row"` 25개(헤더 1 + 24팀)
- 이슈 후보 2건 제기 → **I-203**(리그별 팀 수 스켈레톤 불가 — `loading.tsx`가 `params`를 못 받음) / `/sample` 등록 인계(4팀 소유 파일이라 미수행, 팀장이 4팀에 인계해 당일 해소)
- 팀장 검증에서 별도 발견 → **I-201**(`notFound()`가 HTTP 200) — **5팀 산출물 결함이 아니라** 라우트마다 `loading.tsx`가 있어 셸이 먼저 스트리밍되는 구조적 사안

## 40일차 (2026-09-14) — Task 016 존 구분 확인 + B4 타이브레이커 단계 표시

**존 시각 구분 — 39일차 산출물로 이미 충족, 변경 없음**

- 승격/플레이오프/강등 표기는 색 + 아이콘(▲◆▼) + 텍스트 라벨 3중이라 색상 단독 금지(NFR-A11Y-002) 만족. 색맹 3종(protanopia/deuteranopia/tritanopia)에서 **형태와 텍스트만으로 구분 가능**
- 팀장 실렌더로 확인(승격/PO/강등 라벨 렌더됨)

**B4 타이브레이커 적용 단계 표시 — 신규**

- `src/components/composite/tiebreak-note.ts`(순수 함수) + `.test.ts` + `TiebreakNote.tsx`(`ZoneLegend`와 동일하게 4상태 없이 조건부 렌더) + ko/en `league.tiebreak.*` 키 + `leagues/[leagueId]/page.tsx` 배선
- `Standing.tiebreakApplied`(2팀 Task 026 산출)를 소비만 하고 순위를 재계산하지 않는다(39일차와 같은 원칙)
- 단계 라벨 조사 결합은 `stageNSuffix` 값에 미리 내장(`"골득실로"`). 2~7 밖 값은 방어적으로 렌더 생략
- B4는 색을 아예 쓰지 않는다(텍스트 + ⓘ)

**팀장 피드백 → 재수정 1회 (단독 순위 문구)**

- 3팀이 mock `tiebreakApplied`를 채운 뒤 B4가 처음 렌더되며 드러남: 11줄 중 **4줄이 순위 1개짜리**("5위는 골득실로 순위가 갈렸습니다"). 갈리려면 상대가 있어야 하므로 말이 안 되고, `TiebreakNoteGroup.ranks` 주석("2개 이상") 계약 위반
- 원인: 그룹화 키를 `points` + `tiebreakApplied` **양쪽 일치 + 인접**으로 잡아, 같은 승점 블록이 여러 단계로 갈리면 잔여가 1개만 남음
- 수정(2단 구조): 1차로 `points` 동일 구간(진짜 동률 블록, 2명 이상)으로 묶고, 2차로 그 안에서 단계별 하위구간(subRuns)만 분할 → `"5·6·7·8위 동률 — 5위는 골득실로, 6·7위는 페어플레이로, 8위는 골득실로 순위가 갈렸습니다"`. 블록 크기가 항상 2 이상이라 **단독 문장이 구조적으로 불가능**
- `decidedByMulti`/`clause` 키 추가(ko/en). 팀장 재검증: 11줄 → **7줄, 단독 0건**, `/en` 번역 렌더 확인
- 테스트: typecheck 0 · lint 0 · vitest(tiebreak-note 6케이스 포함) 통과

**판단만 회신(구현 보류)**

- **B4 노이즈 스코프** — 병합으로 11→7줄이 됐으나 24팀 리그엔 여전히 잦다. 전량 나열이 와이어프레임 §4 B4 의도인지, 존 경계 인접 블록 우선 노출로 좁힐지 판단 필요
- **B4 두 번째 줄**(TIEBREAK Fixture "단판 플레이오프 예정" 안내) — Mock에 재경기 생성기가 없어 오늘 제외, Task 016 잔여로 유지

## 41일차 (2026-09-15) — Task 016 일정/결과 + 40일차 잔여 전량

- **B1을 `src/app/[lang]/leagues/[leagueId]/layout.tsx`(신규)로 분리**해 순위표·일정 두 화면이 헤더와 탭을 공유하도록 했다(W-16 확정). `[leagueId]/page.tsx`에서는 헤더 제거
- `fixtures/page.tsx` 전면 구현 — 라운드별 그룹, 킥오프 시각, 스코어/LIVE/예정 배지, 라운드 네비게이션
- `composite/RoundNav.tsx` · `composite/SeasonSelect.tsx` (신규) / `composite/MatchCard.tsx` — row 밀도 4상태 배지 + 팀 엠블럼 / `standings-zone.ts` — `isZoneBoundaryAdjacent` 추가(유닛 7건)
- i18n `league`/`match` 확장 + **`fixtures.ts` 신규 네임스페이스**(생성 전 4팀 통지, `bet.ts` 전례). `messages/index.ts`는 등록 줄만 추가 — 같은 일차에 4팀이 `awards` 등록 줄을 추가했으나 **충돌 없이 병존 확인**
- **40일차 인계 1번(B4 노이즈 스코프) 결론** — 전량 나열이 아니라 **존 경계 인접 블록만 노출**(중위권 순수 동률 제외). 근거는 `standings-zone.ts` 헤더가 단일 소스. 실렌더에서 7줄 → **2줄**로 축소, 남은 2줄이 전부 경계 인접임을 확인
- **40일차 인계 2번(Task 016 잔여) 전량 완료** — B1-t 탭 · B5 리그3 리빌드 제재 · 시즌 선택기. TIEBREAK Fixture 안내는 mock에 재경기 생성기가 없어 계속 제외
- 테스트: typecheck/lint/test 전체 그린(1508). **실렌더**: ko/en 양쪽 `/leagues/{id}`·`/leagues/{id}/fixtures`를 Playwright로 라운드 10→15 이동 · "현재로" 복귀 · 탭 전환 · 시즌 선택기 · B4 · B5 **실제 클릭/쿼리로 확인** 후 `browser_close`
- **이슈 자진 제기 3건** → **I-212**(라운드 목록 LIVE 행 점수가 항상 `null` — `schedule.ts`와 `progress.ts`가 별개 소스) · **I-210**(C2를 `<table>` 아닌 `<ul>`로 마크업, `MatchCard` row 재사용 대가로 NFR-A11Y-005 편차) · **I-211**(layout이 `searchParams`를 못 읽어 헤더 라운드/페이즈가 항상 라이브 시즌 기준)
- **`NAV_GROUPS` pending 판단**: 오늘 채운 것은 `[leagueId]` 상세뿐이고 사이드바 "리그"가 가리키는 **목록 페이지는 여전히 없다 → 플래그 유지가 맞다**(팀장 동의). 레이아웃은 4팀 소유라 직접 건드리지 않고 판단만 회신

## 42일차 (2026-09-16) — Task 016 시맨틱 마크업 · **I-210 결론**

- **41일차 자기 판단을 스스로 뒤집었다.** C2 경기 목록을 `<ul>` → `<table>`로 전면 전환
  - 근거 3가지: 와이어프레임 §3이 **"데스크톱 2열은 시각 배치만 2열, 마크업은 하나의 테이블"**을 명시 / §7 NFR-A11Y-005의 `scope="col"` 요구는 표 밖에서 무의미 / 자매 화면 B3가 이미 실제 `<table>`로 같은 요구를 충족
  - `<table>` + sr-only `<caption>` + 열 헤더 4종(상태/홈/스코어/원정, `scope="col"`). `MatchCard`(div 블록)는 `<td>`로 쪼갤 수 없어 **행을 화면 로컬로 새로 그렸다** — 와이어프레임이 애초에 MatchRow를 화면 로컬로 지정해 둔 것과 같은 근거
  - 행 전체 링크는 stretched link 패턴(첫 셀에 `absolute inset-0` 링크 + `aria-labelledby`로 홈/스코어/원정/상태 합성)
- `StandingsTable.tsx` — 축약 헤더 9열(경/승/무/패/득/실/득실/승점/최근5)에 `<abbr title>` 추가. 순위표 `caption`/`scope`는 39일차에 이미 충족돼 확인만
- 변경: `fixtures/page.tsx` · `StandingsTable.tsx` · i18n `{ko,en}/fixtures.ts`·`league.ts`
- 실렌더(격리 dev 서버, 검증 후 원복): 3개 리그 × ko/en 순위표·일정 — `<table>` 1개씩, `scope="col"` 4종, EN abbr title 정상. **320px에서 body 가로 스크롤 0**(scrollWidth 305 < 320), 표 컨테이너만 내부 스크롤(홈/원정 열 128px 확보). 768px 스크롤 불필요·헤더 폭 깨짐 0
- **팀장 지적 1건 반영**: 늘인 링크가 첫 셀 접근명을 삼켜 **`columnheader "경기 상태"` 아래 셀 이름이 행 전체 문자열**이 됐다(열 단위 탐색 시 상태값 대신 경기가 통째로 읽히고 행마다 두 번 발화). 상태 셀 `<td>`에 `aria-label`을 직접 얹어 accname 계산이 자식 순회를 건너뛰게 해소 — 재검증에서 `라이브 0'`/`종료`/`예정`만 노출되고 링크 접근명은 유지됨을 확인
- **이슈 제기** → **I-216**: 데스크톱 2열 시각 그리드 미구현. 표에 CSS grid/columns를 걸면 일부 브라우저에서 **암묵적 표 ARIA가 사라지는 회귀**가 있어 "마크업은 하나의 테이블" 요구를 우선시하고 전 너비 1열로 통일했다
- 특이사항: 검증 중 `.next/dev/types` torn-write로 typecheck가 1회 실패(I-62 계열) — `.next` 삭제 후 dev 재기동으로 정상화. `next.config.ts`/`tsconfig.json` 임시 변경은 전부 원복

## 43일차 (2026-09-17) — Task 017 D1 스코어보드 + D3 이벤트 타임라인

- `src/app/[lang]/matches/[matchId]/page.tsx` **전면 구현**(자리표시자 → 실렌더)
- `src/components/composite/MatchScoreboard.tsx`(신규) + `match-scoreboard.ts`(순수 로직: `foldMatchScore`/`deriveMatchPhase`/`compareEventChronologically`) + 테스트 13건. `src/i18n/messages/{ko,en}/match.ts`에 `score.*`·`timeline.futureBoundary`
- D1: 리그·라운드·중립지, LIVE 배지 + 페이즈 5종(전반/하프타임/후반/연장/승부차기) + 경과분·추가시간, PSO 분리 표기(R-13)
- D3: 시간역순 타임라인, ASSIST→GOAL 병합(E-2), R-11 경계 문구
- **실렌더 중 자체 발견·수정 1건 → I-212 append**: Mock에 **FINISHED 경기의 이벤트 로그가 아예 없어** E-1 이벤트 폴딩이 항상 0-0을 반환했다. 테스트로는 잡히지 않고 **실제로 띄워 봐야만 보이는 결함**이었다. FINISHED만 `Fixture.homeScore`/`awayScore` 직접 사용으로 우회(LIVE는 계속 폴딩) — 종료 경기는 가릴 미래가 없어 E-1 취지(미래 결과 누출 방지)에 저촉되지 않는다는 근거를 주석 127~130행에 남겼고 팀장이 **임시 조치로 승인**(mock이 이벤트 로그를 갖추면 해소되어야 함)
- **이슈 제기 → I-220**: `Fixture.roundLabel`이 mock에서 한국어로 구워져(`${round}라운드`) en 로케일에 그대로 노출. 3팀 소유라 미수정. 팀장 재현 과정에서 `mapper.ts`가 DB `round_label`을 pass-through한다는 점이 확인돼 **실데이터 전환 후에도 잔존하는 구조적 문제**로 격상
- 실검증: dev + Playwright로 `/ko`·`/en` × LIVE/SCHEDULED/FINISHED/404 5종, 320px 가로 스크롤 0. typecheck 0 · lint 0 · test 1583 통과

## 44일차 (2026-09-18) — Task 017 중계 문구 카탈로그

- `src/i18n/messages/{ko,en}/match.ts` — `match.event.*` 이벤트 23종 템플릿 신설(순수 추가, 기존 키 삭제·변경 0건 / 4팀 소유 경로 협의 조건 충족)
- 와이어프레임 04번 §4 D3 프리픽스 준수, 기존 `enums.matchEvent.*`(뱃지용 단어 라벨)와 역할 분리
- 고유명사(`{playerName}`/`{teamName}`)는 변수 자리표시자 — 번역 대상 제외(D-17/D-18)
- `satisfies Record<MatchEventType, string>`로 23종 전량 매핑을 tsc가 강제
- 검증: `npx vitest run src/i18n` 48건(ko/en 키 수 일치 포함) / composite 87건 / 게이트 3종 통과. **중계 문구 하드코딩 0**
- 잔여: 소비처 `EventTimelineItem.tsx:110` 미배선(I-226) / Task 017 D2·D4~D7은 45~48일차

## 45일차 (2026-09-21) — Task 017 D4·D5 배선

- 직전 세션 미커밋분(`orderStartersByFormation`)이 테스트 누락 + 페이지 배선 전체 미완 상태였음을 확인하고 이어서 완성
- `src/components/composite/PitchLineup.tsx` (기존분, 미수정) / `PitchLineup.test.ts` — `orderStartersByFormation` 테스트 4건 추가
- `src/app/[lang]/matches/[matchId]/page.tsx` — D4(홈/원정 `PitchLineup` 2종 + 선수별 평점 Table) · D5(팀 스탯 대칭 비교바, `StatBar` 조합, mirror는 `[&_[data-slot=progress]]:scale-x-[-1]`) + helper 4개
- `src/i18n/messages/{ko,en}/match.ts` — `lineupTitle`/`rating*`/`stat.*` 그룹 신설(4팀 `cup` 그룹 무손)
- **신규 composite 0종**(와이어프레임 04번 §8 각주 준수) → `/sample` 신규 등록 요건 없음
- 검증: `npm run test` 1625 통과 · typecheck · lint 전부 통과 · ko/en 실렌더 하드코딩 0
- 잔여: `MockDataSource`의 라인업·평점·팀스탯 3종이 `return []` 고정이라 **빈 상태로만 검증 가능** — **I-229**(3팀, 엔진 `selectLineup()`은 21일차부터 존재)

## 46일차 (2026-09-22) — Task 017 D6(날씨·구장) + D7(배당 패널, 표시 전용)

- 산출: `src/app/[lang]/matches/[matchId]/page.tsx`(D6/D7 섹션 + `getMatchWeather` 조회), `src/components/composite/MatchOddsPanel.tsx`(신규), `src/i18n/messages/{ko,en}/match.ts`(`info`/`weather`/`odds` 그룹)
- D7 구조: 3팀 H-19(`src/lib/odds/display.ts` `OddsDisplayPanel`) 타입을 **그대로 소비**하는 신규 composite(4상태) + 기존 `OddsButton`(4팀 013A) 재사용
- **베팅 버튼 비활성(FR-BT-014)은 `OddsButton`에 `onClick` prop 자체가 없어 물리적으로 강제**된다 — 소비처가 실수로도 인터랙션을 붙일 수 없다. 보조텍스트 `match.odds.disabledHint`(I-9) 병기
- i18n: `weather`는 `satisfies Record<WeatherType, string>`로 9종 누락을 tsc가 차단(`event` 그룹과 동일 패턴). 하드코딩 0
- 검증: typecheck · lint · `npm run test` 1646 통과
- 잔여/제보: `DataSource`에 대진별 배당 조회 메서드 부재 → D7 항상 empty(소유팀 조율 필요) / `getMatchWeather` 날씨 생성기 부재로 항상 null / **중립지(`isNeutral`) 경기에 홈팀 구장명 그대로 표시**(중립 구장 엔티티가 동결 타입에 없음 → **I-233**) / `MatchOddsPanel`의 `/sample` 등록은 **D-33 경로 ①**로 4팀이 당일 이행

## 47일차 (2026-09-23) — Task 017 D2 잔여 (라이브 폴링)

- 신규: `matches/[matchId]/{LiveEventTimeline.tsx,timeline.ts}`, `api/live/matches/[matchId]/{route.ts,types.ts}`
- 수정: `matches/[matchId]/page.tsx`, `components/composite/EventTimelineItem.tsx`(`emptyVariant`), `i18n/messages/{ko,en}/match.ts`
- 폴링 주기 **공통코드 경유** — 서버 `page.tsx:130` `resolvePollIntervalMs("live")` → prop → `usePolling`. 클라이언트 리터럴·직접 조회 0건(I-222 패턴, 팀장 grep 재확인)
- `aria-live` sr-only 분리 배치(NFR-A11Y-004), FINISHED/VOID 시 폴링 중단(I-6)
- `buildTimelineRows` 순수 함수 분리로 SSR·폴링 API 공유
- 이슈: **I-237** D1 스코어보드 서버 1회 렌더 → LIVE 중 화면 내 정보 불일치(오늘 스코프 밖)
- **I-235 최초 감지·보고** — 스태시 사고 시 `git show stash@{0}:<path>`로 본인 파일만 복원하고 타 팀 파일은 손대지 않은 대응이 정확했음(팀장 확인)

## 49일차 (2026-09-25) — Task 018 선수 상세 1/2

- `/[lang]/players/[playerId]` 전면 구현 — E1(프로필 헤더) · **E1-r 지표 스트립(D-34)** · E2(능력치 레이더 + 34속성 아코디언) · E3(컨디션·피로 + 가용성 배지) · E4(포지션 맵 + 11군 숙련도 리스트)
- **신규 컴포넌트 0종** — 기존 `AbilityRadar`/`ConditionGauge`/`PositionMap`/`PlayerAvatar`/`TeamBadge`/`StatBar` 재사용으로 갈음(`/sample` 등록 불필요)
- **평점 4지표 실값 실측으로 D-33 판정 기준 충족** — 출전 6 · 최근 평점 6.3 · 시즌 평균 9.90 · 리그 벤치마크 6.04. **색이 아니라 라벨로 구분**(NFR-A11Y-002). `FINISHED` 재필터 없음(데이터 계약 신뢰, W-34/S-4)
- **PA 원값 비노출** — `scoutRating`만 ★1~5로 받아 표시, PA를 입력으로 받는 경로 자체가 없음(P-2)
- 변경: `src/app/[lang]/players/[playerId]/page.tsx`, `src/i18n/messages/{ko,en}/player.ts`
- 테스트: typecheck 본인 파일 0 · eslint 클린 · `npx vitest run src/i18n` 48건 통과(ko/en 키 패리티 포함) · dev 서버 curl로 ko/en 양쪽 실렌더 확인
- 이슈 제보 5건: **I-248**(nationality/preferredFoot 카탈로그 미이행 → 표기 생략) · **I-249**(34속성 라벨 공유 위치 미정, 화면 로컬 채움) · **I-250**(`PositionMap` 단일 포지션만 지원 → 4팀 파일 대신 옆 리스트로 우회) · **I-251**(dev `notFound()` HTTP 200, 기존 증상) · **I-252**(W-28 은퇴 선수 표기 규약 미확정)

## 50일차 (2026-09-28) — Task 018 선수 상세 2/2 (E5~E9) + I-244

- E5 몸값·계약 / E6 시즌별·통산 스탯 표(**`평점` 열 = `avgRating`**, 실측 7.30) / E7 성장 곡선 / E8 부상 타임라인 / E9 트로피·이적 이력 배선, lg 2컬럼
- **PA 원값 비노출** — 스카우트 등급 ★1~5만(FR-PL-004). 팀장 단독 재실측으로 `players/**` 전체 `potentialAbility` grep **0건** 확인
- **I-252**(은퇴 선수 표기 규약) 미판정 → 49일차와 동일하게 empty 폴백만 적용, 별도 UI 없음
- **자체 회귀 발견·수정**: 신규 lg 2컬럼이 49일차 E4 포지션 리스트의 md 전환과 겹쳐 글자 단위 줄바꿈 발생 → md→xl 완화 후 재확인
- **I-244 해소**: `src/app/[lang]/matches/[matchId]/timeline.test.ts` 신설 6건(정렬 순서 · ASSIST 병합 · 같은 분 다중 골). 팀장 단독 재실행으로 통과 확인
- 변경: `players/[playerId]/page.tsx`, `PlayerStatTable.tsx`(신규), `TransferHistoryList.tsx`(신규), `i18n/messages/{ko,en}/player.ts`(대칭 185=185), `timeline.test.ts`(신규)
- 테스트: typecheck · lint 0 · `npm run test` 1718 통과 · dev 실측(ko/en, 1280·375px)
- 이슈 제보: **I-259** 4건 묶음 — ⓐ E9 트로피 현 소속팀만(과거 팀 조인 데이터 없음) ⓑ `competitionType`/`transferType` 카탈로그 부재 → 화면 로컬 임시(**I-249와 동시 판정 권장**) ⓒ E6 그룹당 3~4개 대표 지표만(FR-ST-001 56필드 전량 아님) ⓓ md~lg 중간 티어 생략
- ※ E5/E7/E8/E9 대부분 empty 렌더는 버그가 아니라 기존 Mock 공백(`MockDataSource.ts` 헤더에 문서화됨)

## 51일차 (2026-09-29) — Task 018 클럽 상세 1/2

- `/teams/[teamId]`에 F1(헤더: 엠블럼·명성·팬·순위·최근5경기)·F2(스쿼드 테이블: 포지션 필터·상태 배지·OVR)·F3(감독/전술)·F3-o(구단주 카드, D-35, 공석 폴백) 실렌더. 와이어프레임 `06-클럽상세.md` 51일차 스코프(F1~F3-o)만, F4~F8 미착수
- 진입 동선 확인 — `StandingsTable`이 이미 `/teams/[teamId]`로 링크(49일차 "링크 0건" 재발 아님)
- **음수 시즌 잠정 숨김(팀장 지시)** — 창단 시즌·재임 시즌을 `SHOW_SEASON_ORIGIN_FIELDS=false` 조건부 렌더로 숨김(값 조회·i18n 키 유지, I-260 판정 후 되돌림). 레이아웃 이상 없음(구분자까지 함께 숨겨 댕글링 없음)
- 변경: `teams/[teamId]/page.tsx`, `SquadTable.tsx`(신규), `i18n/messages/{ko,en}/team.ts`(header/squad/manager/owner 4그룹)
- 테스트: typecheck·eslint·vitest(1730) 통과, dev 실측(ko/en, 1280·375·320px, 320px body 가로 스크롤 0)
- 이슈 제보: **I-260**(창단·재임 시즌 음수/미래, 도메인 축) · F2 상태 배지 3단 축소(KNOCK 미분리, 배치 조회 계약 부재 — I-255 계열)
