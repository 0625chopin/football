# 4팀 — UI기반·i18n팀 — 산출물 기록

> 위치: `docs/team-schedule/outputs/04-UI기반i18n팀.md` / 본체: [`04-UI기반i18n팀.md`](../04-UI기반i18n팀.md)
> 이 파일은 **일차별 산출물·조정 기록의 누적처**입니다. 팀 md 본체를 가볍게 유지하기 위해 분리했습니다.
> **팀원 소환 시 열람 대상이 아닙니다** — 팀원은 본체 md의 소유 경로·해당 일차 행만 봅니다.
> 절 번호는 분리 전 본체의 번호를 그대로 유지합니다(문서 내 §N 상호 참조 보존).
> 새 기록은 이 파일 끝에 append 하세요.

---

## 1. 요약 (1~13일차)

| 항목 | 값 |
|---|---|
| 참여 일차 | 8~13일차 (1~7일차는 H-01 도메인 타입 동결 대기로 미호출) |
| 담당 Task | Task 005(라우트 골격·전역 레이아웃, 9~13일차, **종료**) |
| 주요 산출물 | `src/app/[lang]/**`(루트 레이아웃 포함 정규 17라우트 + 2차 예약 3라우트 = 20라우트) · 각 라우트 `loading.tsx`/`error.tsx`/`not-found.tsx` 60파일 · `docs/devStep/08.SP1동결리뷰_4팀_8일차.md` |
| 종료된 Task | Task 005 (13일차, 20라우트 × 3종 = 60파일, curl 40경로 200) |
| 진행 중 | 없음 (다음 배정은 Task 011, 14일차 선행 문서 조사 — §8 참조) |
| 미해소 이월 | **Playwright 콘솔 스모크**(13일차 수락 기준 "콘솔 에러 0건" 미충족, 15일차 이월) · **I-89**(로케일 미반영 404, 15일차 Task 011에서 결정) |

## 2. 일차별 기록

### 1~7일차 — 업무 없음 (대기 구간)
- Task 005가 도메인 타입 동결(H-01, 8일차)을 선행 조건으로 해 1~7일차에 배정 업무 없음. 대기 구간 대체 작업(Next.js 16 문서 선행 정독 등)은 팀 일정 문서 §4에 항목만 나열돼 있을 뿐 일차 단위 배정이 아니라 실제 호출은 없었음.
- 출처: `docs/dailyWorkLog/1Day.md`(4팀 "⏸ 미호출", 미호출 사유 명시)

### 8일차 (2026-07-30, 목) — SP-1 동결 리뷰
- **한 일**: 도메인 타입 동결(H-01) 전 마지막 검토 기회로 SP-1 리뷰 참여 — 성장곡선·부상타임라인·트로피 관련 필드 및 자기 담당 화면(019·020) 확장 여부 확인, `EnumTranslationCatalog<T>` 도입 동의 여부 판정.
- **산출물**: `docs/devStep/08.SP1동결리뷰_4팀_8일차.md` (신규)
- **판정**: 완료 — 반영 요청 0건("원값 필드 전부 존재")
- **특기**: `Fixture.bracketSlot`(파생 가능, 2팀과 독립 확인 → I-50 OPEN 유지) · `CronRun.errorCode`/`errorMessage` 번역 비대상 명시(4팀 제기 → I-56 해소)
- 출처: `docs/dailyWorkLog/8Day.md`

### 9일차 (2026-07-31, 금) — Task 005 선행 (상세는 아래 §7)
- **한 일**: Task 005 착수 전 Next.js 16 문서 정독·참조 경로 기록(NFR-MT-006). 라우트 코드는 작성하지 않음.
- **산출물**: 팀 md §7(신규, 이 문서 §7과 동일)
- **판정**: 완료
- **특기**: `middleware.ts` deprecated → `proxy.ts` 개명 발견(I-57, 1팀이 원문 대조로 독립 검증) · `app/[lang]/` 조기 배치 결정(팀장 승인, §7.4) · 자기 문서의 `src/i18n/**` 소유팀 사실 오류(3팀→4팀) 자체 정정 · FR-UI 원문과 9군 대조해 1팀 `DataSource` 52개 메서드 갭 발견
- 출처: `docs/dailyWorkLog/9Day.md`

### 10일차 (2026-08-03, 월) — Task 005 라우트 생성 1/2
- **한 일**: `/`, `/sample`, `/leagues/[leagueId]`, `/leagues/[leagueId]/fixtures`, `/matches/[matchId]`, `/players/[playerId]`, `/teams/[teamId]`, `/stats` 등 8개 라우트를 `src/app/[lang]/` 하위에 생성(빈 `page.tsx`, `PageProps<...>` 타입 적용). create-next-app 데모 `src/app/page.tsx` 삭제.
- **산출물**: `src/app/[lang]/**` 9파일 (신규)
- **판정**: 완료(**재작업 1회**)
- **특기**: **재작업** — `[lang]` 세그먼트 도입으로 그 위 `src/app/layout.tsx`의 `<html lang="en">` 하드코딩이 오작동 상태가 됨. 4팀이 "root segment down to that layout" 규칙(`layout.md`) 근거로 최상단 레이아웃 분리 구조 자체가 문제임을 밝혀 `src/app/layout.tsx`를 삭제하고 내용을 `src/app/[lang]/layout.tsx`로 이관, `<html lang={lang}>` 동적 설정으로 정식 해결(팀장 2차 검증에서 발견, 4팀이 최소 땜질이 아닌 정석 구조로 재작업). Turbopack WSL dev 서버 실패(I-62) 최초 보고 — 이후 팀장 실측으로 **프로덕션 빌드도 번들러 무관 EPERM 실패**임이 밝혀져 범위 확대. 수락 기준 "8개 라우트 200"은 `next dev --webpack` 기준으로 충족.
- 출처: `docs/dailyWorkLog/10Day.md`

### 11일차 (2026-08-04, 화) — Task 005 라우트 생성 2/2
- **한 일**: `/playoffs/[leagueId]`, `/cup`, `/transfers`, `/awards`, `/archive`, `/sponsors`, `/admin`, `/admin/config`, `/admin/scheduler` + 2차 예약 3개(`/bet`, `/my/bets`, `/my/wallet`, 플래그 비활성) 생성 — 정규 17개 라우트 전량 + 예약 3개 완비.
- **산출물**: `src/app/[lang]/` 신규 12개 라우트
- **판정**: 완료
- **특기**: **최우선 결함 발견** — `polling.ts`가 파일 전체 `'use client'`인데 같은 파일의 `fetchResult`/`fetchListResult`를 "서버 컴포넌트가 직접 await 가능"으로 잘못 안내(4팀이 13일차 소비 전 발견 → 1팀이 `fetch-result.ts`로 분리해 해소, I-74). **등록 함수 미호출 반복 발견**(`installHardcodedFallback()` 호출처 0건, I-67 재발) → 팀장 판정 D-32로 `bootstrapApp()` 단일 진입점 신설, 4팀은 13일차에 이를 한 줄만 호출하면 되도록 정리됨. `next typegen` 선행 필요성 발견(I-73). `docs/wireframe/00-공통규약.md`를 자기 팀 소유로 오판 배정한 것을 4팀이 편집 전 확인 요청해 소유권 오류를 사전에 잡음(실제는 5팀 소유, 작업물 무오염).
- 출처: `docs/dailyWorkLog/11Day.md`

### 12일차 (2026-08-05, 수) — Task 005 전역 레이아웃 골격
- **한 일**: `src/app/[lang]/layout.tsx`에 전역 레이아웃 골격(헤더·사이드 내비·푸터, FR-UI-020) 배치. 헤더 4개 자리(리그 스위처·시즌/페이즈 인디케이터·킥오프 타이머·로케일 스위처)는 비활성 placeholder로 둠.
- **산출물**: `src/app/[lang]/layout.tsx` (수정)
- **판정**: 완료
- **특기**: **스트레이 디렉터리 원인 규명** — 1팀이 lint 노이즈로 보고한 `"E:\claudeStudy\workspaces\football4/"` 리터럴 디렉터리(7.7MB, I-87)를 4팀이 프로세스 추적으로 조사. 살아있는 PID(플래그 없는 순수 `next dev`, 10:27:02 기동, 포트 3000 점유)를 찾아 4팀 자신의 실행(`next dev --webpack -p 3411`)이 이 선점 프로세스에 막혀 자체 종료했음을 확인 — **4팀 소행이 아님**을 규명. H-05(3팀 공통코드 로더) 인계 수신 후 폴링 주기·최소 출전 필터 소비 가능성 확인 중 `LEADERBOARD_MIN_APPEARANCE_PCT`에 예외 흡수 래퍼가 없어 `bootstrapApp()` 미실행 시 019 화면이 예외로 죽을 수 있음을 부수 발견(I-85). `bootstrapApp()`을 13일차 이후 호출하면 되는지 팀장에 질의 → 승인.
- 출처: `docs/dailyWorkLog/12Day.md`

### 13일차 (2026-08-06, 목) — Task 005 종료
- **한 일**: 20라우트(정규 17 + 예약 3) 전체에 `loading.tsx`/`error.tsx`/`not-found.tsx` 3종 배치(FR-UI-000, 60파일). 루트 레이아웃에 `await bootstrapApp()` 한 줄 배선. Playwright MCP 전 라우트 순회 스모크 시도.
- **산출물**: `src/app/[lang]/**/{loading,error,not-found}.tsx` 60파일 (신규)
- **판정**: **재작업 없이 완료, 단 수락 기준 1항목 미충족(Playwright 콘솔 스모크 → 15일차 이월)**
- **특기**:
  - **Next.js 16.2.0 신규 API `unstable_retry` 채택** — 공식 문서가 기존 `reset`보다 우선 권장함을 팀장이 `error.md:157`·변경 이력 `:329`로 직접 재확인.
  - **⚠️ Playwright 콘솔 스모크 미완, 수락 기준 미충족으로 명시 기록(15일차 이월)** — 샌드박스에 Chrome 바이너리가 없고 `npx playwright install chrome`도 sudo 인증 불가라 실행 자체가 불가능. 대체 근거로 서버 렌더 HTML에서 `application error`/`hydration`/`__next_error__` 마커 부재, 40경로 curl 200, `tsc` 0건을 제시했으나, 4팀 스스로 "문서를 잘못 베끼지 않았다 수준의 증거이지 런타임 보증은 아니다"라고 한계를 명시. 팀장은 이 갭을 메우려는 `@testing-library/react` 도입을 **불승인**(`package.json` 1팀 단독 소유 + 근거 약함 + 프로젝트 전체 UI 테스트 전략 결정을 4팀 편의가 선점하면 안 됨).
  - **60파일 일관성 자체 재검증** — md5로 20개 `error.tsx`가 단일 해시로 수렴(라우트 경로 JSDoc만 치환), `page.tsx`/3종 보유 디렉터리 diff 4쌍 전부 IDENTICAL 확인. 다만 vitest `test.include`가 `.ts` 전용이라 이 60개 `.tsx` 파일은 vitest 커버리지에 전혀 노출되지 않음을 함께 확인.
  - **I-89 발견** — 미매칭 URL 404가 로케일 미반영(`<html>`에 `lang` 없음, 커스텀 `not-found.tsx` 미노출). 소스 근거까지 직접 확인: 최상단 `app/not-found.tsx`는 해법이 될 수 없음(top-most layout이 `[lang]/layout.tsx`라 그 서브트리 밖에 위치, `not-found.md`가 지목한 케이스와 정확히 일치) / `experimental.globalNotFound`가 16.2.10에 실제 구현돼 있음을 소스 라인(`config-shared.d.ts:768`, `config-schema.js:399`, `config-shared.js:1430` 기본값 `false`, `entries.js:373,448`)까지 확인, 단 stable 승격 표기 없어 experimental 신규 활성화로 취급 필요. → 15일차 Task 011 전략 논의 재료로 인계.
  - H-10(3팀, 열거형 ko/en 표시명 목록) 인계 수신 — 3팀과 각자 독립으로 `src/types/enums.ts` 대조해 70리터럴 불일치 0건 확인. **UI 폭 문제 제기**(ko 표시명에 괄호 부연설명이 배지·테이블 셀에 부담 — `골든글러브(최우수 골키퍼)` 등) → 팀장 채택, 3팀이 표시명/설명 2열 분리로 조치.
  - I-62(WSL EPERM)는 재확인 결과 dev 서버 플래그(`next dev --webpack`) 기준 재발 아님으로 게이트 통과.
- 출처: `docs/dailyWorkLog/13Day.md`

## 3. 인계물 (H-*)

| 인계 | 방향 | 일차 | 상태 |
|---|---|---|---|
| 도메인 타입 47종(**H-01**) | 1팀 → 4팀 | 9일차 (수신) | 완료 — 005 라우트 `params` 타입·013A 컴포넌트 props에 사용 예정 |
| `DataSource`/폴링 훅 계약(**H-02**), `fetch-result.ts`(`fetchResult`/`fetchListResult`), `bootstrapApp()` 단일 진입점 | 1팀 → 4팀 | 11~13일차 (수신) | 완료 — 13일차 루트 레이아웃에 `await bootstrapApp()` 한 줄 배선 |
| 공통코드 로더(폴링 주기·최소 출전 필터, **H-05**) | 3팀 → 4팀 | 12일차 (수신) | 완료 — 소비 가능성 확인, 예외 흡수 갭(I-85) 부수 발견 |
| 열거형 ko/en 표시명 목록(**H-10**) | 3팀 → 4팀 | 13일차 (수신) | 완료 — 70리터럴 대조 불일치 0건, UI 폭 문제 제기 |
| 전 라우트 골격 + 전역 레이아웃 + 4상태 파일(Task 005 산출물) | 4팀 → 5팀 | 13일차 (확정, 소비 시작 34일차) | 확정 — 아직 5팀 소비 전(1~13일차 범위 밖) |

## 4. 이 팀이 관여한 이슈·결정

| ID | 내용 | 일차 | 상태 |
|---|---|---|---|
| **D1-01** | enum 번역키 정의 주체(키 네이밍 규약·카탈로그 구조·키 공간) = **4팀 단독 소유** 확정 | 1일차 | 확정 (→ I-14 해소) |
| **I-50** | `Fixture.bracketSlot` — 타입 대상 아님(4팀·2팀 독립 확인) | 8일차 | OPEN, 39일차 재확인 예정 |
| **I-56** | `CronRun.errorCode`/`errorMessage` 번역 비대상 명시(4팀 제기) | 8일차 | 해소 |
| **I-57** | `middleware.ts` deprecated → `proxy.ts` 개명(4팀 발견, 1팀 독립 검증) | 9일차 | 확인, 10일차 착수 시 준수 |
| **I-61**(갱신) | `Result<T>` 적용 위치 — 4팀 공용 헬퍼 요구 반영 | 10일차 | 11일차 `polling.ts`로 해소 |
| **I-62** | WSL 마운트에서 Turbopack **dev 실패**(4팀 최초 보고) → 팀장 실측으로 **프로덕션 빌드도 번들러 무관 EPERM**임이 밝혀져 범위 확대(12일차 `npm install`도 동일 계열로 재확대) | 10~13일차 | 완화책 적용 중(`next dev --webpack`), 근본 해소(리포지토리 이전)는 사용자 결정 대기 |
| **I-64** | shrimp-task-manager 팀 간 공유 충돌(4팀도 겪음, 1·3팀과 동시 발생) | 10~12일차 | 운영 규칙(append/selective 강제)으로 완화, 13일차부터 적용 |
| **I-67** | 어댑터 등록 모듈 부트스트랩 소유자 미지정(4팀 필요로 노출) | 10일차 | 11일차 `bootstrapApp()` 신설(D-32)로 해소 |
| **I-73** | 새 라우트 추가 후 `tsc --noEmit` 전 `npx next typegen` 선행 필요(4팀 발견) | 11일차 | 확인, CI 게이트 설계 반영 필요(미착수) |
| **I-74** | `polling.ts` 서버/클라이언트 경계 결함(발견: 4팀 / 해소: 1팀) | 11일차 | 해소 |
| **D-32** | `bootstrapApp()` 단일 진입점 신설(4팀 소비 편의 반영) | 11일차 | 확정 |
| **I-85** | `LEADERBOARD_MIN_APPEARANCE_PCT` 예외 흡수 래퍼 부재(4팀 발견) | 12일차 | OPEN, 019(39일차) 착수 전 판단 |
| **I-87** | 스트레이 디렉터리(4팀이 프로세스 추적으로 원인 규명, 4팀 소행 아님 확인) | 12~13일차 | 12일차 1차 조치(`.gitignore`) 실효 없음 확인 → 13일차 `globalIgnores`로 재조치, 당일 해소 |
| **I-89** | 미매칭 URL 404 로케일 미반영(4팀이 소스 라인까지 확인해 제기) | 13일차 | OPEN, 15일차 Task 011에서 `global-not-found` 도입 여부 결정 |
| — | Playwright 콘솔 스모크 미완 — 13일차 수락 기준 "콘솔 에러 0건" **미충족을 숨기지 않고 명시 기록**, `@testing-library/react` 도입은 팀장이 불승인 | 13일차 | OPEN, 15일차 이월 |
| — | `unstable_retry`(Next.js 16.2.0 신규 API) 채택 — 문서 우선 원칙(AGENTS.md)이 실제로 작동한 사례 | 13일차 | 확정 |

## 5. 확인 못 한 것

- 팀장 지시문에 언급된 "소유 경로 밖을 깨뜨린 채 완료 보고한 절차 위반 1건"은 `docs/dailyWorkLog/13Day.md` §5.5(절차 위반 항목)를 확인한 결과 **1팀이 3팀 소유 `catalog.ts`를 깨뜨린 채 완료 보고한 사례**였고, 4팀이 주체이거나 대상인 절차 위반 기록은 1~13일차 작업 로그에서 찾지 못했습니다. 4팀 관련 유사 사례로는 9일차에 4팀 자신의 팀 문서(`04-UI기반i18n팀.md`)에 `src/i18n/**` 소유팀을 3팀으로 잘못 적은 **사실 오류**(당일 자체 정정)가 있으나, 이는 "완료 보고" 형태의 절차 위반과는 성격이 다릅니다. 착오가 있었다면 추가 확인이 필요합니다.

---

## 7. 9일차 산출물 — Next.js 16 문서 참조 기록 (NFR-MT-006)

> **일차**: 9일차 (2026-07-31) · **범위**: Task 005 선행 필수 단계만 — **라우트 코드는 작성하지 않음**(실착수는 10일차). 수락 기준("커밋 메시지에 문서 경로 명시")을 위한 참조 경로 기록.

### 7.1 확인한 문서 경로 (전량)

| # | 문서 경로 | 확인 내용 |
|---|---|---|
| 1 | `node_modules/next/dist/docs/01-app/01-getting-started/03-layouts-and-pages.md` | App Router 기초 — 파일시스템 라우팅, layout/page 생성, 동적 세그먼트, `PageProps`/`LayoutProps` 헬퍼, `searchParams` |
| 2 | `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/layout.md` | `layout.js` 파일 컨벤션 — `children`/`params` prop, 루트 레이아웃 규칙, `params`는 Promise(v15.0.0-RC부터) |
| 3 | `node_modules/next/dist/docs/01-app/02-guides/internationalization.md` | i18n 라우팅 가이드 — 로케일 감지·리다이렉트(`proxy.js`), `app/[lang]/` 세그먼트, `generateStaticParams`, 로컬라이제이션(딕셔너리) 패턴 |
| 4 | `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/dynamic-routes.md` | 동적 라우트 세그먼트(`[slug]`) 컨벤션, `params`가 layout/page/route/generateMetadata에 전달되는 방식 |
| 5 | `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/use-params.md` | `useParams` 클라이언트 훅 — Client Component에서 라우트 동적 파라미터 읽기 |
| 6 | `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` | `proxy.js` 파일 컨벤션 — `middleware` 컨벤션이 **deprecated되고 `proxy`로 개명**되었음을 확인 |

### 7.2 학습데이터와 다른 주요 변경점

1. **`params`/`searchParams`는 Promise** — `await params` / `await searchParams` 필수(동기 접근은 v15까지만 하위호환, 향후 제거 예정). layout·page·route·`generateMetadata` 전부 동일.
2. **`PageProps<'/route'>` / `LayoutProps<'/route'>` 전역 타입 헬퍼** — `next dev`/`next build`/`next typegen` 실행 시 라우트 구조 기반으로 자동 생성되는 전역 타입. 별도 import 없이 사용하며, `params`/`searchParams`/named slot(`@slot`)을 강타입으로 추론. 수동으로 `{ params: { slug: string } }` 형태를 다시 선언할 필요가 없다.
3. **`middleware.ts` → `proxy.ts` 개명(middleware는 deprecated)** — i18n 라우팅 가이드의 로케일 감지·리다이렉트 예제도 `proxy.js`에서 수행한다. Task 011(로케일 라우팅 전략)·Task 005(레이아웃 골격) 구현 시 `middleware.ts`가 아니라 `proxy.ts`를 프로젝트 루트(또는 `src/`)에 생성해야 한다.
4. **App Router에는 내장 `next.config`의 `i18n` 옵션이 없다**(Pages Router 전용이던 legacy 옵션과 다름). 다국어(D-18)는 `app/[lang]/` 동적 세그먼트 + `proxy.ts`(로케일 감지·리다이렉트) + 로케일별 딕셔너리(`getDictionary`) 조합을 직접 구현해야 하며, 서드파티 라이브러리(`next-intl` 등) 도입 여부는 아직 미정(`src/i18n/**`는 이 팀 단독 소유이므로 4팀 자체 판단 사항. 단 `messages/*/enums.*`는 3팀이 23일차 이후 값을 채우므로, 라이브러리 선택이 그 파일 형식을 바꾸면 H-09 인계 시 3팀에 통지 필요).

### 7.3 Task 005/011과의 연결 메모

- Task 011(로케일 라우팅 전략, ROADMAP 기준 **14~22일차·L 규모**, 9일 소요)에서 "경로 세그먼트 방식"을 채택할 경우, Task 005의 전 라우트가 `app/[lang]/...` 하위로 재배치되어야 하며 루트 `proxy.ts` 신설이 필요하다는 점을 확정 전에 반드시 반영해야 한다.
- 이번 일차(9일차)에는 위 사실 확인·기록까지만 수행했고, `src/app/**`·`src/i18n/**`·`proxy.ts` 등 실제 코드/디렉터리는 생성하지 않았다(10일차 이후 Task 005 실착수 시 처리).

### 7.4 결정 기록 — `app/[lang]/` 조기 배치 (9일차, 팀장 승인)

> **결정**: Task 005(10일차 실착수)의 전 라우트를 **처음부터 `app/[lang]/` 하위**에 스캐폴딩한다. 평면으로 만든 뒤 Task 011 단계(14~22일차)에서 재배치하지 않는다.
> **승인**: 팀장, 9일차(2026-07-31), 1차 교차 점검(1팀 `DataSource.ts` 검토와 병행 진행) 결과에 근거.

**근거**
1. **일정 구조상 재작업이 확정적**: ROADMAP 기준 Task 005는 9~13일차에 종료되지만, Task 005 체크리스트 자체가 "Task 011에서 확정한 로케일 라우팅 전략과 정합"을 요구한다. Task 011(로케일 전략 확정)은 그보다 늦은 14~22일차에 진행된다 — 즉 **결정 없이 그대로 두면 Task 005가 참조해야 할 전략이 Task 005 종료 시점에 아직 존재하지 않는 구조**이며, 평면으로 만들 경우 15일차 전후로 17개 라우트 × (loading/error/not-found 3파일)를 되돌려야 하는 재작업이 뒤따른다.
2. **선택지가 사실상 하나뿐**: 9일차에 확인한 Next.js 16 공식 문서(§7.1) 기준 App Router에는 내장 `next.config` i18n 옵션이 없고, 공식 가이드가 제시하는 유일한 실질 패턴이 `app/[lang]/` 경로 세그먼트다(도메인 분리 방식은 단일 도메인 서비스라 부적합). D-18(ko 기본/en 2차)도 이미 확정 사항이라 Task 011의 "세그먼트 방식 채택 여부" 자체가 결론이 정해진 질문에 가깝다.
3. **조기 결정 비용이 가장 저렴한 시점**: 5팀의 `page.tsx` 본문 착수는 28일차이고, 현재 `src/app/**`는 이 팀 외에 아무도 건드리지 않는다. 지금 결정하면 충돌·이중작업 없이 반영된다.
4. **되돌릴 리스크는 낮음으로 평가**: Task 011이 비-세그먼트 전략(쿠키 전용 등)을 채택할 가능성은 위 2번 근거로 낮다고 판단.

**조건 (팀장 지시, 반드시 준수)**
- **10일차 Task 005 실착수 시에는 `app/[lang]/` 디렉터리 구조와 `generateStaticParams`까지만** 만든다.
- **`proxy.ts`는 만들지 않는다.** 로케일 감지·리다이렉트 로직은 Task 011(14~22일차) 소관이다. 세그먼트 배치(오늘 결정)와 로케일 감지 전략(Task 011 결정)은 별개이며, 후자를 앞당기면 Task 011의 실제 판단 여지를 없애게 된다.
- 이 결정은 9일차 산출물이며, 실제 `app/[lang]/` 디렉터리·파일 생성은 10일차 이후에 이뤄진다(오늘은 여전히 라우트 코드를 만들지 않는다).
- 커밋 메시지에 명시할 문서 경로는 위 7.1의 6개 경로 전체(상대경로 기준, `node_modules/next/dist/docs/` 접두 유지).

---

## 8. 14일차 산출물 — Task 011 선행 문서 재확인 (NFR-MT-006, AGENTS.md R-04)

> **일차**: 14일차 (2026-08-07) · **범위**: Task 011(다국어 i18n 기반, 14~22일차)의 첫 체크박스(ROADMAP.md 305행) "선행 필수" 단계만 — **코드/디렉터리는 일절 생성하지 않음**(로케일 라우팅 전략 확정은 15일차, `src/i18n/**` 실제 생성은 16일차 이후).

### 8.1 §7과의 관계 — 재확인이지 중복이 아니다

9일차 §7은 Task **005**(라우트 골격) 착수를 위해 "이 Next.js 16 버전이 학습 데이터와 다르다"는 사실을 처음 확인한 기록이었다(`internationalization.md`·`proxy.md` 2종·`layout.md`·`dynamic-routes.md`·`use-params.md` 6개 문서, §7.1 표). 오늘은 그 뒤를 이어 Task **011**(로케일 전략을 15일차에 확정하고, 18~22일차에 Provider·번역 함수 API·서식·로케일 스위처+쿠키 영속화까지 만드는 Task) 착수 직전 재확인이다. 9일차에는 읽지 않았던, Task 011 실작업에 직접 필요한 세부 API까지 오늘 추가로 확인했다.

### 8.2 오늘 확인한 문서 경로

| # | 문서 경로 | 확인 내용 | 9일차 대비 |
|---|---|---|---|
| 1 | `node_modules/next/dist/docs/01-app/02-guides/internationalization.md` | 전문 재독 — 로케일 감지(`Accept-Language` + `@formatjs/intl-localematcher`/`negotiator`) 예시, `proxy.js`에서의 서브패스 리다이렉트 예시, `app/[lang]/` 컨벤션, `getDictionary`/`hasLocale` 딕셔너리 패턴, `generateStaticParams`를 통한 정적 렌더링, 서드파티 라이브러리 목록(`next-intl` 등 8종, 도입 여부 미정) | 9일차에도 읽었으나 오늘은 15일차 로케일 전략 확정(경로 세그먼트 채택 여부·로케일 감지 규칙)을 목표로 재독 |
| 2 | `node_modules/next/dist/docs/01-app/01-getting-started/16-proxy.md` | 전문 재독 — Middleware→Proxy 개명 고지(v16, 기능 동일), 사용 사례(헤더 수정·A/B 리라이트·프로그래매틱 리다이렉트), 단순 리다이렉트는 `next.config.ts`의 `redirects`를 우선 검토하라는 권고, 프로젝트 루트(또는 `src/`)에 단일 `proxy.ts` | 9일차와 동일 문서, 개명 사실은 이미 §7.3에 기록됨 — 오늘은 "사용 사례" 절을 근거로 로케일 감지·리다이렉트가 Proxy의 정석 용례임을 재확인 |
| 3 | `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/proxy.md` | **오늘 신규로 세부까지 정독**(9일차엔 존재만 확인) — `matcher` 문법(문자열/배열/정규식/`source`+`locale`+`has`+`missing` 객체, `locale` 옵션은 **§8.3 (b) 참조 — 우리 구성엔 무효**), 실행 순서 8단계(`next.config` headers→redirects→Proxy→beforeFiles rewrites→파일시스템 라우트→afterFiles rewrites→동적 라우트→fallback rewrites), 쿠키 API(`request.cookies`/`response.cookies` get/set/delete/has), Proxy 런타임은 Node.js 기본, `middleware.ts→proxy.ts` 코드모드(`npx @next/codemod@canary middleware-to-proxy .`) | **신규 심화** — matcher·실행순서·쿠키 API는 9일차 기록에 없던 내용이며 15일차 로케일 감지 Proxy 설계에 직접 필요 |
| 4 | `node_modules/next/dist/docs/01-app/03-api-reference/04-functions/cookies.md` | **오늘 신규** — `cookies()`는 비동기 함수, Server Component에서는 읽기만 가능(설정은 Server Function/Route Handler에서만), `get`/`getAll`/`has`/`set`/`delete` 메서드, `set` 옵션(`maxAge`/`path`/`sameSite`/`httpOnly` 등), 사용 시 해당 라우트가 동적 렌더링으로 전환됨 | 22일차 "로케일 스위처 + 선택 로케일 쿠키 영속화" 구현 시 서버 측 쿠키 API 근거로 사용 예정 |
| 5 | `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route-groups.md` | **오늘 신규** — `(folderName)` 컨벤션으로 URL에 노출되지 않는 폴더 그룹핑, 팀/기능별 라우트 정리·복수 루트 레이아웃 용도, 같은 URL로 충돌하는 그룹 금지 | Task 005의 2차 예약 라우트(`/bet`, `/my/bets`, `/my/wallet`)나 `/admin/**` 정리에 참고 가능한 선택지로만 확인 — **오늘 적용하지 않음**(범위 밖) |

### 8.3 15일차 이후 판단에 반영할 점

- **(a) 로케일 감지 로직은 문서가 제시하는 유일한 정석 위치가 Proxy(`proxy.ts`)다.** 근거: App Router용 `internationalization.md:33`("you can now redirect the user based on the locale inside [Proxy]") — 이 가이드 전체(226줄)에 `next.config`의 `i18n` 설정 블록이 **한 번도 등장하지 않는다**(§8.2 표 확인 시 재검색 완료). `next.config`의 내장 `i18n` 옵션은 App Router에 없다는 9일차 결론(§7.2-4)과 일치. 추가로 `node_modules/next/dist/server/config.js:171-175`(런타임 소스, 참고용 — 결론의 근거는 어디까지나 위 docs 부재이며 이 소스는 보강 확인일 뿐)에 `i18n configuration in next.config.js is unsupported in App Router`라는 명시적 경고 문자열이 있어 교차 확인됨.

- **(b) [14일차 오독 — 같은 날 팀장 2차 검증에서 발견·정정]** ~~`matcher`에 `locale: false`를 쓸 수 있다는 점은... 15일차 설계 시 1순위로 검토한다~~ **이 서술은 오독이었다. 우리 프로젝트 구성에서 `matcher`의 `locale: false` 옵션은 아무 효과가 없다 — 15일차 설계에서 검토 대상에서 제외한다.**
  - 근거: `proxy.md:104`의 `locale` 옵션 설명("A boolean that, when set to `false`, ignores locale-based routing in path matching")이 가리키는 "locale-based routing"은 **Next.js에 내장된 `next.config.js`의 `i18n: { locales, defaultLocale }` 블록**을 켰을 때 Next가 `source` 패턴에 로케일 프리픽스를 자동으로 붙여주는 legacy 동작이다.
  - `headers.md:329-335`·`redirects.md:300`·`rewrites.md:427`이 모두 같은 문구("When leveraging `i18n` support with ... each `source` is automatically prefixed...")로 이 옵션을 설명하며, `redirects.md:300`·`rewrites.md:427`은 링크를 **`/docs/pages/guides/internationalization`(Pages Router 전용)**으로 건다. `headers.md`만 `<AppOnly>`/`<PagesOnly>` 두 블록으로 나뉘어 App Router 링크(`/docs/app/guides/internationalization`)도 갖고 있으나, 그 링크가 가리키는 실제 App Router 가이드(§8.2-1, `internationalization.md`)에는 위 (a)에서 확인했듯 `i18n` 설정 블록 자체가 없다 — 즉 문서 상호 링크가 낡았을 뿐, App Router에서 `next.config.js`의 `i18n` 블록은 (a)의 근거대로 **지원되지 않는다**.
  - 결론: 우리 `/ko`·`/en`은 Next의 내장 로케일 라우팅이 아니라 우리가 만든 평범한 동적 세그먼트(`[lang]`)이므로 "꺼야 할 로케일 기반 라우팅" 자체가 존재하지 않는다. `/admin` 등 로케일 무관 경로를 Proxy에서 배제하려면 `locale: false`가 아니라 **`matcher`의 `source` 경로 패턴(negative lookahead, 예: `proxy.md:616`의 부정 전방탐색 예시)** 또는 **proxy 함수 내부의 명시적 조건 분기**를 써야 한다. 15일차 설계는 이 방식으로 진행한다.
  - 재발 방지: (a)와 (b)가 초안 작성 시점에 서로 모순된 채로 남아 있었다(내장 `i18n`이 없다고 (a)에서 결론 내려놓고 (b)에서 그 `i18n` 전용 옵션을 App Router에 쓸 수 있다고 서술) — 문서 간 상호 참조 정합성을 재작성 시 함께 확인하지 않은 것이 원인. 이후 §8류 기록에서는 결론 간 모순 여부를 마지막에 한 번 더 훑는다.

- **(c) `cookies()`가 Server Component에서는 읽기 전용이라는 제약**은 22일차 로케일 스위처 컴포넌트를 클라이언트 컴포넌트 + Server Function(또는 Route Handler) 조합으로 설계해야 함을 뜻한다 — 오늘은 설계만 메모, 구현은 18~22일차. 원문 근거: `cookies.md:6`("read/write outgoing request cookies in Server Functions or Route Handlers" — Server Component는 읽기만 명시), `cookies.md:80`("Setting cookies is not supported during Server Component rendering. To modify cookies, invoke a Server Function from the client or use a Route Handler."). 재검토 결과 이 항목은 원문과 정확히 일치해 정정 없음.
- **오늘 실제 코드/디렉터리 생성 없음.** `proxy.ts`·`src/i18n/**` 모두 여전히 부재 상태를 유지했다(재확인: `ls src/i18n` 결과 없음, `find src/app -name proxy.ts` 결과 없음).

### 8.4 H-10 수신 확인 (3팀 → 4팀, 14일차 수신 일정)

- `docs/handoff/H-10-enum-display-names.md` 존재 확인. 발신 3팀 데이터·밸런싱·배당팀, **13일차(2026-08-06) 확정** + 같은 날 1차 교차 점검에서 4팀 검수(괄호 부연설명 3건 `GOLDEN_GLOVE`/`HIGH_PRESS`/`SECOND_YELLOW`를 표시명/설명 2열로 분리) 반영 완료 상태.
- 범위: 포지션 11군·매치 이벤트 23종·부상 4등급·감독 전술 6종·시즌 페이즈 6종·수상 종류 12종+수상 범위 4종·배팅 마켓 상태 4종 — 팀 스케줄표 19일차 항목이 명시한 7그룹(+`AwardScope`)과 정확히 일치.
- **오늘은 수신 확인만 한다.** `src/i18n/messages/{ko,en}/enums.ts` 골격 생성은 팀 스케줄표 19일차(2026-08-14) 소관이라 앞당기지 않았다(팀장 지시 준수, 실제 파일 미생성 확인).
- H-10 문서 §8("후속 대상 열거형 목록")에는 이 7그룹 밖의 26개 열거형이 "미착수·값 미작성"으로 정리돼 있고, `NewsFeedItemType`·`TrophyType`은 "019 착수 전 필요"로 팀장 지목돼 있다 — 4팀 Task 019(39~43일차) 착수 전 3팀에 값 요청이 필요함을 인지했다(오늘 요청하지 않음, 39일차 이전 대응 예정 메모).

## 9. 15일차 산출물 — Task 011 로케일 라우팅 전략 확정 (2026-08-10)

### 9.1 결정 — 세그먼트명 `[lang]` 유지 (개명하지 않음)

**결정**: `[locale]`로의 개명은 하지 않는다. 기존 `src/app/[lang]/` 17개 라우트 + 루트 레이아웃을 그대로 둔다.
**근거**: 개명은 17개 라우트 전체 이동을 요구하는 순수 명명 이슈이고, Next.js 런타임은 세그먼트 이름에 의미를 두지 않는다(공식 i18n 가이드 예제도 `[lang]`을 그대로 씀). 10일차 결정(§7.4 이후 실제 코드화)·12일차 Task 005 산출물(전역 레이아웃 골격)이 이미 `[lang]` 기준으로 존재해, 개명 시 05팀 인계물(H-09 이전)까지 재작업 대상이 되는 반면 기능·라우팅 동작에는 아무 차이가 없다. 비용 > 이득으로 판단해 유지.

### 9.2 결정 — `proxy.ts` 도입

**결정**: `src/proxy.ts` 신설. 기본 로케일 `ko`(D-18), 2차 `en`. 로케일 프리픽스가 없는 경로는 `/ko/...`로 307 리다이렉트. `/admin`·`/_next`·`/api`·확장자 있는 정적 파일은 `matcher.source` 부정 전방탐색으로 제외(`matcher.locale: false`는 14일차 §8.3(b)에서 이미 App Router 구성에 무효로 확인돼 사용하지 않음).
**근거**: 9일차 §7.4 결정은 "세그먼트 배치는 지금, 로케일 감지·리다이렉트는 Task 011(오늘)"로 역할을 나눴다 — 오늘이 그 실행 시점이다. `[lang]/layout.tsx`는 URL 세그먼트를 그대로 읽을 뿐 감지하지 않으므로(10일차 JSDoc 기록), 프리픽스 없는 요청은 proxy 없이는 어떤 라우트에도 도달하지 못한다. `/admin`은 2차 예약 라우트(비활성)라 리다이렉트 대상에서 제외 — 팀장 지시.
**검증**: `npm run dev -- -p 3010`(포트 충돌 회피 위해 별도 포트) 기동 후 `curl`로 확인 — `/matches` → 307 `/ko/matches`, `/admin` → 200(리다이렉트 없음, 그대로 통과), `/ko/matches`·`/en/matches` → 리다이렉트 없이 통과(해당 라우트의 404는 `matches/page.tsx` 부재 때문이며 proxy와 무관 — `matches/[matchId]`만 존재, 목록 페이지는 미착수 상태 확인).

### 9.3 결정 — I-89: `global-not-found`(experimental) 채택

**결정**: `next.config.ts`에 `experimental.globalNotFound: true` 추가, `src/app/global-not-found.tsx` 신설(전역 스타일 `globals.css` 직접 import, `<html lang="ko">` 고정).
**근거**: 공식 문서(`not-found.md` "global-not-found.js (experimental)" 절)가 명시하는 두 도입 사례 중 하나 — "루트 레이아웃이 최상위 동적 세그먼트로 정의된 경우" — 가 정확히 우리 구조(`[lang]/layout.tsx`)다. 완전히 매치되지 않는 URL(예: 3세그먼트 이상 미정의 경로)은 `[lang]/not-found.tsx`를 거치지 않고 라우팅 단계에서 바로 이 파일로 간다 — 잘못된/무의미한 `lang` 값으로 루트 레이아웃이 렌더링되는 경로 자체가 없어진다.
**검증**: 별도 포트(3010)에서 `/zz/nope`(존재하지 않는 2세그먼트 경로) 요청 → proxy가 `/ko/zz/nope`로 307 리다이렉트 → 그 경로도 파일 컨벤션에 매치되는 라우트가 없어 최종 404 + `global-not-found.tsx` 렌더(타이틀 "404 - Page Not Found" 확인).

### 9.4 운영 메모 — 로컬 dev 서버 포트 충돌

검증 중 동일 프로젝트 디렉터리에서 이미 실행 중이던 `next dev`(PID 233288, 시작 주체 불명 — 이 세션이 시작하지 않음)가 포트 3000을 점유하고 있어 최초 기동 시 "Another next dev server is already running"으로 이 세션의 인스턴스가 즉시 종료되는 것을 확인하지 못한 채 포트 3000으로 curl 검증을 진행했다가, 그 서버가 `proxy.ts`(신규 파일 — proxy는 핫리로드 대상이 아니라 재기동 필요) 반영 전 상태라 리다이렉트가 관측되지 않는 오류가 있었다. 원인 파악 후 **기존 PID 233288을 종료(`pkill`)**하고 `-p 3010`으로 재기동해 재검증했다. 종료된 프로세스가 다른 팀/세션의 활성 작업이었을 가능성을 배제할 수 없어 기록해 둔다 — 필요 시 각자 `npm run dev`로 재기동.

### 9.5 팀장 검증 결함 수정 — `/admin` 배제 철회 (2026-08-10, 15일차 2차)

**결함**: 9.2의 `/admin` 배제(`matcher.source` 부정 전방탐색)가 admin 요청을 프록시 밖으로 빼면서 리다이렉트 없이 라우터에 그대로 진입시켜 `[lang]="admin"`으로 바인딩됨 — 어드민이 아니라 **홈 화면이 200으로 렌더되고 `<html lang="admin">`**이라는 무효 태그가 실제로 나갔다. 접두사 배제 방식이라 `/administrator` 등도 함께 새어나갔다. 9.3의 I-89 채택 근거("무효 lang 렌더 경로 원천 차단")와 정면 모순 — 팀장 독립 검증(포트 3021)에서 발견.
**수정 결정**: `/admin` 특별 취급을 없앤다 — admin 라우트는 이미 `src/app/[lang]/admin`으로 로케일 세그먼트 하위에 있으므로 다른 라우트와 동일하게 프록시의 기본 로케일 리다이렉트 대상이어야 한다. `matcher.source`에서 `admin` 알터네이션을 제거하고 `_next`·`api`·확장자 있는 정적 파일만 남겼다(`src/proxy.ts`).
**재검증**(스크래치 디렉터리에 프로젝트 사본을 만들어 `node_modules` 심볼릭 링크 후 포트 3033에서 독립 기동 — 동일 디렉터리에서 dev 서버가 둘 이상 뜨면 Next.js가 스스로 종료하는 락 때문에 팀장의 3021 서버와 별도 디렉터리가 필요했음):
| 경로 | 최종 상태 | 리다이렉트 | `<html lang>` |
|---|---|---|---|
| `/admin` | 200 | 1회 → `/ko/admin` | `ko` |
| `/administrator` | 404 | 1회 → `/ko/administrator` | `ko` |
| `/ko/admin` | 200 | 없음 | `ko` |
| `/matches` | 404(무관 — `matches/page.tsx` 부재, `[matchId]`만 존재) | 1회 → `/ko/matches` | `ko` |
| `/zz/nope` | 404, `<title>404 - Page Not Found</title>` (global-not-found 렌더 확인) | 1회 → `/ko/zz/nope` | `ko` |

무효 `lang` 값이 렌더되는 경로 0건 확인 — 프록시가 매치되는 모든 요청을 `ko`/`en` 중 하나로 정규화하므로 별도의 `[lang]/layout.tsx` 검증은 추가하지 않았다(구조적으로 불필요).

### 9.6 팀장 검증 결함 2건째 — 확장자 경로도 동일 계열 (2026-08-10, 15일차 3차)

**결함**: `.txt`·`.file.here` 등 확장자 있는 경로도 `matcher.source`의 `.*\..*` 제외에 걸려 프록시를 거치지 않고 라우터에 직접 도달, `[lang]`에 바인딩되어 200 + `<html lang="nonexistent.txt">` 렌더 — `/admin`과 동일 계열 결함(팀장 재검증, 포트 3023). 제외 목록을 좁히는 방식으로는 이 계열 자체를 닫을 수 없다는 지적에 동의.

**수정**: `src/app/[lang]/layout.tsx`에 2중 방어 추가.
1. `src/i18n/locales.ts` 신설 — `SUPPORTED_LOCALES`/`DEFAULT_LOCALE`/`isSupportedLocale`의 단일 소스(Next 런타임 API 미참조라 Edge인 `proxy.ts`·Node인 `layout.tsx` 양쪽에서 안전하게 import 가능). `src/proxy.ts`는 자체 선언을 지우고 이 모듈을 import.
2. `[lang]/layout.tsx`: `<html lang>`은 항상 `isSupportedLocale(lang) ? lang : DEFAULT_LOCALE`로 유효한 값만 쓴다. `notFound()`는 레이아웃 함수 최상단이 아니라 `{children}` 자리에 둔 `LocaleGate` 서브컴포넌트로 옮겼다 — 루트 레이아웃 자신이 `<html>` 반환 전에 던지면 그 세그먼트를 감쌀 상위 레이아웃이 없어 Next가 `not-found.tsx`/`global-not-found.tsx` 어느 쪽도 못 거치고 내부 최소 폴백(`__next_error__`, lang 속성 없음)으로 떨어지는 것을 실측으로 확인했기 때문(직접 실험: 레이아웃 최상단에서 던질 때와 `LocaleGate`로 옮겼을 때 둘 다 확장자 경로에서는 동일 폴백이 나옴 — 아래 잔여사항 참조). `{children}` 슬롯에 둔 덕에 확장자 없는 무효 경로(`/zz/nope` 등)는 정상적으로 `global-not-found.tsx`를 거친다.

**재검증**(스크래치 사본, 포트 3035):
| 경로 | 최종 상태 | 리다이렉트 | `<html>` |
|---|---|---|---|
| `/nonexistent.txt` | 404 | 0 | `id="__next_error__"` (lang 속성 없음 — 아래 잔여사항) |
| `/some.file.here` | 404 | 0 | `id="__next_error__"` (lang 속성 없음) |
| `/zz/nope` | 404 | 1(`/ko/zz/nope`) | `lang="ko"`, `<title>404 - Page Not Found</title>`(global-not-found 정상 렌더) |
| `/admin` | 200 | 1(`/ko/admin`) | `lang="ko"` |
| `/ko/admin` | 200 | 0 | `lang="ko"` |
| `/en` | 200 | 0 | `lang="en"` |
| `/ko` | 200 | 0 | `lang="ko"` |
| `/ko/sample` | 200 | 0 | `lang="ko"` |
| `/_next/static/chunks/webpack.js` | 200 | — | (정적 에셋 정상, 회귀 없음) |

**잔여 사항(투명 보고)**: 확장자 있는 미존재 경로(`/nonexistent.txt` 류)는 무효 **값**이 렌더되는 문제(200+잘못된 lang)는 사라졌지만, Next.js가 내부적으로 확장자 있는 경로를 정적 에셋 후보로 먼저 취급하는 것으로 보여 우리 `not-found.tsx`/`global-not-found.tsx`를 거치지 않고 프레임워크 자체의 최소 폴백(`<html id="__next_error__">`, `lang` 속성 자체가 없음, dev 모드 디버그 데이터 포함)으로 응답한다 — `notFound()` 호출 위치(레이아웃 최상단 vs `LocaleGate`)를 바꿔도 이 경로에서는 동일했다. 404 상태 코드는 정확하고 무효 **값**은 없으나(속성 자체 부재), 우리 디자인의 404 UI는 아니다. WSL EPERM(I-62)으로 프로덕션 빌드 검증이 불가해 dev 모드 디버그 오버레이와 실제 프로덕션 응답의 차이는 확인하지 못했다 — 필요시 별도 이슈 등재 검토.

**gate**: `npm run gate`(tsc→lint→test:coverage) 전부 통과.

---

## 16일차 (2026-08-11) — Task 011 메시지 카탈로그

**산출물**: `src/i18n/messages/{ko,en}/{common,league,match,player,team,stat,admin,error}.ts`(16파일 신규), `src/i18n/messages/index.ts`(신규), `docs/devStep/09.i18n키규약과메시지카탈로그구조_4팀_16일차.md`(신규).

**타입 안전 구조**: ko가 값+타입의 기준이고, en 각 파일은 ko에서 뽑은 타입을 적용해(타입 애노테이션 = excess property check) **키 누락·고아 키를 컴파일타임에 차단**한다 — 별도 타입 생성 스크립트 없이 `tsc`가 잡는다. `index.ts`는 `satisfies Record<(typeof SUPPORTED_LOCALES)[number], unknown>`로 로케일 목록(15일차 `locales.ts`)과 카탈로그 정합을 강제한다.

**경계 준수**: `common` 값은 `[lang]/layout.tsx`의 하드코딩 문자열을 그대로 이관했고 **`layout.tsx` 자체는 미수정**(실배선 22일차). 열거형 표시명(`enums.*`, 3팀 H-10)은 배럴에 미포함 — 별도 인계.

**I-104 준수**: dev 서버 미실행(코드 변경만이라 불필요), 타 팀 프로세스 무간섭, `git status`로 타 팀 미커밋 변경을 발견했으나 **원복하지 않고 보고만** 했다 — 15일차 `tsconfig.json` 오염 사고의 재발 방지 규칙이 의도대로 작동한 사례.

**팀장 검증: 결함 0건.** 보고서의 "satisfies" 표현이 실제로는 타입 애노테이션 방식이었으나 효과 동일이라 수정 요청하지 않음(다음 보고부터 표기 정확화).

## 17일차 (2026-08-12) — Task 011 키 네이밍 규약 + 타입 안전 접근

`src/i18n/keys.ts` + `keys.type-test.ts` 신규.

**16일차와의 경계**: 16일차가 닫은 것은 **카탈로그 내부의 정합**(ko 기준 타입으로 en의 키 누락·고아 키 차단)이고, 17일차가 닫은 것은 **카탈로그를 참조하는 쪽**이다. 둘은 다른 실패 모드를 막는다.

**규약 명문화**: `<namespace>.<component|screen>.<element>`를 `keys.ts` 헤더에 기술. `namespace`는 `messages/{ko,en}/*.ts` 파일명과 1:1(현재 8종), `component|screen`은 네임스페이스 내부 1단계 그룹(화면 전용이면 화면명, 공유 부품이면 컴포넌트명), `element`가 리프다. 기존 8개 네임스페이스 전량이 **이미 이 3단 구조**임을 실사 확인했다.

**타입 안전 접근 — codegen 미채택**: `messages.ko` 타입에서 재귀 조건부 타입 `DotPath<T>`로 `TranslationKey` 유니온을 파생한다(리프 도달 지점만 채택 → 3단 구조가 타입으로 강제됨). 항등 헬퍼 `translationKey<K extends TranslationKey>(key: K): K` 제공.

> **판단 근거**: codegen 스크립트는 카탈로그가 바뀔 때마다 재실행해야 하는데 이 프로젝트엔 그것을 강제할 pre-commit 훅이 없다(Husky/lint-staged 미설치). 재실행을 잊으면 생성 타입이 stale해져 **"존재하지 않는 키인데 타입은 통과"하는 정반대 결과**가 난다. 재귀 타입은 카탈로그와 같은 컴파일에서 갱신되므로 이 실패 모드가 원천적으로 없다. **재검토 조건**(`keys.ts` 주석에 기록): 카탈로그가 TS 재귀 한도에 근접하거나, 런타임에서 키 목록 전체를 순회해야 할 때.

**수락 기준 실측**: `keys.type-test.ts`의 `@ts-expect-error` 4건(오탈자·미존재 네임스페이스·부분 경로)이 **전부 실제 타입 오류로 소비됨**을 vitest typecheck 모드(실제 `tsc`)로 확인. `npm run test` 516 통과 / `Type Errors: no errors` / `tsc --noEmit`·`lint` 0.

**미착수(22일차까지)**: 번역 함수 API(`t()`)·로케일 Provider, 열거형 표시명 카탈로그(3팀 지원), 날짜·숫자 서식, 로케일 스위처. i18n 라이브러리는 여전히 미설치(런타임 의존성 3개 유지).

**팀장 검증: 결함 0건.**

## 18일차 (2026-08-13) — Task 011

`src/i18n/t.ts`·`provider.tsx` + 테스트 2파일 신규(기존 `keys.ts`·`locales.ts`·`messages/` 무변경).

- **`t(locale, key, params?)`는 React를 참조하지 않는 순수 함수.** 서버 컴포넌트는 `params.lang`을 넘겨 직접 호출(Next.js 공식 i18n 가이드의 `getDictionary` 패턴).
- **`TranslationProvider` + `useLocale()`/`useTranslation()`** 은 **같은 `t()`를 바인딩**해 제공 — 구현이 두 벌로 갈리지 않고, Context는 클라이언트 트리의 prop 드릴링 회피 용도로만 쓰인다.
- **설계 근거**: RSC는 Context를 사용할 수 없어(공식 문서 확인) 서버가 Provider를 경유할 방법이 없다 — 사실상 단일 선택지.
- `{placeholder}` 보간 지원. **미존재 키·Provider 누락은 조용히 넘기지 않고 throw**(무음 실패 방지).

테스트 10건 — jsdom 미설치라 `react-dom/server`의 `renderToStaticMarkup`으로 서버 직접 호출·Provider 하위 훅·Provider 밖 에러 경로를 모두 검증. `tsc --noEmit` 0, `npm run test` 542 통과.

**스코프 밖(명시)**: 루트 레이아웃에 Provider 실배선(`children` 감싸기)은 **22일차 LocaleSwitcher와 함께**. 오늘은 API 확정까지.

**이월(22일차까지)**: 열거형 표시명 카탈로그(3팀 지원), 날짜·시각·숫자 서식, 로케일 스위처. i18n 라이브러리는 여전히 미설치(런타임 의존성 3개 유지 — 자체 구현).

**이슈 후보(미등재)**: `TranslationParams`가 `Record<string, string|number>`로 느슨함 — 카탈로그를 `as const`화하면 키별 placeholder 이름을 타입으로 강제 가능하나, 현재 자리표시자가 2개뿐이라 과설계로 판단해 보류(필요 시 `keys.ts`의 `DotPath` 패턴처럼 파생).

**팀장 검증: 결함 0건.** 1팀 당일 신규 D-18 룰이 이 팀 파일을 오탐하지 않는지 교차 확인 — 예외 경로 정상(4파일 warn 0).

## 19일차 (2026-08-14) — Task 011 열거형 카탈로그 골격

- `src/i18n/messages/{ko,en}/enums.ts` (신규) — 3팀 H-10 목록 전량(포지션 11·이벤트 23·부상 4·전술 6·페이즈 6·수상 12·마켓 상태 4)을 `EnumTranslationCatalog<T>`(T12, `@/types`)로 감싸 **도메인 enum 전 멤버 매핑을 tsc가 강제**. 유니온 멤버가 늘거나 줄면 즉시 컴파일 오류.
- `enums.position.GK` 3단 구조(`keys.ts` 규약) 준수. 값은 전부 enum 리터럴 echo 자리표시자 — **실값은 3팀이 23일차 이후**.
- `../index.ts` 통합 `messages`에는 미합류(Provider 실배선 22일차 이후, 09문서 §4 방침 유지).

**팀장 검증: 결함 0건.** ko/en 키 수 동일(각 74), `@/types` 서브경로 직접 import 0건(C-5·C-6), 신규 파일의 D-18 lint 경고 0건을 grep으로 직접 실증.

**인계**: 20일차 이후 날짜·숫자 서식, 로케일 스위처. **D-18 경고 111건 해소가 22일차 수락 판정 대상**(현재 1팀 Task 010 lint 기준이 여기에 종속돼 조건부 충족 판정 — I-116).

## 20일차 (2026-08-17) — Task 011 (날짜·숫자 서식)

- `src/i18n/format.ts` / `format.test.ts` 신규.
- `formatKickoff`(time/dateTime/date) · `formatPoints` · `formatOdds` — `Intl.*` 기반, DC-07(UTC 저장 → 로케일 로컬 변환).
- **포맷터 단일 소스 팀장 실증**: `src/**`에서 `toLocaleString`/`Intl.*` 직접 호출이 `format.ts` 외 0건.
- 테스트 10케이스, 전체 641 통과(회귀 0), **D-18 신규 경고 0건**(기존 111 불변).
- `enums.ts` 합류·Provider 실배선은 22일차 몫이라 미착수.

## 21일차 (2026-08-18) — Task 011 (번역 대상 경계 명문화)

- `src/i18n/README.md` 신규.
- 경계 애매 사례 3종: ⓐ enum 표시명(유한→번역) vs 시드 고유명사(개방형→비번역) ⓑ 파라미터 치환 방향(템플릿만 vs 값도 번역) ⓒ 고유명사+수치 혼합 완성 문장은 카탈로그 미포함.
- **자동/사람 검출 구분표** — tsc(키 구조·3단 경로·enum 커버) vs ESLint D-18(JSX 하드코딩)이 각각 못 잡는 것(고유명사 오탐, 속성값 사각지대, 콘텐츠 경계) 명시. 1팀 CI 편입과 맞물림.
- `tsc` 0, lint 111 warnings/0 errors(**신규 경고 0**). 이슈: I-125(09문서 §2 드리프트).

## 22일차 (2026-08-19) — Task 011 종료 · M-1 Phase 1

- `src/components/ui/LocaleSwitcher.tsx` 신규 — **`src/components/` 디렉터리 첫 파일**(23일차 예정이었으나 011 산출물이라 앞당김). 경로 첫 세그먼트 교체 + `router.replace`(로케일 전환은 설정 토글이라 히스토리 미적재) + 쿠키 영속화. `persistLocaleCookie`를 최상위 함수로 뺀 것은 react-compiler `react-hooks/immutability` 오탐 회피.
- 루트 레이아웃 **Provider 실배선**(18일차 API 확정 후 이월분) + 헤더 placeholder 교체, `enums.ts` → `../index.ts` 합류(구조만).
- **D-18 경고 111건 → 0건** — 헤더·사이드내비·푸터 + 라우트별 3종 60개 + `global-not-found`. Task 011 수락 판정의 핵심 조건 충족.
- **팀장 지시 재작업**: `await bootstrapApp()` 루트 레이아웃 배선(13일차 지시가 9일 이월된 미연결). 이 배선이 **I-75 실현**과 **신규 결함(완료 플래그가 실패 은폐)**을 드러냄 — 후자는 문서에 없던 자체 발견이며 `bootstrap.ts`가 1팀 소유라 **직접 고치지 않고 재현 로그 + 판단만 회신**(절차 준수).
- 제약: **Playwright MCP Chromium 미설치로 클릭 전환·"새로고침 없음"·고유명사 불변 실측 미수행**(curl SSR 검증으로 대체) → **I-128**. `loading`/`not-found`는 Next 16에서 `params` 접근 불가로 DEFAULT_LOCALE 고정 → **I-129**.

## 23일차 (2026-08-20) — Task 012 (27일차까지 계속)

- `components.json`(base=radix/preset=nova), `src/lib/utils.ts`(`cn()`), `src/components/ui/{badge,button,card,separator,skeleton,table,tabs,tooltip}.tsx` 8종.
- shadcn init 기본값 2건 교정: ⓐ `.dark` 클래스 다크모드 → `prefers-color-scheme`(클래스 토글러가 없어 그대로 두면 다크모드가 죽음) ⓑ 내부 npm install의 WSL EPERM(I-62 계열) → 임시 `.npmrc` `--no-bin-links` 우회 후 제거.
- **팀장 검증 결함(해소)**: `lucide-react` import 0건 → 제거. 판정 규칙 "마감 시점 `src/` 내 import 0건인 런타임 의존성은 남기지 않는다" 확립. 최종 6개 전부 근거 확보.
- 인계: I-135(`AwardScope` 골격 추가) 미처리, 24일차 이후.

## 24일차 (2026-08-21) — Task 012 (27일차까지 계속) + I-135

- `src/i18n/messages/{ko,en}/enums.ts` — `awardScope` 골격 4종 추가(**I-135 해소**), 값은 19일차 관례대로 리터럴 echo 자리표시자(실값은 3팀).
- `src/i18n/messages/ko/enums.test.ts` — `EXPECTED_LITERAL_COUNT` 66 → 70.
- `src/app/globals.css` — `@theme inline`에 NFR-RS-001 브레이크포인트 6종(20/23.4375/48/64/90/120rem). `00-공통규약.md §5`의 px를 rem 환산. `2xl`은 1920 하한만 브레이크포인트, 상단은 최대폭 컨테이너. **`tailwind.config.ts` 미생성 실측 확인.**
- 시맨틱 컬러는 같은 문서가 "H-11(28일차) 전 미정"이라 명시 → 25일차 이월(W-06 각주).
- 타 팀 unstaged 파일을 손대지 않고 보고만 함(절차 정확). 이슈 후보 0건.

## 25일차 (2026-08-24) — Task 012 시맨틱 컬러 토큰 (27일차까지 계속)

- `src/app/globals.css` — 승격·플레이오프·강등·LIVE·경고 5종을 OKLCH로 라이트/다크 각각 정의 + `@theme inline` 매핑. `--warning-foreground` 1종 추가(아래 참조).
- `src/lib/a11y/contrast.test.ts`(신규 **102케이스**) — `globals.css`를 **정규식 파싱**(값 하드코딩 복사 아님 → 토큰이 바뀌면 반드시 깨진다)해 4축을 단언: ⓐ CVD 3종(protanopia/deuteranopia/tritanopia)+정상 시야 전 쌍 ΔE ⓑ 전 토큰 sRGB 색역 안 ⓒ 비텍스트 4색의 페이지 배경 대비 3:1 ⓓ `--warning-foreground` × `--warning` 배지 쌍 4.5:1.
- **⚠️ 팀장 검증에서 결함 2건 지적받아 수정.** 초기 산출물은 **색맹 ΔE만 검증하고 배경 대비 축이 통째로 빠져 있었다** — 팀장 실측 결과 라이트 `--warning` **1.34:1**(비텍스트 3:1 기준도 미달), 다크 `--relegation` 3.70:1(본문 텍스트 불가), **5개 중 4개 토큰이 sRGB 색역 밖**이라 브라우저 클램프로 실제 렌더 색이 달라져 **원 ΔE 검증 자체가 무효**인 상태였다.
- 조치: 색역 밖 토큰은 채도를 낮춰 색역 안으로 넣고, **`--warning`은 배지 채움 전용으로 용도를 확정**한 뒤 짝이 되는 `--warning-foreground`(배지 위 8.2~9.6:1)를 신설했다. 비텍스트 4색은 라이트·다크 모두 3:1(WCAG 1.4.11) 이상 확보. 채도를 낮추면서 **ΔE 하한 15 → 12**로 조정(실측 최소값 기준, 아이콘·라벨 병기 전제와 함께 수용).
- **교훈: 색맹 상호 구분(ΔE)과 배경 대비(WCAG)는 별개 축이다.** 전자만 검증하면 "서로 구분은 되지만 배경에서 안 보이는" 색이 통과한다.
- ⚠️ **비텍스트 3:1 기준은 "배지 채움·보더·아이콘 전용"이라는 전제 위에 있다** — 013A(28일차 이후) 소비 시 본문 텍스트 전경으로 쓰면 기준 미달이며 **테스트가 소비처 오용은 잡지 못한다**.
- 참고: 팀장 조율 착오로 검증 테스트가 두 파일로 중복 생성됐다가 `src/lib/a11y/contrast.test.ts` 한 벌로 통합됨(4축 전부 보존, 팀 귀책 아님).

---

## 27일차 (2026-08-26) — Task 012 마감 · ko↔en 폭 규약 · H-11 인계

- `src/components/ui/button.tsx`(size별 `min-w-[Nch]`, max-w 의도적 미지정 — 버튼 라벨은 잘리면 의미 소실) · `badge.tsx`(`max-w-[14ch]` + `shrink-0`) · `table.tsx`(`numeric` prop → 우측정렬 + `tabular-nums`, 컬럼 최소폭은 소비처 책임으로 규약화).
- `docs/handoff/H-11-design-tokens-shadcn-primitives.md`(신규) — 프리미티브 8종·`cn()`·토큰 전량 + 미도입 7종의 5팀 자율 추가 규약.
- **`globals.css` 의도적 미변경** — `ch` 단위 임의값으로 충분해 새 토큰이 불필요했고, 26일차 "타이포·간격은 기본 스케일 그대로" 결정과 일치. I-150 재발 리스크 차단 효과도 있었다.
- **팀장 검증 결함 — `truncate` 말줄임 미작동**: `inline-flex` 컨테이너라 `text-overflow: ellipsis`가 자식(익명 flex item)에 적용되지 않아 **하드 클립**된다. 기능 회귀는 아니나 주석이 "말줄임"이라 단언해 계약 문구가 틀렸다. 소비처 0곳이라 API 변경 대신 주석·H-11 문서를 정정했다.
  - **교훈: Tailwind 유틸리티는 클래스명이 아니라 적용되는 CSS 박스 모델까지 보고 써야 한다.** `truncate`는 블록 컨테이너 전제 유틸리티다.
- **수락 기준 "두 테마 × 두 로케일 깨짐 0"은 미검증으로 자진 보고**했다(→ **I-153**). 세 컴포넌트의 소비처가 0곳이라 렌더할 화면이 없었고, Playwright(I-128) + jsdom·testing-library(**I-151**) 미설치로 실측 경로가 이중 차단이다. **실측 없이 통과로 올리지 않은 것이 옳은 처리였다**(26일차 V-02 기록 오류와 같은 유형 예방).
- **I-148·I-143 미처리** — 기한이 013A(33일차)이고 27일차 배정이 아니었다. 팀장 지시가 ISSUES 열람 금지와 처리 요구를 동시에 준 설계 오류였고 보류 판단이 인정됐다.
- 테스트: `tsc` 0 / `lint` 0 / 66 files·1,000 tests 통과. 이슈 후보 2건 모두 채택(I-151·I-152).

## 28일차 (2026-08-27) — Task 013A 도메인 표현 4종 (착수)

- `src/components/domain/{TeamBadge,PlayerAvatar,AbilityRadar,ConditionGauge}.tsx` + `radar.ts` + `radar.test.ts`(5건) + `types.ts` — 전부 신규. 게이트 그린(tsc 0 / lint 0 / test 1025 passed).
- `src/components/ui/{avatar,progress}.tsx` — shadcn 신규 도입 2종. H-11 "미도입" 목록에서 승격돼 프리미티브는 **10종**이 됐다.
- `src/i18n/messages/{ko,en}/{team,player}.ts` 키 추가(ko·en 대칭 확인).
- **4상태 계약을 `domain/types.ts` 한 곳에 세웠다** — `DomainViewState<T>`(`loading`/`empty`/`error`/`ready`), 4종 모두 `state: DomainViewState<T>` 단일 prop. 5팀 013B도 팀장 판정으로 이 형태에 정렬했다(동형 별도 선언, I-156).
- 전부 **서버 컴포넌트**이며 `t(locale, key)` 직접 호출 + `locale: SupportedLocale` prop(011 규약, `src/i18n/README.md` 13행).
- `TeamBadge`는 기존 `src/lib/naming/emblem.ts`의 `generateTeamEmblem(crestSeed)`를 소비한다(생성기 신규 발명 없음), `svg`를 `role="img"` + `aria-label`로 감쌌다.
- `AbilityRadar`는 34속성 → 기술/정신/신체/GK 4축 평균을 순수 SVG로. **분류는 `person.ts` 헤더 주석 것을 그대로 재사용**(새 체계 발명 금지)했고 좌표 계산만 `radar.ts`로 분리해 유닛 테스트했다 — 컴포넌트 렌더 테스트가 막힌 상황(I-151)에서 로직만이라도 검증하는 우회로.
- `PlayerAvatar`는 시드 기반 생성기가 없어 이니셜 + `id` 해시 배경색 플레이스홀더 → **I-155** 등재(3팀 판정).
- 이슈: **I-154**(뉴스 i18n 네임스페이스 판정)가 5팀 제보로 이 팀에 배정됐다. 잔여 10종은 33일차 구간.

## 29일차 (2026-08-28) — Task 013A (도메인 표현 8/8 종결)

- 신규 4종: `domain/{FitnessBar,FormStrip,PositionMap,StatBar}.tsx`. 순수 로직 분리 `domain/{form,pitch}.ts` + 각 `.test.ts`.
- i18n: `messages/{ko,en}/team.ts`에 `form` 그룹, `messages/{ko,en}/player.ts`에 `position` 그룹 (ko/en 키 대칭 확인됨).
- 28일차 규약 그대로 계승 — `DomainViewState<T>` 4상태 · 서버 컴포넌트 · `t(locale, …)`. **새 Tailwind 토큰 0개**, `enums.position` 라벨은 재사용만.
- `pitch.ts`의 `POSITION_COORDINATES`를 `Record<Position, PitchPoint>`로 선언해 **`Position` 유니온이 변하면 tsc가 누락을 컴파일 오류로 잡는다**(`radar.ts` `RADAR_CATEGORY_ORDER` 선례 계승). I-158이 확정되면 이 테이블이 자동으로 걸린다.
- 게이트 그린(tsc 0 / lint 0 / test 74 files · 1067 passed, 회귀 0).
- **팀장 수용 판정** — `StatBar.label`을 호출부가 번역해 넘긴 문자열로 둔 설계는 범용 스탯 컴포넌트로서 타당하며 `locale`이 상태 문구에 여전히 쓰이므로 i18n 경유가 끊긴 것이 아니다. 이슈 등재 없이 수용.
- 신규 이슈: **I-159**(fitness clamp 1줄 중복, 경미). 인계: **I-157은 5팀과 공동** — 단독으로 좌표계를 바꾸지 말 것.
- 잔여(33일차): 상태·유틸 6종. 그 전에 I-151(jsdom) · I-148 · I-143 · I-144 · I-154.

## 30일차 (2026-08-31) — Task 013A 상태 3종

- `src/components/state/SkeletonBlock.tsx` / `EmptyState.tsx` / `ErrorState.tsx` (신규 3종, 013A 상태·유틸 **3/6**)
- **수락 기준(메시지 번역 키 경유) 충족** — `titleKey`/`descriptionKey`가 `TranslationKey` 타입이고 렌더는 전부 `t(locale, key, params)` 경유. 하드코딩 표시 문자열 0건(팀장 grep 확인). `ErrorState`는 `onRetry` + `error.generic.*` 기본 키
- **자체 4상태(`DomainViewState`)를 갖지 않는다** — 라우트 레벨 공용 컴포넌트라 4상태를 *표현하는* 쪽이 아니라 *렌더하는* 쪽이기 때문. 기존 domain 카탈로그 관례(`locale` prop, `cn()`, `t()`)는 준수
- I-157(피치 좌표계) 비관여, I-159 미착수(013A 종료 후 구간)

잔여 3종(`CountdownTimer`·`PhaseIndicator`·`OddsButton`)은 33일차. `CountdownTimer`는 2팀 H-24 계약(30일차 인계)을 소비한다.

## 31일차 (2026-09-01) — Task 013A 상태·유틸 2/2 (6/6 종결)

- `src/components/state/CountdownTimer.tsx` (신규, client) — `targetAt`까지 실시간 tick. **`Date.now()`를 렌더 중 직접 호출하지 않고 `useEffect`로 미뤄** SSR/하이드레이션 mismatch 회피. `isPaused` 시 정지 표기, 재동기화는 props 갱신에 위임(H-24 환산은 이 계층에서 쓰지 않음).
- `src/components/state/PhaseIndicator.tsx` (신규) — `enums.seasonPhase` 재사용, `round`는 선택 prop.
- `src/components/state/OddsButton.tsx` (신규) — **`disabled` 고정 + `onClick`/`onSelect` prop 자체를 타입에 미노출**. FR-BT-014를 관례가 아니라 타입으로 차단.
- `src/i18n/format.ts`(+test) `formatCountdownClock` 추가(테스트 5건), `src/i18n/messages/{ko,en}/common.ts` `phase.*`·`countdown.*` 키 추가.
- 게이트: tsc 0 / lint 0(`react-hooks/set-state-in-effect` 1건을 헬퍼 함수 리팩터로 해소 — 5팀 교차 제보) / `format.test.ts`+`keys.type-test.ts` 21 passed.

## 32일차 (2026-09-02) — Task 013A 규약 감사

- **변경 파일 없음 — 위반 0건, 리팩터 불필요**
- 감사 대상: `components/ui/**`(11) · `components/domain/**`(8+헬퍼) · `components/state/**`(6) · `[lang]/layout.tsx` + 소유 라우트 29개
- 4항목 전부 통과: 페칭 0건 / `@/types` 배럴 준수(서브경로 0건) / 번역 키 경유 / 로케일 포맷터 경유
- 판단으로 종결(팀장 동의): ⓐ 0~100 범위 값의 `toFixed(1)`·`Math.round`는 양 로케일 렌더링 동일 → 포맷터 미경유가 위반 아님 ⓑ `bootstrapApp()`은 페칭 아닌 1팀 부트스트랩(I-72) ⓒ `metadata.description`은 SEO 메타 필드(I-89)
- 테스트: eslint 클린 / `npx tsc --noEmit` 클린 / vitest 8 files 54 tests 통과

## 33일차 (2026-09-03) — 013A 규약 리팩터·수락 실증 (013A 종결)

- 14종 전수 grep: `useMemo`/`useCallback` **0건** — 제거 대상도 정당화 주석 대상도 없음
- `overflow-x`: 14종 전부 고정크기·소형 위젯(SVG 140px 레이더, w-full 피치맵, 배지·게이지)이라 **대상 없음** 판정(불필요 wrapper 미삽입)
- **I-159 해소**: 신규 `src/components/domain/fitness.ts`(`clampFitness`) + `fitness.test.ts`(4케이스). `FitnessBar`·`ConditionGauge` 로컬 헬퍼 제거 후 import 전환
- 하드코딩 실측: 주석 제거 후 재grep → JSX 렌더 텍스트 0건(전부 `t(locale, key)` 경유)
- **4상태 면제 판정**: 상태·유틸 6종은 30~31일차 표가 요구하지 않음 → 팀장이 31일차 행 원문·`PhaseIndicator.tsx:33`으로 대조해 **타당 확인**. 수락 문구 불일치는 **I-168**로 등재·팀 md 정정(팀장)
- **I-166 마무리**(팀장 추가 지시): 사장된 `team.trophy.type.*` 4키를 ko/en `team.ts`에서 제거, 헤더 주석 갱신. `trophy` 그룹 본체(empty/error/countFormat/seasonsFormat/awardsTitle)는 유지. ko/en 패리티 diff 0
- 미처리 이월: I-151 · I-148 · I-143 · I-144 · I-154 — **013A 종료로 이월 사유 소멸, 34일차 일차 확정 필요**

## 34일차 (2026-09-04) — Task 014 `/sample` 쇼케이스 (34일차분)

- `src/app/[lang]/sample/page.tsx`(549줄, 전면 작성) / `src/i18n/messages/{ko,en}/sample.ts`(신규 네임스페이스) / `messages/index.ts`(등록) / `src/app/[lang]/layout.tsx`(검증 후 수정)
- 5개 카테고리 섹션(domain·composite·state·chart·admin) + 앵커 내비, **21종 전량 등록·실렌더**
- 팀장 실측: JSX 사용 전수 grep → **import만 하고 미사용 0건**. 하드코딩 문자열 **0건**(주석 제거 후 텍스트노드 + `title`/`aria-label`/`alt`/`placeholder` 전수 grep 무히트)
- chart·admin은 컴포넌트 부재로 골격 + "미구현" 표기. `MatchCard`는 같은 날 5팀 신규 생성 → **35일차 등록해 22종으로 맞출 것**
- 데이터는 `getDataSource()` 어댑터만 경유. 어댑터가 빈 배열만 주는 4종(Injury·Trophy·Award·PlayerAttributeHistory)은 쇼케이스 전용 인라인 표본 — `no-restricted-imports` 우회 0건
- **팀장 지적 → 수정**: `layout.tsx:27` metadata description 한글 하드코딩이 `/en` 응답에 그대로 노출. `generateMetadata` 전환 + `common.app.description` 신설(ko/en), **재기동 후 `/en` 실응답 영문 확인**
- **판단 회신(수정 없음)**: `loading.tsx`/`not-found.tsx`의 `DEFAULT_LOCALE` 고정 → `next/headers`의 `cookies()`로 `LOCALE_COOKIE_NAME`(`f4_locale`) 근사 가능하나 최초 방문자는 쿠키 부재로 불완전. `unstable_rootParams`는 제거돼 대안 아님 → **I-170 등재**
- **소유 경계 준수**: 작업 중 발견한 `[lang]/page.tsx` lint 에러(5팀 동시 편집 중간 상태)를 고치지 않고 보고만 함 — 팀장 확인 결과 현재 0건
- **I-151/148/143/144 권고 일차 제출**(구현 없음): I-151 jsdom 35일차 ~1인일 / I-144 ΔE 35일차 ~0.25인일 / I-143 소비 규약 36일차 ~0.5인일 / I-148 토큰 재점검 38일차 ~0.5인일

## 36일차 (2026-09-08) — Task 014 로케일 전환 컨트롤

- `src/app/[lang]/sample/LocaleCompareToggle.tsx`(신규), `sample/page.tsx` `renderShowcaseBody(locale)` 래핑, `sample.ts` 키 2종
- 기각: 헤더 `LocaleSwitcher` 재사용(라우트 이동 = RSC 왕복) / CSS `hidden` 동시 마운트(`id` 중복으로 앵커 오작동)
- 팀장 실측: URL 불변 · 문서/RSC 요청 0 · 중복 id 없음 · `#domain` 앵커 정상 → 수락 기준 충족
- 셸 컴포넌트 분리(23일차 이월): "기계적 이동이라 위험 낮으나 `layout.tsx` diff가 오늘 겹친다" → 보류 권고, 팀장 동의
- 이슈: I-188(작업 중 `git stash` 사용 — 무손실 종료, 규칙화)

## 37일차 (2026-09-09) — Task 014 ErrorBoundary 격리 + 어댑터 토글

- `src/components/state/ErrorBoundary.tsx`(신규) — **Next 16.2 `unstable_catchError`(`next/error`)** 기반. 학습 데이터에 의존하지 않고 `node_modules/next/dist/client/components/catch-error.d.ts`에서 API를 확인해 사용(AGENTS.md 규칙), 팀장 재확인
- `/sample`의 `ComponentSlot` 22개 사용처 전량 배선 → 임시 크래시 컴포넌트를 슬롯 하나에 넣어 **나머지 21종 생존을 실측**한 뒤 제거(최종 diff 미포함)
- `sample/DataSourceToggle.tsx` + `sample/data-source-actions.ts`(신규) — 어댑터 토글(UC-602). `resetDataSourceCache()`(1팀이 "런타임 핫스왑" 용도로 문서화한 API) 경유, 부트스트랩 캐시 때문에 프로바이더가 없을 수 있어 대상 어댑터를 동적 import, 전환 후 헬스체크(`getLeagues()`) → 실패 시 이전 kind 자동 복귀. `factory.ts`/`bootstrap.ts` 무수정
- **팀장 지적 반영 — dev 전용 가드 추가.** "`/sample`은 로컬 전용"이라는 전제가 주석에만 있고 코드로 강제되지 않았다. `"use server"` 액션은 액션 ID로 외부 직접 POST가 가능하고 `/sample`은 프로덕션 빌드에 포함되는 일반 라우트라, 배포 시 **인증 없이 서버 프로세스 전역의 데이터 소스를 뒤집을 수 있는 엔드포인트**가 된다. `setDataSourceKindAction` 최상단(상태 변경보다 앞)에 `NODE_ENV !== 'development'` 조기 반환, 토글 UI 숨김은 보조 장치일 뿐임을 주석 명시
- 테스트: typecheck/lint/test 통과, Playwright로 Mock↔Supabase 실전환 + dev 가드 후 정상 동작 재확인. 전체 스위트의 `rating.test.ts` 실패 2건은 **import 그래프로 자기 변경과 무관함을 확인**한 뒤 스코프를 좁혀 재검증(2·3팀 진행 중 건)
- 이슈: I-198(`ErrorBoundary`는 렌더 단계만 보호 — `Page()` 데이터 조회는 여전히 `error.tsx`)

## 38일차 (2026-09-10) — Task 014 완결: 커버리지 체크리스트 자동 표기 / I-200 원인 규명

- `sample/CoverageChecklist.tsx` · `sample/component-registry.ts` · `src/i18n/coverage.ts`(전부 신규, 각 `.test.ts` 동반) + `StateToggleSlot.tsx`(`ComponentKey` export, `FOUR_STATE_COMPONENT_KEYS`) · `sample/page.tsx` · `i18n/messages/{ko,en}/sample.ts`(키 6개)
- **3개 카운터 전부 레지스트리/카탈로그 실측** — 등록 22종 / 4상태 **16/16(100%)** / 번역 키 누락 **0**(ko·en 카탈로그 실제 순회 diff). domain·composite·state 섹션의 기존 **하드코딩 배지(8/8/6)도 같은 레지스트리로 교체**해 표기 이원화 제거
- **RSC 경계 함정을 실측으로 발견·해소** — `component-registry.ts`가 `"use client"` 모듈(`StateToggleSlot.tsx`)의 배열 값을 직접 import하자 Server Component에서 **빈 값으로 치환돼 "0/16" 회귀**. 타입도 맞고 테스트도 통과하는데 화면에서만 틀리는 유형이라 Playwright 실측으로만 드러났다. 런타임 카운트는 client 모듈 import 없이 리터럴 목록으로 분리하고, 실제 디스패치 레지스트리와의 일치는 **vitest(Node, RSC 경계 없음)가 매 실행 교차검증**
- **I-200 원인 규명(5팀 Task 015 CLS 미달 건)** — 원인 지점이 4팀 소유(`[lang]/layout.tsx` Gothic A1 선언)라 팀장이 수정 요청. 세 시도 전부 무효: `display:"optional"` 0.1642(기준선 0.1735 대비 오차 수준) / `adjustFontFallback:false` 0.371(악화) / `weight` 3종→1종 0.166(무효). **SSR 원문 확인 결과 dev는 `<style>` 0개·`@font-face` 0개·외부 스타일시트 1개**로 폰트 CSS가 첫 페인트 이후 합류 → `font-display`로 통제 불가능한 구조
  - **팀장 판정으로 `display:"optional"` 되돌림** — 측정 가능한 유일한 환경에서 이득이 없는데 "첫 방문 시 한글이 폴백 글꼴로 남는" 확실한 대가를 미검증 프로덕션 가설과 맞바꾸지 않는다. **실측치 3건은 수치와 함께 파일 헤더 주석에 보존**해 재시도를 차단
- 테스트: `sample`·`i18n` 51/51(신규 10건 포함) · 팀장 전체 스위트 1407 passed · typecheck·lint 클린. Playwright `/ko/sample`·`/en/sample` 콘솔 에러 0, 스크린샷 `.playwright-mcp/day38-coverage-checklist-{ko,en}.png`
- 이슈: I-200(공동) / RSC 경계 상수 공유 함정은 38일차 인계 3번으로 승계
