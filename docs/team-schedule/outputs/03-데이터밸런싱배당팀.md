# 3팀 — 데이터·밸런싱·배당팀 — 산출물 기록

> 위치: `docs/team-schedule/outputs/03-데이터밸런싱배당팀.md` / 본체: [`03-데이터밸런싱배당팀.md`](../03-데이터밸런싱배당팀.md)
> 이 파일은 **일차별 산출물·조정 기록의 누적처**입니다. 팀 md 본체를 가볍게 유지하기 위해 분리했습니다.
> **팀원 소환 시 열람 대상이 아닙니다** — 팀원은 본체 md의 소유 경로·해당 일차 행만 봅니다.
> 새 기록은 이 파일 끝에 append 하세요.

---

## 1. 요약 (1~13일차)

| 항목 | 값 |
|---|---|
| 참여 일차 | 1~8일차 **미호출**(H-01 도메인 타입 동결 대기) / 9~13일차 5일 실작업 |
| 담당 Task | Task 003(**종료**, 9~12일차) · Task 007(**진행 중**, 13일차 착수 — `naming/` 부분만 완료, `emblem.ts`·Mock 팩토리는 14일차 이후) |
| 주요 산출물 | `src/lib/config/catalog.ts`, `loader.ts`, `fallback.ts`, `policy.ts`, `snapshot.ts` + 각 `.test.ts`, `src/lib/naming/generate.ts` · `namePools.ts` · `blacklist.ts` + 테스트 |
| 종료된 Task | Task 003(12일차, 36그룹 카탈로그+로더+폴백+발효정책+스냅샷 전량, H-05 인계) |
| 진행 중 | Task 007(13일차 국적 기반 이름 생성기만 완료, H-10 인계 완료. 엠블럼 SVG·Mock 월드 팩토리는 14일차~ 이월) |
| 미해소 이월 | I-66(이벤트 확률·가중치 소유 계층, Task 024 착수 17일차 전 확정) · I-75(`bootstrap.ts` webpack 실컴파일 확인, Mock 착수 시) · I-83(엔진의 공통코드 접근 경로, 사용자 판정 대기) · I-85(`LEADERBOARD_MIN_APPEARANCE_PCT` 예외 흡수, 019 착수 전) · I-88(국적/이름 풀 공통코드화 여부, 13일차 3팀 제기 — **해소는 14일차**, 아래 §4 참고) |

## 2. 일차별 기록

### 1~7일차 — 업무 없음 (대기 구간)
- Task 003(공통코드)이 도메인 타입 동결(H-01, 8일차 예정)을 선행 조건으로 하여 배정 업무가 없었습니다. 팀 일정 문서 §4의 대기 구간 항목(국적별 이름 풀 수집, 공통코드 36그룹 시드값 원문 정리, H-10 표시명 목록 초안, SP-1 사전 검토)은 세션 로그상 실행 여부가 확인되지 않습니다.
- 다만 2일차 교차 점검에서 3팀에 영향을 주는 오류 2건이 해소됐습니다: **I-20**(Task 030 담당팀 오기 "2팀" → **3팀 단독**으로 정정, 2팀 발견) / **I-21**(D-25 엔진 상수 수정 주체가 2팀 누락 — `src/lib/sim/**`는 2팀만 커밋하므로 3팀이 031 튜닝 시 직접 수정 불가, "008 밴드 테스트(1팀) → 031 튜닝 요청(3팀) → 엔진 상수 수정(2팀)" 흐름으로 확정).
- 2일차 말 이월 항목에 "3팀 첫 호출(9일차) 시 전달 대기 2건" 기록: ① D-17 블랙리스트 필터를 Task 007 요건으로 명문화 ② 2팀 시드 네임스페이스 진입 규약(`ODDS_PRESIM`).
- 출처: `docs/dailyWorkLog/1Day.md`~`7Day.md`, `docs/dailyWorkLog/2Day.md`

### 8일차 (2026-07-30, 목) — SP-1 도메인 타입 동결 리뷰
- **한 일**: 계획상 두 번뿐인 전원 참여 동기화 포인트(SP-1)에서 Task 003·007·029·035를 **필드 단위**로 요구사항 원문과 대조.
- **산출물**: `docs/devStep/07.SP1동결리뷰_3팀_8일차.md` (신규)
- **판정**: 완료 — 동결 전 반영 요청 0건(명칭·범위·enum까지 원문과 일치 확인)
- **특기**:
  - 3팀이 유일하게 "동결 전 반영" 채택 항목을 제기 — `Manager.isActing` 필드. D-23(감독 공석 시 즉시 대행) 판정에 대행/정식 구분이 필요한데 `tenureSeasons`만으로는 유도 불가능함을 지적. 3팀 자신은 "소비 시점이 54일차라 급하지 않음"으로 낮게 분류했으나 **팀장이 "파생 불가"를 이유로 격상**, 1팀이 동결 전 반영을 채택.
  - `PointTransactionReasonCode` 리터럴 개수 충돌(3팀 vs 6팀) — 3팀은 **리터럴을 직접 세어 "12개, 요구사항과 일치"**로 정확히 판정, 6팀은 stale 주석("11개")을 신뢰해 오판. 팀장이 원본 확인 후 **3팀이 맞음**으로 결론.
  - 절차 사고: 7일차 3팀 세션이 텍스트 지시만으로 "종료 처리"되어 `shutdown_request` 없이 idle 상태로 컨텍스트를 점유한 채 8일차가 시작됨(사용자 지적으로 교정, 이후 "일차 경계 = 팀원 실제 종료 후 진행" 원칙 확정).
- 출처: `docs/dailyWorkLog/8Day.md`

### 9일차 (2026-07-31, 금) — Task 003 착수
- **한 일**: `src/lib/config/catalog.ts` — 공통코드 그룹 카탈로그 36종 타입 안전 상수 정의(group_code·타입·min/max·apply_policy·description).
- **산출물**: `src/lib/config/catalog.ts` (신규, 567줄)
- **판정**: 완료 — `satisfies`로 36그룹 전량 등록을 **타입 레벨 강제**(불일치 시 tsc 오류)
- **특기**: Mock 팩토리(Task 007, 13일차~) 관련 후속 합의 2건에 참여 — ① 라이브 컷오프 계약을 Mock에도 동일 적용(`Fixture.kickoffAt` 고정 + 이벤트 시드 사전 생성 + 조회 시점 `elapsed` 필터링) ② Mock에 과거 시즌 2~3개 생성 명시. `CUP_PARAM`(대표타입 JSON) 위젯 분기 관련 1팀 제기 사항에 동의(H-05 인계 문서에 명시 예정). 능력치 범위 CHECK(16일차 Task 032)와 Mock 생성 시점 불변식이 서로 겹치지 않음을 3팀·6팀·1팀이 공동 확인.
- 출처: `docs/dailyWorkLog/9Day.md`

### 10일차 (2026-08-03, 월) — Task 003 계속(상수 로더)
- **한 일**: `loadConstants<G>(group)` 상수 로더 인터페이스 — 해석 우선순위(전역 기본값 → 하드코딩 폴백), 그룹 단위 캐시, 무효화 훅. `ConstantSource` 주입 방식(`setGlobalDefaultSource`/`setFallbackSource`)으로 확장 지점만 확정.
- **산출물**: `src/lib/config/loader.ts` · `loader.test.ts` (신규, 10케이스)
- **판정**: 완료 — 수락 기준("엔진이 리터럴 대신 로더를 쓰는 경로가 타입으로 강제") 충족
- **특기**:
  - **동기 함수**로 구현 — 2팀이 교차 점검에서 "비동기였다면 엔진 호출 체인 전체가 async로 오염될 뻔했다"고 확인.
  - 1팀 `factory.ts`의 self-registration 레지스트리(`registerDataSource`) 설계가 3팀 Task 007(Mock 구현체 미착수 상태)과 정합함을 교차 확인.
  - 2팀·3팀이 각각 독립적으로 `catalog.ts` 36그룹을 전수 확인했으나 2팀 `weights`(이벤트 발생 확률)에 대응하는 공통코드 그룹이 **없음**을 발견 — 공통코드 신설 대상인지, Task 024 실시간 산출값이라 정적 테이블 자체가 불필요한지 미확정(→ I-66, Task 024 착수 17일차 전 2·3팀 협의).
  - `ConstantGroupValues<G>` 키 타입(`Record<string, ...>`, 그룹 내부 코드 비구조화)과 2팀 `weights`(`Record<MatchEventType, number>`, 23키 강제) 접합 시 검증·변환 어댑터 1단계 필요 확인.
  - 3팀 관찰(이슈 아님, 기록용): 36그룹 중 `BOOL`·`STRING` 타입 그룹이 실존하지 않아(`INT`/`DECIMAL`/`JSON`만 존재) 해당 매핑은 아직 실전 검증 안 됨.
  - `Result<T>` 래핑 범위 미확정(I-61)에 조건부 의견 제시 — `DataSource.ts` 시그니처 유지 시 11일차 확정으로 충분(Task 007 무영향), 시그니처 자체 변경 시 13일차 착수 전 확정 필수.
- 출처: `docs/dailyWorkLog/10Day.md`

### 11일차 (2026-08-04, 화) — Task 003 계속(폴백·발효 정책)
- **한 일**: 하드코딩 안전 기본값 테이블(36그룹 전량) + 폴백 시 WARN 로그 규약. 발효 정책 3종(`NEXT_SEASON`/`IMMEDIATE`/`NEXT_MARKET`) 시그니처 확정.
- **산출물**: `src/lib/config/fallback.ts`(36그룹 안전 기본값 + WARN 폴백), `policy.ts`(발효 정책 3종) + 각 테스트
- **판정**: 완료
- **특기**:
  - 폴링 안전망 값 하향(정상값 5초/3초 유지, 안전망만 30초/15초로) — 1팀 `polling.ts`와 3팀 `fallback.ts` 양쪽에 동일 적용(두 레이어 값이 어긋나면 취지가 우회되므로 항상 동일 유지 원칙 확정).
  - **I-75(OPEN) 이월**: `bootstrap.ts`의 변수 경유 동적 import가 webpack에서 아직 실컴파일된 적 없음 — 3팀 Mock 착수 시(당초 13일차 예상) `next dev --webpack` 실측 필요. 실제로는 13일차에 Task 007 중 `naming/`만 진행되어 Mock 팩토리 착수가 15일차로 밀리면서 **13일차 시점에도 미해소**로 남음.
  - 프로젝트 종료일 79→81일차 순연(D-31)에 따른 3팀 일정 재배치 확인: Task 030 = 56~64일차, Task 031b = 65~68일차(여유 5일 유지, 흡수 가능).
  - shrimp task manager 공유 인스턴스 충돌(I-64) 재현 — 3·4·6·1팀 모두 계획 소실 겪었으나 파일 산출물 손실은 없음.
- 출처: `docs/dailyWorkLog/11Day.md`

### 12일차 (2026-08-05, 수) — Task 003 종료(스냅샷) + H-05 인계
- **한 일**: 상수 스냅샷 직렬화·해시(SHA-256) 규칙 확정, 해시 중복 제거.
- **산출물**: `src/lib/config/snapshot.ts` + `.test.ts`(8케이스)
- **판정**: 완료 — **Task 003 전량 종료**
- **특기**:
  - **⚑ H-05 인계**(공통코드 36그룹 카탈로그 + `loadConstants(group)` 인터페이스) → 2·4·5팀. 2팀은 **결정론 관점에서 갭 발견**(`loadConstants()`가 모듈 스코프 가변 상태에 의존해 같은 인자로도 시점에 따라 다른 값을 반환 — 3팀 의도된 설계였으나 `src/lib/sim/**`에서 직접 호출 시 엔진 참조 투명성·FR-AD-004 재현성이 깨질 위험 → **I-83** 등록, 사용자 판정 대기, 17일차 Task 024 착수 전 확정 필요). 4팀은 폴링 주기·최소 출전 필터 소비 가능성 확인 중 **I-85**(`LEADERBOARD_MIN_APPEARANCE_PCT` 예외 흡수 래퍼 부재) 부수 발견.
  - H-10(열거형 ko/en 표시명 목록) 텍스트 인계 준비를 다음 일차 인계 사항으로 명시(→ 4팀).
  - CLAUDE.md의 vitest 별칭 미해석 stale 문구 정정 — 3팀·2팀이 공동 지적.
- 출처: `docs/dailyWorkLog/12Day.md`

### 13일차 (2026-08-06, 목) — Task 007 착수(국적 기반 이름 생성기) + H-10 인계
- **한 일**: `src/lib/naming/` 국적 기반 이름 생성기(D-17) — nationality별 이름 풀 조합, 실존 인물명 회피 블랙리스트 필터(D-16). Mock과 실엔진이 공유하는 단일 구현.
- **산출물**: `src/lib/naming/generate.ts` · `namePools.ts` · `blacklist.ts` + 테스트
- **판정**: 완료(해당 범위 한정) — `tsc`·`lint`·`test` 0 오류. Task 007 전체는 미종료(엠블럼 SVG·Mock 팩토리는 14일차 이후)
- **특기**:
  - **H-04(2팀 PRNG) 인계의 첫 소비 사례** — `generate.ts`가 `nextIntBelow`로 `{state, value}`를 스레딩. 블랙리스트 재추첨 루프의 state 재사용 버그 여부를 **팀장·2팀·3팀 자체 테스트 3중으로 검증**(`cursor` 갱신이 루프 안에서만 전진 → 무한 동일값 루프 없음 확인). 2팀이 모듈 소유자로서 `bound=0` RangeError, 거절 샘플링 결정론 등 계약을 재확인하며 `bound===1`일 때만 state가 전진하지 않는 예외를 **I-91**로 신규 등록(3팀 코드에는 현재 미발동).
  - **⚑ H-10 인계**(열거형 ko/en 표시명 목록: 포지션 11군·이벤트 23종·부상 4등급·전술 6종·페이즈 6종·수상·마켓 상태) → 4팀. 3팀·4팀이 각각 독립으로 `src/types/enums.ts`와 대조 — 70리터럴 전량 불일치 0건. 4팀이 ko 표시명의 괄호 부연설명(UI 폭 문제) 3건을 지적, **3팀이 조치**: 표시명/설명 2열 분리, 8개 표 전체 4열로 통일, 표시명 작성 컨벤션 명문화("괄호·부연설명 금지, 부연은 설명 열로"). H-10 범위 밖 26종(`NewsFeedItemType` 등)은 후속 목록으로만 인계, 값은 미작성.
  - `CUP_PARAM` 위젯 분기 관련 5팀 폼 스펙 오류를 **3팀이 반증** — `catalog.ts:484-495`와 E-42 CHECK 제약 근거로 "코드별 valueNum/valueJson null 분기"가 애초에 성립하지 않음을 지적(그룹 전체가 JSON 단일 valueType). 5팀이 확인 후 정정.
  - **공통코드 범위 검증 구조 결함 발견(3팀·6팀 독립 도달)**: E-41 `minValue/maxValue`가 그룹당 1쌍인데 그룹 내 코드별 유효 범위가 상이(예: `SQUAD_PARAM`의 `MIN=22` vs `GK_MIN=2`). 팀장이 확정 1~4로 정리: ① 16일차부터 트리거(plain CHECK 불가, `min_value IS NULL OR BETWEEN` 조건) ② 값 채우기는 37일차 이후 그룹별 점진(3팀 근거 3건 채택) ③ 범위의 단일 소스는 DB(`common_code_group`) ④ `minValue`/`maxValue`/`jsonSchema`를 E-41→E-42로 이동(**I-93**, 3팀·6팀 독립 제기, 1팀 판정 당일 반영).
  - **절차 위반 파급**: 1팀이 "판단만 회신, 타입 수정은 14일차"라는 팀장 지시를 어기고 확정 4를 즉시 반영해 **3팀 소유 `catalog.ts`가 컴파일 에러 상태로 남고 전 팀 게이트가 차단**되는 사고 발생(`TS2344: 'minValue' is not assignable to keyof CommonCodeGroup`). **3팀이 당일 후속 조치**: `catalog.ts` 56~68행 `Pick<>`에서 3키 제거 + 36항목 × 3줄(108줄) 제거 + stale 주석 갱신(값은 채우지 않음). 팀장 실측 게이트(전 팀 변경 합류 후) 결과 `tsc --noEmit` 0건으로 당일 내 정상 복구 확인.
  - **I-88 제기(3팀, 1차 교차 점검)**: 국적/이름 풀이 공통코드로 관리되지 않음 — D-17 결정문 파급이 "국가 목록과 각국 비중은 공통코드로 관리"를 명시했으나, 12일차 동결된 공통코드 36그룹 카탈로그에 해당 그룹이 애초에 없어(05문서 5.12.1절 표 자체 누락) 3팀이 13일차에 20개국 이름 풀을 **정적 데이터**(`namePools.ts`)로 구현. "각국 비중" 로직 자체도 미구현(15일차 Mock 월드 팩토리 책임). 13일차 시점에는 **판정 대기**로 남고, **해소는 14일차**(§4 참고).
- 출처: `docs/dailyWorkLog/13Day.md`

## 3. 인계물 (H-*)

| 인계 | 방향 | 일차 | 상태 |
|---|---|---|---|
| **H-01** | 1팀 → 3팀 (도메인 타입 47종, 동결) | 8일차 확정 → 9일차 소비(Task 003 착수) | 수신 완료 |
| **H-05** | 3팀 → 2·4·5팀 (공통코드 36그룹 카탈로그 + `loadConstants(group)`) | 12일차 확정 → 13일차 소비 개시 | 제공 완료. 수신 측이 갭 2건 발견(I-83·I-85, 판정/조치 대기) |
| **H-04** | 2팀 → 3팀 (시드 PRNG `src/lib/sim/rng/`) | 13일차 수신, 당일 첫 소비(`generate.ts`) | 수신 완료 |
| **H-10** | 3팀 → 4팀 (열거형 ko/en 표시명 목록, 텍스트 인계) | 13일차 확정 → 14일차 소비 | 제공 완료 |

## 4. 이 팀이 관여한 이슈·결정

| ID | 내용 | 일차 | 상태 |
|---|---|---|---|
| I-20 | Task 030 담당팀 오기("2팀") → **3팀 단독**으로 정정 | 2일차 | 해소 |
| I-21 | D-25 엔진 상수 수정 주체(2팀) 누락 — 3팀은 튜닝 요청만, 실제 수정은 2팀 | 2일차 | 해소 |
| — | `Manager.isActing` 필드 제기 — 3팀 제기, 팀장이 "파생 불가"로 격상해 동결 전 반영 채택(D-23 대행/정식 구분 근거) | 8일차 | 반영 완료 |
| — | `PointTransactionReasonCode` 개수 충돌(3팀 12개 vs 6팀 11개) — 3팀이 원본 대조로 정확히 판정 | 8일차 | 3팀 판정 채택 |
| I-58 | 05문서 `value_type` 8그룹 복수 표기 vs E-41 단일 enum — 3팀 제기, "TS 우선" 05 비동기화 정책 확정 | 9일차 | 정정 권고 유지 |
| I-61 | `Result<T>` 적용 위치 미확정 — 3팀이 조건부 기한 제시 | 10일차 | 11일차 이관(조건부 13일차) |
| I-66 | 이벤트 확률·가중치(`weights`) 소유 계층 미확정 — 2·3팀 공통코드 36그룹에 대응 그룹 없음 발견 | 10일차 | **미해소** — Task 024 착수(17일차) 전 2·3팀 협의 |
| I-75 | `bootstrap.ts` 동적 import webpack 실컴파일 미확인 — 3팀 Mock 착수 시 검증 예정 | 11일차 | **미해소** — 13일차 시점 Mock 착수 전이라 이월 |
| I-83 | `loadConstants()`가 엔진 참조 투명성·재현성(FR-AD-004)을 깨뜨릴 위험 — 2팀이 H-05 소비 중 발견, 3팀 설계가 원인 | 12일차 | **미해소** — 사용자 판정 대기, 17일차 전 필요 |
| I-85 | `LEADERBOARD_MIN_APPEARANCE_PCT` 예외 흡수 래퍼 부재 | 12일차 | **미해소** — 019(39일차) 착수 전 판정(1·3팀) |
| I-91 | `nextIntBelow(state,1)`이 state를 소비하지 않음 — 2팀이 3팀 코드 검증 중 발견, 3팀 코드에는 미발동 | 13일차 | 참고용, 미발동 |
| I-93 | E-41 `minValue`/`maxValue`/`jsonSchema` → E-42 이동(동결 타입 배치 반영) — 3팀·6팀 독립 제기 | 13일차 | 당일 반영. 3팀은 `catalog.ts` 108줄 제거로 후속 대응 |
| I-88 | 국적/이름 풀이 공통코드 미관리(D-17 파급 미이행), "각국 비중" 미구현 — 3팀 제기 | **13일차 제기** | **판정 대기(13일차 시점) → 14일차 해소**: 공통코드 37번째 그룹으로 신규 추가 확정(D-17 원문 준수, 3팀의 정적 데이터 유지 권고는 기각). 공통코드 그룹 수가 **36 → 37**로 갱신됨 |

---

## 5. 확인 못 한 것

- 1~7일차 대기 구간 §4의 4개 대체 작업(국적별 이름 풀 수집, 공통코드 시드값 원문 정리, H-10 초안, SP-1 사전 검토)이 실제로 수행됐는지는 해당 일차 로그에 3팀이 "미호출"로만 기록돼 있어 확인되지 않았습니다.
- H-02(`DataSource` 어댑터 계약, 1팀→3팀) 공식 수신 일자는 팀 일정표상 13일차로 계획돼 있으나, 13일차 로그에 3팀의 명시적 수신·소비 기록은 없습니다(Mock 어댑터 구현 자체가 18일차 예정이라 아직 미착수).
- I-88의 최종 해소(그룹 수 36→37 반영)는 2026-08-07(14일차) 자로, 요청 범위인 1~13일차를 벗어나지만 팀장 요청에 따라 §4에 함께 기록했습니다.

---

## 15일차 (2026-08-10) — Task 007 Mock 월드 팩토리

**산출물**: `src/lib/mock/world.ts`(930줄, 신규), `src/lib/mock/world.test.ts`(12케이스, 신규)

- `generateMockWorld(worldSeed)` — `worldSeed` 하나로 전 엔티티 결정론 생성. 실측 **팀 60 / 감독 60 / 선수 1,577명 / 스폰서 41**, 전 팀 스쿼드 22~30명·GK≥2·CB≥3 만족.
- 리그 규모는 `LEAGUE_TEAM_COUNT`(24/20/16), 스쿼드 한계는 `SQUAD_PARAM.MIN/MAX/GK_MIN/CB_MIN`을 **`loadConstants` 경유**로 읽어 리터럴 0건(NFR-CFG-001). `installHardcodedFallback()` 방어적 호출.
- 14일차 인계 계약 이행 — `generateTeamEmblem(team.crestSeed)`를 **팀당 1회만** 호출, `svg`는 렌더용·`colorPrimary`/`colorSecondary`는 `Team` 필드에 대입. 국적은 `SUPPORTED_NATIONALITY_CODES` 균등분포(실 비중은 031b).
- 리그 티어별 품질 중심값으로 OVR 차등 — 19일차 "상위 리그 평균 OVR 유의차" 수락 기준 대비.
- `TasteTag`/`Formation` 값 목록 미확정이라 Mock 전용 로컬 풀 사용 — 확정 시 이 파일만 교체(구조 변경 아님).

**팀장 검증: 결함 0건** — 배럴 `@/types`만 사용(서브경로 직접 import 0), 난수 전량 `sim/rng/prng.ts` 경유, `Math.random()`/`Date.now()` 0건 grep 실증.

**I-97 의견**: ⓐ(자기 팩토리 확장) 지지 — 결정론 로직 이중화 우려. **팀장 판정은 ⓑ(6팀 034a 시드)이나, 3팀 우려를 조건으로 반영**: 034a는 `generateMockWorld` 출력을 INSERT로 매핑하는 얇은 어댑터로 구현하고 생성 로직을 재구현하지 않는다.

---

## 16일차 (2026-08-11) — Task 007 진행 상태 Mock 6종

**산출물**: `src/lib/mock/progress.ts`(신규 42.8KB), `progress.test.ts`(신규 11케이스).

`generateMockWorld()` 산출물을 입력받아 6종(라이브 경기·이벤트 타임라인·순위표·스탯 리더보드·뉴스 피드·플레이오프/컵 브래킷)을 결정론 생성. **`src/lib/data/DataSource.ts`(1팀) 6개 메서드 반환 타입과 1:1 대응**해 18일차 MockDataSource가 바로 슬라이스 가능하도록 맞췄다. 시드는 `deriveSeasonSeed`/`deriveMatchSeed`/`stateForSeed`로 계층 정식 경유.

**스코프 축소 2건(파일 헤더 명시 + 이슈 등재)**:
- **I-106** — 순위표는 "전 팀 동일 10라운드 소화" 가정 표본이며 17일차 풀 일정 역산이 아니다.
- **I-107** — 플레이오프 시딩이 `playoffTeamCount`(10/4/2)를 2의 거듭제곱으로 내림(8/4/2)해 **부전승 미처리**, 컵은 스펙 6라운드(64강) 대신 5라운드(32강). **5팀 브래킷 화면 착수 전 확정 필요.**

**팀장 검증: 결함 0건** — 배럴 `@/types`만 사용(서브경로 0건), `Math.random()`/`Date.now()` 실사용 0건, 11케이스 재실행 통과, 커버리지 lines 98.01% / branches 91.22%.

**운영 기록**: 대형 파일 작업으로 응답이 늦은 것을 팀장이 세션 단절로 오판해 3팀을 중복 소환했다. 최초 확인 시점(15:56)엔 중복 쓰기가 없었으나, **재소환 세션(r2)이 중단 지시 이후인 16:00:34에 `progress.ts`를 1회 수정했고 그 변경이 커밋 `f557fac`에 포함됐다**(팀장이 게이트~커밋 사이 working tree를 재확인하지 않음).

**따라서 이 파일의 승점 계산 부분은 3팀 원 작업이 아니라 r2 기여분이다** — `generateStandings`가 `won*3+drawn`으로 하드코딩하던 것을 `loadConstants('MATCH_POINTS')`(WIN/DRAW/LOSS) 경유로 바꾸고 진입점에 `installHardcodedFallback()`을 추가했다. `world.ts` 전례와 동일한 패턴이고 NFR-CFG-001(리터럴 금지)에 부합해 **되돌리지 않고 유지**했으며, 커밋 상태로 `npm run gate` 재실행 통과를 확인했다. 폴백 기본값이 기존 하드코딩과 같아(WIN=3/DRAW=1/LOSS=0) 테스트 영향 없음.

그 외 `progress.ts`/`progress.test.ts`의 구조·6종 생성 로직·11케이스는 전부 3팀 원 작업이다. 팀장 측 재발 방지책은 16Day.md 6절 참조.

## 17일차 (2026-08-12) — Task 007 4상태 시나리오 Mock

`src/lib/mock/fixtures/` 신규 6파일 — `schedule.ts`(436줄) / `states.ts` / `screens.ts`(310줄) / `schedule.test.ts` / `screens.test.ts` / `index.ts`.

- **`schedule.ts`** — 서클법 더블 라운드로빈으로 **풀 일정을 생성**하고 `FINISHED` 경기만 집계해 **순위표를 역산**한다. 16일차 `progress.ts`의 "전 팀 동일 10라운드" 표본 가정을 대체하는 부분이다.
- **`states.ts`** — `Result<T>`(1팀 Task 004) 경유 4상태 제네릭 빌더. LOADING/EMPTY/ERROR는 화면과 무관하게 공통 모양이라(EMPTY에 사유 필드 없음, ERROR 메시지는 진단용이지 UI 카피가 아님 — 10일차 결론 재확인) 실작업량은 화면별 "정상" 표본 조립에 몰린다.
- **`screens.ts`** — `world.ts`/`progress.ts`/`schedule.ts` 산출물을 조합해 **11개 화면**(FR-UI-002·003·004·005부분·006부분·007·008·009·010·011·014)의 4상태 픽스처를 만든다. 완전 8 + 부분 2(선수·클럽 상세는 계약/부상/수상/이적 등 미생성 섹션 제외).

검증: `tsc --noEmit` 0 / `lint` 0 / `npm run test` 516 통과(신규 12케이스 포함). `@/types` 서브경로 import 0건, `Math.random()`/`Date.now()` 0건 grep 확인.

**I-106 판정 — 부분 해소.** `schedule.ts` 경로에서는 동일 라운드 가정이 사라졌다. 다만 `progress.ts`의 `generateStandings`는 **수정하지 않았다** — 자체 헤더가 이미 "진행 중 스냅샷 전용, 전체 일정 역산 아님"으로 경계를 그어 뒀고 오늘 산출물과 목적이 다르기(가벼운 단발 스냅샷) 때문이다. **완전 해소 조건: 18일차 MockDataSource의 `getStandings`를 `schedule.ts` 파생값 슬라이스로 배선**.

**스코프컷 → I-111**: 나머지 8개 화면(FR-UI-001·012 수상·013 아카이브·015~019·025·026 운영콘솔 `CronRun` 등)은 **대응 Mock 엔티티 생성기 자체가 없어** 픽스처를 만들 수 없었다. 근거는 `screens.ts` 헤더에 상술. 어느 일차에 채울지 5팀 화면 일정과 역산해 확정 필요.

**팀장 검증: 결함 0건.** 6팀이 보고 시점에 관측한 `screens.ts:132` lint 경고는 작업 완료 시점에 해소돼 최종 게이트에서 경고 0 — 동시 작업 중 타 팀 미완 파일 관측이라 실결함 아님.

## 18일차 (2026-08-13) — Task 007

`src/lib/data/mock/MockDataSource.ts`(34.8KB) + `MockDataSource.test.ts`(13케이스) 신규. **`DataSource` 56개 메서드 전량 구현.**

- **I-106 완전 해소** — `getStandings`가 `progress.ts` 진행 스냅샷이 아니라 `schedule.ts`의 대진 역산 순위표를 슬라이스한다. `round` 지정 시 그 라운드 이하로 필터한 fixtures에 `deriveStandingsFromFixtures`(`schedule.ts` 신규 export)를 재적용해 시점별 스냅샷 생성 — 재구현 없이 단일 소스 재사용. 이 재계산 경로만 쿼리 시점에 결정론 PRNG를 다시 스레딩(같은 입력→같은 출력).
- `screens.ts`가 `toPublicProfile`을 export해 선수 공개 프로필 변환 재사용(`pa` 미노출 유지).
- `getCommonCodeGroups/Codes`는 `config/catalog.ts` + `loadConstants`를 감싸 실제 값 반영.
- **생성기 없는 축**(계약·부상·수상·이적·임대·통산/경기 스탯·라인업·날씨·시즌지표·원장·스폰서계약·트로피·크론·감사로그)은 값을 발명하지 않고 `null`/`[]` 반환, 사유를 파일 헤더에 명시.

**의도적 미착수 — I-113**: `src/lib/data/mock/index.ts` 배럴 + `registerDataSource` 배선(H-07). 배럴을 만드는 순간 1팀 `bootstrap.test.ts` 5건이 확정 파손되므로 **1팀 갱신 후**로 순서 고정. 4·5팀 20일차 Mock 소비가 여기 걸려 있다.

**이슈 2건 등재**: **I-114**(`MOCK_NOW` 2026-08-11T15:00 vs `FIXTURE_NOW` 2026-08-12T12:00 — Mock 기준 시각 하루 불일치, `MockDataSource`는 `MOCK_NOW`로 통일) / **I-111 정정**(제목 "8종"이 본문 열거 10종과 불일치 — 팀장 원문 대조로 확인, 소관 미배정 6종으로 잔여 판정 대상 축소).

**팀장 검증: 산출물 결함 0건.** 팀이 보고한 "`bootstrap.test.ts` 5건 회귀"는 **오탐으로 기각** — 단독 실행 5/5 통과, 전체 542 passed. 배럴이 없어 `import('./mock')`이 여전히 reject되며, 파일 작성 중간 상태를 관측한 것이다(17일차에 예고된 패턴). 단, 배럴 추가 시점에는 실제로 깨지므로 I-113으로 등재했다.

**⚑ 운영 사고**: 이 팀 인스턴스가 2개 병행 기동됐다(팀장 소환 절차 오류). 동일 산출물로 수렴해 손실은 없었으나 `index.ts`를 만들었다 되돌리는 왕복이 발생했다. 팀장이 정본을 지정해 해소.

## 19일차 (2026-08-14) — Task 007 **종료** (⚑ H-07 인계)

- `src/lib/data/mock/index.ts` (신규) + `index.test.ts` (신규) — `registerDataSource('mock', () => new MockDataSource())`. **프로바이더 함수로 지연 생성**해 부트스트랩만 하고 조회하지 않는 경로에서 무거운 월드 생성 비용을 지불하지 않는다.
- Task 007 종료 Vitest 스위트 — 시드 재현성, 스쿼드 구조 불변식, 등번호 중복 0, 국적별 이름 풀 매칭, 정적 JSON 하드코딩 0건.
- **I-114 해소** — Mock 기준 시각 3종(world 08-10 / progress 08-11 / MockDataSource 08-13, 최대 3일 편차)을 `world.ts`의 `MOCK_EPOCH_NOW` 단일 앵커로 통일. `WORLD_CREATED_AT`은 앵커−40일로 재계산(시즌 시작 앵커−10일보다 앞서도록), MockDataSource 설정 타임스탬프는 `world.createdAt` 재사용.

**팀장 검증: 결함 0건.** 배럴의 지연 생성 계약과 `MOCK_EPOCH_NOW` 단일 앵커 전파를 직접 확인. `npm run test` 46파일 570케이스 통과.

**인계**: **4·5·6팀이 20일차부터 Mock 소비 개시 가능.** `enums.ts` 실값 기입은 23일차 이후 3팀 몫(4팀이 골격만 제공).

**19일차 추가 조치(팀장 피드백 — I-117)**: 게이트 perFile 커버리지 미달 2건 해소. `MockDataSource.ts` lines 77.59→**98.45** / branches 45.65→**89.23**(미테스트 메서드 대거 보강), `mock/fixtures/screens.ts` branches 54.54→**100**. 도달 불가능한 방어 가드에만 근거 주석과 함께 `/* v8 ignore */`를 썼고 **팀장이 8곳 전수 확인 후 승인**(전부 `throw`/`?? null` 폴백, 실로직 은폐 없음). 임계값·제외 설정 미변경.

## 20일차 (2026-08-17) — Task 029 (포인트 원장)

- `src/lib/economy/ledger.ts` / `ledger.test.ts` 신규.
- `postPointTransaction(currentBalance, input)` **단일 진입점**만 잔고 변동 생성 — 항상 레코드 + `balanceAfter` 반환. **잔고 직접 변경 함수 부재**로 "원장 없는 잔고 변동 0건"을 API 표면에서 구조적으로 차단.
- `deriveBalance(transactions)`로 원장 합 = 잔고 항등식(NFR-QA-005) 검증. DC-08 위반 시 `NonIntegerPointsError`.
- 동결 타입 `@/types` 배럴 재사용(재선언 0). ID·Timestamp는 호출자 주입(NFR-DT-001 관례).
- 테스트 7케이스 통과. **영속화(DB)는 6팀 DataSource 경계 너머 — 미배선.**

## 21일차 (2026-08-18) — Task 029 (몸값 공식)

- `src/lib/economy/valuation.ts` / `valuation.test.ts` 신규. `calculateMarketValue()`.
- 20일차 `ledger.ts` 관례 + 2팀 `tactics.ts`의 `override ?? loadConstants` 패턴 승계.
- 폴백 있는 6키(`OVR_DIVISOR`/`OVR_EXP`/`POT_STEP`/`REP_BASE`/`REP_STEP`/`FLOOR`)는 그대로 사용. `AGE_*`/`CONTRACT_*`/`TIER_*`는 05문서 원본부터 숫자 부재라 키 없으면 중립값(배율 1) → **I-121**.
- **하한 100pt는 `Math.max(rounded, floor)`를 배율 로직과 분리된 마지막 줄에 고정** + `Math.max(0, ovr)`로 NaN 전파 차단. 반올림 `Math.round`(floor/ceil은 계통적 편향).
- 테스트 13(최악 입력 조합 전수 ≥100), 전체 689 통과. 이슈: I-121, I-122.
