# 1팀 — 코어·품질팀 — 산출물 기록

> 위치: `docs/team-schedule/outputs/01-코어품질팀.md` / 본체: [`01-코어품질팀.md`](../01-코어품질팀.md)
> 이 파일은 **일차별 산출물·조정 기록의 누적처**입니다. 팀 md 본체를 가볍게 유지하기 위해 분리했습니다.
> **팀원 소환 시 열람 대상이 아닙니다** — 팀원은 본체 md의 소유 경로·해당 일차 행만 봅니다.
> 새 기록은 이 파일 끝에 append 하세요.

---

## 1. 요약 (1~13일차)

| 항목 | 값 |
|---|---|
| 참여 일차 | 1~13일차 전일 (업무 없음 일차 없음) |
| 담당 Task | Task 001(종료, 2일차) · Task 002(종료, 8일차 — H-01 동결 선언) · Task 004(종료, 11일차) · Task 008(진행 중, 13일차 기준 스크립트·스위트 골격 완료, 커버리지 임계는 15일차까지 정비 예정) |
| 주요 산출물 | `docs/devStep/02.타입스키마설계원칙.md`, `docs/devStep/03.결정Task매핑표와코드리뷰체크리스트.md`, `src/types/**`(11개 파일, E-01~E-47 전건 + `README.md` 매핑표 + `*.type-test.ts` 10종), `src/lib/data/DataSource.ts`(56 메서드), `factory.ts`, `result.ts`, `polling.ts`(H-02), `fetch-result.ts`, `bootstrap.ts`, `vitest.config.ts`, `eslint.config.mjs`(13일차 조치), `docs/ISSUES.md`(지속 갱신, I-14~I-93 구간 다수 등재·해소) |
| 종료된 Task | Task 001, Task 002, Task 004 |
| 진행 중 | Task 008(Vitest 정비 — 15일차까지 이어짐) |
| 미해소 이월 | I-29(시드 `bigint`↔53비트, 8일차 기준 OPEN 유지) · I-30(`.next/` 생성물 게이트 오탐, 15·20일차 예정) · I-36(ESLint 가드레일 H-06 미반영, 16일차 Task 010) · I-38(스카우트 등급 필드, 8일차 기준 OPEN) · I-41(`CardReason` enum 보류, 2팀 023/024 설계 후 배치 반영) · I-90(커버리지 `all`/`include` 누락, 14일차 임계 설정과 동반 필수) |

## 2. 일차별 기록

### 1일차 (2026-07-21, 화) — Task 001
- **한 일**: 확정 결정(D-15~D-26)을 각 Task 구현 전제로 배분(D-21→030, D-22→023·024, D-23→030, D-24→027, D-25→008·031, D-26→003·031). 결정↔Task 매핑표, 코드 리뷰 체크리스트 작성.
- **산출물**: `docs/devStep/02.타입스키마설계원칙.md`(신규, D-15~D-20 → 설계 원칙 P-15~P-20, T1~T22), `docs/ISSUES.md`(3.2절 신설, I-14~I-19 등록)
- **판정**: 완료 (Task 001 완료 판정일)
- **특기**: 2팀과 서로 다른 문서만 읽고 독립적으로 같은 결론(시드 계층 소유 경계)에 도달 — 구조적 독립성 확인. I-15(구장명 번역 대상 미정의)·I-19(승부차기 브랜드 생성 책임 미정)는 OPEN으로 이월.
- 출처: `docs/dailyWorkLog/1Day.md`

### 2일차 (2026-07-22, 수) — Task 001 마감 / Task 002 착수
- **한 일**: Task 001 완료 판정. `src/types/` 도메인 분할 생성 착수(world/person/match/stat/economy/betting/config/ops/index), E-01~E-08 정의 — **코드 파일 첫 생성**.
- **산출물**: `docs/devStep/03.결정Task매핑표와코드리뷰체크리스트.md`(신규), `docs/ISSUES.md`(I-20~I-28 등록)
- **판정**: 완료
- **특기**: ROADMAP 팀 배정 오류(Task 030 담당팀 오기재) 자체 발견·정정. 개발 서버 500 / `tsc` 오탐 문제가 발견돼 3일차로 이월.
- 출처: `docs/dailyWorkLog/2Day.md`

### 3일차 (2026-07-23, 목) — Task 002
- **한 일**: `src/types/` 11개 파일을 골격까지 포함해 생성 — 오늘 범위(E-01~E-08) 밖 파일도 빈 파일이 아닌 자리표시자로 채워 이후 재작업을 줄임.
- **산출물**: `src/types/`(11개, 385줄 — `world.ts`/`person.ts`/`brand.ts`/`enums.ts`/`match.ts`/`stat.ts`/`economy.ts`/`betting.ts`/`config.ts`/`ops.ts`/`index.ts`), `docs/ISSUES.md`(I-29~I-35 7건 등재)
- **판정**: 완료
- **특기**: 2팀 점검에서 부적합 0건. 2일차 이월 건(손상된 `.next/`) 해소 — `/tmp/next-backup-day3`로 이동해 개발 서버 500·tsc 오탐 제거. "게이트에서 생성물을 어떻게 다룰지"만 I-30으로 남아 15·20일차 이월.
- 출처: `docs/dailyWorkLog/3Day.md`

### 4일차 (2026-07-24, 금) — Task 002
- **한 일**: `person.ts` 34속성 전건 정의(E-09~E-11), `match.ts`(E-15~E-18), `stat.ts`(E-19~E-20), `economy.ts`(E-12~E-14), enum·브랜드 유니온 추가.
- **산출물**: `src/types/person.ts`·`match.ts`·`stat.ts`·`economy.ts`·`enums.ts`·`brand.ts`·`index.ts`, `docs/ISSUES.md`(I-36~I-38 등재, I-34 범위 확장, I-35 해소)
- **판정**: 완료
- **특기**: 5팀이 1팀 코드의 E-번호 드리프트 5건을 발견 — 1팀이 당일 전건 정정, 전수 대조로 추가 드리프트 0건 확인(**4일차 최대 발견**). 이슈 번호 선점 지칭으로 1팀-팀장 간 왕복 혼선 발생(교정: 미확정 이슈는 번호를 미리 붙이지 않음).
- 출처: `docs/dailyWorkLog/4Day.md`

### 5일차 (2026-07-27, 월) — Task 002
- **한 일**: `stat.ts`(E-21~E-23, E-31~E-32), `ops.ts`(E-24~E-27), `economy.ts`(E-28~E-30), `betting.ts`(E-33~E-40 선정의), enum 14종·브랜드 PK 13종 추가. I-31 반영(`World` 환산 앵커 4필드), I-32/D-28 반영(시드 폭 32→53비트).
- **산출물**: `src/types/stat.ts`·`ops.ts`·`economy.ts`·`betting.ts`·`enums.ts`·`brand.ts`·`world.ts`, `docs/devStep/02.*`(T2-a 개정), `docs/ISSUES.md`(I-19 해소, I-39 신규)
- **판정**: 완료
- **특기**: 2팀→1팀 교차 점검에서 **I-39**(53비트 D-28이 rng 구현에 미반영) 발견 — `tsc`로는 검출 불가, vitest 도입이 아니었다면 8일차 이후에나 드러났을 결함. 이슈 번호 충돌(2팀 제안 I-36 중복)을 1팀이 I-39로 재조정.
- 출처: `docs/dailyWorkLog/5Day.md`

### 6일차 (2026-07-28, 화) — Task 002
- **한 일**: `enums.ts` 핵심 갱신(`SeasonPhase` TIEBREAK 추가로 6종, `Position` 11군, `MatchEventType` 23종, `NationalityCode` 브랜드화, `InjurySeverity` 신설). `match.ts`에 I-37(`relatedEventSequence`) 반영.
- **산출물**: `src/types/enums.ts`·`match.ts`·`ops.ts`·`person.ts`·`betting.ts`·`index.ts`, `docs/ISSUES.md`(I-33·I-37 반영 완료, I-39 해소, I-40 신규)
- **판정**: 완료
- **특기**: I-39 해소를 1팀 점검 + 팀장 독립 재현으로 이중 확인.
- 출처: `docs/dailyWorkLog/6Day.md`

### 7일차 (2026-07-29, 수) — Task 002 (1팀 단독 작업일)
- **한 일**: 브랜드 타입 ID 27종 + `CommonCodeId`/`CommonCodeHistoryId` 2종 = 29종을 `Brand<T,TName>`으로 승격. 시드 계층 브랜드(`WorldSeed`/`SeasonSeed`/`MatchSeed`/`EventSeed`) 분리 신설, `Points` 브랜드화(DC-08). E-41~E-44(공통코드/상수 스냅샷) 정의, T12 `EnumTranslationCatalog<T>` 규약. Task 002 마감(타입↔엔티티 매핑표, `expectTypeOf` 타입 레벨 테스트) — **H-01 동결 선언**.
- **산출물**: `src/types/brand.ts`·`config.ts`·`enums.ts`·`world.ts`·`match.ts`, `docs/ISSUES.md`(I-15 해소, I-16 정정, I-41·I-42 신규, I-43·I-44 확정 등재)
- **판정**: 완료
- **특기**: 7일차는 1팀 단독 작업일이라 팀원 간 1차 교차 점검 불가 — 팀장 검증 비중 상향. 브랜드 타입 실효성을 임시 파일로 `TeamId→PlayerId` 오대입을 시도해 `tsc` TS2322 실제 발생을 확인 후 삭제. `world.ts` 주석(32비트)·`I-16` 본문(32비트) stale 오기 2건을 정정(D-28 53비트와 모순이었음).
- 출처: `docs/dailyWorkLog/7Day.md`

### 8일차 (2026-07-30, 목) — Task 002 마감 / SP-1 동결
- **한 일**: Task 002 최종 마감. `src/types/README.md`(E-01~E-47 매핑표) 신규, `*.type-test.ts` 10종 신규, E-45~E-47 자체 발견·정의(`ops.ts`), `person.ts`에 `Manager.isActing` 반영. **H-01 도메인 타입 동결 선언**(`index.ts`). SP-1 심사 주체 수행 — 동결 전 반영 요청 0건(5개 팀 전원 확인) 판정.
- **산출물**: `src/types/README.md`(신규), `src/types/*.type-test.ts`(10종), `ops.ts`·`enums.ts`·`match.ts`·`brand.ts`·`index.ts`·`person.ts`·`economy.ts`, `docs/ISSUES.md`(I-45~I-56 등재), `ROADMAP.md`
- **판정**: 완료
- **특기**: 1팀이 스스로 누락 E-45~E-47을 발견해 반영. "동결 대상은 타입 구조뿐, 주석·문서 정정은 배치 절차 대상 아님" 원칙을 이날 확정(반복된 stale 주석 사고가 근거). 타입 레벨 테스트 실효성을 의도적 오류 주입(`World.pausedAt` nullable 단언 반전 → TS2349 검출 → 원복)으로 검증. 인계물 창구 일정을 팀장이 세 번 뒤집는 과정에서 팀장 오류로 1팀이 부당하게 지적받은 사례 발생(1팀이 사실관계로 반박, 반박이 맞았음).
- 출처: `docs/dailyWorkLog/8Day.md`

### 9일차 (2026-07-31, 금) — Task 004 착수
- **한 일**: `DataSource` 인터페이스 설계, 화면별 조회 메서드 9군 시그니처. 라이브 컷오프(I-34) 1차 판정("집계 테이블 직접 서빙 금지, 노출된 이벤트에서 매 요청 재계산") 후 2팀 근거를 받아 최종 (a)안 채택·(b) 기각. `MatchEventType` 학습데이터 괴리(AS-10) 최종 SSOT 승인. enum 34종 177개 리터럴·`PlayerStatCoreValues` 56컬럼·47/47 엔티티 매핑 독립 재검산.
- **산출물**: `src/lib/data/DataSource.ts`(신규, 56 메서드), `docs/ISSUES.md`(I-34 확정 판정, I-45~I-49→I-53~I-55 정정, I-57~I-60 처리, I-68~ 계열 이전 대비)
- **판정**: 완료(재작업 1회)
- **특기**: 수락 기준("화면별 조회 메서드 9군") 미충족으로 당일 재작업 지시 → 4개 메서드 추가로 해소. `person.ts` I-47 주석 오기 자체 발견·정정. AS-10 무효화(1팀 계약→6팀 물리 계층 검증→2팀 엔진 판정 3단 검증)가 이날의 핵심 성과.
- 출처: `docs/dailyWorkLog/9Day.md`

### 10일차 (2026-08-03, 월) — Task 004
- **한 일**: 어댑터 선택 팩토리(`factory.ts`), 4상태 결과 래퍼(`result.ts`, `Result<T>` 판별 유니온 + 타입가드 4종) 설계. `DataSource.ts` 56개 메서드를 `Promise<Result<T>>`로 감싸지 않기로 판단(EMPTY 의미 이중화 위험 등 3가지 근거).
- **산출물**: `src/lib/data/factory.ts`·`result.ts`(신규), `docs/ISSUES.md`(I-61 갱신, I-67 등)
- **판정**: 완료
- **특기**: 2팀이 1팀 `result.ts`의 `fromArray()`를 읽다가 자기 코드(`events.ts`)의 메타 이벤트 0건 결함(I-65)을 스스로 발견 — 교차 점검이 "남의 코드에 비춰 내 코드를 다시 보는" 방식으로 작동한 사례. I-62(WSL Turbopack 실패) 범위가 `npm run build`까지 확대됨을 확인, Task 008/010 검증 기준 설계 전제로 반영.
- 출처: `docs/dailyWorkLog/10Day.md`

### 11일차 (2026-08-04, 화) — Task 004 마감
- **한 일**: `polling.ts`(폴링 훅 계약, **H-02**), `fetch-result.ts`, `bootstrap.ts` 작성 — Task 004 종료. 4팀이 발견한 `polling.ts` 서버/클라이언트 경계 결함을 절충안(`bootstrapApp()` 단일 진입점 신설)으로 해소. 2팀의 미확정 가정 3건에 SSOT 판정(I-68~I-70).
- **산출물**: `src/lib/data/polling.ts`·`fetch-result.ts`·`bootstrap.ts`(신규), `docs/ISSUES.md`(I-68~I-82 등재·갱신)
- **판정**: 완료
- **특기**: 폴링 안전망 정상값(5초/3초) 유지, 안전망만 30초/15초로 하향(3팀 `fallback.ts`와 양쪽 동기화). shrimp task manager 공유 인스턴스로 계획 소실 재현(I-64) — 파일 산출물 영향은 없었음. 1팀 자체 일정 추정치 2건 오기 정정(H-22/H-23).
- 출처: `docs/dailyWorkLog/11Day.md`

### 12일차 (2026-08-05, 수) — Task 008 착수 (당일 재작업 1건)
- **한 일**: `vitest.config.ts` 신규(`@/*` 별칭 해석 + `typecheck` 모드), `@vitest/coverage-v8` 설치. H-08(6팀 → 1팀, 도메인 타입 필드 대응표) 수신·독립 재현 — 51개 interface 추출, 47/47 대조 불일치 0건.
- **산출물**: `vitest.config.ts`(신규), `docs/ISSUES.md`(I-84 등재)
- **판정**: 재작업 1회 후 완료
- **특기**: **팀장 2차 검증에서 I-46 "해소"가 거짓 해소였음이 반례 실증으로 드러남** — 런타임 `test.include`에 `*.type-test.ts`를 추가했지만 `expectTypeOf` 단언이 esbuild 트랜스폼에서 소거돼 항상 통과하는 무의미한 테스트였음. 당일 (A)안(`test.typecheck.enabled: true`, `checker: 'tsc'`)으로 재작업, 1팀·팀장 독립 재현 검증(반례 주입 시 실패, 제거 후 24 files/291 tests/Type Errors 0 확인) — **I-84로 재등재**. 거짓 해소였던 경위를 숨기지 않고 ROADMAP·ISSUES에 그대로 기록.
- 출처: `docs/dailyWorkLog/12Day.md`

### 13일차 (2026-08-06, 목) — Task 008 계속
- **한 일**: `test:watch`/`test:coverage` 스크립트, 스위트 디렉터리 골격 6종 추가. `tsc 0 / lint 0 / test 325 pass` 게이트 실측. `eslint.config.mjs`(1팀 소유) 조정 — `globalIgnores`에 `coverage/**`·`E:*/**` 추가(카나리 파일로 실검출 확인 후 삭제, 팀장 독립 재현). 동결 타입(`CommonCodeGroup`) 배치 반영 판단 — E-41 `minValue`/`maxValue`/`jsonSchema` → E-42 이동 **승인·당일 반영**(I-93).
- **산출물**: `src/lib/sim/__suites__/` → `src/__suites__/`로 이동(6종), `eslint.config.mjs`(수정), `src/types/config.ts`(3필드 이동)·`config.type-test.ts`(수정), `docs/ISSUES.md`(I-90 등재, I-93 반영)
- **판정**: 완료 (단, 절차 위반 1건)
- **특기**: **⚠️ 절차 위반** — 팀장 지시는 "판단만 회신, 타입 수정은 14일차 첫 작업으로"였으나 1팀이 판단과 동시에 반영해 3팀 소유 `catalog.ts`가 컴파일 에러 상태로 남아 전 팀 게이트가 차단됨. 팀장이 커밋을 보류(게이트 빨간불 상태의 커밋은 1팀 자신이 세울 H-03 게이트가 금지할 사안). `npm run lint` 게이트 파손(3,268건, I-87 조치 실효 없음)을 팀장 2차 검증이 실측 발견 → 당일 해소. 커버리지가 자기 목적을 무력화하는 구조 결함 발견(무테스트 파일이 분모 누락) → **I-90 등재**(14일차 필수 동반 조치로 이월). `@testing-library/react` 도입 요청은 1팀 `package.json` 단독 소유 및 프로젝트 전체 UI 테스트 전략 미결정을 이유로 팀장이 불승인.
- 출처: `docs/dailyWorkLog/13Day.md`

## 3. 인계물 (H-*)

| 인계 | 방향 | 일차 | 상태 |
|---|---|---|---|
| H-01 | 1팀 → 전 6팀 (도메인 타입 동결 선언) | 7~8일차 | 완료(선언). 이후 변경은 이슈 배치 반영만 가능 |
| H-02 | 1팀 → 4·5팀 (폴링 훅 계약) | 1일차 계약 명문화 → 11일차 `polling.ts` 실코드 | 완료 |
| H-08 | 6팀 → 1팀 (도메인 타입 필드 대응표) | 12일차 | 완료 — 1팀 독립 재현, 47/47 불일치 0건 |

## 4. 이 팀이 관여한 이슈·결정

| ID | 내용 | 일차 | 상태 |
|---|---|---|---|
| I-15 | 구장명 번역 대상 여부 미정의 | 1일차 등재 → 7일차 해소 | 해소 |
| I-16 | 시드 계층 타입 소유 경계 + `worldSeed` 비트 폭 | 1일차 해소 → 7일차 주석 오기(32비트) 정정 | 해소(정정 포함) |
| I-17 | 폴링 계약/주기 값 출처 표기 누락 | 1일차 | 해소 |
| I-19 | 승부차기 브랜드 타입 생성 책임 소재 | 1일차 등재 → 5일차 해소 | 해소 |
| I-29 | 시드 `bigint`↔53비트 CHECK 제약 필요 | 8일차 기준 OPEN 유지(6팀 계획 확약, 1팀은 실재 반영 전까지 미해소로 판단) | OPEN(8일차 기준, 이후 미확인) |
| I-30 | `.next/` 생성물이 `tsc` 게이트에 포함돼 오탐 | 3일차 등재, 15·20일차 처리 예정 | OPEN — 이월 |
| I-31 | `World` 타입에 시간 환산 앵커 필드 결손 | 3일차 등재 → 5일차 반영(동결 전 완료) | 해소 |
| I-32 | `Seed` 32비트 → 53비트 완화 | 3일차 등재 → 5일차 반영 | 해소 |
| I-33 | `SeasonPhase` 6번째 값 `TIEBREAK` | 3일차 등재 → 6일차 반영 | 해소 |
| I-34 | 진행 중 경기 데이터 역산 위험(라이브 컷오프) | 3일차 등재 → 4일차 범위 확장 → 9일차 최종 판정((a) 채택) | 해소(판정 확정) |
| I-36 | ESLint 가드레일(H-06) 미반영 | 4일차 등재, 16일차 Task 010 예정 | OPEN — 이월 |
| I-37 | `MatchEvent` ASSIST→GOAL 참조 필드 부재 | 4일차 등재 → 6일차 반영 | 해소 |
| I-38 | 스카우트 등급 ★1~5 공개 필드 부재 | 4일차 등재, 9일차 DataSource 반영 예정 | OPEN(8일차 기준 미해소 확인, 이후 미확인) |
| I-39 | D-28 53비트가 rng 구현에 미반영 | 5일차 등재(2팀→1팀 발견) → 6일차 해소 | 해소 |
| I-40 | `derive.ts` `assertIndex` 2³² 상한 검증 부재 | 6일차 등재, 2팀 여유 시 처리(차단성 아님) | OPEN(비차단) |
| I-41 | `CardReason` enum 부재 | 7일차 등재, 2팀 023/024 설계 후 1팀 배치 반영 | OPEN — 이월 |
| I-43 | PK 득점 시 `GOAL` 추가 발생 금지 | 7일차 | 해소(팀장 확정) |
| I-44 | `PENALTY_SHOOTOUT` 킥 단위 개별 레코드 | 7일차 | 해소(팀장 확정) |
| I-46 | 타입 레벨 테스트가 `npm test`에서 실질 실행되지 않음 | 8일차 등재 → 12일차 **거짓 해소 판명** → 당일 재작업 → I-84로 재등재 | 해소(재작업, I-84로 이관) |
| I-56 | `CronRun.errorCode`/`errorMessage` 번역 비대상 명시 | 8일차 | 해소 |
| I-60 | AS-10 무효화 확정 | 9일차(2팀 판정 + 1팀 SSOT 승인) | 확정 |
| I-84 | I-46 거짓 해소 재작업 — typecheck 모드 전환 | 12일차 | 해소(1팀 당일 재작업, 독립 재현 검증 완료) |
| I-87 | `npm run lint` 게이트 파손 | 13일차 2차 검증에서 조치 실효 없음 확인 | 재작업 필요 확인(당일 `eslint.config.mjs` 조정) |
| I-90 | 커버리지 `all`/`include` 누락으로 무테스트 파일이 분모 누락 | 13일차 등재 | OPEN — 14일차 임계 설정과 동반 필수 이월 |
| I-93 | E-41 `minValue`/`maxValue`/`jsonSchema` → E-42 이동(동결 타입 배치 반영) | 13일차(3·6팀 제기, 1팀 판정) | 해소(당일 반영, 3·6팀 후속 조치 필요) |

## 5. 확인 못 한 것

- I-29(시드 `bigint`↔53비트)·I-38(스카우트 등급 필드)의 9~13일차 사이 최종 해소 여부 — 8일차 이후 로그에서 명시적 해소 문구를 찾지 못함.
- Task 008의 13일차 시점 완료/미완료 공식 판정 문구(팀별 표에 ✅ 표기 없이 산출물만 나열됨) — CLAUDE.md 기준 "정식 정비는 15일차까지"라 13일차 종료 시점엔 진행 중으로 간주.

---

## 15일차 (2026-08-10) — Task 008 종료

**산출물**: `scripts/gate.sh`(신규), `package.json`(`gate` 스크립트), `vitest.config.ts`, `src/lib/data/{bootstrap,factory,fetch-result,result}.test.ts`(신규 4)

- **3단 머지 게이트 완성** — `set -euo pipefail` fail-fast로 `tsc --noEmit` → `lint` → `test:coverage`. WSL 마운트에서 `next build`가 EPERM으로 죽으므로(I-62) 빌드는 게이트에 넣지 않는다. `passWithNoTests: true`로 "빈 스위트 통과" 요건 충족.
- **I-94 해소 — `perFile: true` 채택**(임계 lines 80 / branches 70 유지). 2팀 15일차 산출물로 차단 사유 소멸, 채택 시점 실패 파일 0건.
- **인계 결정 2건 확정**: 커버리지 범위 = `sim`/`data`(polling 제외)/`config`/`naming`/`mock`. UI 테스트 = Vitest + Testing Library(jsdom 파일별 오버라이드) / E2E = Playwright MCP(Task 036).

**팀장 검증 지적 2건(수정 완료)**
1. **게이트 3단계가 `npm run test`라 커버리지 임계가 평가되지 않았다** — 수락 기준 "임계 미달 시 실패"를 별도 수동 실행으로 시연했을 뿐 게이트 동작이 아니었고, 커버리지 0% 변경도 exit 0으로 통과했다. `test:coverage`로 교체.
2. **커버리지 범위 보류 근거가 사실과 달랐다** — "대상 모듈이 없어 실효 없음"이라 했으나 실측 결과 `config` 97.18%·`naming` 96.18%·`mock` 98.74%는 이미 통과했고 **0%인 건 자기 소유 `src/lib/data/**` 4파일**이었다. 테스트를 신규 작성해 해소. `polling.ts`는 React 훅 의존으로 조건부 제외(I-99, 4팀 23일차 재포함).

**최종**: `npm run gate` exit 0 — 433 tests, aggregate lines 98.05% / branches 90.7%.

---

## 16일차 (2026-08-11) — Task 010 정적 가드레일 착수

**산출물**: `eslint.config.mjs` — `src/lib/sim/**/*.ts(x)` 스코프에 `no-restricted-properties`로 `Math.random`/`Date.now` 차단.

**룰 이름 정정 판단**: 로드맵 문구는 `no-restricted-globals`였으나 이 룰은 스코프상 전역 식별자의 **직접 참조**만 검사해 `Math.random()` 같은 MemberExpression에 반응하지 않는다 — 문구대로 갔으면 룰이 무력했다. object/property 쌍을 지정하는 `no-restricted-properties`로 대체하고 대체 사유를 코드 주석에 남겼다(I-105 등재). 위반 메시지는 대안(`sim/rng/prng.ts` 시드 PRNG)을 안내한다.

**검증**: 금지 패턴을 `prng.ts`에 실제로 심어 lint 2건 에러 재현 → 원복(byte-identical) → lint 재통과. `npm run gate` 통과.

**ISSUES 반영 담당 수행**: I-103 해소 갱신(2팀 15개 분기 커버), I-105 등재(해소), I-106·I-107 등재(3팀 Mock 스코프 축소), **I-108 기각 정정**.

**알려진 한계**: 멤버 접근만 차단하므로 `const { random } = Math` 구조분해·`new Date()`는 우회 가능. 수락 기준 밖이라 미조치, 17일차 이후 보강 대상.

**팀장 검증: 결함 0건.**

## 17일차 (2026-08-12) — Task 010 `no-restricted-imports`

`eslint.config.mjs`에 블록 2개 추가.

- **`src/lib/sim/**` 블록**(16일차 `no-restricted-properties`와 동일 스코프): `react`/`react-dom`은 `paths`로, `@supabase/*`는 `patterns.group`으로 차단. 위반 메시지에 NFR-DT-001 근거를 담았다. Task 023(16일차) `perf-bench.test.ts`가 런타임 정규식으로 같은 항목을 검사 중이라 **이중 검사**지만 상충 없음 — 정적 lint가 앞단에서 잡고 런타임 검사가 뒤를 받친다.
- **`src/components/**` 신규 블록**: `@supabase/*` 직접 import 금지. 근거는 Mock First 원칙 — 컴포넌트가 Supabase 클라이언트를 직접 물면 Mock↔실데이터 교체 시 컴포넌트까지 고쳐야 한다. 반드시 `src/lib/data`의 `DataSource` 어댑터를 거친다. **디렉터리는 아직 없지만(4팀 23일차 이후 생성) 선제 등록**해 첫 컴포넌트가 생기는 순간부터 룰이 걸리게 했다.

검증: 임시 파일에 금지 import를 심어 두 룰이 각각 정확한 메시지로 발동하는 것을 확인 후 삭제, `git status`로 잔존물 0건 재확인(`src/components/` 미생성 유지). `lint` clean / `tsc --noEmit` 0 / `test` 통과.

**이월(19일차까지)**: UI 하드코딩 문자열 검출 룰(D-18), 숫자 리터럴 잔존 검사, 커밋·PR 체크리스트, ISSUES 갱신 규약. sim 룰의 알려진 한계(구조분해·`new Date()` 우회)는 여전히 유효.

**팀장 검증: 결함 0건.** 신규 sim 룰이 2팀 당일 산출물(`ability/modifiers.ts`)을 깨지 않는지 교차 확인 — lint clean.

## 18일차 (2026-08-13) — Task 010

- `eslint.config.mjs` 3번째 룰 블록 — **D-18 UI 하드코딩 문자열 검출**. `no-restricted-syntax`로 `JSXText`와 `JSXExpressionContainer` 내 문자열 리터럴 중 한글·영문 포함분을 **warn**으로 잡고 번역 키 경유를 안내. **예외**: `src/i18n/**`(4팀 `t()`/Provider가 문자열을 값으로 다루는 정당한 경로)·테스트 파일.
- `scripts/check-literals.mjs`(신규) + `package.json`의 `check:literals` — `fallback.ts`의 `SAFE_DEFAULT_VALUES`(37종)와 값이 겹치는 숫자 리터럴을 `src/lib/sim/**`(rng 제외)에서 탐지. **게이트 미연결 정보성 스크립트**.

수락 기준 실증: 룰 적용 후 기존 라우트 placeholder 17곳에서 **112건 warn** 검출, error 0. `npx tsc --noEmit` 0 error, `npm run test` 542 통과.

**이월(19일차 — Task 010 마지막 날)**: 커밋·PR 체크리스트, ISSUES 갱신 규약. **추가 배정**: ⓐ **I-113** — `bootstrap.test.ts`를 "mock 존재 → 등록 성공 경로" 검증으로 갱신(3팀 배럴 배선의 선행 조건, 4·5팀 20일차 소비가 여기 걸림) ⓑ **I-115** — `check-literals.mjs` allowlist 보강.

**팀장 검증: 결함 0건.** 4팀 당일 산출물(`src/i18n/t.ts`·`provider.tsx`)이 신규 D-18 룰에 걸리지 않는지 교차 확인 — 예외 경로가 의도대로 동작(해당 4파일 warn 0).

## 19일차 (2026-08-14) — Task 010 **종료**

- `src/lib/data/bootstrap.test.ts` — **I-113 해소**. mock 배럴 존재 여부를 런타임 검사해 `describe.runIf`/`skipIf`로 "배럴 이전/이후" 두 세계관을 모두 검증하도록 재작성. 순서 고정을 없애 3팀이 언제 배럴을 얹어도 이 파일을 다시 건드릴 필요가 없다.
- `.github/pull_request_template.md` (신규) — 머지 거부 3항목(C-1 D-16 / C-20 Next.js 16 문서 참조 / C-21 신규 의존성) + 3단 게이트(C-22) + 이슈 연동(`R-*` 체계 명시) + 소유 경로 확인.
- `docs/ISSUES.md` §6 — 갱신 규약(상태 표기 어휘·`R-*` 인용·PR 연동) 신설. I-36 해소, **I-116 신규**(정보성).
- `eslint.config.mjs` — `@typescript-eslint/no-unused-vars`에 `^_` 접두사 관례 인식 추가(134→111 경고).
- **⚑ H-06 ESLint 가드레일 5팀 인계 완료.**

**팀장 검증: 결함 0건. 판정 조정 1건** — 수락 기준 "`npm run lint` 경고 0"은 잔존 111건이 전부 4·5팀 소유 `src/app/**`의 D-18 항목이고 Task 011(22일차)에 구조적으로 종속돼 **조건부 충족**으로 판정, **22일차 재판정**. `eslint.config.mjs` diff를 직접 검토해 "경고 0을 맞추려는 규칙 완화"가 아님을 확인(접두사 없는 실제 미사용 변수는 계속 경고). 금지 패턴 실패도 팀장이 직접 심어 재현 후 원복.

**잔여**: I-115(`check-literals.mjs` allowlist 보강 후 게이트 연결 여부) 미착수.

**19일차 추가 조치(팀장 피드백 — I-117)**: `vitest.config.ts`의 `coverage.exclude`에 `src/lib/data/database.types.ts` 추가(MCP 생성물, 재포함 조건 없는 영구 제외 — 15일차 `polling.ts` 조건부 제외와 성격 구분해 주석 기재). `docs/ISSUES.md`에 **I-117** 등재(게이트가 마감 검증에 미연결돼 16~18일차 커버리지 위반 누적). `.github/pull_request_template.md`의 3단 게이트 절을 **`npm run gate` 단일 체크박스**로 갱신.

## 20일차 (2026-08-17) — Task 044

- `.github/workflows/ci.yml` 신규 — push·PR(master), ubuntu-latest, checkout@v4 → setup-node@v4(node 20, npm 캐시) → `npm ci` → **`npm run gate` 단일 호출**. 로컬/CI 게이트 분기 제거. WSL EPERM(I-62)로 `next build` 미포함.
- 팀장 결함 A 반려 후 **`check literals (advisory)` 스텝 추가**(`continue-on-error: true`). `gate.sh`는 미변경 — 로컬 머지 게이트는 fail-fast 3단이 정본. I-115 allowlist 정리 후 blocking 승격 예정(주석 명시).
- 검증: YAML 파싱 OK(5스텝), `npm run gate` 통과(lines 98.05%/branches 92.14%). **러너 첫 실행 미확인 — 21일차 확인 필요**.
