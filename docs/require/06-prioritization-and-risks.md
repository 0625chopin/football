# 06. 우선순위화 · 릴리스 범위 · 리스크 · 결정 기록

> 상위 문서: `00-requirements-summary.md`
> 문서 버전: v1.1 / 2026-07-20

---

## 6.1 MoSCoW 요약

### 6.1.1 FR 그룹별 분포

| 그룹 | 영역 | M | S | C | W | 계 |
|---|---|---|---|---|---|---|
| FR-LG | 리그·시즌·대진표·순위·승강 | 17 | 0 | 0 | 0 | 17 |
| FR-MT | 경기 시뮬레이션 엔진 | 14 | 2 | 0 | 0 | 16 |
| FR-PL | 선수 | 13 | 3 | 0 | 0 | 16 |
| FR-TM | 클럽·감독·전술·스쿼드 | 7 | 0 | 3 | 0 | 10 |
| FR-TR | 이적·계약·임대·트레이드 | 9 | 3 | 0 | 0 | 12 |
| FR-EC | 포인트 경제·스폰서·재정 | 10 | 2 | 0 | 1 | 13 |
| FR-YT | 유소년·은퇴·세대교체 | 4 | 1 | 1 | 0 | 6 |
| FR-AW | 수상 | 4 | 1 | 1 | 0 | 6 |
| FR-ST | 통계/지표 | 4 | 1 | 0 | 0 | 5 |
| FR-BT | 배팅 | 3 | 11 | 0 | 0 | 14 |
| FR-UI | 화면 | 14 | 8 | 3 | 1 | 26 |
| FR-AD | 관리/운영 (공통코드·크론) | 19 | 2 | 1 | 0 | 22 |
| **FR 합계** | | **118** | **34** | **9** | **2** | **163** |

### 6.1.2 NFR 영역별 분포

| 영역 | M | S | C | 계 |
|---|---|---|---|---|
| NFR-PF 성능 | 12 | 2 | 0 | 14 |
| NFR-DT 결정론 | 7 | 1 | 0 | 8 |
| NFR-QA 검증 (Vitest) | 8 | 2 | 0 | 10 |
| NFR-CFG 설정 외부화 | 6 | 1 | 0 | 7 |
| NFR-CR 크론 신뢰성 | 9 | 0 | 0 | 9 |
| NFR-SEC 보안 | 9 | 3 | 0 | 12 |
| NFR-A11Y 접근성 | 1 | 4 | 1 | 6 |
| NFR-RS 반응형 | 3 | 2 | 0 | 5 |
| NFR-SC 확장성 | 3 | 0 | 2 | 5 |
| NFR-OB 관측성 | 3 | 2 | 0 | 5 |
| NFR-MT 유지보수성 | 6 | 3 | 0 | 9 |
| **NFR 합계** | **67** | **20** | **3** | **90** |

### 6.1.3 전체 합계

| 우선순위 | FR | NFR | 합계 | 비중 |
|---|---|---|---|---|
| **Must (M)** | 118 | 67 | **185** | 73.1% |
| **Should (S)** | 34 | 20 | **54** | 21.3% |
| **Could (C)** | 9 | 3 | **12** | 4.7% |
| **Won't now (W)** | 2 | 0 | **2** | 0.8% |
| **총계** | **163** | **90** | **253** | 100% |

> Must 비중이 73%로 높은 이유: 시뮬레이션 세계는 부분 구현이 성립하지 않는다. 경기 엔진·순위·경제·성장이 하나라도 빠지면 "돌아가는 세계"가 되지 않기 때문에 MVP가 두꺼울 수밖에 없다. 대신 **UI 화면 수와 배팅 기능을 2차로 미루어** 1차 범위를 통제한다.

---

## 6.2 릴리스 범위

### 🟢 1차 릴리스 (MVP) — "돌아가는 세계와 관전 UI"

**정의**: 인증·배팅 없이 3개 리그가 실시간 자동 진행되고, 그 결과를 완결된 UI로 관전할 수 있다. 밸런스는 공통코드로 조정 가능하고, 결과는 재현 가능하다.

#### 포함 요구사항

| 영역 | 요구사항 ID |
|---|---|
| 리그 운영 | FR-LG-001 ~ 017 (전량) |
| 경기 엔진 | FR-MT-001 ~ 016 (전량) |
| 선수 | FR-PL-001 ~ 016 (전량) |
| 클럽·감독 | FR-TM-001 ~ 002, 004, 006 ~ 010 (FR-TM-003 팬 규모, 005 감독 교체는 C → 여력 시) |
| 이적·계약 | FR-TR-001 ~ 012 (전량) |
| 경제·스폰서 | FR-EC-001 ~ 012 |
| 유소년·은퇴 | FR-YT-001, 002, 004, 005, 006 |
| 수상 | FR-AW-001, 002, 003, 005, 006 |
| 통계 | FR-ST-001 ~ 005 (전량) |
| 배당 | FR-BT-005(산출), FR-BT-014(표시 전용) |
| 화면 | FR-UI-000 ~ 013, 019 ~ 026 |
| **공통코드** | **FR-AD-011 ~ 016, FR-UI-025** |
| **크론** | **FR-AD-017 ~ 022, FR-UI-026** |
| 운영 | FR-AD-001 ~ 005, 007, 010 |
| NFR | NFR-PF / NFR-DT / NFR-QA / NFR-CFG / NFR-CR / NFR-MT 전량, NFR-SEC-001·002·011·012, NFR-RS 전량, NFR-A11Y-002, NFR-SC-001·003·005, NFR-OB-001·003·004·005 |

#### 개발 순서 (Mock First 준수)

| 단계 | 작업 | 산출물 | 검증 |
|---|---|---|---|
| **1** | 도메인 타입 전량 정의 | `src/types/` (05번 문서 E-01~E-47 기준) | `tsc --noEmit` 무오류 |
| **2** | 공통코드 그룹·기본값 정의 + 상수 로더 인터페이스 | `src/lib/config/` | NFR-QA-008 |
| **3** | 시드 기반 Mock 팩토리 | `src/lib/mock/` | 동일 시드 재현 |
| **4** | `/sample` + 공통 컴포넌트 21종 (4상태 전부) | `src/app/sample/`, `src/components/` | KPI-6 커버율 100% |
| **5** | 주요 화면 Mock 데이터로 구현 | `src/app/**` | 4상태 확인 |
| **6** | 엔진 코어 수직 슬라이스 — 경기 → 시즌 → 프리시즌 | `src/lib/sim/` | NFR-QA-002·003·004 |
| **7** | Vitest 스위트 완성 | `src/**/*.test.ts` | NFR-QA-001~010 |
| **8** | Supabase 스키마 + 공통코드 테이블 + 시드 | 마이그레이션 | 스키마 검증 |
| **9** | Edge Function 크론 구현 | `supabase/functions/` | NFR-CR-001~009 |
| **10** | 어댑터 교체 (Mock → Supabase) | `src/lib/data/` | UI 코드 변경 0 (FR-UI-023) |

> **주의**: 6단계(엔진)를 4·5단계(UI)보다 먼저 하고 싶은 유혹이 있으나, CLAUDE.md의 Mock First 원칙에 따라 **UI를 먼저 완성**한다. 엔진 없이도 Mock 팩토리가 타입에 맞는 데이터를 만들어주므로 UI는 완결 가능하다.

### 🟡 2차 릴리스 — "배팅 오픈"

| 영역 | 요구사항 ID |
|---|---|
| 배팅 전 기능 | FR-BT-001 ~ 013 |
| 배팅 화면 | FR-UI-015 ~ 017 |
| 사용자·지갑 | FR-EC-013 (타입 활성화), E-38 ~ E-40 |
| 인증 | UC-101, NFR-SEC-007 |
| RLS 전면 | NFR-SEC-003 ~ 006, 008 ~ 010 |
| 운영 | FR-AD-008 |
| 검증 | NFR-QA-007 |
| 미뤄둔 항목 | FR-TM-003, FR-TM-005, FR-YT-003, FR-AW-004, FR-UI-014, FR-AD-009, NFR-A11Y 전량 |

### 🔵 3차 릴리스 — "확장"

캐시 충전/결제(FR-UI-018, WalletTransaction TOPUP), 인플레이 배팅, 리그4 확장(NFR-SC-003 활용), 팬·관중·티켓 수입, 소셜 기능, 아카이빙 자동화(NFR-SC-002), 모바일 앱 셸.

---

## 6.3 승인된 결정 기록 (Decision Log)

> 구 미해결 질문 Q-01 / Q-02 / Q-06 / Q-07에 대한 사용자 답변을 결정으로 확정했다.

### D-01 · 시즌 주기 및 상수 외부화

| 항목 | 내용 |
|---|---|
| **구 질문** | Q-01 — 시즌 1주기 약 14.7시간이 적절한가? |
| **결정** | ① "라운드 단위 동시 킥오프 + 리그별 간격 차등" 구조 **채택**, 세 리그 동시 종료 조건 유지<br>② 속도는 **더 느리게** — 비율 15:18:23 유지하되 약 5배 완만화 → **리그1 75분 / 리그2 90분 / 리그3 115분**<br>③ 검산: 75×46=3450, 90×38=3420, 115×30=3450 → 정규시즌 ≈57.5h ≈2.4일, 1시즌 ≈3일<br>④ **이 값들을 하드코딩하지 않고 DB 공통코드 테이블로 관리** |
| **반영 위치** | FR-LG-009(간격), FR-LG-010(사이클), FR-AD-011~016(공통코드 체계), FR-AD-014(상수 스냅샷), FR-UI-025(관리 콘솔), NFR-CFG-001~007, NFR-DT-007, E-41~E-44(엔티티), 05 문서 5.12절 |
| **파급** | 튜닝 상수 30개 이상이 공통코드로 이관. 결정론 유지를 위해 상수 스냅샷 엔티티가 추가됨 |
| **결정일** | 2026-07-20 |

### D-02 · 순위 포인트 역전 폭 축소

| 항목 | 내용 |
|---|---|
| **구 질문** | Q-02 — 리그1 최하위가 리그2 1위보다 낮은 설계를 유지할 것인가? |
| **결정** | **역전 폭 축소**. 역전 자체는 남기되 근접하도록 Base/Range 재조정 |
| **재계산 결과** | 리그1 Base 700→**1500** / Range 2300→**1500** (24위 700→**1500**)<br>리그2 Base 400→**850** / Range 1400→**950** (20위 400→**850**)<br>리그3 Base 200→**400** / Range 800→**600** (16위 200→**400**)<br>지수 1.8 유지, 각 리그 1위 3000 / 1800 / 1000 유지 |
| **검증** | 리그1 24위 1500 / 리그2 1위 1800 = **83.3%** (목표 80~90% 충족)<br>리그2 20위 850 / 리그3 1위 1000 = **85.0%** (충족) |
| **반영 위치** | FR-EC-002 (전 구간 수치표 재작성), 공통코드 그룹 `LEAGUE_FINISH_POINT` |
| **결정일** | 2026-07-20 |

### D-03 · Vitest 도입

| 항목 | 내용 |
|---|---|
| **구 질문** | Q-06 — 테스트 러너를 도입할 것인가? |
| **결정** | **Vitest 도입 확정.** 구 "테스트 러너 부재 → tsc + 골든 파일로 대체" 우회안 폐기 |
| **정식 요구사항화** | ① 엔진 순수 함수 단위 테스트 (커버리지 라인 80% / 브랜치 70%, 핵심 9개 함수 100%)<br>② 시드 고정 스냅샷 (경기 100건, 시즌 3건)<br>③ 확률 분포 불변식 (경기당 득점 2.5~3.0, 홈 승률 42~48% 등 7개 밴드)<br>④ 회계 항등식 (포인트 총량 보존, zero-sum 검증)<br>⑤ 구조 불변식 (팀 수, 스쿼드, 나이, 계약 등 9개)<br>⑥ 공통코드 로딩·발효 정책 테스트<br>⑦ 성능 회귀 테스트 |
| **반영 위치** | NFR-QA-001 ~ 010, NFR-MT-003(대체안 폐기 명시), DC-03 |
| **제약** | **이번 단계에서 패키지 설치하지 않음.** 구현 단계에서 도입 |
| **결정일** | 2026-07-20 |

### D-04 · Supabase Edge Function 크론

| 항목 | 내용 |
|---|---|
| **구 질문** | Q-07 — 배포 환경 및 스케줄러 구현 방식은? |
| **결정** | **Supabase Edge Function 크론이 정규 경로.** catch-up 방식은 보조/폴백으로 격하 |
| **필수 다룸 항목** | ① 크론 주기와 라운드 간격의 관계(기본 1분, 최소 간격 75분의 약수)<br>② 중복 실행 방지 — 어드바이저리 락 + 상태 전이 멱등<br>③ 실행 실패 재시도(지수 백오프 3회)와 밀린 라운드 따라잡기(1회 상한 50경기)<br>④ 크론 중단 구간 감지(주기 3배 초과)<br>⑤ 서비스 롤 키를 Edge Function 내부에서만 사용<br>⑥ 실행 로그·관측성(`cron_run`, `cron_gap`) |
| **반영 위치** | FR-AD-017 ~ 022, FR-UI-026, NFR-CR-001 ~ 009, E-45(CronRun), E-46(CronGap), DC-15, DC-16 |
| **결정일** | 2026-07-20 |

### D-05 ~ D-11 · 분석가 확정 사항 (원본 "확인 필요" 해소)

| ID | 항목 | 결정 요약 | 근거 위치 |
|---|---|---|---|
| **D-05** | 타이브레이커 전체 순서 | 승점 → 골득실 → 다득점 → 승자승 → 다승 → 페어플레이 → 시드 추첨. 승강 경계 동률만 중립지 단판 플레이오프 | FR-LG-005 |
| **D-06** | 리그3 최하위 처리 | 강등 대신 "리빌드 제재" — 페널티 3종 + 구제 2종. death spiral 방지 | FR-LG-007 |
| **D-07** | 시즌 종료 시점 어긋남 | 라운드 단위 동시 킥오프 + 리그별 간격 차등(D-01로 수치 확정) | FR-LG-009 |
| **D-08** | 컵대회 포맷 | 3개 리그 통합 단일 넉아웃 60팀, bye 4, 6라운드 59경기, 정규시즌 6슬롯 삽입 | FR-LG-015 |
| **D-09** | 컨디션 30% 삭감 수식 | `M = 0.70 + 0.30 × (C−1)/9` 선형. C=1→0.70, C=10→1.00 | FR-MT-008 |
| **D-10** | 다중 포지션 숙련도·페널티 | 5단계 1.00/0.95/0.88/0.75/0.60 + 미보유 `max(0.45, 0.88−0.11×dist)`, GK 교차 0.35 | FR-PL-006, FR-PL-007 |
| **D-11** | 리그별 순위 포인트 배분표 | 지수 1.8 곡선, 전 구간 수치표(D-02 반영 재계산) | FR-EC-002 |

### D-12 ~ D-14 · 부수 확정 사항

| ID | 항목 | 결정 | 근거 |
|---|---|---|---|
| **D-12** | 팀 수 | 원본 25/20/17 → **24/20/16** | 홀수 팀은 라운드마다 bye가 발생해 라운드 동시 킥오프·동시 종료 설계와 충돌 |
| **D-13** | 리그2 순위 포인트 범위 | 원본 "1~29위" → **1~20위** | 명백한 오타 (리그2는 20팀) |
| **D-14** | 승격 경로 | 정규시즌 상위 3팀만. 플레이오프는 별도 트로피 | 이중 승격 경로는 팀 수 불변 조건(24/20/16)을 깨뜨림 (FR-LG-014) |

### D-15 · 단일 월드 확정

| 항목 | 내용 |
|---|---|
| **구 질문** | Q-08 — 월드는 1개만 운영하는가, 여러 월드(서버)를 병렬 운영할 것인가 |
| **결정** | **단일 월드로 확정.** 다중 월드 병렬 운영은 도입하지 않는다 |
| **파급** | ① `E-01 World`는 **단일 레코드 전제**로 설계<br>② 전 쿼리에 `world_id` 스코핑 필터를 강제하지 않는다<br>③ 월드 선택/스위처 UI는 요구사항에서 제외<br>④ 가정 `AS-09`는 가정에서 **확정 사항**으로 승격<br>⑤ 향후 다중 월드 확장 시 `world_id` FK는 이미 스키마에 존재하므로 마이그레이션 없이 스코핑만 추가하면 됨 (확장 여지 보존) |
| **효과** | 1차 1단계(타입 정의)의 **블로킹 게이트 해소** — 즉시 착수 가능 |
| **결정일** | 2026-07-20 |

### D-16 · FM 실데이터 미사용 확정

| 항목 | 내용 |
|---|---|
| **구 가정** | AS-02 — 원본 기획의 "FM 풋볼매니저를 마케팅 후 능력치가 주어진다"는 표현은 실제 FM 데이터 사용이 아니라 체계 참고를 뜻한다 |
| **결정** | **FM 실데이터를 사용하지 않는다.** 참고하는 것은 **능력치 체계**(1~30 스케일, 세부 능력치 카테고리 구성)뿐이다 |
| **파급** | ① 선수 이름·클럽 이름·능력치·국적은 **전부 시드 기반 절차적 생성**<br>② 실명 선수·실존 클럽·실제 리그 명칭·엠블럼·초상은 일절 사용하지 않음<br>③ FM 데이터 파일 임포트, 스크래핑, 외부 축구 데이터 API 연동 모두 **범위 밖**<br>④ 생성기는 결정론적이어야 함 — 동일 `world_seed` → 동일 선수단 (NFR-DT와 정합)<br>⑤ 가정 `AS-02`는 가정에서 **확정 사항**으로 승격 |
| **근거** | 저작권·상표권 리스크 회피. 절차적 생성은 밸런싱 자유도(리그 티어별 능력치 분포 제어)도 함께 확보한다 |
| **반영 위치** | DC-12, FR-PL-002, FR-PL-014, FR-TM-001 |
| **잔여 과제** | 없음 — 생성될 이름의 문화권은 **D-17로 확정** |
| **결정일** | 2026-07-20 |

### D-17 · 이름 문화권 = 국적 기반 다국적 혼합

| 항목 | 내용 |
|---|---|
| **구 질문** | Q-05 — 선수/팀 이름의 문화권은 유럽식 가상 이름인가, 한국식인가, 혼합인가 |
| **결정** | **국적 기반 다국적 혼합으로 확정.** 선수의 `nationality` 필드를 근거로 해당 국가의 이름 풀에서 성·이름을 생성한다 |
| **파급** | ① 국가 코드별 **이름 풀(성/이름 후보군)** 데이터가 필요 — 국가 목록과 각국 비중은 공통코드로 관리<br>② 리그 티어별·팀별 국적 분포를 제어 가능하게 (상위 리그일수록 다국적 비중 상승 등)<br>③ 클럽 이름도 연고 국가에 맞춰 생성 (접두·접미 패턴을 국가별로 분리)<br>④ 이름 생성은 **결정론적** — 동일 `world_seed` → 동일 이름 (NFR-DT와 정합)<br>⑤ **Mock 팩토리와 실제 생성기가 동일한 이름 생성 로직을 공유**해야 한다 (Mock↔DB 동일 타입 원칙의 연장) |
| **주의** | 실존 인물·실존 클럽과 우연히 동일한 이름이 생성될 수 있으므로, **실명 블랙리스트 필터**를 생성기에 둔다 (D-16의 저작권·초상권 회피 취지 유지) |
| **반영 위치** | FR-PL-014, FR-TM-001, DC-12 |
| **결정일** | 2026-07-20 |

### D-18 · 다국어(i18n) 대응 확정

| 항목 | 내용 |
|---|---|
| **구 질문** | Q-04 — 서비스 언어는 한국어 단일인가, 다국어 대응이 필요한가 |
| **결정** | **다국어 대응으로 확정.** 기본 로케일 **한국어(ko)**, 2차 로케일 **영어(en)**, 그 외는 메시지 카탈로그 추가만으로 확장 가능한 구조 |
| **핵심 파급** | ① **i18n 기반은 Phase 1(골격)에 배치한다.** UI를 완성한 뒤 국제화를 얹으면 전 컴포넌트 재작업이 발생<br>② 모든 UI 문자열은 **하드코딩 금지, 번역 키 사용** — Phase 2 UI Task의 완료 조건<br>③ 로케일 라우팅 전략·메시지 카탈로그 구조·번역 키 네이밍 규약을 선정의<br>④ 로케일별 **날짜·시각·숫자 서식** (경기 시각, 포인트, 배당 표기)<br>⑤ `/sample` 쇼케이스에 **로케일 전환 확인**을 포함 |
| **번역 대상** | UI 레이블 / 포지션·경기 이벤트·수상·부상 등 **열거형 표시명** / 안내·에러·빈 상태 문구 |
| **번역 비대상** | **선수·클럽 이름** — D-17에 따라 국적 기반으로 생성된 고유명사이므로 번역하지 않는다. 국가명 표시는 번역 대상 |
| **기술 주의** | Next.js 16의 i18n 처리 방식은 학습 데이터와 다를 수 있다. 구현 전 **`node_modules/next/dist/docs/` 확인 필수** (AGENTS.md 규칙, NFR-MT-006) |
| **반영 위치** | FR-UI 전반, NFR-A11Y·NFR-MT-006, DC-02 |
| **결정일** | 2026-07-20 |

### D-19 · 승부차기 득점은 개인 통산 득점에 미포함

| 항목 | 내용 |
|---|---|
| **구 질문** | Q-09 — 승부차기 득점을 개인 통산 득점에 포함할 것인가 |
| **결정** | **미포함 확정** (기본 가정대로). 실제 축구 통계 관행을 따른다 |
| **파급** | ① 승부차기 성공/실패는 **별도 승부차기 기록**으로만 저장하고 `goals` 집계에 합산하지 않는다<br>② 시즌·통산 득점, **득점왕(골든부트) 산정에서 제외**<br>③ 경기 승패 판정에는 정상 반영 (토너먼트 진출 결정)<br>④ 연장전 득점은 **정상 득점으로 포함** — 승부차기와 구분할 것<br>⑤ 선수 상세 화면에서 승부차기 기록은 별도 항목으로 표시 |
| **반영 위치** | FR-MT-013, FR-ST-001, FR-AW-002 |
| **결정일** | 2026-07-20 |

### D-20 · 감독은 독립 엔티티 + 최소 이적

| 항목 | 내용 |
|---|---|
| **구 질문** | Q-13 — 감독도 이적·명성·수상 트랙을 완전하게 가져갈 것인가, 팀 부속 속성으로 둘 것인가 |
| **결정** | **독립 엔티티 + 최소 이적 확정** (기본 가정대로) |
| **파급** | ① 감독은 팀의 컬럼이 아니라 **독립 엔티티**로 두고 팀과 N:1 관계 (감독 교체 이력 추적 가능)<br>② **명성·수상 트랙 보유** — 감독상 수상, 명성 변동<br>③ **이적은 최소 범위** — 프리시즌 경질/선임 정도만. 선수처럼 완전한 협상·계약·이적료 로직은 두지 않는다<br>④ 감독 공석 시 전술 폴백은 `BALANCED` (공석 최대 유지 기간은 I-03으로 여전히 구현 시 확정)<br>⑤ 감독 전용 화면은 1차 범위 밖 — 팀 상세 내 정보로 표시 |
| **반영 위치** | FR-TM-004, FR-TM-005, FR-AW-001 |
| **결정일** | 2026-07-20 |

### D-21 ~ D-26 · 구현 세부 규칙 확정 (구 I-01 ~ I-06)

> `docs/ISSUES.md` 3절에 "구현 시 확정"으로 미뤄둔 6건을 기본값으로 확정한다 (2026-07-20).
> 여기서 정한 수치는 **전부 공통코드 대상**이므로, 밸런싱 과정에서 코드 변경 없이 조정할 수 있다.

#### D-21 · 이중 임대 방지 (구 I-01)

| 항목 | 내용 |
|---|---|
| **규칙** | **재임대(sub-loan) 금지.** 임대는 원소속팀 → 임대팀 **1단계만** 성립한다 |
| **세부** | ① 임대 기간은 **1시즌 고정**, 프리시즌에만 성립·종료<br>② 임대 중인 선수는 **매각·방출 대상에서 제외**. 임대팀은 해당 선수를 매각할 수 없다<br>③ 임대 복귀 후 **다음 프리시즌부터 재임대 가능**<br>④ 팀당 동시 **임대 영입 상한 3명 / 임대 방출 상한 5명** (공통코드)<br>⑤ 원소속팀과의 맞대결 출전은 **허용**한다 — 실제 축구 관행과 다르나 시뮬레이션 단순화를 우선 (재검토 여지 있음) |
| **반영 위치** | FR-TR-010 |

#### D-22 · GK 부재 시 대체 골키퍼 선정 (구 I-02)

| 항목 | 내용 |
|---|---|
| **규칙** | 아래 우선순위를 순서대로 적용한다 |
| **세부** | ① 벤치에 GK가 있고 **교체 카드가 남아 있으면** → 유효 능력치가 가장 낮은 필드플레이어를 빼고 GK 투입<br>② 교체 소진이거나 벤치 GK가 없으면 → **출전 중 선수 중 `goalkeeping` 능력치 최고**인 선수<br>③ ②가 동률이면 → **유효 능력치가 가장 낮은 선수** (팀 전력 손실 최소화)<br>④ 그래도 동률이면 → `player_id` 기반 **시드 결정론적 선택** (재현성 보장)<br>⑤ 선정된 필드플레이어에게 **GK 교차 배율 0.35** 적용, 원 포지션 기여는 상실 |
| **반영 위치** | FR-PL-006, FR-MT-* |

#### D-23 · 감독 공석 처리 (구 I-03)

| 항목 | 내용 |
|---|---|
| **규칙** | **공석 유지 기간 0라운드.** 경질·이탈 즉시 **대행 감독을 자동 선임**한다 |
| **세부** | ① 대행 감독: 전술 성향 `BALANCED`, 전술 숙련도 = **직전 감독의 50%**, 명성 = **팀 명성의 50%**<br>② 정식 감독 선임은 **다음 프리시즌**에만 이뤄진다<br>③ 대행 체제 최대 유지 = **1시즌**. 초과 시 프리시즌에 강제 정식 선임<br>④ 공석 상태로 경기를 치르는 경우는 발생하지 않는다 |
| **근거** | 공석 상태를 허용하면 전력 산출식에 널 케이스가 생겨 엔진 분기가 늘어난다. 대행을 즉시 채워 **항상 감독이 존재**하는 불변식을 유지한다 |
| **반영 위치** | FR-TM-004, FR-TM-005, D-20 |

#### D-24 · 컵대회 시딩 폴백 (구 I-04)

| 항목 | 내용 |
|---|---|
| **규칙** | 티어 교차 매칭을 우선하되, 불가능하면 시드 순 상하위 교차로 폴백한다 |
| **세부** | ① **1순위**: 티어 교차 매칭 (리그1 ↔ 리그3 우선, 다음 리그1 ↔ 리그2, 리그2 ↔ 리그3)<br>② **2순위**: 잔여가 동일 티어만 남으면 **시드 순 상하위 교차** (1↔n, 2↔n−1, …)<br>③ **동일 리그 소속끼리의 대진은 허용**한다 — 정규시즌에서 이미 맞대결하므로 별도 제약을 두지 않는다<br>④ 시드 산출과 대진 확정은 `seasonId` 기반 **결정론적 처리** |
| **반영 위치** | FR-LG-015 |

#### D-25 · 확률 분포 밴드 캘리브레이션 방침 (구 I-05)

| 항목 | 내용 |
|---|---|
| **규칙** | `NFR-QA-004`에 정의된 밴드를 **그대로 확정 기준으로 유지**한다 |
| **세부** | ① 엔진 1차 구현 직후 **10,000경기 실측으로 1회 캘리브레이션**을 수행<br>② 실측이 밴드를 벗어나면 **밴드를 넓히지 말고 엔진 상수(공통코드)를 조정**한다 — 밴드는 품질 기준이지 관측 결과가 아니다<br>③ 밴드 자체의 변경이 불가피하다고 판단되면 공통코드 수정이 아니라 **요구사항 문서 개정**으로 처리한다 (추적성 유지) |
| **근거** | 밴드를 실측에 맞춰 넓히면 회귀 검출력이 사라져 테스트가 무의미해진다 |
| **반영 위치** | NFR-QA-004, R-01 |

#### D-26 · 공통코드 기본값 확정 (구 I-06)

| 항목 | 내용 |
|---|---|
| **규칙** | `05-data-requirements.md` 5.12절 카탈로그의 **초기 시드값을 그대로 기본값으로 확정**한다 |
| **세부** | ① 해당 값을 `CommonCode.default_value` 에 넣고 **되돌리기 기준**으로 삼는다<br>② 밸런싱 조정은 `value` 만 수정한다 — `default_value` 는 불변이므로 언제든 초기 상태로 복원 가능<br>③ 조정에 **코드 변경·재배포가 필요 없다** (NFR-CFG-001)<br>④ 1차 재튜닝 시점은 **20시즌 장기 시뮬 검증 직후** (R-01 완화책) |
| **주의** | 이 값들은 **밸런싱이 검증되지 않은 출발점**이다. AS-12(반복 튜닝 전제)가 그대로 유효하며, 초기값이 최종값이라는 뜻이 아니다 |
| **반영 위치** | E-42, FR-AD-011, NFR-CFG-001, AS-12 |

---

### D-27 · 시즌 페이즈 6번째 = `TIEBREAK` 확정 (구 `docs/ISSUES.md` I-33)

> 승인: 팀장, **2026-07-23(3일차)** / 제기: 2팀 시뮬엔진 교차 점검, 확인: 1팀 코어·품질

| 항목 | 내용 |
|---|---|
| **결정** | 시즌 페이즈에 **6번째 값 `TIEBREAK`를 신설**한다. 상태머신은 `REGULAR ⇄ CUP_SLOT → PLAYOFF → (TIEBREAK?) → SETTLEMENT → PRESEASON → REGULAR(next)`이며, **`TIEBREAK`는 승강 경계 동률이 발생한 시즌에만 진입하는 조건부 페이즈**다 |
| **근거** | ① `SETTLEMENT` 예산 50분은 경기 1회(리그별 75~115분)를 담을 수 없다<br>② `PLAYOFF`로 흡수하면 "승강 동률 해소가 **플레이오프 진입 전에** 끝나야 한다"는 순서를 위반한다<br>③ `E-15 Fixture.competition_type`에 `TIEBREAK`가 이미 있어 **대회 축은 정합**했고, **페이즈 축만 공백**이었다 |
| **경위** | `ROADMAP.md` 173·305행만 "페이즈 6종(FR-LG-010)"이라 적었고, **FR-LG-010 본문(03:118)·E-01 `current_phase`(05:46)·공통코드 `PHASE_DURATION_MIN`(05:624)은 전부 5종**이어서 6번째의 정의가 요구사항에 존재하지 않았다. 한편 Task 026이 생성하는 `competition_type = TIEBREAK` Fixture(FR-LG-008 수용기준 ③)는 킥오프할 페이즈가 없었다 |
| **구현 전제 배분** | **1팀** — 6일차 `src/types/enums.ts`에 값 추가 및 조건부 페이즈 주석<br>**2팀** — Task 025 페이즈 상태머신(28일차)에 전이 반영<br>**3팀** — Task 003 `PHASE_DURATION_MIN`에 `TIEBREAK` 예산 신설 |
| **반영 위치** | FR-LG-010, E-01 `current_phase`, E-15 `competition_type`, `PHASE_DURATION_MIN`(05 5.12절) |

### D-28 · 시드 정수 폭 = 53비트 안전 정수(`number`), `bigint` 기각 (구 `docs/ISSUES.md` I-32)

> 승인: 팀장, **2026-07-23(3일차)** / 제기: 2팀 시뮬엔진 교차 점검, 판정: 1팀 코어·품질(타입 소유)

| 항목 | 내용 |
|---|---|
| **결정** | 시드 값은 **`number`를 유지**하되 상한 규약을 **`Number.MAX_SAFE_INTEGER`(2⁵³−1) = 53비트 안전 정수**로 규정한다. **`bigint` 도입은 기각**한다 |
| **근거** | 2팀 시드 파생 규약이 상위 2비트를 네임스페이스 태그로 예약하므로 32비트 규약에서는 실효 payload가 **30비트**뿐이다. 시즌당 이벤트 시드 ~6×10⁵ 기준으로 **생일 충돌이 수백 건/시즌** 발생한다(재현성 영향 0, 통계적 상관만 미미). 53비트로 완화하면 payload가 51비트가 되어 충돌이 소멸한다 |
| **`bigint` 기각 사유** | ① PRNG(xoshiro128**)가 **32비트 워드 연산으로만** 구성돼 있다 ② **JSON 직렬화 불가**로 스냅샷·API 계약이 깨진다 ③ 틱 핫패스 성능이 저하되어 **Task 023 p95 ≤ 50ms**를 위협한다 |
| **파급** | **1팀** — `docs/devStep/02.타입스키마설계원칙.md` **T2-a(32비트 확정) 개정이 선행**(5일차), 7일차 `src/types/brand.ts` 반영, 8일차 H-01 동결 전 완료<br>**6팀** — 물리 컬럼은 `bigint`가 정답이 되므로 **I-29(`bigint` vs 32비트 표기 불일치)가 동시 해소**된다(12일차 H-08에서 확인) |
| **재현성 영향** | 없음. 기준 시드 스냅샷 생성(2팀 Task 023, 34일차) **이전**의 규약 변경이므로 R-03 회귀 대상이 아니다 |
| **반영 위치** | E-01 `world_seed`, E-03 `season_seed`, E-15 `match_seed`, NFR-DT-001, DC-08 |

---

### D-29 · 폴링 안전망 폴백 = 30초 / 라이브 15초, 정상값 5초/3초는 유지 (구 `docs/ISSUES.md` I-77)

> 승인: 사용자, **2026-08-04(11일차)** / 제기: 6팀 DB·인프라 1차 교차 점검 / 반영: 1팀(`polling.ts`)·3팀(`fallback.ts`)

| 항목 | 내용 |
|---|---|
| **결정** | 폴링 주기의 **정상값은 ROADMAP Task 004 원문대로 기본 5초 / 라이브 3초를 유지**하되, **공통코드 조회 실패 시의 안전망 폴백값만 기본 30초 / 라이브 15초로 낮춘다**. 두 값은 성격이 다르며 문서·주석에서 반드시 구분한다 |
| **근거** | `docs/business/03-budget-plan.md` §2.5 실측표(케이스 A, 일 평균 동시 125명) 기준 **5초 = 월 $133.7 / 15초 = $23.2 / 30초 = $1.6**. 문제의 핵심은 5000/3000ms가 정상값이 아니라 **장애 시에만 쓰이는 안전망**이었다는 점 — 즉 **장애 상황에서 시스템이 가장 비싼 설정으로 폴백하는 역전 구조**였다. 안전망은 가장 보수적(저비용) 값으로 떨어져야 한다 |
| **정상값 공급 경로** | 5000/3000은 6팀이 `common_code` 실데이터를 적재한 뒤 전역 기본값 소스가 공급한다. `loader.ts` 해석 우선순위상 전역 기본값이 항상 하드코딩 폴백보다 우선하므로, 정상 운영 시 이 안전망 값은 쓰이지 않는다 |
| **2중 안전망 주의** | `polling.ts`의 리터럴 폴백은 3팀 `fallback.ts`보다 **더 바깥쪽 최후 안전망**이다(양쪽 소스 모두 미등록 시 발동). 두 레이어의 수치가 어긋나면 결정의 취지가 우회되므로 **항상 같은 값으로 유지**한다 |
| **파급** | **6팀** — 공통코드 시드에 `UI_PARAM` 그룹(`POLL_INTERVAL_MS`/`POLL_LIVE_MS`/`LEADERBOARD_MIN_APPEARANCE_PCT`) 포함 필수. **42(58~62일차)** 에서 실사용 데이터로 재검토 |
| **반영 위치** | `src/lib/data/polling.ts`, `src/lib/config/fallback.ts`, `docs/wireframe/00-공통규약.md` C-03a, NFR-CFG-005, AS-13 |

### D-30 · Tier B 40필드 — 스키마 NOT NULL 유지, 재시뮬레이션 컴포넌트 소유 = 2팀 (구 `docs/ISSUES.md` I-79·I-80·I-81)

> 승인: 사용자, **2026-08-04(11일차)** / 제기: 6팀 DB·인프라 1차 교차 점검 / 설계: 2팀 시뮬엔진

| 항목 | 내용 |
|---|---|
| **문제** | `player_match_stat` 등 공유 56컬럼이 **전부 NOT NULL**인데, 2팀 `accumulatePlayerMatchStats()`는 이벤트 로그에서 **Tier A 16필드만** 산출한다. 나머지 40필드를 **누가 언제 계산해 저장하는지 어느 팀도 소유를 선언하지 않은 상태**였다 |
| **결정** | ① **스키마는 NOT NULL을 유지**한다 — nullable 완화는 기각(AS-10이 "0 자리표시자"를 이미 기각한 것과 같은 취지로 데이터 정합성을 우선)<br>② `matchSeed` 시드 통계 모델 컴포넌트의 **소유를 2팀에 배정**한다<br>③ 6팀 **Task 032 착수 전 계약 확정 + 6팀 재검수**를 조건으로 건다. 그때까지 계약이 안 나오면 nullable 완화를 재논의한다 |
| **56필드 산출 경로 3갈래** | **26개**(`NO_EVENT_TYPE` — 패스 8·드리블 4·수비 11·GK 3) → 30B 재시뮬레이션이 **영구적으로** 책임진다. 23종 이벤트 카탈로그에 대응 타입이 없고 볼륨(패스 700~1000건/경기) 때문에 이벤트화가 기각됐다<br>**9개**(`NEEDS_ROSTER_CONTEXT`) → 12일차 교체·14일차 GK 대체·21일차 라인업이 갖춰지면 **이벤트+로스터 폴드**로 Tier A 재분류(재시뮬레이션 불필요)<br>**5개**(`DETAIL_SCHEMA_UNDEFINED`) → `detail` 스키마 확정 후 폴드 |
| **설계 규명** | "matchSeed 재시뮬레이션"은 **tick 엔진 재실행이 아니다** — 다시 돌려도 같은 23종 이벤트 로그만 나오므로(패스 타입 자체가 카탈로그에 없음). 실제로는 Task 024의 능력치 계수 + Tier A 확정 사실(슛 수·xG 등)을 입력으로 필드 그룹별 기댓값 공식 + 시드된 지터로 정수 카운트를 산출하는 **별도 시드 통계 모델**이다. **Tier A → Tier B 단방향 의존**이며 역방향은 없다 |
| **파급** | **2팀** — 30B 신설(D-31). 계약은 `src/lib/sim/match/tier-b-resim-contract.ts`(타입만, 런타임 구현 없음)와 `TIER_B_RESIM_DESIGN.md`<br>**6팀** — Task 032 매퍼가 3갈래 경로를 구분해야 한다 |
| **반영 위치** | `docs/db/schema-design.md` §3.5, `src/lib/sim/match/stats.ts`, AS-10, FR-ST-001 |

### D-31 · 2팀 31·32일차 2일 신설 — Task 026~028 및 H-15·H-16 2일 순연 (구 `docs/ISSUES.md` I-78)

> 승인: 사용자, **2026-08-04(11일차)** / 제기: 2팀 시뮬엔진 (D-30 후속)

| 항목 | 내용 |
|---|---|
| **결정** | 2팀 일정의 **30일차(H-24) 직후에 2일을 신설**한다 — **신 31일차**(2026-09-01, Tier B 26필드 재시뮬레이션 계약 구현 + I-65 `events.ts` 구조 마커 결정론 보정) / **신 32일차**(2026-09-02, 전용 Vitest). 신설 이후 구 31~79일차는 **전부 "구 번호 +2"로 재번호**한다(Task 026~028 = 33~55일차, 엔진 트랙 종료 구간 = 56~81일차). Task 025의 H-24는 건드리지 않고 Task 026이 즉시 이어받는 배치다 |
| **근거** | 2팀 9~53일차에 빈 날이 없어 기존 일정 내 흡수가 불가능했다. D-30이 Task 032(6팀, 13~18일차) 착수 전 계약 확정을 조건으로 걸었고, 실제 산출은 Task 026(31일차 후처리 파이프라인)이 소비하므로 그 사이에 들어가야 한다 |
| **순연 범위** | Task 026~028 및 인계 **H-15(37→39일차, 6·3팀 소비)**, **H-16(53→55일차, 3팀 소비)** 이 각각 2일 순연 |
| **다운스트림 영향 — 3팀** | Task 030(54~62 → **56~64일차**), 031b(63~66 → **65~68일차**) 2일 순연. 031a(36~38일차)는 H-16과 무관해 영향 없음. 031b 종료 후 SP-6까지 여유가 5일 → 3일로 줄 뿐 **마이너스가 아니므로 3팀은 흡수 가능** |
| **다운스트림 영향 — 6팀** | H-15 수신 38 → **40일차**, Task 033 체인 38~48 → **40~50일차**, SP-4 48 → **50일차**, 037 → **51~55일차**, 038 → **56~59일차**, 042 → **60~64일차**. 034b 대기(63~65일차)·045(69~71일차)는 4팀 022·H-21이 지배 게이트라 **날짜 유지**. 26~37일차 대기구간·V-01 실측·SP-3(37일차)도 영향 없음<br>⚠️ **042 종료가 62→64일차가 되어 034b 착수(65일차) 전 여유가 3일 → 1일로 축소** — 새 병목 후보. 042가 하루라도 밀리면 034b가 즉시 영향받는다 |
| **다운스트림 영향 — 5팀 (종료일 슬립)** | 5팀은 **slack = 0일**(문서 헤더 명문, 34~59일차 26일 연속이 전부 CP)이고 §5 공수 추정이 §2 배정 일수와 정확히 일치해 흡수할 잉여가 없다. H-18 수신일과 039 착수일이 **같은 날로 맞물려** 완충일이 0이므로 2일이 1:1로 전이된다 — H-18 60 → **62일차**, 039 60~67 → **62~69일차**, 040 68~73 → **70~75일차**, 트랙 종료 구간 74~79 → **76~81일차** |
| **최종 일정 영향 (사용자 승인)** | **SP-6(전 팀 E2E 입회) 71 → 73일차**, **프로젝트 최종 종료 79 → 81일차**. 회수안 2종(6팀 037/038 재배치로 H-18 앞당기기, 5팀 039/040 스코프 축소)은 **기각** — Tier B 데이터 정합성과 기능 범위를 종료일보다 우선한다. SP-6는 전 6팀 입회 고정 일정이므로 **6개 팀 md 전부에 73일차로 반영**한다 |
| **확정 후반 일정 (11일차 종료 시점)** | **1팀** 036 = 71~76, 041 = **77~81일차**<br>**2팀** Task 026~028 = 33~55, 대기조 = 56~81일차<br>**3팀** 030 = 56~64, 031b = 65~68, 트랙 종료 = 69~81일차<br>**4팀** 022 = 60~65, H-21 = **66일차(유지)**<br>**5팀** 039 = 62~69, 040 = 70~75, 트랙 종료 = 76~81일차<br>**6팀** 033 = 40~50, 037 = 51~55, 038 = 56~59, 042 = 60~64, 034b 착수 66·완료 **68일차(유지)**, 045 = **69~71일차(유지)**<br>**공통** SP-4 = 50, **SP-6 = 73**, 최종 종료 = **81일차** |
| **순연되지 않는 항목** | 4팀 022·H-21, 6팀 034b·045는 **4팀 UI 트랙이 지배 게이트**라 2팀 엔진 순연의 영향을 받지 않는다. 인계 **H-22(034b→1팀) = 68일차**, **H-23(040→1팀) = 75일차**로 확정 |
| **잔여 리스크** | 5팀 slack이 계속 0이므로 **이후 어떤 작은 지연도 종료일을 그대로 민다.** 또한 H-18과 L-02(규제 검토 게이트)가 같은 날에 몰려 완충이 없다는 5팀 지적은 별도 검토 대상으로 남긴다 |
| **slack 영향** | 신설 2일은 대기 구간이 아니라 **활성 개발 구간 내부에 삽입**되므로, 2팀 후반 대기조 26일 블록은 갉아먹히지 않고 이름만 56~81일차로 밀린다. 즉 slack을 소모하는 지연이 아니라 활성 구간의 확장이다 |
| **반영 위치** | `docs/team-schedule/02-시뮬레이션엔진팀.md`, `docs/team-schedule/03-데이터밸런싱배당팀.md`, `docs/team-schedule/06-DB인프라팀.md` |

### D-32 · 앱 부트스트랩 단일 진입점 `bootstrapApp()` (구 `docs/ISSUES.md` I-67·I-72)

> 판정: 팀장, **2026-08-04(11일차)** / 제기: 4팀 UI기반·i18n 1차 교차 점검 / 반영: 1팀 코어·품질

| 항목 | 내용 |
|---|---|
| **문제** | 등록 함수를 만들어 두고 **아무도 호출하지 않아 기능이 죽어 있는** 버그가 두 번 반복됐다(I-67 어댑터 등록, I-72 공통코드 폴백 등록). 후자 때문에 `loadConstants('UI_PARAM')`이 항상 실패해 "공통코드에서 폴링 주기를 읽는다"는 설계 의도가 실현되지 않고 있었다 |
| **결정** | **`bootstrapApp()`을 앱 부트스트랩 단일 진입점**으로 두고, 내부에서 `installHardcodedFallback()`(3팀) → `bootstrapDataSource()`(1팀) 순으로 호출한다. `bootstrapDataSource()`는 어댑터 등록 전용 의미를 그대로 유지해 결합도를 올리지 않는다 |
| **호출 규약** | **4팀은 13일차 이후 루트 레이아웃(`src/app/[lang]/layout.tsx`)에서 `await bootstrapApp()` 한 줄만** 호출한다. `bootstrapDataSource()`를 직접 호출하면 공통코드 폴백 등록이 누락된다. **향후 레지스트리가 늘어도 `bootstrapApp()` 본문에만 한 줄 추가**하고 소비 측 코드는 고치지 않는다 |
| **기각안** | 호출처를 레이아웃에 2개로 늘리는 안 — 세 번째 레지스트리가 생기면 같은 누락이 재발한다 |
| **미검증 리스크** | `bootstrap.ts`의 **변수 경유 동적 import**가 webpack에서 아직 실컴파일된 적이 없다(대상 `./mock`·`./supabase` 미존재). "Critical dependency" 경고나 해석 실패 가능성 → **3팀 Mock 착수(13~19일차) 시 `next dev --webpack`으로 실측**, 실패 시 리터럴 `if/else` 분기로 대체. 6팀 Task 034 착수(20일차) 전 결론 필요(`docs/ISSUES.md` I-75) |
| **반영 위치** | `src/lib/data/bootstrap.ts`, `src/lib/data/polling.ts`, `src/app/[lang]/layout.tsx`(13일차 예정) |

---

### D-33 · 소유가 갈린 인계물의 등록·소비 책임 = **생산자 DoD** (구 `docs/ISSUES.md` I-221·I-226·I-229·I-231)

> 판정: 팀장, **2026-09-22(46일차)** / 제기: 42~46일차 5일 연속 재발(I-206 → I-221 → I-226 → I-229 → I-231) / 반영: 전 팀 공통 규약

| 항목 | 내용 |
|---|---|
| **문제** | **"만든 팀과 쓰는 팀이 다르면 배선이 조용히 새는" 형태가 다섯 일차 연속 6건 재발**했다 — I-206(42일차) · I-221(43일차, `/sample` 미등록 3종) · I-226(44일차, 중계 카탈로그 소비처 0건) · I-229(45일차, 라인업 어댑터 `return []`) · I-231(46일차, 스폰서 계약 mock 부재) + 46일차 `MatchOddsPanel` 미등록. 매번 "개별 건을 메우는" 방식으로 처리해 **다음 일차에 같은 형태가 다시 왔다**. 공통 원인은 **완료 정의(DoD)에 "소비처까지 살아 있음"이 없고 산출물 생성만 있다**는 것이다 |
| **결정** | **산출물을 만든 팀(생산자)의 DoD는 "파일 생성"이 아니라 "소비처에서 실제로 값이 보이는 상태"까지다.** 소비처 파일이 남의 소유라도 생산자가 책임을 진다 — 아래 두 경로로 갈라 이행한다 |
| **경로 ① 등록형(1~3줄 추가)** | **생산자가 직접 편집한다 — I-188(타 팀 파일 무수정)의 명시적 예외다.** 대상은 **신규 컴포넌트의 `/sample` 쇼케이스 등록**과 **i18n 네임스페이스의 `messages/index.ts` 등록** 2종으로 한정한다. 배열/객체에 한 줄 더하는 기계적 편집이라 병렬 충돌 위험이 사실상 없고, 소유팀 대기로 인한 누락 비용이 그 위험보다 압도적으로 크다(6회 재발이 근거). 편집 시 커밋 메시지에 `(D-33 등록)`을 표기해 소유팀이 추적할 수 있게 한다 |
| **경로 ② 배선형(로직 변경)** | 어댑터 구현·소비처 컴포넌트 개조 등 **판단이 필요한 변경은 생산자가 편집하지 않는다.** 대신 생산자 DoD에 **ⓐ 이슈 등재(소비처 파일·줄 번호·필요한 배선 명시) ⓑ 소유팀에 인계 통보**가 포함되며, **소유팀은 그 이슈를 자기 다음 등판 일차의 최우선 항목으로 배정한다**(팀장이 소환 지시문에 명시). "언젠가"가 아니라 **다음 등판**이 기한이다 |
| **판정 기준** | 소비처에서 **정상 상태가 실측된 적이 없으면 그 Task는 완료가 아니다.** 빈 상태 렌더 확인만으로 "수락 기준 충족"을 선언하지 않는다 — I-227·I-228·I-229·I-231이 전부 이 형태로 통과 처리됐다 |
| **기각안** | ⓐ **소유팀이 등록까지 전담**하는 안 — 지금까지의 방식이고 6회 실패했다. 생산자가 만든 순간과 소유팀이 등판하는 일차 사이에 반드시 공백이 생긴다. ⓑ **I-188을 전면 완화**하는 안 — 병렬 트리 충돌 방지가 실제로 작동 중이므로(45일차 4팀 동시 트리 위반 0건) 예외를 등록형 2종으로만 좁힌다 |
| **소급 적용** | 46일차에 **I-221 잔여 3종(`RoundNav`·`SeasonSelect`·`TiebreakNote`)과 신규 `MatchOddsPanel`을 함께 `/sample`에 등록해 I-221을 닫는다.** 경로 ② 대상인 I-226·I-229·I-231은 3·4팀 다음 등판 일차 최우선 배정 |
| **반영 위치** | `docs/team-schedule/README.md`(DoD 절), 전 팀 md §1 각주, 팀장 소환 지시문 템플릿 |

---

### D-34 · 선수 평점 지표 4종의 노출과 저장 위치 (`docs/ISSUES.md` I-238)

> 판정: 사용자 + 팀장, **2026-09-23(47일차)** / 제기: 사용자 요청("선수정보: 출전경기수, 최근경기평점, 리그평균평점, 지난리그평균평점") / 반영: 1팀 타입 · 3·6팀 어댑터 · 5팀 Task 018

| 항목 | 내용 |
|---|---|
| **문제** | 요청된 4지표 중 **출전경기수만 존재**하고(`PlayerStatCoreValues.appearances`) 평점 3종은 타입에 없다. `matchRating`은 `PlayerMatchStat`(경기 단위)뿐이라 시즌 평균을 **파생으로도 얻을 수 없다.** 그런데 `docs/wireframe/05-선수상세.md` E6 표는 이미 `평점` 열을 명세하고 있어, Task 018을 그대로 진행하면 확정 명세를 채우지 못한 채 완료 처리된다 |
| **결정 ①(저장)** | **`avgRating: number`(1.0~10.0)를 `PlayerSeasonStat`·`PlayerCareerStat`에만 추가한다.** `PlayerStatCoreValues`에는 넣지 않는다 — ⓐ 그 인터페이스는 파일 헤더가 못박은 "합산형만 저장, 파생 비율은 계산" 대상이고 ⓑ `PlayerStatRankingMetric = keyof PlayerStatCoreValues`(`src/lib/data/DataSource.ts:281`)라 키를 늘리면 **완료된 Task 019 통계 랭킹 화면의 지표 목록이 조용히 바뀐다**(회귀). 집계 인터페이스에 저장형 평균을 두는 선례는 같은 인터페이스의 `avgCondition`이다 |
| **결정 ②(지난 시즌)** | **신규 필드·메서드를 만들지 않는다.** `getPlayerSeasonStats(playerId)`가 이미 전 시즌 행을 반환하므로 `avgRating` 하나로 현 시즌과 직전 시즌이 모두 해결된다 |
| **결정 ③(벤치마크)** | 선수 평균과 함께 **리그 전체 평균을 병기**한다(사용자 선택). `getLeagueAverageRating({seasonId, leagueId, competitionType})`를 `DataSource`에 추가하며, 표본 0이면 `null`을 반환한다 |
| **결정 ④(최근경기평점)** | `getPlayerRecentMatchStats({playerId, limit})`를 추가하고 **어댑터 레벨에서 `FINISHED` 경기만 거른다.** 진행 중 경기 평점 컷오프는 **UI 필터가 아니라 데이터 계약**이라는 기존 판정(와이어프레임 05 W-34 / S-4~S-8, R-11, NFR-SEC-004)의 연장이며 Mock·Supabase 양쪽이 동일하게 보장한다 |
| **범위 밖(명시)** | **평점 랭킹 개방**(`PlayerStatRankingMetric`을 `keyof PlayerStatCoreValues \| 'avgRating'`로 확장)은 FR-ST-004가 요구하지만 4팀 `/stats` 드롭다운 동반 수정이 필요해 **이번 반영에서 분리**한다. I-238에 잔여로 남긴다 |
| **기각안** | ⓐ `PlayerStatCoreValues`에 `ratingSum`(합산형)을 넣고 평균을 파생하는 안 — 원칙에는 가장 정합하나 `PlayerMatchStat`에서 `ratingSum === matchRating`이 되는 중복이 생기고, 위 ⓑ의 랭킹 메트릭 회귀를 똑같이 유발한다 ⓑ 화면에서 경기별 평점을 매번 평균 내는 안 — 시즌 전 경기를 조회해야 해 목록·상세 p95(≤300ms, NFR-PF) 예산을 깬다 |
| **반영 위치** | `src/types/stat.ts`, `src/types/stat.type-test.ts`, `src/lib/data/DataSource.ts`, `src/lib/mock/**`·`src/lib/data/mock/MockDataSource.ts`(3팀), `src/lib/data/supabase/mapper.ts` + 신규 마이그레이션(6팀), `src/app/[lang]/players/[playerId]/page.tsx` E1·E6(5팀 Task 018) |

---

### D-35 · 구단주(ClubOwner) 도입 — 스폰서 계약의 주체는 구단주, 수입 귀속은 팀 (`docs/ISSUES.md` I-239)

> 판정: 사용자 + 팀장, **2026-09-23(47일차)** / 제기: 사용자 요청("구단주가 있고 구단주가 스폰서와 계약하는 방식으로 변경") / 반영: 전 팀

| 항목 | 내용 |
|---|---|
| **문제** | 현재 `SponsorContract`는 `sponsorId ↔ teamId` 직결이라 **계약 주체 개념이 없다.** 구단주에 해당하는 엔티티·필드·문서는 저장소 전체에 0건이라(전수 grep 확인) 겹치는 Task·일차도 없다 |
| **결정 ①(엔티티)** | **E-48 `ClubOwner`를 신설**하고 팀과 **1:1**로 둔다. `teamId: null` = 공석을 허용해 **E-06 Manager(D-20/T19·T21)의 독립 엔티티 패턴을 그대로 승계**한다 — 새 관계 패턴을 만들지 않는다. 필드는 `id`/`teamId`/`name`/`age`/`nationality`/`wealth`/`negotiation`/`reputation`/`sinceSeason`. `wealth`·`negotiation`은 **1~30 정수 스케일**(T5 관례, 브랜드 없음), 이름은 3팀 공유 생성기 경유 고유명사(D-17)이며 **번역 비대상**(D-18/T14) |
| **결정 ②(자금 모델 — 핵심)** | **계약 주체만 구단주로 바꾸고 돈 흐름은 건드리지 않는다.** `SponsorContract.teamId`(수입 귀속처)를 **유지한 채** `signedByOwnerId: ClubOwnerId`를 추가한다. 스폰서 수입의 zero-sum 2건 기록(팀 잔고 +, 스폰서 잔고 −)이 그대로이므로 **26일차에 검증 끝난 회계 항등식(NFR-QA-005)과 `src/lib/economy/accounting-identity.test.ts`가 한 줄도 깨지지 않는다.** **`PointTransactionOwnerType`에 `'OWNER'`를 추가하지 않는다** |
| **결정 ③(명명)** | **`ClubOwner` / `club_owner` / `ClubOwnerId`로 고정한다.** `Owner` 단독 명칭 금지 — `PointTransaction.ownerType`/`ownerId`와 물리 테이블 `point_transaction.owner_id`(다형 참조, `core_tables_phase1.sql:581`)가 이미 "원장 소유자"라는 **다른 뜻**으로 `owner`를 점유하고 있어 혼용 시 잘못된 조인이 컴파일을 통과한다 |
| **결정 ④(협상 반영)** | `proposeSponsorContract()`(3팀 `src/lib/economy/sponsor.ts`)가 구단주 축(`wealth`·`negotiation`·`reputation`)을 제안 금액·`sharePct` 산출에 반영한다. **신규 계수는 05문서에 근거 수치가 없으므로 억측하지 않고**, 키 부재 시 중립값(배율 1)으로 처리한다(`valuation.ts`의 `AGE_*` 선례) — 임시값은 Task 031b(63~66일차) 실값 정렬 대상에 넣는다(I-136 계열). **팀당 ACTIVE ≤ 3 제약은 팀 축 그대로**이며 1구단주=1팀이라 해석 변경이 없다 |
| **기각안** | ⓐ **구단주가 자체 잔고를 갖고 팀에 출자**하는 안(사용자 검토 후 기각) — `PointTransactionOwnerType` 확장이 원장·회계 항등식 테스트·DB CHECK 제약·zero-sum 검증을 전부 재작업 대상으로 만든다. 요청의 본질("계약 주체가 구단주")은 결정 ②로 충족된다 ⓑ **1구단주 다팀(멀티클럽 오너십)** — 밸런싱 축이 하나 늘고 스폰서 슬롯 제약 해석을 재정의해야 한다 ⓒ **`Team.ownerId`로 팀에 필드만 추가**하는 안 — 감독을 팀 속성으로 두지 않기로 한 D-20과 정면으로 어긋난다 |
| **반영 위치** | `src/types/brand.ts`(`ClubOwnerId`)·`src/types/person.ts`(`ClubOwner`)·`src/types/economy.ts`(`signedByOwnerId`)·`src/types/index.ts`, `src/lib/data/DataSource.ts`(`getClubOwner`), `src/lib/economy/sponsor.ts`·`src/lib/mock/world.ts`·`src/lib/data/mock/MockDataSource.ts`(3팀), `supabase/migrations/**`·`src/lib/data/supabase/mapper.ts`(6팀), `src/app/[lang]/teams/[teamId]/page.tsx` F3-o·F6(5팀), `src/app/[lang]/sponsors/page.tsx`(4팀), `docs/wireframe/06-클럽상세.md` |

---

## 6.4 리스크 목록

| ID | 리스크 | 영향도 | 발생 가능성 | 완화책 | 관련 요구사항 |
|---|---|---|---|---|---|
| **R-01** | **시뮬레이션 밸런스 붕괴** — 성장·재정·이적 파라미터가 상호작용해 몇 시즌 내 승자독식 또는 전체 붕괴 | 높음 | 높음 | ① 공통코드 외부화로 배포 없이 즉시 튜닝(D-01)<br>② Vitest 확률 분포·불변식 테스트로 조기 검출(NFR-QA-004, 006)<br>③ 20시즌 장기 시뮬 검증<br>④ 시즌 밸런스 리포트(NFR-OB-003)<br>⑤ 리그3 구제책(FR-LG-007), 상금 Base 상향(FR-EC-002 D-02) | FR-AD-011, NFR-QA |
| **R-02** | **기능 범위 과다** — Must 비중 73%로 MVP가 두꺼움 | 높음 | 높음 | ① Mock First로 UI를 먼저 고정해 병렬 작업 가능하게<br>② 엔진은 경기 → 시즌 → 프리시즌 수직 슬라이스로 완성<br>③ C 우선순위 9건은 과감히 2차로<br>④ 배팅 전체를 2차로 분리 | 6.2 개발 순서 |
| **R-03** | **엔진 회귀 검출 실패** — 밸런스 조정 중 의도치 않은 결과 변화 | 높음 | 중간 | Vitest 시드 스냅샷(NFR-QA-003)을 **엔진 코어 착수와 동시에** 구축. 스냅샷 갱신이 커밋 diff로 드러나게 함 | NFR-QA-003 |
| **R-04** | **Next.js 16 API 차이로 인한 재작업** | 중간 | 높음 | AGENTS.md 규칙 의무화 — 코드 작성 전 `node_modules/next/dist/docs/` 확인, 참조 경로를 커밋 메시지에 명시 | NFR-MT-006, DC-02 |
| **R-05** | **결정론 위반** — 부동소수·Map 순회 순서·Date 사용으로 재현 실패 | 높음 | 중간 | ① ESLint 룰로 `Math.random()`/`Date.now()` 금지(NFR-DT-001)<br>② 고정 정밀도 비교(NFR-DT-005)<br>③ 정렬 시 명시적 tiebreak 키(NFR-DT-008)<br>④ 시드 스냅샷 CI 검증 | NFR-DT |
| **R-06** | **공통코드 외부화가 결정론을 깨뜨림** — 상수 변경 후 과거 경기 재현 실패 | 높음 | 중간 | **상수 스냅샷 엔티티**로 시드+상수 쌍을 고정(FR-AD-014). `fixture.snapshot_id` NOT NULL 강제(DC-14). 발효 정책으로 진행 중 시즌 불변(FR-AD-013) | FR-AD-013, 014, NFR-DT-007 |
| **R-07** | **공통코드 오설정으로 세계 붕괴** — 잘못된 값이 저장되어 시뮬이 비정상 동작 | 중간 | 중간 | ① 타입·범위 메타데이터와 DB 제약(NFR-CFG-004)<br>② JSON 스키마 검증<br>③ 기본값 폴백(NFR-CFG-005)<br>④ 변경 이력으로 롤백 가능(NFR-CFG-002)<br>⑤ 발효 시점이 다음 시즌이므로 즉시 파급되지 않음 | NFR-CFG |
| **R-08** | **크론 단일 장애점** — Edge Function 중단 시 라운드 누락 | 높음 | 중간 | ① 중단 감지(FR-AD-020)<br>② catch-up 폴백(FR-AD-019)<br>③ 헬스 체크(NFR-OB-004)<br>④ 재시도(NFR-CR-006)<br>⑤ 라운드 간격이 75분으로 넉넉해 복구 여유가 큼(D-01의 부수 효과) | FR-AD-017~022, NFR-CR |
| **R-09** | **Edge Function 실행 시간 한계** — 30경기 일괄 시뮬이 타임아웃 | 중간 | 중간 | ① 1회 처리 상한(기본 50경기, 공통코드)<br>② 초과분 다음 틱 이월<br>③ NFR-PF-003으로 성능 기준 강제(30경기 ≤ 1.5초) | DC-15, NFR-CR-004 |
| **R-10** | **배당 산출 비용** — 라운드당 30경기 × N=3,000 프리시뮬이 서버 포화 | 중간 | 중간 | ① 킥오프 T−30분 여유(NFR-PF-004)<br>② N을 공통코드로 축소 가능<br>③ 워커 분리 구조(NFR-SC-004)<br>④ 1차는 1X2 등 핵심 마켓만 | FR-BT-005 |
| **R-11** | **사후 배팅 / 결과 유출** — 선계산 결과가 API·DB로 새어나감 | 높음 | 중간 | ① 경과 시간 필터를 뷰·보안 함수로 강제(NFR-SEC-004, DC-05)<br>② RLS로 원시 테이블 차단<br>③ 침투 테스트 시나리오 5종 필수 | FR-MT-016, FR-BT-008 |
| **R-12** | **데이터 볼륨 폭증** — 이벤트 로그가 시즌당 7.5만 건씩 누적 | 중간 | 높음 | ① 인덱스·파티셔닝(DC-06)<br>② 3시즌 초과분 아카이브(NFR-SC-002)<br>③ 이벤트 `detail` JSON 최소화<br>④ 스냅샷은 해시 중복 제거로 선형 증가 억제(NFR-CFG-006) | NFR-SC-001 |
| **R-13** | **시즌 사이클 3일이 여전히 부적절** | 낮음 | 중간 | 배속(FR-AD-001) + 공통코드(D-01)로 운영 중 즉시 조정 가능. 코드 변경 불필요 | FR-AD-001, FR-AD-011 |
| **R-14** | **저작권** — FM 데이터·실명 사용 | 높음 | 낮음 | DC-12 — 능력치 체계만 참고, 전 데이터 절차적 생성. 엠블럼도 절차적 SVG(DC-11) | DC-11, DC-12 |
| **R-15** | **파라미터 초기값 설정 부담** — 한 번에 정할 상수가 36개 그룹 | 중간 | 높음 | ① 1차는 합리적 초기값 + 공통코드 외부화<br>② "밸런싱은 반복 작업"을 전제로 튜닝 루프(시뮬 → 리포트 → 조정) 구축<br>③ `default_value` 필드로 언제든 초기값 복원 | FR-AD-011, NFR-OB-003 |
| **R-16** | **Mock ↔ 실데이터 전환 시 UI 수정 발생** — 어댑터 추상화가 불충분 | 중간 | 중간 | ① 어댑터 인터페이스를 타입 정의 단계(1단계)에서 확정<br>② 컴포넌트의 Supabase 직접 import를 ESLint로 차단<br>③ 전환 전후 스냅샷 비교 | FR-UI-023, NFR-MT-002 |

---

## 6.5 미해결 질문 (4건 — 전부 2차 릴리스 이후)

> Q-01 / Q-02 / Q-06 / Q-07은 D-01 ~ D-04로 결정되어 제거됨.
> **2026-07-20 추가 결정**: Q-08 → D-15(단일 월드), Q-05 → D-17(국적 기반 다국적 혼합),
> Q-04 → D-18(다국어 대응), Q-09 → D-19(승부차기 득점 미포함), Q-13 → D-20(감독 독립 엔티티).
>
> **1차 릴리스 전 단계에 남은 블로킹 게이트는 없다.** 아래 4건은 전부 2차 이후 사항이며 `docs/ISSUES.md`에도 이관되어 있다.

| ID | 질문 | 영향 요구사항 | 결정 지연 시 기본 가정 | 결정 필요 시점 |
|---|---|---|---|---|
| **Q-03** | 배당 표시 형식 — 유럽식(decimal) 고정인가, 분수/미국식 지원이 필요한가 | FR-BT-005, FR-UI-015 | decimal만 지원 | 2차 착수 전 |
| **Q-10** | 컵대회 우승팀에 대륙대회 진출 같은 추가 보상 트랙을 둘 것인가 | FR-LG-015, FR-EC-004 | 두지 않음 (범위 통제) | 2차 이후 |
| **Q-11** | 시즌 중 이적창(겨울 이적시장)을 도입할 것인가 | FR-TR-001 | 미도입 (MVP) | 2차 이후 |
| **Q-12** | 관중·티켓 수입을 재정 모델에 넣을 것인가 (현재 수입은 순위 포인트 + 스폰서 + 이적만) | FR-EC-*, FR-TM-003 | 미포함 (C 우선순위) | 2차 이후 |

### 우선 답변 필요 (개발 착수 순서 기준)

**해당 없음 — 1차 릴리스 전 단계에 선행 결정이 필요한 항목이 남아 있지 않다.**

| 구 질문 | 결정 | 내용 |
|---|---|---|
| Q-08 | **D-15** | 단일 월드 |
| AS-02 | **D-16** | FM 실데이터 미사용, 전부 시드 기반 절차적 생성 |
| Q-05 | **D-17** | 국적 기반 다국적 혼합 이름 |
| Q-04 | **D-18** | 다국어 대응, i18n 기반은 Phase 1 배치 |
| Q-09 | **D-19** | 승부차기 득점은 개인 통산 득점 미포함 |
| Q-13 | **D-20** | 감독은 독립 엔티티 + 최소 이적 |

> 따라서 1차 릴리스의 9개 개발 단계 전부가 **선행 결정 대기 없이 착수 가능**하다.
> 잔여 미결(Q-03·10·11·12)은 전부 2차 릴리스 이후 사항이므로 1차 진행을 막지 않는다.
> 다만 구현 단계에서 확정할 세부 규칙(`docs/ISSUES.md` 3절 I-01~I-06)은 여전히 남아 있다.

---

## 6.6 가정 (Assumptions)

| ID | 가정 | 근거 | 무효화 시 영향 | 검증 방법 |
|---|---|---|---|---|
| **AS-01** | "15분마다 1경기 킥오프"는 **라운드 단위 동시 킥오프**를 의미한다 | 경기 단위 해석 시 리그1 시즌이 138시간이 되어 서비스 불가 (D-01/D-07) | 전체 스케줄링 재설계 | 사용자 확인 완료(D-01) |
| ~~**AS-02**~~ | ~~원본의 "FM 마케팅"은 "FM 스타일 능력치 체계 참고"를 뜻하며 실제 FM 데이터 사용이 아니다~~ | **가정 해제 — D-16으로 확정 사항이 됨** | — | 확인 완료(2026-07-20) |
| **AS-03** | 승격·강등은 정규시즌 순위 기준이며 플레이오프는 별도 트로피다 | 팀 수 24/20/16 불변 조건 (D-14) | 팀 수 변동 처리 로직 필요 | FR-LG-014 |
| **AS-04** | 경기 결과는 킥오프 시점에 선계산되며 실시간 틱 계산이 아니다 | 성능·재현성·정산 신뢰성 (FR-MT-016) | 실시간 계산 인프라 및 장애 복구 설계 필요 | NFR-PF-001 |
| **AS-05** | 1차 릴리스에는 인증·지갑·실제 배팅이 없다 | 사용자 확정사항 7번 | 2차 범위가 1차로 당겨져 일정 2배 | 6.2 릴리스 범위 |
| **AS-06** | 통화는 포인트 단일이며 실화폐 환전은 없다 | 원본 "money 개념이 아닌 포인트" | 규제·컴플라이언스 요건 대거 추가 | FR-TR-002 |
| **AS-07** | 컵대회는 3개 리그 통합 단일 대회다 | D-08 근거 | 대회 스케줄·마켓 구조 재설계 | FR-LG-015 |
| **AS-08** | 리그3 최하위는 강등되지 않고 제재+구제를 받는다 | D-06 근거 | 리그4 도입 또는 팀 해체 로직 필요 | FR-LG-007 |
| ~~**AS-09**~~ | ~~사용자는 단일 월드를 관전하며 월드 선택 UI가 필요 없다~~ | **가정 해제 — D-15로 확정 사항이 됨** | — | 확인 완료(2026-07-20) |
| **AS-10** | 스탯은 이벤트 로그로부터 파생 가능하며 이벤트가 SSOT다 | FR-ST-005, 정합성 복구 요건 | 스탯 독립 저장 및 별도 정합성 보정 전략 필요 | NFR-QA-005 |
| **AS-11** | 실시간 갱신은 1차에 폴링으로 충분하다 (동시 접속 500명 규모) | NFR-PF-010 | Realtime/WebSocket 조기 도입 | 부하 테스트 |
| **AS-12** | 밸런스 파라미터는 초기값으로 시작해 반복 튜닝한다 (1회에 확정하지 않는다) | R-15 | — | NFR-OB-003 리포트 |
| **AS-13** | 공통코드가 미등록·손상되어도 하드코딩 폴백으로 시스템이 정지하지 않는다 | NFR-CFG-005, DC-13 | 부트스트랩 순서 의존성 발생, 장애 시 전면 정지 | NFR-QA-008 |
| **AS-14** | Supabase Edge Function 크론이 분 단위로 안정 실행된다 | D-04 | 외부 크론(GitHub Actions 등) 또는 상주 프로세스로 전환 | NFR-CR-001 |
| **AS-15** | 상수 스냅샷의 해시 중복 제거로 저장량이 시즌당 20건 이하로 유지된다 | NFR-CFG-006 | 스냅샷 압축 또는 델타 저장 방식 필요 | NFR-CFG-006 측정 |
| **AS-16** | 배속 변경(0.25×~20×)이 세 리그 동시 종료 정렬을 깨뜨리지 않는다 | FR-AD-001, FR-LG-009 | 배속별 재정렬 로직 필요 | FR-LG-009 수용 기준 ③ |

---

## 6.7 요구사항 추적 매트릭스

| 비즈니스 목표 (01 문서) | 관련 FR 그룹 | 관련 NFR | 검증 KPI |
|---|---|---|---|
| P-1 24/7 상시 진행 | FR-LG-008, 009, 010, FR-AD-017~022 | NFR-CR 전량 | KPI-1 |
| P-2 살아있는 지속 세계 | FR-PL, FR-TM, FR-TR, FR-YT, FR-AW, FR-EC | NFR-QA-004, 006 | KPI-8 |
| P-3 설명 가능한 시뮬레이션 | FR-MT-002, 003, FR-ST, FR-UI-007 | NFR-MT-001 | KPI-6 |
| P-4 신뢰할 수 있는 배당 | FR-BT-005, 014 | NFR-DT-006 | KPI-4 |
| P-5 조작 불가·재현 가능 | FR-MT-015, FR-AD-003, 004, 014 | NFR-DT 전량, NFR-SEC-004 | KPI-3, KPI-7 |
| P-6 코드 없는 밸런싱 | FR-AD-011~016, FR-UI-025 | NFR-CFG 전량 | KPI-10 |
| P-7 Mock First 개발 효율 | FR-UI-001, 023 | NFR-MT-001, 002 | KPI-6, KPI-11 |

---

## 6.8 자기 점검 결과

| 점검 항목 | 결과 |
|---|---|
| **모든 요구사항이 검증 가능한가?** | FR 163건 / NFR 90건 전부에 Given-When-Then 또는 수치 기반 수용 기준을 부여했다. "적절히", "빠르게" 같은 정성 표현은 전부 수치로 치환했다. |
| **충돌을 표시했는가?** | ① 승격 경로 이중화(정규시즌 vs 플레이오프) → FR-LG-014로 해소 ② 상수 외부화 vs 결정론 → FR-AD-014 스냅샷 + FR-AD-013 발효 정책으로 구조적 해소 ③ 결과 선계산 vs 사후 배팅 위험 → FR-MT-016 + NFR-SEC-004 상호 보완 ④ 리그3 최하위 페널티 vs 리그 경쟁력 → FR-LG-007 구제책 병기 ⑤ 상수 즉시 반영 vs 배팅 무결성 → `NEXT_MARKET` 정책으로 분리 |
| **추적성이 유지되는가?** | 전 항목 고유 ID(결번·중복 없음), 6.7 추적 매트릭스, 03 문서 3.13절 원본 역추적 표(원본 37문장 100%, 추가 요구사항 18건 100%) |
| **예외/엣지 케이스 누락은?** | **커버함**: 승점 완전 동률(7단계), 하위 리그 부재, 리그별 종료 시각 어긋남, 스쿼드 하한 미달, 잔고 음수, 스폰서 부도, 승부차기, 확률 0 셀렉션, 마켓 마감 경합, 크론 중단·중복·재시도, 배속 변경 중 정렬 유지, 공통코드 미등록·범위 위반, 상수 변경 후 과거 경기 재현<br>**미커버(구현 단계 상세화 필요)**: ① 동일 선수 이중 임대 방지 규칙 ② 경기 중 GK 퇴장 + 교체 소진 시 필드플레이어 골키퍼 배치 상세 절차(배율 0.35로 부분 대응) ③ 감독 공석 상태의 전술 기본값(BALANCED로 폴백 명시했으나 지속 기간 규칙 미정) ④ 컵대회 1라운드에서 동일 티어만 남는 경우의 시딩 폴백 |
| **원본 기획 누락 여부** | 원본 37개 문장 전부 매핑 완료(03 문서 3.13.1). 원본의 "확인 필요" 1건 + "데이터 추천 바람" 2건 + 오타 1건을 모두 확정 처리했다. |
| **D-01~D-04 반영 여부** | ① D-01 → FR-LG-009(75/90/115), FR-AD-011~016, FR-AD-014, E-41~E-44, NFR-CFG ② D-02 → FR-EC-002 전 구간 재계산 ③ D-03 → NFR-QA-001~010, NFR-MT-003 대체안 폐기 ④ D-04 → FR-AD-017~022, NFR-CR-001~009, E-45/E-46 |
