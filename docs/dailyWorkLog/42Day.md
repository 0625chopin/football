# 42일차 (2026-09-16 수)

**참여 팀**: 1팀(코어품질, **예외 소환**) · 2팀(시뮬레이션엔진) · 3팀(데이터밸런싱배당) · 4팀(UI기반i18n) · 5팀(화면배팅UX) · 6팀(DB인프라) — **6팀 전원**

**게이트**: typecheck 0 · lint 0 · test **1564 passed / 6 todo** — 41일차 1508 → **+56**

> **1팀은 배정 행이 없었으나 사용자 승인으로 예외 소환했다.** 24~68일차가 「⏸ 상시 리뷰 게이트 구간」이라 다음 개발 배정이 71일차인데, **1팀 소유 파일이 있어야만 풀리는 판정·배선이 3건**(I-206·I-207·I-213) 쌓여 그대로 두면 29일간 방치될 사안이었다.

---

## 1. 팀별 작업

### ⭐ I-206 해소 — 세 팀이 하루에 나눠 닫았다 (36일차부터 6일 이월)

증상은 "공통코드를 DB에 적재했는데도 앱이 안 읽는다"였고, 41일차에 원인(등록 호출처 0건)까지는 규명됐으나 **파일 소유가 셋으로 갈려 어느 팀도 혼자서는 닫을 수 없었다.**

| 팀 | 담당 조각 | 산출물 |
|---|---|---|
| 1팀 | **등록 지점·우선순위·부팅 순서** | `factory.ts`에 `registerConstantSource`/`getRegisteredConstantSource` 신설(`registerDataSource`와 동일 self-registration), `bootstrapApp()`이 `installHardcodedFallback()` → `bootstrapDataSource()` → (등록 소스 있으면)`setGlobalDefaultSource()` 순 승격 |
| 3팀 | **mock 소스** (기본 경로) | `mock/mock-constant-source.ts` + 등록 1줄. **순환 회피**가 핵심 — `MockDataSource.getCommonCodes()`가 내부에서 `loadConstants()`를 부르므로 그 결과를 그대로 등록하면 무한재귀. `SAFE_DEFAULT_VALUES`를 순수 데이터로 재사용하고 `UI_PARAM`만 정상값으로 오버라이드 |
| 6팀 | **supabase 소스** | `supabase/constant-source.ts` + 등록 1줄. `getGroupConstants`가 **동기 계약**이라 REST I/O를 등록 이전(모듈 최상위 `await load()`)에 끝내 경합 제거 |

**팀장 실측 판정**: `config/fallback` 경고 **45건 → 0건**, `bootstrapApp()` 후 `loadConstants('UI_PARAM')`이 `{POLL_INTERVAL_MS: 5000, POLL_LIVE_MS: 3000}` 반환(안전망 30000/15000 아님). **폴링 5초가 실제로 적용된다.**

판정 기준을 도중에 바꾼 것이 결정적이었다 — 3팀의 노이즈 억제만으로도 경고가 45→6건대로 떨어져 **건수 감소가 근거가 못 됐고**, "조회값이 시드값을 반환하는가"로 바꾸자 비로소 판정이 가능해졌다.

### 1팀 — I-207 · I-213 판정

- **I-207 판정: "라운드마다 재추첨" 채택.** FR-LG-015 원문(`03-functional-requirements.md:164`)의 "2라운드 이후"가 1라운드 규칙과 병렬 서술된 반복형이고 실제 컵대회 관행과도 부합. 2팀 구현 유지, "최초 1회 고정" 기각.
- **I-213 판정: 추가함.** `DataSource.ts`에 `getManager?(managerId)`를 **선택 메서드(`?`)** 로 넣었다 — 필수로 넣으면 Mock(3팀)·Supabase(6팀)가 즉시 tsc로 깨진다. 구현체 불건드림 + 조율 후 반영 규약 준수이며, 두 구현체가 채우면 `?`를 떼는 것이 다음 정리 대상. **"감독 통산 랭킹"은 이걸로 해결되지 않는다**(→ I-217).

### 2팀 — Task 027 컵 시딩

**지시대로 `cup.ts`를 먼저 읽고 중복 구현을 피했다.** D-24 시딩이 41일차에 이미 들어가 있어, 새로 짜는 대신 `seeding.ts`(+테스트)로 분리·일반화하고 `cup.ts`가 import하도록 축소했다. 외부 소비자가 없어 breaking change 0. 테스트는 28쌍 결정론·D-24 ①④·pools 재실행 동일성.

### 3팀 — Task 043 이상 탐지 + (추가) mock 소스

- `obs/alert.ts`(신규, 테스트 21건) — 이상 탐지 4종(`roundMissed`/`simDelay`/`bankruptcyExceeded`/`settlementFailure`) 순수 detect + `evaluateAlerts` 조합기, 시즌별 `AlertHistory`, `computeSystemHealth`(degraded 계약), 폴백 WARN 카운터
- **I-208 재발을 실제로 막았다** — 계약만 만들고 끝내지 않고 **`config/fallback.ts`에 직접 배선까지 완료**(config도 3팀 소유). 그룹별 최초 1회만 `console.warn`, 이후 카운트만 누적
- 수락 기준: "시즌 간 비교" = `AlertHistory.compare(a,b)` / "크론 중단 시 degraded" = `computeSystemHealth()`. `/api/health` 배선은 6팀 소관이라 계약만 확정

### 4팀 — Task 019 시즌 아카이브

**착수 전 판단 보고가 정확했다.** "완료 시즌 0건은 버그가 아니라 스펙이 예견한 케이스"임을 **FR-UI-013 + UC-011 선행조건 원문**에서 찾아와, `/awards`처럼 링크 1개짜리 선택기로 흉내내지 않고 스펙 지정 빈 상태("완료된 시즌이 없습니다")를 그대로 썼다.

- `[lang]/archive/page.tsx` · `i18n/{ko,en}/archive.ts`(신규) · `messages/index.ts`. 완료 시즌(`endedAt !== null`)만 후보로 삼는 GET 링크 선택기 + 3섹션(최종 순위=`StandingsTable`/`ZoneLegend` 재사용, 우승=`Standing.rank===1` 파생, 수상 요약)
- **팀장 지시로 게이팅 경로를 테스트로 실행**(§2) — `archive/page.render.test.tsx`(신규). `registerDataSource('mock', () => fake)` + `resetDataSourceCache()`로 이 테스트 프로세스에서만 완료 시즌 픽스처 주입, `MockDataSource.ts`(3팀 소유)는 무변경

### 5팀 — Task 016 시맨틱 마크업 · **I-210 결론**

**41일차 자기 판단을 스스로 뒤집었다.** C2 경기 목록을 `<ul>` → `<table>`로 전환. 근거 3가지: 와이어프레임 §3 "데스크톱 2열은 시각 배치만 2열, **마크업은 하나의 테이블**" 명시 / §7 NFR-A11Y-005의 `scope="col"` 요구는 표 밖에서 무의미 / 자매 화면 B3가 이미 `<table>`.

- `fixtures/page.tsx` — `<table>` + sr-only `<caption>` + 열 헤더 4종. `MatchCard`(div 블록)는 `<td>`로 쪼갤 수 없어 행을 화면 로컬로 재작성(와이어프레임이 애초에 MatchRow를 화면 로컬로 지정) / `StandingsTable.tsx` — 축약 헤더 9열에 `<abbr title>` 추가
- 실렌더: 3개 리그 × ko/en, 320px에서 body 가로 스크롤 0(scrollWidth 305<320), 표 컨테이너만 내부 스크롤

### 6팀 — Task 033 멱등성 + 이월 2건 해소

- `20260721104357_tick_run_idempotent_claim.sql`(신규) — 탐지+상태전이를 **조건부 `UPDATE fixture ... WHERE status='SCHEDULED' ... RETURNING`(원자적 클레임)** 하나로 통합. 락과 별개인 **행 단위 멱등성 게이트**
- **실증**: SCHEDULED fixture 1건 시딩 후 **동시 curl 10건**(다른 PID) → 락 획득 5건 중 `fixtures_processed=1`이 **정확히 1건**. 이어 FINISHED에서 **재호출 2회** → 둘 다 0건, `simulated_at` 불변
- **SP-3 6팀 관점 검토 완료(3일 이월분 해소)** — "같은 경기를 두 번 진행시키지 않는 것"은 **호출자(6팀) 책임**이라고 `RETURN_CONTRACT.md` 142행이 명시하며, 오늘의 원자적 클레임이 그 지점을 이행한다. 이견 없음
- **H-15 판단: 보류(크론 미점등)** → I-214. 주기 1분은 75분의 약수라 수치상 문제없으나, **스코어가 STUB인 상태로 점등하면 2팀 실엔진이 붙기 전에 매분 가짜 점수로 `FINISHED` 처리**되어 되돌릴 수 없는 오염이 발생한다

---

## 2. 팀장 검증

게이트는 1차부터 그린이었고, **결함 2건은 접근성 트리와 파일 목록 대조에서 나왔다.**

| 대상 | 결함 | 발견 수단 | 결과 |
|---|---|---|---|
| 5팀 | **첫 열 접근명이 행 전체** — `columnheader "경기 상태"` 아래 셀 이름이 경기 전체 문자열이라, 열 단위 탐색 시 상태값 대신 경기가 통째로 읽히고 행마다 두 번 발화됨 | 접근성 스냅샷 | 수정 완료 — 상태 셀 `<td>`에 `aria-label` 부여해 accname 계산이 자식(늘인 링크) 순회를 건너뛰게 함. 재검증에서 `라이브 0'`/`종료`/`예정`만 노출 |
| 6팀 | **`supabase/constant-source.ts`가 회귀 테스트 0** — 검증용 임시 테스트를 삭제해 부팅 경로 신규 모듈이 무커버리지로 남음(같은 디렉터리 다른 4개는 전부 보유) | 파일 목록 대조 | 수정 완료 — `constant-source.test.ts` 15케이스 |

**4팀에는 결함이 아니라 사전 조건을 붙였다.** "완료 시즌 로직은 배선하되 렌더 조건만 게이팅"하면 그 코드는 실렌더로도 테스트로도 실행되지 않는 **죽은 코드**가 된다(41일차 B4와 같은 사각지대). 테스트 픽스처로 실행하게 했고, 4팀이 이행하며 부수적으로 **I-215**까지 찾아냈다.

기타 확인: 소유 경로 위반 0건, i18n 키 패리티 일치, **en 한글 누출 0건**, 임시 파일·설정 변경 잔존 0건(`next.config.ts`·`tsconfig.json` 원복 확인).

---

## 3. 이슈

**해소 4건** — I-206 · I-207 · I-210 · I-213. **신규 5건** — 상세는 `docs/ISSUES.md`

- **I-214** `tick_run()` 스코어 STUB 상태에서 pg_cron 점등 금지 — **차단성 있음**, 해제 조건은 2팀 실엔진 연동
- **I-215** vitest `setupFiles` 부재 → 렌더 테스트 2건 이상 시 DOM 누적 오탐 / **I-216** 표 데스크톱 2열 시각 그리드 미구현(표 ARIA 회귀 회피)
- **I-217** 감독 통산 랭킹용 타입 부재 — 동결 타입 배치 반영 대상 / **I-218** `alert.ts` 입력 3종 미공급 — I-208 계열

---

## 4. 다음 일차 인계

1. **I-214가 오늘 최우선 인계다** — 크론을 켜면 안 되는 상태이며 해제 조건은 **2팀 실엔진 연동 + 후처리 배선**이다. 멱등성이 있어도 "잘못된 값으로 일관되게" 오염될 뿐이라 오늘 확보한 멱등성으로 상쇄되지 않는다.
2. **I-208이 4건 → 5건이 됐다**(+`alert`). 다만 3팀이 `fallback.ts` 배선을 직접 끝내 일부는 연결됐다. 남은 공통 병목은 **`/admin` 대시보드(5팀)가 없다는 것** — 관측 결과를 볼 곳이 없으므로 **5팀 `/admin` 일차 배정이 선행 조건**이다(I-218).
3. **1팀 예외 소환이 오늘 3건을 닫았다.** 24~68일차가 「상시 리뷰 게이트」로만 잡혀 판정 대기가 자연 누적되는 구조다. **I-215·I-217도 1팀 소관**이므로 같은 예외 소환을 다음에도 검토할 것.
4. **`getManager?`의 `?`를 떼는 것이 다음 정리 대상** — Mock(3팀)·Supabase(6팀)가 구현을 채우면 필수로 승격한다.
5. **인덱스 화면**: `/ko/leagues`는 여전히 404 — `NAV_GROUPS`의 `pending` 유지가 맞다. **I-201** 방침도 계속 미뤄짐(프로덕션 빌드 불가가 병목). **셸 분리**(I-186) 미이행. **I-188**은 지시문에 계속 포함 — 오늘도 6팀 전원 준수.
6. **오늘의 교훈: 소유가 갈린 문제는 한 팀에게 물으면 영원히 안 풀린다.** I-206은 6일간 이월되는 동안 매번 "우리 소유가 아니다"로 정확히 회신됐다 — 각 팀 판단은 전부 옳았고 아무도 잘못하지 않았는데도 문제가 남았다. **조각을 나눠 동시에 배정하자 하루에 닫혔다.** I-192(경계를 넘는 값은 소비 지점에서 관측해야 판정된다)의 조직 버전이다.

---

## 5. 미해결·판정 대기

- **I-192 규약 판정**(경계를 넘는 값 공유에 접점 테스트 의무화) — 39일차부터 이월. **오늘 I-206 해소가 네 번째 근거를 더했다. 가장 급함**
- **I-214** 크론 점등 금지 — **차단성 있음** / **I-218** `/admin` 선행 배정 필요 / **I-208** 소비처 배선 팀·일차 배정
- **I-215 · I-217** 1팀 소관(테스트 인프라 / 동결 타입 배치) / **I-216** 표 2열 시각 배치
- **I-209** 마이그레이션 백필 / **I-211** 시즌 경로 승격 / **I-212** mock 라이브 점수 / **I-204** 4강 대진 / **I-205** en 뉴스 한국어 — 전부 비차단
- **I-200 · I-201 · I-202 · I-203** 프로덕션 빌드 불가가 병목이거나 배정 대기(변동 없음)
- **I-189 · I-190 · I-191 · I-194** 값·규칙·표기 정합(변동 없음) / **I-193 · I-197** 031b 밸런싱 루프(66~68일차)
- **I-195** mock 킥오프 시각 — 오늘도 전 경기 "오전 12:00" 재확인 / **I-196** 재실측 **3일 연속 미이행** / **I-198** `/sample` 격리 / **I-199** 사용자 조치 대기
