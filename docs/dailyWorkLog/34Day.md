# 34일차 (2026-09-04, 금)

## 1. 참여 팀

| 팀 | Task | 오늘 범위 |
|---|---|---|
| **2팀** 시뮬엔진 | 026 | 후처리 파이프라인 실패 롤백 + 최대 3회 재시도 + 알림, 재실행 멱등 |
| **3팀** 데이터·밸런싱·배당 | 035 | 1X2 배당 1차 표시 전용 모드 (`src/lib/odds/display.ts`) |
| **4팀** UI기반·i18n | 014 | `/sample` 카테고리 섹션 + 앵커 내비 + 21종 전량 등록 |
| **5팀** 화면·배팅UX | 015 | 홈 3리그 진행 중 경기 카드 그리드 + `MatchCard` 신규 |

1팀·6팀은 34일차 배정 행 없음(미소환).

**오늘의 의미: 화면이 처음으로 실제로 보이기 시작한 일차.** 10일차 이후 자리표시자였던
`/[lang]` 홈과 `/[lang]/sample`이 실데이터/실컴포넌트로 렌더된다.

## 2. 최종 게이트 (팀장 직접 실행)

| 게이트 | 결과 |
|---|---|
| `npx tsc --noEmit` (tsbuildinfo 삭제 후 클린 실행 — I-163) | **0 errors** |
| `npm run lint` | **0** |
| `npm run test` | **90 files / 1288 passed**, 6 todo, Type Errors none |
| dev 실렌더 (`--webpack`, 포트 3113, 재기동 후) | `/ko` `/en` `/ko/sample` `/en/sample` **전부 200, 에러 마커 0건** |

빌드는 판정 수단에서 제외(I-62, WSL EPERM).

## 3. 팀별 산출물

### 2팀 — Task 026 (후처리 재시도·롤백·멱등)

- `src/lib/sim/postmatch/pipeline.ts` / `pipeline.test.ts` — **순증분 171줄, 기존 스테이지 로직 변경 0줄**
- `runPostMatchPipelineWithRetry()` — 기본 3회, 첫 성공 즉시 반환, 전부 실패 시 throw 없이
  `ok:false` + 알림 페이로드 반환(발송은 오케스트레이션 계층 소관)
- 실패 분기가 `PostMatchPipelineResult`의 어떤 필드도 담지 않는 **유니온 타입** — "전체 롤백"을
  구조적으로 강제(부분 산출물 접근 불가)
- `computePostMatchIdempotencyKey()` — `fixtureId` 기반 결정론 키
- 테스트 20 passed. NFR-DT-001 위반 0건(grep 히트는 전부 주석 내 언급)

### 3팀 — Task 035 (배당 표시 전용 모드)

- `src/lib/odds/display.ts` / `display.test.ts` (신규, 9 테스트)
- `toOddsDisplayPanel`(범용) / `toMatchOddsDisplayPanel`(1X2, HOME→DRAW→AWAY 고정 순서)
- `OddsDisplayPanel = { format:'decimal'(Q-03 리터럴 고정), bettingEnabled:false(리터럴 타입 —
  override 파라미터 자체 없음, FR-BT-014), selections }`
- 확률 0으로 제외된 셀렉션은 키 자체가 없어 그대로 누락(placeholder로 채우지 않음, FR-BT-005)

### 4팀 — Task 014 (`/sample` 쇼케이스 34일차분)

- `src/app/[lang]/sample/page.tsx` (549줄, 전면 작성) / `src/i18n/messages/{ko,en}/sample.ts` (신규)
  / `messages/index.ts` (sample 네임스페이스 등록) / `src/app/[lang]/layout.tsx` (검증 후 수정)
- 5개 카테고리 섹션(domain·composite·state·chart·admin) + 앵커 내비
- **21종 전량 실제 렌더** — domain 8 + composite 7 + state 6. import만 하고 미사용 0건(팀장 실측)
- chart·admin은 컴포넌트 부재라 골격 + "미구현" 표기
- 데이터는 `getDataSource()` 어댑터 경유. 어댑터가 아직 `[]`만 주는 4종은 쇼케이스 전용
  인라인 표본 사용(`no-restricted-imports` 가드레일 우회하지 않음)

### 5팀 — Task 015 (홈 라이브 카드 그리드 34일차분)

- `src/components/composite/MatchCard.tsx` / `.test.ts` (신규 — 013B 잔여 1종 종결)
- `src/app/[lang]/page.tsx` (자리표시자 → 카드 그리드), `src/i18n/messages/{ko,en}/match.ts` (`card` 그룹)
- `MatchCard` — named export, 서버 컴포넌트, **I-156 단일 prop 규약 준수**
  (`state: CompositeViewState<MatchCardData>`), `density:"card"|"row"`
- 데이터는 `getDataSource()`(getLiveFixtures/getTeamsByIds/getLeagues)만 경유

## 4. 팀장 검증

전원 개발 완료 후 단독 검증. **피드백 → 재수정 → 재검증 3건 발생, 전부 해소.**

| 대상 | 지적 | 조치 |
|---|---|---|
| **3팀** | `formatDecimalOdds()`가 `toFixed(2)`로 표시 문자열 자체 생성 — 4팀 H-09 인계물 `src/i18n/format.ts:53`의 `formatOdds(odds, locale)`가 이미 있고 같은 파일이 "포인트/배당 단일 경유지 원칙"을 명시. ko/en 모두 소수점이 `.`이라 **자체 테스트 9건 전부 통과했고 육안으로도 안 잡힘** | (A)안 채택 — `display` 필드 제거, 문자열화는 소비 측 `formatOdds` 위임. 유한 양수 검증은 `assertValidDecimalOdds`로 분리 유지. `toFixed` 잔존 0건, `format.ts`(4팀 소유) 무변경 확인 |
| **2팀** | 멱등 키가 `fixtureId` 단독 — D-30·D-31 **Tier B 재시뮬레이션**은 같은 fixture를 다른 시드/리비전으로 재계산하는데 키가 동일. 오케스트레이션이 이 키로 중복 판정하면 **정당한 재계산 결과가 통째로 버려짐**(막으려던 이중 누적보다 발견이 어려운 방향의 오작동) | (B)안 채택 — 타입 확장((A))은 오늘 범위 밖. 키 의미를 경고 주석으로 못박고("재시뮬레이션 구분자가 아니다, 이 키로 중복 판정하지 말 것") **I-171로 이월**. 로직 변경 0줄 |
| **4팀** | `/en` 실응답에 한글 누출 2건 — ⓐ `layout.tsx:27` metadata description 하드코딩 ⓑ `loading`/`not-found`의 `DEFAULT_LOCALE` 고정 | ⓐ `generateMetadata` 전환 + `common.app.description` 신설(ko/en) → **재기동 후 `/en` 실응답 영문 확인** ⓑ 문서화된 제약이라 수정 지시 없이 판단만 요청 → **I-170 등재** |

**5팀 이탈 1건은 판단이 옳아 그대로 승인**: 경과분(`elapsedMinutes`)을 `null`로 두고 미표시.
`DataSource`에 "현재 시각" 메서드가 없고(팀장 grep 확인), `MOCK_NOW` 직접 import는
`eslint.config.mjs:82`의 `@/lib/mock/**` 금지에 정면 위반(주석: "21일차 결함 A — SupabaseDataSource가
mock을 import해 실제로 벌어졌던 결함"), `Date.now()`는 Mock 세계 시각(2026-08월 고정)과 어긋나 음수.
**가드레일을 뚫는 대신 기능을 축소한 판단을 지지한다.** 순수 함수 `computeElapsedMinutes`는
테스트까지 완비되어 `now` 소스가 생기면 즉시 연결 가능 → **I-169**.

**검증 과정에서 확인된 환경 사실 2가지 (다음 일차 필수 참고)**

1. **dev 서버 응답은 재기동 없이는 검증 근거가 될 수 없다.** WSL 마운트 watch 미반영으로
   수정 후에도 구버전 캐시가 200으로 응답한다. 4팀이 자기 보고에서 먼저 짚었고, 팀장 검증에서도
   동일 재현(`layout.tsx` 수정 후 `/en`이 계속 한글 meta 반환 → 재기동 후 해소).
2. **Next.js 16은 같은 디렉터리에서 두 번째 dev 서버를 거부한다**("Another next dev server is
   already running" + PID 안내). 포트만 바꿔 격리 기동하는 기존 방식이 이 버전에서는 통하지 않는다.

4팀이 보고한 `[lang]/page.tsx` lint 에러는 5팀 동시 편집 중간 상태였고 현재 0건. **남의 소유
파일을 고치지 않고 보고만 한 판단이 정확했다.**

## 5. 신규/갱신 이슈

| 번호 | 내용 | 소관 |
|---|---|---|
| **I-169** | `DataSource`에 "현재 조회 시각" 반환 메서드 부재 — H-24 경과분을 화면에서 쓸 수 없음. 새 메서드 신설 vs 35일차 폴링 훅이 자체 `now` 주입 중 택1 | **1팀** 판정 → 5팀 소비 (35일차 015 폴링 훅과 직결, **차단성 있음**) |
| **I-170** | `/en`에서 `loading`/`not-found`가 `DEFAULT_LOCALE` 고정으로 한글 표시. 4팀 조사 결과 `next/headers`의 `cookies()`로 기존 `LOCALE_COOKIE_NAME`(`f4_locale`)을 읽어 근사 가능(`locales.ts` 주석이 이미 예고, 미구현일 뿐. `unstable_rootParams`는 제거돼 대안 아님). 단 최초 방문자는 쿠키가 없어 완전 해결 아님 | **4팀** — 근사 도입 vs 2차 이월 판정 |
| **I-171** | 후처리 멱등 키가 `fixtureId` 단독 — Tier B 재시뮬레이션 충돌. 현재는 경고 주석으로만 방어. 아울러 **재시도 래퍼 소재 계층** 재검토 병합(파이프라인이 순수 함수라 이 계층 재시도는 실패를 성공으로 바꾸지 못함 — 실제 일시적 장애는 DB 커밋 쪽) | **2팀** — H-15 오케스트레이션 배선 시점 |
| **I-172** | `scripts/check-literals.mjs`가 숫자값만으로 공통코드 후보를 오탐(`DEFAULT_MAX_ATTEMPTS = 3` ↔ `fallback.ts`의 무관한 3). 게이트 정확도 문제 | **1팀** (게이트 소유) |

**해소**: 없음(기존 OPEN 건 중 오늘 닫힌 것 없음).
**I-147 중복 기동 재발 방지**: 34일차 0건(4일 연속).

## 6. 다음 일차(35일차) 인계

| 팀 | 인계 사항 |
|---|---|
| **2팀** | 026 재시도·멱등 종결. **3종(순위 갱신·컨디션·피로·부상 판정) 실산식 일차 여전히 미배정 — 4일 연속, 팀장 배정 필요.** I-123 재개 트리거는 오직 "CONDITION_FATIGUE 실산식 착수일". I-164·I-145·**I-171**은 H-15 이후 오케스트레이션 계층 몫 |
| **3팀** | 035 34일차분 종결. **H-19(배당 엔진 반환 타입) 43일차 예정 — 오늘 산출물은 엔진 호출 없이 `overround.ts` 산출물만 소비하므로 일정 영향 없음.** 시즌 MC 러너 호출부 **4일 연속 미배정**. I-160(`MANAGER_STYLE_XG` 실값)이 xG 배율 소비 계속 차단 |
| **4팀** | 014 첫날치 종결(섹션·앵커·21종). 35일차 이후 잔여: **4상태 토글 / 뷰포트 프리뷰 / 로케일 토글 / ErrorBoundary 격리 / 커버리지 카운터 / 어댑터 토글**. `MatchCard`가 오늘 생겼으므로 **35일차에 `/sample` 등록해 22종으로 맞출 것**. chart·admin 섹션은 골격만 — 컴포넌트 생길 때 채움. **I-170 판정** 필요 |
| **5팀** | 015 첫날치 종결, `MatchCard`로 013B 22종 전량 완료. 35일차 잔여: **폴링 훅(기본 5초, 탭 비활성 시 중단) / 카운트다운 / 뉴스 요약 / 4상태 / LCP·CLS 목표 / 320px 무횡스크롤**. **경과분은 I-169 해소가 선행돼야 함.** I-157(피치 좌표계)은 015에 피치가 없어 미착수 유지 |
| **1팀** | **I-169가 35일차 5팀 폴링 훅을 직접 막는다 — 최우선 판정.** I-172(게이트 오탐) 신규. **I-142 9일 연속 미착수.** I-163(tsc 증분 캐시) 소관 — 팀장이 캐시 삭제로 임시 회피 7일 연속. 동결 타입 배치 대기: `Player.avatarSeed`(I-155). I-158 판정 미착수 |
| **팀장** | ⓐ I-131 브랜치 보호 사용자 판단 ⓑ I-121 태그 규약 36일차 전 확정 ⓒ **`gh` 부재로 CI 러너 결과 7일 연속 미확인** ⓓ **미배정 2건 4일 연속**: 시즌 MC 러너 호출부 · CONDITION_FATIGUE 실산식 ⓔ 4팀 권고 일차 확정 필요 — **I-151(jsdom) 35일차 ~1인일 / I-144(ΔE) 35일차 ~0.25인일 / I-143(소비 규약) 36일차 ~0.5인일 / I-148(토큰 재점검) 38일차 ~0.5인일** ⓕ I-168 타 팀 md 총량 표현 점검 잔여 |

## 7. 미해결·판정 대기

| 건 | 상태 | 기한 |
|---|---|---|
| **I-169** | `DataSource` 현재 시각 메서드 부재 | **1팀 — 35일차 5팀 폴링 훅 차단** |
| **I-131 브랜치 보호** | CI 실패가 머지를 못 막음 | **사용자 판단 대기** |
| **I-132 마이그레이션 드리프트** | 원격 19 vs 로컬 2 | **사용자 판단 대기** |
| **I-128** | Playwright Chromium 미설치 — UI 실측 차단 | **사용자 판단 대기** (014 수락 기준의 Playwright 스크린샷 검증이 이것 때문에 불가) |
| **V-01** | Edge 런타임 30경기 CPU 2초 이내 | Task 033 착수 전 |
| **I-170** | `/en` loading·not-found 한글 | 4팀 — 쿠키 근사 vs 2차 이월 |
| **I-171** | 멱등 키 + 재시도 래퍼 계층 | 2팀, H-15 이후 |
| **I-172** | check-literals 숫자 오탐 | 1팀 (게이트 소유) |
| **I-164** · **I-145** | 구조 마커 미배선 · 경고 소비처 | 2팀, H-15 이후 |
| **I-160** | `MANAGER_STYLE_XG` 실값 미확정 | 3팀, xG 배율 소비 차단 중 |
| **I-163** | tsc 증분 캐시 거짓 통과 위험 | 1팀 (게이트 소유) |
| **I-151** · **I-144** · **I-143** · **I-148** | jsdom · ΔE 0.56 · 소비 규약 · 토큰 재점검 | 4팀 — 권고 35/35/36/38일차, **팀장 확정 대기** |
| **I-153** | Task 012 수락 기준 실측 | 4팀, 013A/013B 실사용 시 |
| **I-154** | 뉴스 i18n 네임스페이스 판정 | 4팀, news 네임스페이스 생성 시 |
| **I-161** | 브래킷 i18n 네임스페이스 분리 | 4팀 주도·5팀, 020 착수 시 |
| **I-155** | 아바타 생성기 — 판정 완료(필요) | **1팀** 타입 배치 접수 → 3팀 구현 |
| **I-156** | 4상태 계약 중복 — **`MatchCard`가 규약 준수로 신규 생성돼 추가 발산 없음** | 해소 아님, 신규 컴포넌트 시 계속 확인 |
| **I-157** | 피치 좌표계 발산 | 4팀·5팀, 피치 실사용 Task(017 라인업 뷰) |
| **I-158** | `Position` 측면MF·윙백 코드 추가 여부 | **1팀** 판정 (미착수) |
| **I-168** | 수락 문구 총량 표현 예외 명시 | 팀장 — 타 팀 md 점검 잔여 |
| **I-149** | KPI-8 계약 밀도 재측정 | 3팀, Task 030 이후 |
| **I-142** | 가드레일 자동 검증 | 1팀 (**9일 연속 미착수**) |
| **I-147** | 중복 기동 재발 방지 | 팀장 상시 — **34일차 0건(4일 연속)** |
| **I-121** | 공통코드 키 선확정 태그 규약 | 36일차(031a) 전, 팀장 |
| **I-136** · **I-118** | 시드 상수 실값 정렬 | 36일차(031a) |
| **D-23 폴백 분리** | 현행 유지, 트리거 축소 재예약 | 감독·전술 실착수 Task |
| **I-111** | 3팀·6팀 배정 완료 | 각 팀 해당 구간 |
