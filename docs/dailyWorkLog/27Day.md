# 27일차 (2026-08-26, 수)

## 1. 참여 팀

| 팀 | Task | 소환 근거 |
|---|---|---|
| **2팀** 시뮬레이션엔진 | 025 | 팀 md 27일차 행 |
| **3팀** 데이터밸런싱배당 | 035 | 팀 md 27일차 행 |
| **4팀** UI기반i18n | 012 (**마지막 날**) | 팀 md 27일차 행 |
| **5팀** 화면배팅UX | — (SP-2 판단만) | 범위 행 `25~27`의 "SP-2(27일차) 분할 합의 준비" + 4팀 행의 "(4·5팀, 2h)" |

1팀·6팀은 27일차 행이 없어 미소환.

## 2. 최종 게이트

| 항목 | 결과 |
|---|---|
| `npx tsc --noEmit` | 오류 **0** |
| `npm run lint` | 오류 **0** |
| `npm run test` | 66 files passed / 6 skipped · **1,000 tests passed** / 6 todo · Type Errors **no errors** · 회귀 **0건** |

프로덕션 빌드는 I-62(WSL EPERM)로 로컬 판정 불가 — 러너 결과로 확인.

## 3. 팀별 산출물

### 2팀 — Task 025 킥오프 산출
- `src/lib/sim/schedule/kickoff.ts`(신규) · `kickoff.test.ts`(신규, 10 tests)
- `berger.ts` 라운드 번호에 킥오프 시각을 붙이는 순수 함수 계층. 간격(75/90/115분)·REGULAR 페이즈 길이(3,450분)는 **전부 파라미터 주입**, 숫자 리터럴 0건(NFR-CFG-001).
- **I-12 라운드 오프셋**: `computeLeagueRoundOffsetsMin`이 최소 간격÷리그 수로 1라운드를 어긋나게 배치. 리그 수는 입력 배열 길이에서 도출(하드코딩 없음).
- **T+3,450분 강제 정렬**: 자연 계산으로는 어긋나(46R×75=3,375) 라운드 간격을 선형 스케일링해 마지막 라운드가 정확히 T+`regularPhaseDurationMin`에 오도록 강제.

### 3팀 — Task 035 프리시뮬 러너 + I-08 해소
- `src/lib/odds/runner.ts`(신규) · `runner.test.ts`(신규, 14 tests) · `src/lib/config/catalog.ts` · `fallback.ts`
- `runOddsPresimMatch()`가 `deriveSeasonSeed(..., SEED_NAMESPACE.ODDS_PRESIM)`로 시즌 시드를 파생하고 `runIndex`를 `deriveMatchSeed`의 재추첨 구분자로 사용, MC 반복마다 독립 시드로 **2팀 풀 엔진을 그대로 호출**(`buildTickSequence→generateMatchEvents→linkPenaltyOutcomes→accumulatePlayerMatchStats`). 26일차 V-02 통과 결론대로 **경량 모델 미제작**.
- 이벤트 확률·가중치는 여전히 **호출자 주입**(024 계수 체인 몫). 실제 배당률(OVERROUND) 변환은 범위 밖.
- **I-08 해소**: `MC_N_SEASON` 300→**1,500**, `REFRESH_ROUND_INTERVAL` **5** 추가 — 이슈 원문의 "CPU 총량 동일, 정확도 2.2배" 권고 그대로.

### 4팀 — Task 012 마감 + H-11 인계
- `src/components/ui/button.tsx`(size별 `min-w-[Nch]`) · `badge.tsx`(`max-w-[14ch]`) · `table.tsx`(`numeric` prop → 우측정렬 + `tabular-nums`)
- `docs/handoff/H-11-design-tokens-shadcn-primitives.md`(신규)
- **`globals.css` 의도적 미변경** — `ch` 단위 Tailwind 임의값으로 충분해 새 토큰이 불필요했고, 26일차 "타이포·간격은 기본 스케일 그대로" 결정과 일치. I-150 재발 리스크 회피 효과도 있음.
- **I-148·I-143은 이월** — 미해결 표상 기한이 013A(33일차)이며 27일차 배정이 아님. 팀장의 소환 지시가 과했던 것으로, 4팀 보류 판단이 옳음.

### 5팀 — SP-2 판단 (구현 없음)
- 배분 입장 + H-11 수령 요구사항 + 미결 2건 판단 회신. 코드 산출물 없음(013B는 28일차 이후).

## 4. 팀장 검증에서 발견된 결함

| # | 대상 | 내용 | 처리 |
|---|---|---|---|
| 1 | 2팀 **I-146 보류 사유 오류** | "대상 함수가 아직 존재하지 않음(컵/브래킷 미착수)"으로 보류했으나, I-146의 대상은 **이미 존재하는 `detectVenueStreaks`**(`berger.ts:126`). 팀장이 이슈 원문 대조로 확인해 반려 | 2팀이 JSDoc 3항목 명시 → **I-146 해소** |
| 2 | 4팀 **`badge.tsx` 말줄임 미작동** | `truncate`를 `inline-flex` 컨테이너에 적용 — `text-overflow: ellipsis`는 flex item에 적용되지 않아(자식이 익명 flex item으로 감싸짐) **말줄임(…)이 렌더되지 않고 하드 클립**된다. 주석은 "truncate(말줄임)로 자른다"라고 단언 → 계약 문구와 실제 렌더가 불일치. 기능 회귀는 아님(기존 `overflow-hidden`/`whitespace-nowrap`과 동일 동작) | 소비처 0곳인 마감일이라 API 변경 대신 **주석·H-11 문서 정정**(하드 클립 명시 + `<span className="min-w-0 truncate">` 대안 + `title` 권고 강화) |

2팀 I-145 보류는 **타당**으로 인정(이슈 자체가 "소비 계층 생성 시점에 닫는다"를 종료 조건으로 명시).

## 5. SP-2 컴포넌트 분할 — **확정**

4팀 초안과 5팀 입장이 **완전 일치**하여 이견 없이 확정. 배분 근거는 §1 소유 경로(`domain/`·`state/`=4팀 / `composite/`=5팀)로 이미 고정돼 있어 재협상이 아닌 **대조 확인** 성격이었음.

| 소유 | 컴포넌트 | 개수 |
|---|---|---|
| 4팀 013A 도메인 표현 | TeamBadge, PlayerAvatar, AbilityRadar, ConditionGauge, FitnessBar, FormStrip, PositionMap, StatBar | 8 |
| 4팀 013A 상태·유틸 | SkeletonBlock, EmptyState, ErrorState, CountdownTimer, PhaseIndicator, OddsButton | 6 |
| 5팀 013B 복합 | EventTimelineItem, PitchLineup, BracketTree, TrophyCase, NewsItem, GrowthChart, InjuryTimeline | 7 |
| **5팀 013B 신규 승격** | **MatchCard** (`density:"card"\|"row"` 단일 통합) | **1** |
| | | **22** |

**미결 2건 판정**
- **W-02 MatchCard → 승격(21→22)**: 홈 A2-c·클럽상세 F8·일정결과 C2-r 3곳이 동일 도메인 모델이라 `density` prop 단일 통합으로 충분(5팀 확인). LIVE 배지·경과분(H-24)은 카드형 전용이므로 조건부 렌더 분기 필요. **공수는 013B 4.25인일에 흡수하지 않고 +0.6~0.7인일 증분 별도 산정**(5팀 산정, 2모드×3소비처 동기화 비용).
- **W-31 F4-p 시간대별 득점/실점 분포 → 화면 로컬 유지**: 클럽상세 1곳 전용, 카탈로그 미등재(013B 공수 재초과 방지). 양 팀 동의.

## 6. H-11 인계 (4팀 → 5팀, 28일차 013B 착수 조건)

- **shadcn 프리미티브 정확히 8종** (`src/components/ui/**`, 실물 존재 팀장 직접 확인): `Button`(variant 6 × size 8), `Badge`(variant 6), `Card`, `Separator`, `Skeleton`, `Table`(서브 8종), `Tabs`, `Tooltip`. `LocaleSwitcher`는 011 산출물이라 개수 제외.
- **`cn()`**: `src/lib/utils.ts` (clsx + tailwind-merge)
- **디자인 토큰**: 브레이크포인트 6종(24일차), 시맨틱 컬러 5종 + CVD 검증(25일차), radius 7단계, 타이포·간격은 Tailwind 기본값, 다크모드는 `prefers-color-scheme`(클래스 토글 없음)
- **미도입**: Avatar, Dialog/Sheet, Select, Popover, Progress, Accordion, Alert — 013B 중 필요해지면 **5팀이 shadcn MCP로 직접 추가 가능**(4팀 승인 불필요, dailyWorkLog 기록만)
- **⚠️ H-12(도메인 표현·상태 14종)는 013B가 아니라 015(34일차) 착수 조건** — 28일차 인계물에 포함하지 않음(5팀 지적, 채택)

## 7. 신규/갱신 이슈

| 건 | 처리 |
|---|---|
| **I-08** | **해소** — MC_N_SEASON 1,500 / REFRESH_ROUND_INTERVAL 5 (3팀) |
| **I-146** | **해소** — `detectVenueStreaks` JSDoc 전제 명시 (2팀) |
| **I-145** | OPEN 유지 — 소비 계층 생성 시점까지 정당 보류 |
| **I-151** (신규) | jsdom·`@testing-library/react` 미설치로 UI 렌더 테스트 0건 → 4팀, 013A(33일차) 전 |
| **I-152** (신규) | 차트 라이브러리 미정 → **1차 자체 SVG, 실패 시 31일차 recharts 폴백**(5팀 방침) |
| **I-153** (신규) | Task 012 수락 기준 "레이아웃 깨짐 0" 미검증 이월 → 4팀, 실사용 시점 |
| **I-148·I-143** | 27일차 미처리, 기한대로 013A(33일차) 유지 |

**운영 메모**: 26일차 잔여 팀 패널(`d26-*` 3개 + 접두사 없는 3개)이 27일차 착수 후 뒤늦게 종료 통보됨 — I-147(중복 기동) 잔재. 이번 일차 시작 시점에는 이미 정리 완료 상태였고 산출물 유실 없음(`git status`로 확인).

## 8. 다음 일차(28일차) 인계

| 팀 | 인계 사항 |
|---|---|
| **5팀** | **013B 착수** — H-11(프리미티브 8종·토큰·`cn()`) 수령 완료, 착수 조건 충족. SP-2 5팀 몫은 **복합 7종 + MatchCard = 8종**, 공수 4.25인일 + **0.6~0.7인일 증분**. 차트는 자체 SVG 1차(I-152). H-12는 34일차(015) 조건이니 기다리지 말 것 |
| **4팀** | Task 012 종료(수락 기준은 I-153으로 이월). 다음 구간은 013A(33일차) — 착수 전 **I-151(jsdom 도입)**, **I-148·I-143·I-144**(토큰 전수 재점검·소비 규약·ΔE) 처리 필요 |
| **2팀** | Task 025 계속(~30일차). 잔여: 시즌 페이즈 상태머신, 배속 재계산. **I-145**는 소비 계층 생성 시점, I-119는 Task 026(30일차) 스코프 |
| **3팀** | Task 035 계속 — 오늘은 "엔진 호출"까지이며 **실제 배당률 변환(OVERROUND 등)이 잔여**. I-149(KPI-8 재측정)는 Task 030 이후, I-136은 36일차 |
| **1팀** | I-142(가드레일 자동 검증) 여전히 미착수 — 26·27일차 연속 행 없음 |
| **팀장** | ⓐ I-131 브랜치 보호 사용자 판단 ⓑ I-121 태그 규약 36일차 전 확정 ⓒ 마감 CI는 러너 결과까지 확인(I-150 교훈) ⓓ **소환 지시에 이슈 처리를 넣을 때는 ISSUES 열람 금지와 충돌하지 않도록 이슈 본문 요약을 함께 줄 것**(27일차 4팀 사례) |

## 9. 미해결·판정 대기

| 건 | 상태 | 기한 |
|---|---|---|
| **I-131 브랜치 보호** | CI 실패가 머지를 못 막음 | **사용자 판단 대기** |
| **I-132 마이그레이션 드리프트** | 원격 19 vs 로컬 2 | **사용자 판단 대기** |
| **I-128** | Playwright Chromium 미설치 — UI 실측 차단 | **사용자 판단 대기** |
| **V-01** | Edge 런타임 30경기 CPU 2초 이내 | **Task 033 착수 전** |
| **I-151** | jsdom·testing-library 도입 | 4팀, 013A(33일차) 전 |
| **I-153** | Task 012 수락 기준 실측 | 4팀, 013A/013B 실사용 시 |
| **I-152** | 차트 라이브러리(자체 SVG → recharts 폴백) | 5팀, 31일차 |
| **I-148** · **I-143** · **I-144** | 토큰 전수 재점검 · 소비 규약 강제 · ΔE 여유 0.56 | 4팀, 013A(33일차) |
| **I-145** | 경고 소비처 | 2팀, 오케스트레이션 계층 생성 시 |
| **I-149** | KPI-8 계약 밀도 재측정 | 3팀, Task 030 이후 |
| **I-142** | 가드레일 자동 검증 | 1팀 |
| **I-147** | 중복 기동 재발 방지 | 팀장, 상시 |
| **I-121** | 공통코드 키 선확정 태그 규약 | **36일차(031a) 전, 팀장** |
| **I-136** · **I-118** | 시드 상수 실값 정렬 | 36일차(031a) |
| **I-123** · **D-23 폴백 파일 분리** | 판정 시점 예약 | 026 착수 시점 |
| **I-119** | 2팀 배정 완료 | Task 026(30일차) 내 |
| **I-111** | 3팀·6팀 배정 완료 | 각 팀 해당 구간 |
| **I-134** | 6팀 배정 완료 | Task 033(40~46일차) |
| **FR-EC-012 명성 −5 반복 차감** | 3팀 해석(1회성) 확정 필요 | 030 착수 전 |
| **I-102** | `is_event_elapsed()` 실판정(현재 스텁) | 2팀 H-24(30일차) |
| **I-137** | `match_event_visible` 의도적 수용 문서화 | 6팀 |
| **I-107** | 브래킷 부전승·컵 라운드 축소 (I-146 문서화로 연결됨) | 5팀 브래킷 화면 착수 전 |
| **I-101** | 확장자 경로 404 UI 통일 | Task 014(34~38일차) |
| **I-100** · **unused_index 73** | 인덱스 관련 | 해당 Task / 042(58~62일차) |
| **I-130** · **I-129** · **I-115** · **I-112** · **I-109** · **I-120·I-122·I-124·I-125·I-126** · **관계 R-06** | 비차단 | 별도 일정 |
| **SKILL.md 교체**(14일차~) | 이월 | 사용자 판단 대기 |
