# 59일차 (2026-10-09, 금)

**참여 팀**: 5팀 화면·배팅UX팀(Task 021) · 6팀 DB·인프라팀(Task 038)
**미참여**: 1·2·3·4팀 (`docs/team-schedule/` 해당 일차 행 없음)

**한 줄 요약**: Task 021의 마지막 미구현 항목이던 **NFR-SEC-007 1차(환경 플래그)** 가 실제로 구현됐고,
Task 038은 **레이트 리밋 2버킷 + 정책 테스트 192건**으로 마감했다. 재수정 1회(6팀).

---

## 1. 팀별 작업

### 5팀 화면·배팅UX팀 — Task 021 (54~59일차, 최종일)

1. **NFR-SEC-007 1차 — 비공개 경로 + 환경 플래그(신규 구현)**
   `console-flag.ts` 신설. `ADMIN_CONSOLE_ENABLED`가 **정확히 문자열 `"true"`일 때만 활성**이고
   미설정·오타·`"1"`은 전부 비활성으로 **fail-closed**한다(`isAuthorizedAdminToken` 선례 준수).
   콘솔 3종 `page.tsx`는 `notFound()`로 "비공개 경로"를 구현하고, 소유 Server Action 5개는
   `assertAdminConsoleEnabled()`로 **재검증**한다 — 페이지 레벨 검사가 그 안의 Server Action으로
   이어지지 않는다는 Next.js 16 경고가 이 플래그에도 그대로 적용되기 때문.
   ⚠️ **ROADMAP 565행이 이 항목을 54일차에 이미 `[x]`로 표기하고 있었으나 실제로 구현된 것은
   인증(2차)뿐이었다** — 1차 플래그는 W-45가 미결로 남긴 채 한 번도 존재한 적이 없다. 오늘 그 공백이 메워졌다.
2. **위험 조작 2단계 확인 + 사유 입력** — 리셋(G5)은 54~55일차에 이미 완비돼 Playwright 회귀 재검증만.
   **"강제 정산"은 Task 040(71일차) 미착수**라 오늘 대상 없음(스코프 판정, 누락 아님).
3. **I-283 선제 조치** — `service-role-audit.ts` 신설. `createSupabaseRestQueryClient`에
   `SUPABASE_SERVICE_ROLE_KEY`를 주입해 `audit_log`·`common_code_history` 두 조회만 service_role로
   태운다. `SupabaseDataSource`(6팀 소유)는 미수정. 서비스키 부재 시 anon 폴백 — "새 접근 제어가 아니라
   기존 조회의 키 교체"이므로 여기서 던지면 정리 자체가 새 장애점이 된다는 근거.

**변경**: `console-flag.ts`·`service-role-audit.ts`(신규), `admin/page.tsx`·`config/page.tsx`·
`scheduler/page.tsx`, `admin/actions.ts`·`config/actions.ts` + 테스트 2종, `.env.local`(gitignore)

### 6팀 DB·인프라팀 — Task 038 (54~59일차, 최종일)

1. **레이트 리밋(NFR-SEC-009)** — `src/lib/data/supabase/rate-limit.ts` 신설.
   슬라이딩 윈도우 로그 방식(고정 윈도우의 경계 2배 버스트 회피), 시간은 호출자 주입(`now: number`)으로
   테스트 결정성 확보. Deno(Edge Function)·Node(Route Handler) 양쪽에서 쓰므로 런타임 전용 API 없는 순수 TS.
   - **베팅 30/분** → `supabase/functions/wallet/index.ts`의 `reason === 'BET_PLACE'`에만.
     WIN/VOID/TOPUP은 시스템 발신이라 대상 아님.
   - **공개 300/분** → `api/live/matches`·`api/live/matches/[matchId]/events` **2종**(IP당 공유 버킷, 429 + `Retry-After`).
     ※ 이 배선은 **팀장 재수정 요청으로 추가된 것**(2절 참조).
   - `/api/admin/session`의 리미터는 브루트포스 방어용으로 **완전히 별도 인스턴스** 유지(키 공간 미공유).
2. **정책 테스트 스위트** — `supabase/policy-tests/`. 로컬 마이그레이션 SQL을 **직접 파싱**해
   (하드코딩 사본 없음 — `contrast.test.ts`와 동일 패턴) 액터 4종 × 오퍼레이션 4종 × 테이블 11개 = **192건**.
   "운영자 DB 레벨 권한 상승 없음"(`profile.role` 미참조) 불변식도 별도 단언.
   **스코프 노트**: Group A 공개 읽기 34개·`match_event`는 **로컬 SQL 자체가 없어**(I-263) 대상 밖.
   Task 038 본연의 액터 세분화 대상(B그룹 2종 + D그룹 8개 + 감사 1종 = 11개)은 전건 커버.

**변경**: `rate-limit.ts`·`rate-limit.test.ts`·`api/live/rate-limiter.ts`(+테스트)·
`api/admin/session/rate-limiter.ts`·`policy-tests/`(2파일) 신규, `live/matches` 2종·
`admin/session/route.ts`·`wallet/index.ts` 수정

---

## 2. 팀장 검증

**전역 게이트(최종)**: `npm run typecheck` clean · `npm run lint` clean ·
`npx vitest run` **2072 passed / 0 failed** (143 files, 재수정 전 2069 → +3)

### 재수정 1건 (6팀)

**공개 300/분 버킷이 실제 공개 API에 0건 적용돼 있었다.** 이 프로젝트의 API Route는 4개인데,
6팀은 `/api/admin/session`에 300/분을 걸고 이를 "공개 버킷"으로 보고했다. 그러나 그 라우트는
**어드민 인증 엔드포인트지 공개 API가 아니며**, 6팀 자신의 주석도 목적을 "무차별 대입 방어"로 적고 있었다.
진짜 공개 라우트인 `live/matches` 2종은 `grep` 결과 레이트리밋 키워드 0건 + `curl` 5연속 200으로
미적용이 확정됐다. → 두 라우트 적용 지시, 6팀이 공유 버킷 + `getClientIp` 통합으로 처리.

**재검증(런타임 실측)**: `/api/live/matches`에 305회 연속 요청 → **297×200 + 8×429**.
직전 회귀 3회를 더하면 정확히 300건 허용 후 차단으로, NFR-SEC-009의 "초과 시 429 응답"이
코드가 아니라 **동작으로** 확인됐다.

### 팀장 판정 (재수정 대상 아님)

- **`/api/health` 보류 수용** — NFR-OB-004 "항상 200" 계약과 충돌하고, 헬스체크에 리밋을 걸면
  모니터링이 차단된다. 6팀 판단이 옳다. → **I-285**로 근거 등재.
- **`get_advisors` 2건은 사전 존재 확정** — `match_event_visible`이 로컬 `supabase/` 전체에
  정의 0건이고 원격에만 있다(팀장 직접 `grep` 확인). **I-263 계열**이며 오늘 원격 DDL 0회이므로
  **신규 유입 0건으로 수락 기준 실질 충족 판정**. → **I-286**.
- **5팀 재수정 없음** — 플래그 이중 검증, 폴백 설계, 강제 정산 스코프 판정 모두 타당.

### 준수 확인

`apply_migration` **0회**(I-269·I-283) · 크론 미점등(I-214) · `proxy.ts` matcher 미변경으로
Route Handler 직접 `assertAdminSession()` 유지 → **I-271 기한(59일차) 준수 마감** ·
공유 트리 `git stash`/`reset` 0건(I-235) · 재소환 전 `ps` 확인 이행(I-284, 중복 기동 0건)

---

## 3. 이슈

**신규 5건** (I-285 ~ I-289, 상세는 `docs/ISSUES.md`)

| ID | 요지 | 소유 |
|---|---|---|
| **I-285** | `/api/health` 레이트리밋 미적용 — NFR-OB-004 충돌로 **의도적 보류**(팀장 수용) | 6팀/1팀 |
| **I-286** | `get_advisors` 사전 존재 2건 — `match_event_visible` 로컬 정의 0건, **I-263 한 패스** | 6팀 |
| **I-287** | `proxy.ts`가 플래그(1차) 미검사 → 비인가 요청이 404 아닌 403, **I-272와 한 패스** | 4팀 |
| **I-288** | 어드민 로그인 리미터가 300/분 재사용 — 브루트포스 방어로는 과대(시간당 18,000회) | 6팀/1팀 |
| **I-289** | I-283 대응 코드 **실행 경로 0건**(mock 분기) — 원격 적용 시점에 최초 실행 | 5팀 |

---

## 4. 다음 일차 인계

1. **I-289가 I-283·I-269 패스의 새 선행 조건입니다.** 5팀이 `service-role-audit.ts`를 만들었지만
   **현재 한 번도 실행된 적이 없습니다**(mock 분기로 빠짐). 원격 마이그레이션을 적용하면 그 코드가
   **처음으로** 실행되는 것이므로, 적용 전에 `NEXT_PUBLIC_DATA_SOURCE=supabase` 격리 환경에서
   두 조회의 왕복을 반드시 먼저 실측하세요. mock에서는 `MockDataSource`가 `[]`를 반환해
   "0행이 정상인지 장애인지"를 화면으로 구분할 수 없습니다.
2. **Task 038 잔여는 여전히 3단계**입니다 — 원격 적용 → `database.types.ts` 재생성 → `mapper.ts`
   배팅 매퍼. 착수 조건은 **I-269 절차 정비 + I-283 선행(5팀 몫은 완료) + I-289 실측**입니다.
   적용 직전 `SELECT count(*) FROM wallet_transaction WHERE ref_bet_id IS NOT NULL` 확인은 유효.
3. **I-271은 59일차 기한을 준수하고 종료했습니다.** matcher를 넓히지 않고 Route Handler가
   `assertAdminSession()`을 직접 호출하는 방식이 확정 패턴입니다 — 되돌리지 마세요.
4. **`/sample` 등록 대기가 15종 그대로입니다**(4팀 소유). 59일차 5팀 신규 2종은 컴포넌트가 아니라
   순수 로직 모듈(`console-flag.ts`·`service-role-audit.ts`)이라 등록 대상이 아닙니다.
5. **60일차 4팀 등판 시 `proxy.ts` 소유 판정(I-272)에 I-287을 함께 얹으세요** — 플래그 미검사로
   404 은폐가 불완전한 건이 같은 파일입니다. 별도 패스로 두면 같은 파일을 두 번 엽니다.
6. **W-48(스케줄러 폴링 주기) 판정 여전히 대기** — 58일차 수동 새로고침으로 갈음한 상태 유지.
7. **I-288은 수치를 6팀 단독으로 정하지 마세요** — NFR에 어드민 로그인 리밋 규정 자체가 없어
   1팀과 NFR 보강을 함께 해야 합니다.
8. **I-275·I-276·I-282·I-273 한 패스**, **H-16(2팀→3팀 프리시즌)**, **PS-2 수락 판정(3팀 생성기 대기)**,
   **H-28 4팀 62일차 `pending` 제거**, **H-20(→4팀 022)·H-18(→5팀 039) 인계 발효** — 그대로 유효.
9. **어드민 계정은 상시 존재**(`0625chopin@gmail.com` / `qwer1234`, `profile.role='ADMIN'`).
   ⚠️ 비밀번호가 저장소에 평문으로 있으므로 **외부 배포 전 반드시 회수**.
   추가로 **`ADMIN_CONSOLE_ENABLED=true`가 `.env.local`(gitignore)에만 있습니다** — 새 체크아웃·CI·
   배포 환경에서는 콘솔 3종이 **의도대로 404**입니다. 이걸 회귀로 오진하지 마세요.

---

## 5. 미해결·판정 대기

- **I-289** I-283 대응 코드 실행 경로 0건 — 5팀, **원격 적용 전 격리 실측 필수**(신규)
- **I-288** 어드민 로그인 리미터 300/분 과대 — 6·1팀, NFR 보강 동반(신규)
- **I-287** `proxy.ts` 플래그 미검사 → 403/404 — 4팀, **I-272와 한 패스**(신규)
- **I-286** `get_advisors` 사전 존재 2건 — 6팀, **I-263과 한 패스**(신규)
- **I-285** `/api/health` 리밋 미적용 — 6·1팀, 의도적 보류로 확정(신규)
- **I-284** 재소환 전 `ps` 필수 — 59일차 이행, 중복 기동 0건
- **I-283** 감사 SELECT service_role 전용화 — **5팀 몫 조치 완료(59일차)**, 6팀 원격 적용 대기
- **I-282** `CommonCodeHistory.changedBy` 자리표시자 — 5·6팀, I-276과 한 패스
- **I-280** "36그룹" 문서 표기 vs 실측 38 — 문서, 숫자 재기입 금지
- **I-279** 공통코드 4그룹 코드 0건 — 3팀 Mock 시드, **I-289와 동계열**(감사 로그 표본도 함께)
- **I-278** 시즌 종료 ≤20초 실측이 축소 스케일 — 2팀, 3팀 생성기 붙는 시점 재측정
- **I-277** 일반 사용자 인증 UI 0건 — 5팀, 039(62일차~)
- **I-276** 감사 로그 `actorId` null — 5·6팀 계약 조율(파괴적 변경), I-282와 동계열
- **I-275** G2·G3 조작 감사 로그 미기록 — 5팀, I-273과 한 패스
- **I-273** `/admin` 배속·정지 + G6 로그 오버레이 휘발 — 5·2·6팀, **스토어 3종**
- **I-272** `proxy.ts` 소유 공백 — 4팀 60일차, **I-287 동반**
- **I-271** `matcher` api 제외 — **59일차 기한 준수하고 종료**
- **I-269** 원격 마이그레이션 절차 — 6팀, Task 038 잔여 3단계 착수 조건. 59일차 원격 DDL 0건(준수)
- **I-268** 수상 계수 미등록 — 3팀, I-265·I-121·I-136과 031b 한 패스(4건)
- **I-267** 지갑 멱등성 키 부재 — 6팀, Task 037 잔여(비차단, 호출자 0건)
- **I-266** 클럽 상세 2/2 데이터 계약 공백 4건 — 5팀 발견, 계약은 1·3팀
- **I-264** `User` 선호 로케일 도메인 필드 부재 — 1팀 배치(비차단)
- **I-263**(17건) · **I-243** 마이그레이션 로컬 미문서·채번 불일치 — 6팀 한 패스, **I-286 동반**, 034b 전 필수
- **I-262** OVR 산출식 중복 — 2·3팀, I-257과 한 패스
- **I-261** I-229 라인업↔이벤트 독립 표본 — 3팀, 다음 등판
- **I-260** 창단·재임 시즌 도메인 축 — 3·5팀, 화면 숨김 우회 중
- **I-259** 선수 상세 잔여 4건 — 5팀, ⓑ는 I-249와 동시 판정
- **I-257 · I-258** 순위 포인트 중복·리빌드 보조금 단위 — 2·3팀 조율
- **I-256** SP-4 잔여 2건 — 팀장/6팀
- **I-255** 전역 경기 조회 계약 — 1팀, 034b 이후
- **W-48** 스케줄러 폴링 주기 — 팀장 판정 대기
- **I-252 · I-251 · I-250 · I-248 · I-247 · I-245** 49일차 신규 — 변동 없음
- **I-223** 종결은 62일차 `pending` 0건 확인 시 / **I-241**(MOCK_NOW 고정) — 3팀
- **I-236** `homeModifier` 공식(2팀) / **I-214** 크론 점등 금지(차단성, 59일차 준수)
- **I-235** 공유 트리 git 조작 — 48~59일차 연속 사고 0건
- **I-142** `lint-guardrails` 동시 부하 flake — 59일차 재발 없음
