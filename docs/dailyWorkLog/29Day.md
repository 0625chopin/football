# 29일차 (2026-08-28, 금)

## 1. 참여 팀

| 팀 | Task | 결과 |
|---|---|---|
| **2팀** 시뮬레이션엔진 | 025 — 배속 비례 재계산 · 정지/재개 오프셋 (AS-16) | 완료 |
| **3팀** 데이터밸런싱배당 | 035 — 오버라운드 1.06 · 배당 클램프 (FR-BT-005 ①②) | 완료 |
| **4팀** UI기반i18n | 013A — 도메인 표현 컴포넌트 4종 (**8/8 종결**) | 완료 |
| **5팀** 화면배팅UX | 013B — `PitchLineup` 7 포메이션 | 완료 |

1팀·6팀은 29일차 배정 없음(팀 md에 해당 행 없음).

## 2. 최종 게이트

`npx tsc --noEmit` 오류 0 / `npm run lint` 오류·경고 0 /
`npm run test` **74 files (6 skipped) · 1067 passed · 6 todo**, typecheck 모드 오류 0.
프로덕션 빌드는 I-62(WSL EPERM)로 판정 수단에서 제외.

## 3. 팀별 산출물

### 2팀 — Task 025 (`src/lib/sim/schedule/speed.ts`)

- `rescaleKickoffsForSpeedChange` — 배속 0.25×~20× **비례(곱셈)** 재계산
- `rescheduleKickoffsForPauseResume` — 정지/재개 **오프셋(덧셈)** 재계산
- 공통 규칙: **기준 시각 이전(이미 지난) 킥오프는 불변**
- `rescaleLeagueKickoffs*` / `rescheduleLeagueKickoffs*` — 여러 리그에 동일
  context/window를 일괄 적용해 `kickoff.ts`의 리그 간 **동시 종료 정렬(AS-16)이
  재계산 후에도 구조적으로 보존**됨을 보장
- 월드시계 실시간↔월드분 적분 일반식(H-24)은 **재구현하지 않고** 30일차 소관으로 남김
- 신규 파일만 추가, 기존 `kickoff.ts` 무수정. 테스트 10건
- 결정론 제약 준수 — `Math.random`/`Date.now`/`react`/`@supabase/*` 0건

### 3팀 — Task 035 (`src/lib/odds/overround.ts`)

- `convertProbabilityToOdds` / `computeMarketOdds` — 배당 = (1/p) × (1/overround)
- 상수는 **기존 `ODDS_PARAM.OVERROUND/MIN_ODDS/MAX_ODDS`(`fallback.ts`, 27일차)를
  `loadConstants`로 재사용** — 중복 선언 없음
- 셀렉션 키→확률(units) 레코드를 받는 **범용 시그니처**라 1X2 외 시즌 마켓도 재사용 가능
- 확률 0 셀렉션은 마켓에서 제외
- 테스트 13건 — 클램프 경계(→1.01 / →500.00), Σ(1/odds)=1.06±0.005 포함
- `src/lib/odds/` 3파일 29건 전체 통과

### 4팀 — Task 013A (도메인 표현 8/8 종결)

- 신규 4종: `FitnessBar` · `FormStrip` · `PositionMap` · `StatBar`
- 순수 로직 분리: `domain/form.ts` · `domain/pitch.ts` (+ 각 `.test.ts`)
  — `.tsx` 렌더 테스트가 불가한 상황(I-151)에서의 커버리지 확보 수단
- `pitch.ts`의 `POSITION_COORDINATES`는 `Record<Position, PitchPoint>`로 선언해
  `Position` 유니온이 변하면 **tsc가 누락을 컴파일 오류로 잡도록** 함(`radar.ts` 선례 계승)
- i18n: `team.ts`에 `form` 그룹, `player.ts`에 `position` 그룹 (ko/en 대칭)
- 28일차 규약(`DomainViewState<T>` 4상태 · 서버 컴포넌트 · `t()`) 그대로 계승,
  **새 Tailwind 토큰 0개**, `enums.position` 라벨은 재사용만

### 5팀 — Task 013B (`src/components/composite/PitchLineup.tsx`)

- 7종 포메이션 좌표 배치: 4-4-2 / 4-3-3 / 4-2-3-1 / 3-5-2 / 3-4-3 / 4-5-1 / 5-3-2
- `Formation`(`src/types`)이 **값 미확정 `string`**이라 도메인 타입을 재선언하지 않고
  **로컬 UI 상수** `FORMATION_LAYOUTS`로 처리 — C-5/C-6 위반 회피. 판단 타당
- 미확정 포메이션 문자열은 **방어 상태**(`unsupportedFormation`)로 렌더
- 순수 함수 `resolvePitchSlots`로 렌더 로직 분리 → `.test.ts`로 7종 각각
  **GK 포함 11슬롯 · 유효 좌표 · 유효 Position** 검증(신규 12건)
- i18n: `match.ts`에 `lineup` 그룹 (ko/en 대칭 6키)
- **28일차 지적(서버 컴포넌트 · `t(locale,…)` · `state` 단일 prop)이 1차 산출물부터
  지켜졌다** — 같은 지적의 재발 0건

## 4. 팀장 검증에서 발견된 결함

**재수정 지시 0건.** 4개 팀 산출물 모두 자체 게이트·규약 준수 상태로 도착했다.

28일차 인계 ⓓ("병렬 일차에는 규약 발산을 게이트가 못 잡으므로 산출물 직접 대조
필수")에 따라 4팀·5팀 산출물을 직접 대조한 결과 **발산 1건을 실제로 검출**했다
(→ I-157). 게이트 3단은 이 건에 대해 전부 green이었다 — **경고가 두 일차 연속으로
유효함이 확인된 셈**이다.

추가 대조 항목과 결과:

| 대조 항목 | 결과 |
|---|---|
| i18n ko/en 키 대칭 (`match`·`player`·`team` 6파일 42줄 추가) | 이상 없음 — ko 초과분은 전부 주석 |
| 두 팀의 i18n 파일 충돌 | 없음 — 5팀 `match.ts` / 4팀 `player.ts`·`team.ts`로 분리 |
| `PitchLineup` 4상태 구현 | loading·empty·error 명시 + ready 폴스루로 4상태 충족 |
| 세미콜론 스타일 차이 (`composite/` 무 · `domain/` 유) | **29일차 회귀 아님** — 디렉터리별로 28일차부터 일관, 조치 불요 |
| `sim/**` 결정론 금지 심볼 | 0건 |

**수용 판정 1건** — 4팀 `StatBar.label`을 "호출부가 이미 번역해 넘긴 문자열"로 둔
설계. 범용 스탯 컴포넌트라 키를 전수 선언하는 대신 프레젠테이셔널화한 것으로,
`locale`은 loading/empty/error 문구에 여전히 쓰이므로 i18n 경유가 끊긴 것이 아니다.
**이슈 등재 없이 수용**한다.

## 5. 신규/갱신 이슈

| ID | 요지 | 소유·시점 |
|---|---|---|
| **I-157** | **피치 좌표계 발산** — 4팀 `pitch.ts`는 세로(y 0~150, y=140 자기 골문), 5팀 `FORMATION_LAYOUTS`는 가로(x=0 자기 골라인). 같은 제품 안에서 피치가 **전치된 방향**으로 그려지고, `LW`의 의미도 전방 윙(4팀) ↔ 측면MF(5팀)로 갈렸다. 두 테이블은 데이터 형태가 달라(1포지션 1점 vs 11슬롯) **통합 대상은 테이블이 아니라 방향·종횡비 규약** | 4팀·5팀, **015(34일차)** — I-156과 같은 시점·같은 팀이라 함께 처리 |
| **I-158** | **`Position` 11종에 측면MF·윙백 전용 코드 부재** — 4-4-2 측면MF를 `LW`/`RW`로, 3-5-2 윙백을 `LB`/`RB`로 재사용 중. 향후 전술 로직이 `Position`으로 분기하면 구분 불가 | **1팀** 판정 (동결 타입 배치 반영 경로) |
| **I-159** | **`fitness` clamp 1줄 수식이 `FitnessBar`·`ConditionGauge`에 중복** — 상호 참조 주석이 있어 위험은 낮음. 8종 완성으로 추출 시점 도래 | 4팀, 013A 종료 후 (경미) |

기존 이슈 상태 변동 없음. **I-142는 1팀 미배정 일차라 4일 연속 미착수.**

## 6. 다음 일차(30일차) 인계

| 팀 | 인계 사항 |
|---|---|
| **2팀** | Task 025 **마지막 일차**. 잔여 = 월드시계 실시간↔월드분 **적분 일반식(H-24)** — 29일차에 의도적으로 남긴 부분이다. **I-119**도 Task 026(30일차) 내 처리. I-145(경고 소비처)는 오케스트레이션 계층 생성 시점 |
| **3팀** | Task 035 경기 마켓 경로는 종결. **시즌 마켓 호출부**는 별도 일차 소관임을 3팀이 명시했으니 다음 배정 시 스코프 확인할 것. **I-155**(아바타 생성기 필요 여부) 판정 미착수 |
| **4팀** | 013A 도메인 표현 **8/8 종결**. 잔여 6종(SkeletonBlock, EmptyState, ErrorState, CountdownTimer, PhaseIndicator, OddsButton)은 33일차 구간. 그 전에 **I-151(jsdom) · I-148 · I-143 · I-144 · I-154** 처리 필요. 신규 **I-159**. **I-157은 5팀과 공동**이니 단독으로 좌표계를 바꾸지 말 것 |
| **5팀** | 013B 잔여 5종(BracketTree, TrophyCase, GrowthChart, InjuryTimeline + **MatchCard**). MatchCard는 `density:"card"\|"row"` 단일 통합 + LIVE 배지·경과분(H-24) 조건부 렌더. 차트류는 자체 SVG 1차(**I-152, 31일차 판정**). 신규 **I-158**(1팀 판정 대기) — 판정 나기 전까지 현 `LW`/`LB` 재사용 유지 |
| **1팀** | **I-142 4일 연속 미착수** — 다음 배정 구간 최우선. **I-158**(`Position` 코드 추가 여부)이 신규로 얹혔다 |
| **팀장** | ⓐ I-131 브랜치 보호 사용자 판단 ⓑ I-121 태그 규약 36일차 전 확정 ⓒ **셸에 `gh`가 없어 CI 러너 결과를 2일 연속 미확인**으로 남기고 있다 — 확인 수단 확보가 필요하다 ⓓ **산출물 직접 대조는 2일 연속으로 게이트가 못 잡는 결함을 잡았다**(28일차 2건, 29일차 I-157). 병렬 일차에는 계속 유지 ⓔ 팀원 산출물은 검증 직후 `git add`로 고정(I-147) |

## 7. 미해결·판정 대기

| 건 | 상태 | 기한 |
|---|---|---|
| **I-131 브랜치 보호** | CI 실패가 머지를 못 막음 | **사용자 판단 대기** |
| **I-132 마이그레이션 드리프트** | 원격 19 vs 로컬 2 | **사용자 판단 대기** |
| **I-128** | Playwright Chromium 미설치 — UI 실측 차단 | **사용자 판단 대기** |
| **V-01** | Edge 런타임 30경기 CPU 2초 이내 | Task 033 착수 전 |
| **I-151** | jsdom·testing-library 도입 | 4팀, 013A(33일차) 전 |
| **I-153** | Task 012 수락 기준 실측 | 4팀, 013A/013B 실사용 시 |
| **I-152** | 차트 라이브러리(자체 SVG → recharts 폴백) | 5팀, 31일차 |
| **I-148** · **I-143** · **I-144** | 토큰 전수 재점검 · 소비 규약 강제 · ΔE 여유 0.56 | 4팀, 013A(33일차) |
| **I-154** | 뉴스 i18n 네임스페이스 판정 | 4팀, 013A(33일차) |
| **I-155** | 선수 아바타 생성기 필요 여부 | 3팀 판정 → (필요 시) 1팀 타입 |
| **I-156** · **I-157** | 4상태 계약 중복 · **피치 좌표계 발산** | 4팀·5팀, 015(34일차) — 함께 처리 |
| **I-158** | `Position`에 측면MF·윙백 코드 추가 여부 | **1팀** 판정 |
| **I-159** | fitness clamp 공유 유틸 추출 | 4팀, 013A 종료 후 (경미) |
| **I-145** | 경고 소비처 | 2팀, 오케스트레이션 계층 생성 시 |
| **I-149** | KPI-8 계약 밀도 재측정 | 3팀, Task 030 이후 |
| **I-142** | 가드레일 자동 검증 | 1팀 (**4일 연속 미착수**) |
| **I-147** | 중복 기동 재발 방지 | 팀장, 상시 |
| **I-121** | 공통코드 키 선확정 태그 규약 | 36일차(031a) 전, 팀장 |
| **I-136** · **I-118** | 시드 상수 실값 정렬 | 36일차(031a) |
| **I-123** · **D-23 폴백 파일 분리** | 판정 시점 예약 | 026 착수 시점 |
| **I-119** | 2팀 배정 완료 | Task 026(30일차) 내 |
| **I-111** | 3팀·6팀 배정 완료 | 각 팀 해당 구간 |
