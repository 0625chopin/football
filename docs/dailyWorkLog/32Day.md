# 32일차 (2026-09-02, 수) 작업 로그

## 1. 참여 팀

| 팀 | Task | 그날 행 요약 |
|---|---|---|
| **2팀** 시뮬레이션엔진 | 023 | Vitest — Tier B 결정론 재현·그룹 내부 불변식·26필드 커버리지·구조 마커 삽입 회귀 |
| **3팀** 데이터밸런싱배당 | 035 | 킥오프 T−30분 산출, 라인업 확정·부상 발생 시 재산출(킥오프 이후 미수행) |
| **4팀** UI기반i18n | 013A | 전 컴포넌트 규약 검증 — 도메인 타입 props만·페칭 0·번역 키 경유·로케일 포맷터 |
| **5팀** 화면배팅UX | 013B | `TrophyCase` + 전 컴포넌트 규약 검증 (+ I-165 해소) |

1팀·6팀은 32일차 행이 없어 미소환. **중복 기동 0건**(2일 연속).

## 2. 최종 게이트 (팀장 직접 실행)

| 게이트 | 결과 |
|---|---|
| `npm run test` | **85 passed / 6 skipped (91 files), 1230 passed / 6 todo (1236 tests)**, Type Errors: no errors |
| `npx tsc --noEmit` (tsbuildinfo 삭제 후) | 오류 0건 — I-163(증분 캐시 거짓 통과) 회피 위해 캐시 제거 후 실행 |
| `npm run lint` | 각 팀 대상 파일 0 에러 (팀 보고, 전체 스위트에서 재확인) |

프로덕션 빌드는 WSL EPERM(I-62)으로 판정 수단에서 제외.

## 3. 팀별 산출물

### 2팀 — Task 023 (구조 마커 회귀 3건)

32일차 4항목 중 결정론 재현·그룹 불변식·26필드 커버리지는 **31일차 `tier-b-resim.test.ts`에 이미 구현돼 있었고**(팀장이 describe/it 목록으로 대조 확인), 실제로 비어 있던 "구조 마커 삽입 회귀" 3케이스만 `events.test.ts`(`ensureStructuralMarkers` 소유 파일)에 추가:

1. HALF_TIME이 FIRST_HALF_STOPPAGE 마지막 틱(45분+추가시간)에 위치 — 기존 테스트는 real seed의 스토피지가 0일 수 있어 실측 불가였음
2. 연장전 포함 시 FULL_TIME이 EXTRA_SECOND 마지막 틱(120분)에 위치
3. **정위치가 아닌 동일 타입 기존 이벤트는 마커로 오인되지 않음** — `hasMarker`가 type만 보도록 완화되면 I-65가 재발하는 지점의 오탐 방지 회귀

변경: `src/lib/sim/match/events.test.ts` (+66줄, 3 tests). I-164는 손대지 않음 — **배선 전제 테스트도 작성하지 않았음**(인계 지시 준수).

### 3팀 — Task 035 (배당 스케줄링 정책)

`src/lib/odds/schedule.ts` 신설 — 실행 여부만 판정하는 순수 함수 4종: `hasKickoffPassed` / `computeInitialComputeAt` / `decideInitialCompute` / `decideRecompute`. 핵심 불변식은 **`hasKickoffPassed`(now >= kickoffAt) 단일 지점**이며 최초 산출·재산출 전 경로가 이를 경유해 킥오프 이후를 차단한다(등호 포함 — 킥오프 정각부터 차단). `Date.now()` 미사용, 리드타임도 `leadMinutes` 인자 주입(NFR-CFG-001).

변경: `src/lib/odds/schedule.ts`, `src/lib/odds/schedule.test.ts` (신규, 14 tests).

### 4팀 — Task 013A (규약 감사)

소유 경로 전 컴포넌트 감사 결과 **위반 0건, 리팩터 없음**(변경 파일 없음).

| 경로 | 규모 | 결과 |
|---|---|---|
| `components/ui/**` | 11 | 페칭 0 (프리미티브, 나머지 항목 N/A) |
| `components/domain/**` | 8 + 헬퍼 | 전 항목 통과 |
| `components/state/**` | 6 | `formatOdds`/`formatCountdownClock` 경유 확인 |
| `[lang]/layout.tsx` + 소유 라우트 29개 | 30 | 통과 |

**판단으로 종결한 3건**(팀장 동의): ⓐ `ConditionGauge`/`FitnessBar`/`StatBar`의 `toFixed(1)`·`Math.round` — `format.ts`는 ko/en 표기가 실제로 갈리는 값(천단위·소수 2자리)만 경유지로 설계됐고 0~100 범위 값은 양 로케일 렌더링이 동일 ⓑ `[lang]/layout.tsx`의 `await bootstrapApp()` — 데이터 페칭이 아닌 1팀 소관 부트스트랩(I-72), 재사용 컴포넌트 아님 ⓒ `global-not-found.tsx`의 `metadata.description` — SEO 메타 필드로 JSX 텍스트 노드 아님(I-89 10일차 결정).

### 5팀 — Task 013B (7/7 종결)

`TrophyCase` 구현으로 **복합 컴포넌트 7종 완결**(BracketTree·EventTimelineItem·GrowthChart·InjuryTimeline·NewsItem·PitchLineup·TrophyCase). `Trophy`(필수) + `Award`(optional)를 함께 받아 클럽 상세(F7)·선수 상세(E9) 양쪽을 분기 없이 커버하고, TrophyType별 그룹핑(count + 시즌 라벨)은 순수 함수로 분리해 테스트. 013B 7종 규약 감사도 위반 0건.

**I-165 해소** — `InjuryTimeline`이 `enums.injuryStatus.*`를 직접 경유하도록 교체, `player.injuryTimeline.status{Active,Recovered}` 로컬 키 제거(ko/en).

변경: `TrophyCase.tsx`·`TrophyCase.test.ts`(신규), `InjuryTimeline.tsx`, `i18n/messages/{ko,en}/team.ts`, `i18n/messages/{ko,en}/player.ts`.

## 4. 팀장 검증

**전 팀 주장을 산출물로 직접 재현**(5일 연속 유지). 이번에는 결함이 나오지 않았고, 대신 **자진 보고된 판단 지점 2건이 실제 리스크로 확인돼 이슈로 승격**됐다.

| 검증 항목 | 방법 | 결과 |
|---|---|---|
| 컴포넌트 페칭 0건 | `grep -rnE "\b(fetch\|axios\|createClient\|supabase)\b" src/components/` | **0건** — 4팀·5팀 주장 일치 |
| `@/types` 서브경로 import | `grep -rn 'from "@/types/' src/` (전체) | **0건** (C-5·C-6 준수) |
| 로케일 포맷터 우회 | `grep -rnE "toLocaleString\|toLocaleDate\|new Intl\." src/components/` | **0건** |
| NFR-DT-001 (sim·odds) | `Math.random`/`Date.now()` grep | 실사용 0건 — 히트는 전부 준수 선언 주석 |
| 2팀 "이미 충족" 주장 | `tier-b-resim.test.ts` describe/it 전수 대조 | 결정론·26필드·`completed ≤ attempted`·볼륨 스케일링·입력 검증 **실재 확인** |
| 2팀 신규 3건 실질성 | diff의 `expect` 단언 확인 | 공허하지 않음(minute/addedTime 정위치, 마커 2건 분리 등 구체 단언) |
| 5팀 I-165 해소 | `InjuryTimeline.tsx:191` + 양 로케일 diff | `enums.injuryStatus.${row.injury.status}` 경유, 로컬 키 제거 확인 |
| i18n ko/en 키 정합 | `TeamMessages`/`PlayerMessages` 타입 + tsc | ko가 타입 원천이라 en 누락 시 tsc가 잡음 — 통과 |
| 5팀 `trophyType` 부재 주장 | `grep -rn "trophyType" src/i18n/` | 정본 카탈로그 **0건** — 주장 확인 → I-166 |
| 3팀 리드타임 소재 | `grep -rn "leadMinutes" src/` | `schedule.ts`+테스트 외 **0건** → I-167 |

3팀의 "재산출 트리거가 T−30 윈도 이전이어도 킥오프 전이면 허용" 설계는 **타당**으로 판정한다 — 조기 라인업 확정 시 최선 정보로 즉시 갱신하는 편이 낫고, "최초 산출이 아직 없었는지"는 상태가 필요해 순수 함수 밖(33일차 `worker.ts`) 몫이 맞다. 다만 이 분기가 `worker.ts`에서 **반드시 처리돼야** 하며 누락 시 재산출이 최초 산출을 대신 여는 경로가 열린다 — 33일차 인계에 명시.

재수정 지시 없음(0건).

## 5. 신규/갱신 이슈

| 건 | 내용 | 소관 |
|---|---|---|
| **I-165** | **해소(2026-09-02)** — InjuryTimeline 상태 표시명 이중화 정리 완료 | 5팀 (종결) |
| **I-166** | **신규** — `TrophyType`(E-32, 4종) 정본 표시명 카탈로그 부재, `team.trophy.type.*` 로컬 키로 우회 중. **I-165와 동일 구조의 재발** | 3팀 신설 → 5팀 교체 |
| **I-167** | **신규** — 배당 리드타임 정책값 30이 코드베이스에 없음(`leadMinutes` 인자만 존재). 33일차 `worker.ts`가 리터럴을 박을 위험 | 3팀, 33일차 |

I-166은 같은 패턴의 2회 반복이므로 **"신규 enum 표시명이 필요하면 로컬 키 우회 대신 3팀에 카탈로그를 먼저 요청"을 관례로 등재**했다.

## 6. 다음 일차(33일차) 인계

| 팀 | 인계 사항 |
|---|---|
| **2팀** | Task 023 32일차분 종결. **I-164**(구조 마커·Tier B 파이프라인 미배선)는 여전히 오케스트레이션 계층(H-15 이후) 몫 — 배선 시 **호출 순서 위반 테스트를 반드시 동반**. 그때까지 "구조 마커 누락 0건"을 파이프라인 산출물 속성으로 인용 금지. **I-145**(경고 소비처)도 동일 시점 |
| **3팀** | 33일차 `worker.ts` 착수 시 ⓐ **I-167** — 리드타임 30을 config 카탈로그에 등록 후 주입(리터럴 금지) ⓑ **최초 산출 미실행 상태에서 재산출 트리거만 온 케이스**를 상태로 구분(`schedule.ts`가 남긴 몫) ⓒ **I-166** `enums.trophyType` 신설. **시즌 MC 러너 호출부는 여전히 미배정** — 팀장 배정 필요. **I-149**는 Task 030 이후 |
| **4팀** | 013A 규약 감사 통과. 잔여 이슈 묶음 **I-151(jsdom) · I-148 · I-143 · I-144 · I-154**가 그대로 — 013A 종료 구간에 처리. **I-159** 경미. **I-161**은 020 착수 시 4팀 주도 |
| **5팀** | **013B 7/7 종결**. **I-166** 카탈로그가 생기면 `TrophyCase` 소비처 교체(3팀 선행). **I-156·I-157**은 015(34일차)에 4팀과 함께 |
| **1팀** | **I-142 7일 연속 미착수** — 다음 배정 구간 최우선. **I-163**(tsc 증분 캐시) 소관 — 팀장이 32일차에 캐시 삭제 후 실행으로 임시 회피 중. 동결 타입 배치 2건 대기: `Player.avatarSeed`(I-155) · `Position` 코드 추가(I-158, 판정 미착수) |
| **팀장** | ⓐ I-131 브랜치 보호 사용자 판단 ⓑ I-121 태그 규약 36일차 전 확정 ⓒ **`gh` 부재로 CI 러너 결과 5일 연속 미확인** — 확인 수단 확보 필요 ⓓ **시즌 MC 러너 배정 일차 결정**(2일 연속 미결) ⓔ 산출물 직접 대조 5일 연속 유지 — 32일차는 결함 0건이나 판단 지점 2건을 이슈로 승격 |

## 7. 미해결·판정 대기

| 건 | 상태 | 기한 |
|---|---|---|
| **I-131 브랜치 보호** | CI 실패가 머지를 못 막음 | **사용자 판단 대기** |
| **I-132 마이그레이션 드리프트** | 원격 19 vs 로컬 2 | **사용자 판단 대기** |
| **I-128** | Playwright Chromium 미설치 — UI 실측 차단 | **사용자 판단 대기** |
| **V-01** | Edge 런타임 30경기 CPU 2초 이내 | Task 033 착수 전 |
| **I-164** | 구조 마커·Tier B 파이프라인 미배선 | 2팀, 오케스트레이션 계층 착수 시 |
| **I-167** | 배당 리드타임 정책값 소재 부재 | **3팀, 33일차** |
| **I-166** | `TrophyType` 정본 카탈로그 부재 | 3팀 신설 → 5팀 교체 |
| **I-163** | tsc 증분 캐시 거짓 통과 위험 | 1팀 (게이트 소유) |
| **I-151** | jsdom·testing-library 도입 | 4팀, 013A 종료 전 |
| **I-153** | Task 012 수락 기준 실측 | 4팀, 013A/013B 실사용 시 |
| **I-148** · **I-143** · **I-144** | 토큰 전수 재점검 · 소비 규약 강제 · ΔE 여유 0.56 | 4팀, 013A |
| **I-154** | 뉴스 i18n 네임스페이스 판정 | 4팀, 013A |
| **I-161** | 브래킷 i18n 네임스페이스 분리 | 4팀 주도·5팀, 020 착수 시 |
| **I-155** | 아바타 생성기 — 판정 완료(필요) | **1팀** 타입 배치 접수 → 3팀 구현 |
| **I-156** · **I-157** | 4상태 계약 중복 · 피치 좌표계 발산 | 4팀·5팀, 015(34일차) |
| **I-158** | `Position` 측면MF·윙백 코드 추가 여부 | **1팀** 판정 (미착수) |
| **I-159** | fitness clamp 공유 유틸 추출 | 4팀, 013A 종료 후 (경미) |
| **I-145** | 경고 소비처 | 2팀, 오케스트레이션 계층 생성 시 |
| **I-149** | KPI-8 계약 밀도 재측정 | 3팀, Task 030 이후 |
| **I-142** | 가드레일 자동 검증 | 1팀 (**7일 연속 미착수**) |
| **I-147** | 중복 기동 재발 방지 | 팀장 상시 — **32일차 0건(2일 연속)** |
| **I-121** | 공통코드 키 선확정 태그 규약 | 36일차(031a) 전, 팀장 |
| **I-136** · **I-118** | 시드 상수 실값 정렬 | 36일차(031a) |
| **I-123** · **D-23 폴백 파일 분리** | 판정 시점 예약 | 026 착수 시점 |
| **I-111** | 3팀·6팀 배정 완료 | 각 팀 해당 구간 |
| **`MANAGER_STYLE_XG` 실값** | 5종 잠정치 | 031b 밸런싱 튜닝 |
| **시즌 MC 러너** | 시즌·토너먼트 마켓 호출부 미배정 | 팀장 — 배정 일차 결정 필요 (2일 연속) |
