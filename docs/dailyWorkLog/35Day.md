# 35일차 (2026-09-07 월)

## 1. 참여 팀

**2팀**(026) · **3팀**(035) · **4팀**(014) · **5팀**(015) — 팀 md 35일차 행.
**1팀** — 행 없음(24~68일차 상시 게이트 구간)이나 **I-169가 5팀 폴링 훅을 직접 차단**해 팀장이 추가 소환(4단계).
**6팀** 미참여. 단 본 일차에는 **이전 세션이 커밋 없이 남긴 34일차 6팀 합동 리뷰 후속**이 포함된다(3-6절).

## 2. 최종 게이트 (팀장 직접 실행)

| 항목 | 결과 |
|---|---|
| `npm run typecheck` | **EXIT 0** — `src/` 0건 (35일차 신설, I-181) |
| `npm run lint` | **0건** |
| `npm run test` | **EXIT 0** — 94 files / **1331 passed** / 6 todo. `Errors` 줄 소멸 |
| typecheck 소요 | 6.4초 → **1.6초** (I-180 효과) |

증분 캐시 삭제 후 판정(I-163 거짓 통과 회피, 8일 연속 수동).

**판정 명령이 오늘 바뀌었다** — raw `npx tsc --noEmit`은 dev 서버 가동 중 무작위 실패하므로(4-1) **`npm run typecheck`을 쓴다**(I-181, `scripts/typecheck.mjs`). 오류가 전부 `.next/` 아래일 때만 재시도하고 `src/**` 오류가 하나라도 섞이면 재시도 없이 실패시킨다. `npm run gate`도 이 명령을 경유한다.

## 3. 팀별 산출물

### 3-1. 1팀 — I-169 · I-172 · I-142 · I-180 · I-181 (5건 종결)

- **I-169** `DataSource.getMatchClockContext(fixtureIds)` + `WorldClockContext` 신설. **단순 `now` 추가는 기각**하고 킥오프 앵커(I-174)와 한 묶음 처리 — 따로 조회하면 두 호출 사이에 배속이 바뀌어 앵커와 질의 시각이 어긋나므로 **원자 조회**가 필요했다. 판정 즉시 중간 보고해 5팀 차단 해소. 인터페이스 추가가 Mock/Supabase 구현체를 tsc 에러로 깨뜨려 **3·6팀 소유 파일에 최소 구현 직접 투입**(팀장 승인 → I-179).
- **I-172** check-literals 오탐 필터 66 → 11건(`_DEFAULT` 토큰이 공통코드 그룹명과 겹칠 때만 보고).
- **I-142**(**9일 미착수**) 가드레일 회귀 11건. **일부러 훼손시켜 탐지 실효성 실증 후 원복.**
- **I-180**(팀장 제보, 4-1) `tsconfig.typecheck.json` 분리. **1차 시도 실패 기록 포함** — `src/**`로 좁혔더니 라우트 파일이 딸려와 22건 신규 오류, type-test 글롭으로 재차 좁혀 해결.
- **I-181**(팀장 격상 제보, 4-1) **`npm run typecheck` 신설**(`scripts/typecheck.mjs`) — raw `npx tsc --noEmit`이 dev 서버 가동 중 무작위 EXIT 1이 되던 것을 해소. `tsconfig.json`의 `.next/dev/types/**` include는 **Next.js 16이 dev/build 전환용으로 의도 관리**하는 것이라(지워도 되살아남) 손댈 수 없어, **오류가 전부 `.next/` 아래일 때만 재시도**하고 `src/**` 오류가 섞이면 즉시 실패시키는 래퍼로 우회했다. `package.json`·`scripts/gate.sh`·`CLAUDE.md` 동반 갱신.
- 등재 5건: I-178 · I-179 · I-180 · I-181 · **I-62 ⑤ 병합**(npm install EPERM — 신규 번호를 붙이려다 grep으로 12일차 기록 발견해 병합).
- ⚠️ **번호 충돌 1건 발생·정정** — 1팀이 I-181(typecheck 래퍼)을 등재하는 사이 팀장이 같은 번호를 폴링 건에 썼다. **1팀 것이 이미 `CLAUDE.md`·`package.json`에서 참조되고 있어 팀장 등재분을 I-182·I-183으로 밀어 정정**했다. 병렬 등재 시 번호 선점 경합이 실재함을 확인.

### 3-2. 2팀 — Task 026 타이브레이커

- `standing/tiebreak.ts` 신규. 7단계 **재귀 그룹 분해**(직전까지 완전 동률인 그룹에서만 실행). `derive.ts`에 `LAYER_TAG.STANDING` + `deriveStandingDrawSeed()`, `nextIntBelow` 기반 Fisher–Yates.
- **19개 테스트가 7단계를 각각 단독 결정 케이스로 덮음**(단계별 매핑 제시). 핵심: 3팀 이상 동률 미니리그 재계산 + 그룹 밖 경기 필터링 / **PRNG `state` 이어받기를 독립 재구현과 대조**(5팀 이상으로 뽑아 우연 일치 배제).
- `precision.ts` 미사용은 **적용 대상 아님**(정수 인덱스 뽑기지 확률 임계 비교가 아님) — 팀장 수용.
- **추가**: check-literals 잔여 11건 판정 → **확정 위반 2건 발견·수정**. `conditionModifier`/`fitnessModifier`가 `CONDITION_MULT`(0.70/0.30)·`FITNESS_PARAM`(0.75/0.25)과 값이 일치하는데 **주입 경로 자체가 없었다**(형제 함수는 이미 I-83 패턴 — 18일차 패턴 적용 누락). 나머지 9건은 의도된 로컬 기본값/후보 아님.
- **I-178** re-export 대신 상호 참조 주석 — `prng.ts`(범용 저수준)와 `derive.ts`(도메인 시드 소유자)의 책임 경계를 흐리지 않기 위함(근거 제시, 수용).

### 3-3. 3팀 — Task 035

- `odds/pipeline-kpi.test.ts` 신규. 27~29일차가 이미 모듈별로 검증 중임을 확인하고 **중복 대신 통합 회귀 1건 + 성능/KPI 측정**으로 좁힘.
- **성능**: 경기당 **~165~180ms**(≤10초) / 라운드 **~1.28초**(≤60초). 33일차 추정(~1.5s/경기)보다 빠름.
- **KPI-4 Brier 0.1701**(≤0.21) — ⚠️ **조건부 수용**. I-160 회피로 실제 밸런싱값이 아닌 **테스트 픽스처 기반 self-consistency 추정치**다. "자기 자신과 일관적"임을 보인 것이지 예측력 검증이 아니다. 3팀이 한계 명시. **최종 판정은 I-160 해소 후 재측정.**
- **추가**: `enums.newsFeedItemType` **10종 ko·en 양쪽**(소유 확인 후, 76→86) — 5팀 배지 차단 해소.

### 3-4. 4팀 — Task 014 + 판정 3건

- 22종에 `StateToggleSlot`(4상태) 배선, 전 섹션 `ViewportFrame`. **`MatchCard` 등록으로 21 → 22종**(KPI-6).
- **뷰포트 프리뷰 결함 발견·수정(실측 최대 수확)** — 최초 구현이 컨테이너 `max-width`만 바꿨는데 Tailwind `sm:`/`lg:`는 **뷰포트 기준**이라 그리드가 재배치되지 않았다. **코드 리뷰로는 원리적으로 못 잡는 결함.** 컨테이너 쿼리(`@container`)로 교체해 모바일 1열/태블릿 3열 실재배치 확인.
- **I-170** 쿠키 근사 채택·구현, 최초 방문자 한계 주석 명시. `not-found.tsx` 실트리거 불가(18개 placeholder가 `notFound()` 미호출) → **소스 검증까지만, 실동작 미확인**으로 정직 보고.
- **I-144** `globals.css` 미수정 판단, 대비 124/124(ΔE 여유 0.56 유지). **I-151** jsdom+testing-library 도입, badge 14ch 회귀 2건(WSL EPERM → `--no-bin-links`, I-62 ⑤). **I-153** 시간상 미착수(정직 보고).
- `.gitignore`에 `testPng/`·`.playwright-mcp/`(I-173 부수).

### 3-5. 5팀 — Task 015 A1~A4

- **A1** `PhaseIndicator`+`CountdownTimer`. **정지 중 카운트다운 정지(`isPaused`)** 성립 — 오늘 명시적 수락 기준.
- **A2** `LiveMatchGrid.tsx` 신규 — `usePollingList` 소비 + `getMatchClockContext` 원자 조회. ⚠️ **규약 위반 → I-182**(4-2).
- **A3** 킥오프 목록 — `getNextKickoff`가 1건만 주므로 리그별 라운드 경계로 상위 5건 병합. 한계(현재 라운드 SCHEDULED 0건 시 리그 누락) 파일 헤더 명시.
- **A4** 뉴스 요약 + 카테고리 배지(`enums` 경유, 하드코딩 없음).
- **파괴적 변경 자체 차단** — 필드명을 `category`→`type`으로 바꿨다가 4팀 `sample/page.tsx`가 이미 소비 중인 걸 tsc로 발견, **필드명 유지 + 타입만 `string`→`NewsFeedItemType`으로 좁힘.** 남의 파일 무수정 통과 + 타입 안전성 확보.
- 승인된 이월: FixtureStatus 4종 렌더링 / `clockRevision` 재동기화(D-2 → 021·54일차).

### 3-6. 이전 세션 산출물 (34일차 6팀 합동 리뷰 후속, 커밋 없이 잔류)

- **I-128 해소** — 진단이 **틀렸었다**. 미설치가 아니라 **리비전 불일치**(1228 vs 1232)였고 명령 1회로 해소. **22일차부터 13일간 "사용자 판단 대기"로 묶여 있던 것.** 잔여(러너 미설치)는 I-173 분리.
- **I-174~I-177 등재**(킥오프 앵커 부재 / `is_event_elapsed()` 스텁 / Mock 컷오프 부재). **소유 경로 정정** — 11일차 로케일 재배치 이후 **19일간 갱신되지 않아 `[lang]` 누락**, `src/proxy.ts`·`api/**` 소유자 부재를 정정(`api/live`=5팀 / `api/admin`·`health`=6팀 / 응답 타입=1팀).
- `.mcp.json`(**I-128 해소의 실체**), `CLAUDE.md` stale 정정(12일차 → 34일차 기준).

## 4. 팀장 검증

### 4-1. 게이트 결함 2건 — 전원이 놓친 신호

`npm run test`가 매 실행 **`Errors 1`**을 보고했는데 **5개 팀 전원이 "1331 passed"만 보고 통과 판정**했다. 격리 확인(`--typecheck.enabled=false` → 0건) 후 1팀 제보 → I-180 종결.

이어 **더 심각한 사실** — 프로젝트 지정 검증 명령인 **`npx tsc --noEmit` 자체가 EXIT 1**이었다(`src/` 0건, `.next/dev/types` 4건). `CLAUDE.md`가 "빌드 대신 tsc/lint/test로 판정하라"고 못박은 그 명령이 **dev 서버 가동 여부로 통과/실패가 갈렸다** → I-181 종결(`npm run typecheck` 신설).

⚠️ **팀장 서술 정정**: 최초 제보에서 "CI가 종료 코드로 판정하면 오늘 산출물 전부가 실패한다"고 썼으나 **과장이었다.** 1팀이 확인한 대로 **CI에는 이 경합 자체가 없다** — `gate.sh`가 클린 체크아웃에서 `next typegen`으로 산출물을 새로 만들고, 그 시점에 동시에 쓰는 dev 서버가 없기 때문이다. **현상은 로컬(dev 서버 동시 가동) 전용**이다. 다만 판정 명령이 환경에 따라 흔들린다는 문제 자체는 유효해 수정은 그대로 진행했다.

**tsconfig는 손댈 수 없음이 확정됐다**(1팀, Next.js 소스 근거) — `.next/types`와 `.next/dev/types`를 함께 include하는 것은 **dev/build 전환 시 tsconfig 흔들림을 막기 위한 Next.js 16의 의도적 설계**이며(`lib/typescript/type-paths.js` 주석), `writeConfigurationDefaults.js`가 `next dev`/`next build`마다 자동으로 되채운다. 따라서 "include에서 제거" 방향은 기각되고 래퍼 우회가 유일한 경로였다.

부수 2건: ⓐ `vitest.config.ts`가 **존재하지 않는 I-180을 인용**했다(`grep -c` → 0). ⓑ 그 주석의 *"루트 tsc 게이트가 담당하므로 커버리지 손실 없다"*는 전제가 **틀렸다**(동일 결함 잔존). **팀장 자기 정정**: 최초 보고 시 파이프 뒤 `$?`를 읽어 `tail`의 종료 코드를 tsc 것으로 오독 → 재측정 후 정정.

### 4-2. Playwright 실측 (팀장 단독)

- **I-182 폴링 규약 위반** — `LiveMatchGrid.tsx`가 클라이언트에서 `bootstrapApp()`/`getDataSource()` 직접 호출. 35일차 개정이 **"폴링 fetcher는 반드시 Route Handler 경유"**를 못박았으나 규약 확정과 파일 작성이 같은 날이라 놓쳤다. 근거: **12초간 네트워크 0건** + **번들에 `bootstrapApp` 심볼 실재**. 5팀 자진 인정, (B) 이월 합의.
  - **팀장 오측정 자기 정정**: "요청 0건"을 처음엔 폴링 미동작으로 읽었으나 Mock 어댑터가 인메모리라 애초에 네트워크를 안 탄다. 방법을 바꿔 **폴링 훅·카운트다운 모두 정상**(3초에 3초 감소) 확인. 그대로 보고했으면 없는 결함을 씌울 뻔했다.
- **I-183 320px — 원인 귀속 정정.** 4팀 제보는 "헤더 placeholder"였으나 실측상 **헤더·푸터는 320px에 정확히 맞는다**(`right 320`). 진짜 원인은 `nav`의 `w-48 shrink-0`(192px 고정, 미접힘) + `main`에 `min-w-0` 부재. **오진대로 등재됐으면 후속 담당자가 엉뚱한 곳을 조사했을 건.**

### 4-3. 팀장 지시 오류 (자기 기록)

**34일차 로그의 "Playwright 미설치로 실측 불가"를 실환경과 대조하지 않고 인용해 4·5팀 지시에 넣었다.** 두 팀 모두 그 전제로 "검증 불가"를 보고했다. 실제로 I-128은 이미 해소돼 있었고 **그 해소 실기동을 수행한 것이 4팀 자신**이었다. ISSUES.md가 I-128 정체 원인으로 지목한 *"문서를 실환경과 대조하지 않고 인용"*을 팀장이 그대로 반복한 것이다. 마감 전 정정했고, 그 결과 **뷰포트 프리뷰가 실제로는 동작하지 않던 결함**(3-4)이 드러났다 — 정정 없었으면 "완료"로 마감될 뻔했다.

### 4-4. 오탐 4건 vs 놓친 진짜 신호 2건

각 팀이 전체 게이트를 돌리다 **남의 미완성 중간 상태를 "타 팀 결함"으로 오인한 사례 4건**(5팀→2팀 tiebreak / 3팀→page.tsx / 3팀→en/sample.ts / 5팀→loading.tsx) — **전부 실제 결함이 아니었다.** 반면 진짜 신호(`Errors 1`, tsc EXIT 1)는 **전원이 놓쳤다.**

I-172 오탐 55건 → `Errors 1` 간과 → tsc EXIT 1 간과. 셋 다 **"게이트가 잡음을 내면 사람이 무시하는 법을 배우고, 그래서 진짜 신호를 놓친다"**는 같은 병이다. 실증도 나왔다 — 1팀이 오탐을 66→11로 줄이자 **2팀이 즉시 실제 위반 2건을 발견**했다.

### 4-5. 운영 제약 2건 (다음 일차부터 적용)

- **Playwright MCP 브라우저는 세션당 1개** — 4팀·5팀·팀장이 동시에 실측하려다 `Browser is already in use`로 상호 차단됐다. **실측은 팀장 단독 검증(5단계)에서 일괄 직렬 수행**한다.
- **이슈 번호 선점 경합** — 1팀과 팀장이 동시에 I-181을 등재했다(3-1). 등재 전 `grep`만으로는 부족하며, **동시 등재 시 코드·문서에서 이미 참조 중인 쪽이 번호를 갖는다**를 원칙으로 한다.

## 5. 신규/갱신 이슈

| 번호 | 상태 | 요지 |
|---|---|---|
| **I-169** | OPEN(1차 해소) | `getMatchClockContext` 신설. I-174 미해소라 앵커는 근사 — **완전 해소 아님** |
| **I-172** · **I-142** · **I-180** · **I-128** · **I-178** | **해소** | 오탐 필터 / 가드레일 회귀(9일 만) / typecheck 분리 / Playwright(13일 만) / rng 주석 |
| **I-179** | 기록 | 1팀의 3·6팀 소유 경로 침범(승인) — **6팀 통지 필요** |
| **I-182** | **신규** | 폴링 Route Handler 우회·번들 오염 — 5팀 36일차 |
| **I-183** | **신규** | 320px 536px 오버플로우, 원인=SideNav 고정폭 — 4팀 |
| **I-62** | ⑤ 병합 | npm install EPERM 재발(12일차 기록 존재) → 전 팀 권고 승격 |
| **I-173~I-177** | OPEN | 이전 세션 등재(러너 / 킥오프 앵커 / `is_event_elapsed` / Mock 컷오프) |

## 6. 다음 일차(36일차) 인계

| 팀 | 인계 사항 |
|---|---|
| **1팀** | **I-182 응답 타입 계약 제공**(5팀 Route Handler 선행 조건, 최우선). I-173 러너 도입 판정(022 전). **I-163 8일 연속 수동 회피** — I-180 흐름에서 함께 처리 권고. `Player.avatarSeed`(I-155) 배치. I-158 판정 미착수 |
| **2팀** | 026 종결. **3종 실산식 일차 미배정 — 5일 연속, 팀장 배정 필요.** I-123 재개 트리거는 "CONDITION_FATIGUE 착수일". **I-176 `isEventVisibleAt` 공용 함수 제공(017 착수 43일차 전).** I-164·I-145·I-171은 H-15 이후 |
| **3팀** | 035 종결. **I-160이 xG 배율 소비와 KPI-4 재산출을 동시에 차단 — 우선순위 상향 권고.** 시즌 MC 러너 호출부 **5일 연속 미배정**. I-155 구현(1팀 배치 후) |
| **4팀** | 014 계속(38일차까지). **I-183** — `SiteHeader`/`SideNav`/`SiteFooter` 분리(23일차부터 미이행)와 함께 처리 권고. **I-143 36일차 확정**, I-148 38일차. **I-153 재개 가능**(실측 경로 열림). I-170 실동작 미확인분 |
| **5팀** | 015 종결. **36일차 최우선 = I-182 `src/app/api/live/**` Route Handler**(1팀 계약 수신 후). A3 경계 케이스. FixtureStatus 4종. I-157은 017 착수 시 |
| **6팀** | **미참여 — 3건 반드시 통지**: ⓐ **I-179** 1팀이 `SupabaseDataSource.ts`에 최소 구현 직접 투입(재검토) ⓑ **I-174** `fixture` 월드분 앵커 컬럼 마이그레이션 **일차 배정 요청** ⓒ **I-175** `is_event_elapsed()` 스텁 — ⓑ→ⓒ→뷰 전환 3단 순서 필수 |
| **팀장** | ⓐ I-131 · ⓑ I-132 **사용자 판단 대기** ⓒ I-121 태그 규약 36일차 전 ⓓ **`gh` 부재로 CI 러너 결과 8일 연속 미확인** ⓔ **미배정 2건 5일 연속** ⓕ 실측 직렬화(4-5) 적용 ⓖ I-168 잔여 |

## 7. 미해결·판정 대기

| 건 | 상태 | 기한 |
|---|---|---|
| **I-131** · **I-132** | 브랜치 보호 / 마이그레이션 드리프트(원격 19 vs 로컬 2) | **사용자 판단 대기** |
| **I-182** | 폴링 Route Handler 우회 · 번들 오염 | **5팀 36일차**, 1팀 계약 선행. 034b 전 필수 |
| **I-174** · **I-175** | 킥오프 앵커 부재(I-169 완전 해소 차단) / `is_event_elapsed()` 스텁 | **6팀 일차 배정 필요**, 034b 전. I-174 → I-175 순 |
| **I-176** | Mock 어댑터 경과 컷오프 부재 | 2팀 함수 + 각 어댑터, **017(43일차) 전** |
| **I-160** | `MANAGER_STYLE_XG` — xG 배율 + KPI-4 이중 차단 | 3팀, **우선순위 상향 권고** |
| **I-183** · **I-170** · **I-153** | 320px(SideNav 고정폭) / 쿠키 근사 실동작 미확인 / Task 012 실측 | 4팀 — I-153은 **재개 가능**(실측 경로 열림) |
| **I-173** · **I-163** | 러너 미설치(자동 회귀 불가) / tsc 증분 캐시 거짓 통과 | 1팀 — 전자 022(60일차) 전, 후자 **8일 연속 회피** |
| **I-155** · **I-158** | 아바타 타입 배치 / `Position` 코드 추가 | 1팀(후자 판정 미착수) |
| **I-154** · **I-161** · **I-156** · **I-157** · **I-149** | i18n 네임스페이스 2건 / 4상태 중복 / 피치 좌표계 / KPI-8 밀도 | 해당 Task 착수 시 |
| **I-147** | 중복 기동 재발 방지 | 팀장 상시 — **35일차 0건(5일 연속)** |
| **I-121** · **I-136** · **I-118** | 태그 규약 / 시드 상수 실값 | 36일차(031a) |
| **V-01** · **KPI-4** | Edge CPU 2초 / Brier 0.1701은 **self-consistency 추정치** | Task 033 전 / I-160 해소 후 재측정 |
