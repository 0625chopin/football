# 52일차 (2026-09-30 수)

**참여**: 2팀 · 5팀 · 6팀 (1·3·4팀 미참여 — 1팀 상시 리뷰 게이트, 3·4팀 대기 구간)
**요약**: 재수정 라운드 없이 전원 게이트 통과. Task 028 은퇴·명성, 018 클럽 상세 2/2, 037 지갑 테이블.

---

## 1. 팀별 작업

### 2팀 — Task 028 은퇴 판정 + 명성 갱신

- `src/lib/sim/season/retire.ts` 신설(FR-PL-015·012, FR-TM-002). **40세 강제 은퇴를 확률 분기가 아닌 무조건 분기**(PRNG 미소비)로 두어 "40세 이상 0명"을 구조 강제. 34세부터 나이·OVR하락·저출전 3축 확률 상승(`precision.ts`의 `clampToUnits`/`rollSucceeds` 경유).
- 명성 갱신은 순수 산술(PRNG 미사용): 선수=리그티어+수상+평점·팀성적, 팀=리그티어+트로피+순위. `fanBase`는 리그 정규화 기준 부재로 입력 제외(헤더 명시).
- 은퇴는 `resolveSeasonRetirements`가 결정만 반환(영속화는 오케스트레이션 계층 — `promotion.ts` 선례).
- 변경: `retire.ts`·`retire.test.ts`(둘 다 신규). 테스트: `vitest src/lib/sim` 48파일 762건, 40세 검증(FORCE_AGE, `age≥40 && !willRetire` 실패 조건) 실재.

### 5팀 — Task 018 클럽 상세 2/2 (F4~F8)

- F4 시즌지표(그룹탭 5종)·F5 재정·F6 스폰서 3슬롯(D-35 체결 구단주)·F7 트로피(`TrophyCase` 재사용)·F8 최근/예정(`MatchCard` row, LIVE 분리) 배선. 와이어프레임 8→9섹션은 48일차 `eeccc1b`에 이미 반영돼 확인만.
- **F6 실값 확인** — 표본 팀 3슬롯 전부 채워짐("Sterling Corp 규모5 시즌3,060pt 체결 Emiliano Sanchez" 등), **부도위험 배지도 실렌더**(Summit Dynamics). 체결 구단주는 `signedByOwnerId`→`getClubOwner` 조인.
- **I-260 유지** — 51일차 창단·재임 시즌 숨김(`SHOW_SEASON_ORIGIN_FIELDS=false`) 그대로. F4~F8의 트로피·계약 시즌은 별개 축이라 정상 표시.
- F4/F5/F7은 `getTeamSeasonStat`/`getTeamTrophies` mock 미착수로 상시 empty 정상 폴백. F8은 실 fixture 5건.
- 변경: `teams/[teamId]/page.tsx`, 신규 `SeasonStatPanel`·`FinancePanel`·`SponsorSlots`·`RecentUpcomingFixtures`, `i18n/messages/{ko,en}/team.ts`. 테스트: typecheck·lint·test(1746), dev 실측(ko/en, 1280·375px, 가로 스크롤 0).

### 6팀 — Task 037 User/Wallet/WalletTransaction 테이블

- `profile.locale` 컬럼 추가(D-18, ko/en, default 'ko') + `wallet_transaction`(E-40) 신규 생성(RLS 4종 동일 패턴). `mapper.ts`에 `mapProfileRow`/`mapWalletRow`/`mapWalletTransactionRow` 추가, `database.types.ts` 재생성.
- **I-243·I-263 판정만(미착수)** — 지시대로 이력 조작 실행 안 함. I-243=로컬 3파일 타임스탬프 원격 불일치 재확인(3건), rename은 팀장 조율 후. I-263=초기 RLS **17건**(51일차 "18건" 정정) 원격에만 존재, 추가 전용이라 상대적 안전.
- 변경: 마이그레이션 2건 신규, `database.types.ts`, `mapper.ts`. 테스트: `list_tables`로 3테이블 실존, `pg_policies` RLS 4건, `get_advisors` 신규 경고 0, `vitest src/lib/data/supabase` 137건.

---

## 2. 팀장 검증

- **전역 게이트**: typecheck·lint·test 전부 클린, **test 1746 passed**(51일차 1730 → +16). **재수정 라운드 0회.**
- **2팀 수락 기준(40세 0명)**: 테스트가 무의미한 통과가 아님을 확인 — `retire.test.ts:68·106`이 40/41/55세 FORCE_AGE + `age≥FORCE_AGE && !willRetire`를 실패 조건으로 단언.
- **6팀 도메인 필드 부재 직접 확인**: `betting.ts:119` `User`는 id·displayName·role 3필드뿐이고 `mapper.ts:1096`이 `locale`을 드롭. 52일차 행 "선호 로케일 필드 포함"이 DB까지만 충족 → **6팀 몫 부분 완료**, I-264 등재(1팀 배치 대기).
- **I-260 플래그 유지 확인**: `SHOW_SEASON_ORIGIN_FIELDS=false` 그대로, 5팀이 F4~F8 추가 시 미접촉.
- **결함 0건** — 발견 사항은 전부 데이터 계약/타입 공백(재수정이 아니라 이슈 등재 대상).

---

## 3. 이슈

- **신규 3건**: I-264(`User` 선호 로케일 도메인 필드 부재, 매퍼 드롭 — 1팀 배치) · I-265(은퇴·명성 계수 공통코드 미등록, I-121·I-136 3회차 — 031b) · I-266(클럽 상세 2/2 데이터 계약 공백 4건, I-259 클럽 판).
- **갱신 1건**: I-263 건수 18→**17** 정정(6팀 52일차 실측) + "추가 전용이라 I-243보다 안전" 관측 반영.
- **부분 완료 1건**: Task 037 52일차 행 — DB/매퍼 완료, 도메인 로케일 필드 연결은 I-264(1팀)로 잔여.

---

## 4. 다음 일차 인계

1. **I-264를 1팀 상시 리뷰 게이트에서 배치 반영** — `User`에 `preferredLocale` 필드 추가(8일차 동결 타입이라 이슈 배치 경로). 그 후 6팀이 `mapProfileRow` 드롭을 연결하면 Task 037 로케일 항목이 완결된다. 소비처 0건이라 급하지 않으나 인증(Task 037 잔여) 착수 전 필요.
2. **I-243 + I-263을 6팀 한 패스로** — 51일차부터 이월. I-263은 추가 전용이라 먼저 처리 가능(위험 낮음), I-243 rename은 팀장 조율 후. 실DB 전환(034b) 전 필수.
3. **5팀 018 잔여는 53일차** — 클럽 상세 524행 "섹션 단위 로딩·에러 격리 + 빈 상태 문구" `[ ]` 유지. 51일차부터 018 후속 53일차 유지가 팀장 승인 확정.
4. **I-265 · I-121 · I-136을 031b(65~68일차)에서 한 패스 시드 정리** — 세 건 모두 "카탈로그 미등록 계수를 파일이 자체 결정"한 동일 패턴.
5. **I-260(창단·재임 시즌 도메인 축)** — 여전히 화면 숨김 우회 중. 3팀 다음 등판 시 값·표시 규약 판정. 3팀 주간 한도는 리셋됐을 것이나 52일차 미참여였다.
6. **I-266 ⓐ(`ClubOwner` id 단건 조회 계약 부재)**가 가장 먼저 곪는다 — 소유주 교체 이력이 실DB 전환 시 현실화. 계약 확장은 1팀 소유.

---

## 5. 미해결·판정 대기

- **I-266** 클럽 상세 2/2 데이터 계약 공백 4건 — 5팀 발견, 계약은 1·3팀
- **I-265** 은퇴·명성 계수 미등록 — 3팀, **I-121·I-136과 031b 한 패스**
- **I-264** `User` 선호 로케일 도메인 필드 부재 — 1팀 배치(비차단, 소비처 0)
- **I-263**(17건) · **I-243** 마이그레이션 로컬 미문서·채번 불일치 — 6팀 한 패스, 034b 전 필수
- **I-260** 창단·재임 시즌 도메인 축 — 3·5팀, 화면 숨김 우회 중
- **I-262** OVR 산출식 중복 — 2·3팀, **I-257과 한 패스**
- **I-261** I-229 라인업↔이벤트 독립 표본 — 3팀, 다음 등판
- **I-257 · I-258** 순위 포인트 중복·리빌드 보조금 단위 — 2·3팀 조율
- **I-259** 선수 상세 잔여 4건 — 5팀, ⓑ는 I-249와 동시 판정
- **I-256** SP-4 잔여 2건(성공률 정의·킥오프 p95 스키마) — 팀장/6팀
- **I-255** 전역 경기 조회 계약 — 1팀, 034b 이후
- **I-252 · I-251 · I-250 · I-248 · I-247 · I-245** 49일차 신규 — 변동 없음
- **I-223** 종결은 62일차 `pending` 0건 확인 시 / **I-241**(MOCK_NOW 고정) — 3팀
- **I-236** `homeModifier` 공식(2팀) / **I-214** 크론 점등 금지(차단성, 52일차 준수 재확인)
- **I-235** 공유 트리 git 조작 — 48~52일차 연속 사고 0건
- **I-233 · I-232 · I-230 · I-228 · I-227 · I-225 · I-220 · I-217 · I-216 · I-215 · I-212 · I-211 · I-209 · I-208 · I-205 · I-204 · I-192** 비차단·배정 대기
