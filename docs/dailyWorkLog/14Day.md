# 14일차 (2026-08-07, 금)

## 1. 참여 팀

| 팀 | Task | 결과 |
|---|---|---|
| 1팀 코어·품질 | 008 — 커버리지 임계 | 완료 |
| 2팀 시뮬레이션엔진 | 023 — GK 퇴장 폴백(D-22) | 완료(재작업 2회) |
| 3팀 데이터·밸런싱·배당 | 007 — 절차적 엠블럼 SVG 생성기 | 완료(재작업 1회) |
| 4팀 UI기반·i18n | 011 선행 — Next.js 16 i18n 문서 조사 | 완료(정정 2회) |
| 6팀 DB·인프라 | 032 — 공통코드 4 + 운영 3테이블 | 완료 |

**5팀 화면·배팅UX 제외** — 1~27일차 장기 대기 구간(013B가 4팀 012, 27일차 완료 필요).

## 2. 최종 게이트

`npx tsc --noEmit` 0 / `npm run lint` 0 / `npm run test` **28 files · 367 tests 통과** / Type Errors 0.
프로덕션 빌드는 I-62(WSL 마운트 EPERM)로 검증 수단에서 제외.

## 3. 팀별 산출물

### 1팀 — Task 008 커버리지 임계
- `vitest.config.ts` — `include: ['src/lib/sim/**/*.ts']` + `thresholds: { lines: 80, branches: 70 }`(aggregate).
- **13일차 인계 문구의 오류를 실측으로 정정** — I-90이 요구한 `coverage.all: true`는 이 프로젝트의 vitest 4.1.10 `CoverageOptions` 타입에 **존재하지 않는 옵션**(구버전 지식 기반 인계). `include` 지정만으로 미테스트 파일이 자동 포함됨을 `@vitest/coverage-v8/dist/provider.js`에서 확인.
- `perFile` **미채택** — `events.ts`(0%)·`stats.ts`(branch 66.66%)가 둘 다 2팀 소유라, 켜면 남의 구현체 테스트 보강을 강제하는 파괴적 변경. I-94로 분리.
- 팀장 독립 재현: CLI로 임계 99/99 오버라이드 → `ERROR: Coverage for lines (88.36%) does not meet global threshold` + **exit code 1**. `events.ts` 0%가 분모에 정상 포함됨 확인.

### 2팀 — Task 023 GK 폴백
- `src/lib/sim/match/gk-fallback.ts` + 테스트 16케이스. `applySubstitution` 위임, `stableSortBy` 후 시드 추첨으로 동률 결정론.
- `eventIndex 3` 예약 — 팀장 실측 확인(tick.ts=0, events.ts=1·2, penalty.ts=예약 tick 1000). 충돌 없음.
- **재작업 ①** `GK_CROSS = 0.35` 리터럴 → 파라미터 주입 전환(§5.1).
- **재작업 ②** `crossPositionModifierSource: 'INJECTED' | 'DEFAULT' | null` 관측 필드 추가(§5.2). WARN 로그는 미채택 — 선행 5개 파일이 `console.*` 0건인 레이어 불변식 유지.

### 3팀 — Task 007 엠블럼 생성기
- `src/lib/naming/emblem.ts` + 테스트 26케이스. `crestSeed` 하나로 도형·배색·문양·색상 전부 결정, 외부 의존 0.
- 팀장 독립 검증: 결정론 200회, 시드 500개 분산, 외부 참조 부재, hex 형식 — 전부 통과. `clipPath id`를 `emblem-clip-${crestSeed}`로 시드 파생시켜 다중 인라인 시 DOM id 충돌 없음.
- **재작업** — 4팀 교차 점검 지적 2건 반영(§5.3).
- I-88 결정 반영 — 37번째 그룹 `NATIONALITY_WEIGHT` 추가(§4.1).

### 4팀 — Task 011 선행 문서 조사
- `docs/team-schedule/04-UI기반i18n팀.md` §8 신설. Next.js 16 문서 5건 정독·경로 기록(팀장이 5건 전량 실재 확인 — R-04 준수).
- H-10 수신 확인. 19일차 골격은 조기 생성하지 않음.
- **정정 2건** — `locale: false` 오독(§5.4), 일차 표기 오류.

### 6팀 — Task 032 공통코드·운영 7테이블
- `supabase/migrations/20260720044145_common_code_ops_tables.sql` — E-41~E-44, E-45~E-47.
- **13일차 §9.1 인계 이행** — `fixture.snapshot_id`(NOT NULL)·`season.snapshot_id`(NULL) → `sim_constant_snapshot(id)` FK 연결. 착수 전 `fixture` 0건 2회 확인.
- 파일명은 `list_migrations` 원격 채번값 사용(13일차 확정 컨벤션).
- 팀장 DB 직접 조회 검증: 7테이블 실재 / I-93 3컬럼이 `common_code`에만·`common_code_group`엔 0건 / FK 2건 정상 / `world_id` nullable 여부가 동결 타입 `config.ts:70`·`:149`와 일치.

## 4. 결정 사항

### 4.1 I-88 — 국적 비중을 공통코드 그룹으로 신규 추가 (사용자 판정)
- **결정**: 공통코드 37번째 그룹 `NATIONALITY_WEIGHT` 추가(D-17 원문 준수). `D-17` 결정문은 **정정하지 않음** — 구현을 결정문에 맞추는 방향.
- **3팀 권고는 반대였음**(정적 데이터 유지). 근거도 타당했다 — `_assertCatalogSize: 36`이 여러 팀 산출물에 박혀 있어 37번째 추가가 연쇄 재작업을 부른다는 지적. 분석이 틀린 게 아니라 우선순위 판단이 달랐던 것.
- 파급 분담: 3팀(`config`/`naming`) · 1팀(ROADMAP·devStep·TEAM) · 6팀(schema-design). **`docs/dailyWorkLog/1~13Day.md`는 수정 금지**(그 시점 사실 기록).
- 1팀이 **ROADMAP의 완료 `[x]` 체크박스 "36" 서술도 그대로 유지**하고 경위 노트만 추가 — 지시 없이 같은 원칙을 확장 적용한 것으로, 팀장이 확정. 37로 소급하면 "9일차에 37그룹을 만들었다"는 허위 기록이 된다.

### 4.2 I-83 — 스냅샷 주입 방식 (사용자 판정)
엔진 순수 함수는 `SimConstantSnapshot`에서 꺼낸 값을 **파라미터로 주입**받고, `loadConstants()` 직접 호출은 **엔진 밖 오케스트레이션 계층** 소관. 2팀 `docs/team-schedule/02-*.md:45`의 "호출만 합니다" 문구 정정 완료. 12일차부터 대기하던 건.

### 4.3 주입 누락 관측 (사용자 판정)
2팀 `gk-fallback.ts`의 조용한 폴백에 **관측 필드를 오늘 바로 추가**. 1팀 제안(`crossPositionModifierSource`) 채택.

### 4.4 팀장 확정 4건
- **perFile** — 15일차 2팀 작업("이벤트↔스탯 재계산 일치")으로 커버리지가 자연 해소되므로, **15일차 완료 후 재측정하고 그때 판단**(2팀 합의).
- **`get_advisors` 게이트화** — 15일차 3단 게이트에 넣지 않음(39테이블 RLS 미적용이 전부 걸려 예정된 미완료를 실패로 오판). H-22(68일차)·M-5(79일차) 직전 체크포인트로.
- **2번째 커버리지 그룹**(3팀 제안) — 오늘 `include` 확대하지 않음. 15일차 3단 머지 게이트에서 전 `src/lib/**` 범위를 한 번에 결정.
- **UI 테스트 전략** — 15일차 H-03 논의에 4팀 일정(23일차~)을 함께 반영(4팀 요청).

### 4.5 I-87 해소 (사용자 승인)
스트레이 디렉터리 `E:\claudeStudy\workspaces\football4`(7.7MB) 삭제 완료. 삭제 전 확인: 파일 20개 전량 Turbopack 빌드 캐시 청크, git 미추적, 마지막 쓰기 10:58. 원인 프로세스(PID 162980, 플래그 없는 `next dev`)는 이미 종료돼 있었고, 현재 살아 있는 `next dev --webpack`(CLAUDE.md 지정 정상 실행)은 유지.

## 5. 교차 점검에서 발견된 결함

> **1차 팀원 간 교차 점검에서 3건, 팀장 2차 검증에서 1건.** 팀원 간 점검이 더 많이 잡았다.

### 5.1 [팀장 2차] 2팀 — 공통코드 값을 리터럴로 소유 (I-95)
`GK_CROSS_POSITION_MODIFIER = 0.35`를 "H-05 목록 밖이라 리터럴 허용"으로 판단했으나, 팀장이 카탈로그를 직접 조회한 결과 **`POSITION_PROFICIENCY_MULT` 그룹에 등재된 값**(`catalog.ts:145-153`, `fallback.ts:117`)이었다. NFR-CFG-001 위반.
- 원인: H-05 핸드오프 표만 보고 36그룹 카탈로그 원본을 대조하지 않음(2팀 자체 기록).
- **연동 방향 역전 위험도 함께 발견** — 2팀은 "19일차 `position.ts`가 이 파일의 export 상수를 재사용"하려 했으나, `position.ts`가 구현할 대상이 바로 `POSITION_PROFICIENCY_MULT` 그룹 자체다. 공통코드가 정답인 값을 엔진이 소유하고 공통코드 구현체가 역참조하는 구조가 될 뻔했다.
- **전수 대조 2회 독립 수행** — 2팀은 `export const` 선언 기준, 팀장은 `fallback.ts` 실값 기준으로 접근해 **결론 일치**: 이 1건 외 위반 0건. 정수 39건 매칭은 전부 오탐(SHA-256·xoshiro 비트시프트 상수, `SECOND_HALF_END_MINUTE=90` 등).

### 5.2 [1팀 → 2팀] 주입 누락이 조용히 삼켜짐
`crossPositionModifier` 생략 시 경고 없이 기본값 폴백하고, 반환값에 출처 구분이 없었다. 오케스트레이션 배선에서 주입을 빠뜨려도 함수가 조용히 계속 동작 → **어드민이 `GK_CROSS`를 조정해도 반영되지 않는 상태가 소리 없이 지속**(FR-AD-004 무력화 경로). 프로젝트 관례(`fallback.ts`/`loader.ts`의 폴백 시 WARN)와도 어긋남.
- 1팀 분류가 정확했다: **정적 ESLint 룰로는 못 잡는 런타임 데이터플로우 문제** → Task 010은 정적 리터럴 검출에 집중하고, 이 계열은 관측 필드/리뷰 체크리스트로.
- 2팀 조치 시 **"우연히 0.35를 명시 주입한 경우"와 "생략해서 기본값이 된 경우"를 구분**하는 테스트를 별도로 넣음(값만 비교했으면 섞였다).

### 5.3 [4팀 → 3팀] `emblem.ts` 2건
- **다크모드 대비 불가** — 윤곽선이 `rgba(0,0,0,0.28)`로 SVG 문자열 내부에 하드코딩돼 CSS override 불가. **Task 012(26일차) 대비 4.5:1 검증에서 깨짐.** → `stroke="currentColor" stroke-opacity="0.28"`.
- **영문 `aria-label` 하드코딩** — D-18 위반이고 ko 로케일에서 스크린리더가 영어를 읽음. → 3팀이 4팀 권고보다 한 단계 더 나감: `role="img"`만 남기면 라벨 없는 이미지 노드가 접근성 트리에 잡혀 바깥 wrapper 라벨과 **중복 안내**될 수 있으므로 `aria-hidden="true" focusable="false"`로 **완전 장식화**(인라인 SVG 아이콘 표준 패턴). 소비처 계약을 JSDoc에 명시.
- 팀 색상은 다크모드 대상에서 제외(다크모드라고 팀 색이 바뀌면 오정보) — 3팀 판단, 팀장 동의.

### 5.4 [팀장 2차] 4팀 — `matcher: { locale: false }` 오독
4팀이 "15일차 설계 1순위 검토 대상"으로 올렸으나 **우리 구성에서 효과가 없다.**
- `proxy.md:104`의 "ignores locale-based routing"은 **Next 내장 i18n 라우팅**을 뜻하고, 같은 문구를 쓰는 `headers.md:335`·`redirects.md:300`·`rewrites.md:427`이 전부 **Pages Router** 가이드로 링크. App Router 가이드엔 `i18n` 블록이 아예 없다.
- 4팀이 재검증하며 **더 강한 근거를 찾아옴** — `node_modules/next/dist/server/config.js:171-175`에 `i18n configuration in next.config.js is unsupported in App Router` 경고가 `hasAppDir` 조건과 함께 실재(주석도 `// i18n deprecation for App Router`). 팀장 확인 완료.
- **4팀 자신의 (a)항 결론과 (b)항이 정면 모순**이었다 — "App Router엔 내장 i18n 없음"이라 해놓고 그 i18n 전용 옵션을 쓸 수 있다고 서술. 그대로 뒀으면 15일차 설계가 헛돌았다.
- `/admin` 배제는 `matcher.source` negative lookahead 또는 proxy 내부 분기로.

## 6. 신규/갱신 이슈

| ID | 요지 | 제보 | 상태 |
|---|---|---|---|
| **I-83** | 엔진의 공통코드 접근 경로 | 2팀(12일차) | **확정** — 스냅샷 주입(사용자 판정) |
| **I-87** | 스트레이 디렉터리 | 1팀·4팀 | **해소** — 삭제 완료(사용자 승인) |
| **I-88** | 국적/이름 풀 공통코드 미관리 | 3팀 | **해소** — 37번째 그룹 추가(사용자 판정) |
| **I-90** | coverage `all`/`include` 누락 | 1팀 | **해소** — `include`만으로 해결, `all`은 존재하지 않는 옵션이었음 |
| **I-94** | perFile 미채택 | 1팀 | OPEN — 15일차 후 재측정(2팀 합의) |
| **I-95** | 엔진이 카탈로그 등재값을 리터럴로 소유 | 2팀 | **해소** — 주입 전환 + 관측 필드 |
| **I-96** | shrimp 태스크 전역 삭제 충돌 | 1·3·4팀 독립 | **해소** — 재현 조건 규명 + 팀 규칙 명문화 |
| **I-97** | `world` 최초 1행 생성 담당 미배정 | 3팀·6팀 독립 | OPEN — **034a(20일차) 전 배정 필요** |
| **I-98** | 034a·031a 16일 일정 역전 | 3팀 | OPEN — 비차단(AS-13 안전망) |

### I-96 — shrimp 충돌 재현 조건
`split_tasks`의 `updateMode` **기본값이 `clearAllTasks`**이고 shrimp가 **팀/세션별로 분리되지 않은 전역 저장소**를 쓴다. 한 팀이 기본값으로 호출하면 **다른 팀 계획이 에러 없이 조용히 전부 삭제**되고, 사후에 `verify_task`가 "ID 없음"으로 실패해야 알아챈다. 1·3·4팀이 독립적으로 겪었고 실무 산출물 피해는 없었다.
→ **팀 공통 규칙**: 병렬 일차에는 전 팀이 `updateMode: 'append' | 'selective'`를 명시한다.

### I-97 — `world` 행 생성 담당 공백
`world_id`가 8개 테이블에 걸려 있고 7개가 NOT NULL이라 `world` 1행이 모든 도메인 데이터의 루트인데, ROADMAP·전 팀 일정표를 "월드 생성"으로 검색하면 **등장하는 곳이 전부 검증·E2E 항목**(Task 036 `ROADMAP.md:709`, SP-6 `:968`, Task 045)뿐이고 **생성 주체를 배정한 Task가 0건**이다. Task 021 "월드 리셋"은 기존 월드 삭제라 다르다. 3팀("절차적 콘텐츠 생성이 아님")·6팀("도메인 데이터 콘텐츠 미소유")의 "우리 몫 아님" 판단이 **양쪽 다 타당**했고, 그래서 아무도 안 맡은 진짜 공백이었다.

### I-98 — 일정 역전
6팀 034a(**20~25일차**)가 `setGlobalDefaultSource(supabaseAdapter)`를 등록하는데, 그 어댑터가 읽을 `common_code` 행은 3팀 031a(**36~38일차**)가 만든다 — 어댑터가 데이터보다 16일 먼저 존재. `loadConstants`의 DB→fallback 순서와 AS-13 안전망 덕에 기능 장애는 아니지만, 20~38일차에 "어드민이 DB에서 편집해도 시뮬에 반영되지 않는" 구간이 생긴다. **I-95와 같은 계열의 위험**(조용한 폴백을 정상으로 오인).

## 7. 팀장 조치 기록

1. **13Day.md §9의 "확정 4 판정 대기"가 낡은 기록임을 실측으로 확인** — 이미 13일차에 전부 반영돼 있었다(타입 이동, `catalog.ts` 잔존 0건, `tsc` 클린, schema-design §3.11·§9.3). 이 낡은 표를 근거로 6팀에 E-41/E-42 보류를 지시했다가 **즉시 철회**하고 1팀의 불필요한 재판단도 취소.
2. **팀장 판단 오류 2건을 팀들이 바로잡음** — ⓐ "15일차 Mock 팩토리가 FK에 막힌다"는 경고에 3팀·6팀이 **독립적으로 같은 반박**(`@supabase/*` 미설치 + Mock-First 원칙상 인메모리 TS 객체, DB INSERT 없음). ⓑ "공통코드 DB 적재가 미계획"이라 했으나 실제로는 031a(36~38일차)로 계획돼 있었다. 두 건 다 정정해 팀들에게 알림.
3. 보고를 그대로 믿지 않고 **독립 실측**: 커버리지 임계 실패를 CLI 오버라이드로 재현(exit code 1), 엠블럼 결정론 200회·분산 500시드 테스트 작성, DB를 직접 조회해 7테이블·FK·컬럼 배치 확인, Next.js 문서 5건 경로 실재 확인, `config.js` 경고 문자열 원문 확인.
4. **검증 중 팀장이 틀린 것 2건** — `Seed`를 문자열로 넣어 실패(오히려 입력 가드 정상 동작 확인), `clipPath id` 충돌 의심(시드 파생이라 문제없음).
5. 사용자 판정 3건 회부·반영(I-83·I-87·I-88 + 관측 필드).
6. I-88 파급을 **팀별 소유 경계로 분담**하고, 과거 일차 로그 수정 금지를 명시.

## 8. 다음 일차(15일차) 인계

| 팀 | 인계 사항 |
|---|---|
| **1팀** | Task 008 마지막 — **3단 머지 게이트 스크립트화**(`tsc`+`lint`+`test`). ⚑ H-03 인계. 이때 ⓐ 전 `src/lib/**` 커버리지 범위 결정(3팀 제안: 회계 항등식·배당 확률 합=1·시드 재현성이 sim 못지않게 중요) ⓑ **UI 테스트 전략 확정**(4팀 일정 23일차~ 감안) ⓒ 2팀 15일차 작업 후 **perFile 재측정**(I-94) |
| **2팀** | Vitest — 시드 스냅샷 100경기 전건 일치(NFR-QA-003), **이벤트↔스탯 재계산 일치**. 이 작업이 `events.ts`(0%)·`stats.ts`(66.66%) 커버리지를 실질 해소한다 |
| **3팀** | Mock 월드 팩토리 착수. `generateTeamEmblem(team.crestSeed)` 팀당 1회 호출 → `svg`는 렌더용, `colorPrimary`/`colorSecondary`는 `Team` 필드에 대입. 국적은 `SUPPORTED_NATIONALITY_CODES` **균등분포**로 시작(비중 실값은 031b) |
| **4팀** | Task 011 — 로케일 라우팅 전략 확정. **I-89**(`global-not-found` experimental 도입 여부) 결정. `/admin` 배제는 `matcher.source` 또는 proxy 내부 분기(`locale: false` 아님) |
| **6팀** | 13개 인덱스 + `fixture(status, kickoff_at)` 부분 인덱스. Task 032 RLS 1차 체크박스 |
| **팀장** | **I-97 소유자 배정(034a 20일차 전)** — 1팀이 후보 2안 제시(3팀 Mock 팩토리를 DB 단계로 확장 / 6팀 034a 시드 스크립트에 포함). 소환 지시문에 **shrimp `updateMode` 규칙**(I-96) 포함 |

## 9. 운영 메모

- **메시지 순서 엇갈림이 2팀·3팀에서 반복** — 팀장 지시가 팀원 작업 완료보다 먼저 발송돼 큐에 늦게 도착, 팀원이 "이미 반영했다"고 회신하는 상황이 3회. 병렬 6팀 메시징의 구조적 특성이며 양쪽 다 과실 없음. 다음 일차에는 지시 전 파일 상태를 재확인하는 편이 낫다.
- **`.claude/skills/agent-team-daily-sprint/SKILL.md`가 세션 도중 교체됨**(273줄 → 96줄, `SKILL copy*.md` 2개 생성). 워크플로우가 "팀원 간 1차 교차 점검 → **팀장 단독 검증**"으로 바뀌었으나, **오늘 결함 3건이 전부 1차 팀원 간 점검에서 나왔고**(§5.2·5.3) 팀장 단독으로 잡은 건 1건(§5.1)이며 오히려 팀장 오판 2건을 팀들이 바로잡았다(§7.2). 사용자 확인 전까지 기존 절차를 유지했고, 이 파일들은 14일차 산출물이 아니므로 커밋에서 제외했다. **사용자 판단 대기.**

## 10. 미해결·판정 대기

| 건 | 상태 | 기한 |
|---|---|---|
| **I-97** — `world` 행 생성 담당 | 소유자 배정 필요 | **034a 착수(20일차) 전** |
| **I-89** — `global-not-found` 도입 | 4팀 15일차 Task 011에서 결정 | 15일차 |
| **I-94** — perFile 채택 | 15일차 2팀 작업 후 재측정 | 15일차 종료 시 |
| **I-98** — 일정 역전 | 비차단, 인지 상태 유지 | 20일차 이후 관찰 |
| **SKILL.md 교체** | 사용자 판단 대기 | — |
| **Playwright 콘솔 스모크** | Chrome 확보 필요, 13일차부터 이월 | 15일차 |
