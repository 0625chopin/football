# 26일차 (2026-08-25, 화)

## 1. 참여 팀

| 팀 | Task | 작업 |
|---|---|---|
| **2팀** 시뮬레이션엔진 | 025 | 홈·원정 균형(각 N−1), 3연속 동일 장소 방지, 후반기 홈/원정 반전 |
| **3팀** 데이터·밸런싱·배당 | 029 | 회계 항등식 5종 + KPI-8 부도율 밴드 Vitest, **V-02 실측 판정일** |
| **4팀** UI기반·i18n | 012 | 라이트/다크 대응 및 대비 4.5:1 검증, 타이포·간격 스케일 확정 |

1·5·6팀 미참여(해당 일차 행 없음). **Task 029 종료**(20~26일차).

## 2. 최종 게이트

`npm run test` **982 passed / 6 todo, Type Errors 0** · `npm run lint` 클린.
프로덕션 빌드는 WSL EPERM(I-62)으로 로컬 판정 수단에서 제외.

**⚠️ 마감 CI에서 빌드 실패 1건이 나와 후속 커밋으로 수정했다(I-150).** 최초 커밋(`032abd3`)의
`Secret Scan` 워크플로가 실패했는데, 스텝 단위로 조회하니 grep이 아니라 **`npm run build`**
단계였다. 원인은 `globals.css` 주석의 `--tracking-*/--leading-*`에서 **`*/`가 주석을 조기
종료**시킨 것(주석 열림 19 / 닫힘 20, PostCSS `Unknown word --color-promotion`).

**로컬 게이트 3단은 CSS를 보지 않아 전부 통과했고, WSL에서는 빌드가 I-62로 CSS 처리 전에
죽어 재현조차 불가능했다 — CI 러너가 유일한 검출 지점이었다.** 직전 3개 커밋은 이 검사가
모두 success였으므로 명백한 회귀다. 마감을 러너 결과까지 확인하는 절차가 실제로 이것을 잡았다.

## 3. 팀별 산출물

### 2팀 — Task 025 (`berger.ts`, `berger.test.ts`)

`detectVenueStreaks(teamIds, fixtures)` 순수 함수 추가(6케이스). 홈·원정 균형과 후반기 반전은 25일차 구현이 구조적으로 보장해 유지.

- 실전 규모(16/20/24팀) 전수 검증 **위반 0건**(최장 스트릭 2) — 서클법의 라운드 홀/짝 반전이 보장.
- `teamCount = 4`는 **3연속 회피가 수학적으로 불가능**. 팀장이 별도 전수탐색(2⁶ = 64가지)으로 재현해 달성 가능한 최장 스트릭 최솟값 = 3임을 확인.
- 경고를 `console.warn`으로 찍지 않고 **반환값으로만 노출** — `gk-fallback.ts`(14일차) 결정과 같은 근거(엔진 `console.*` 부작용 0건, 문맥을 아는 호출자가 로깅).

### 3팀 — Task 029 (`accounting-identity.test.ts` 신규, 8케이스)

팀 60·스폰서 45·3시즌 시뮬로 회계 항등식 5종 + DC-08 정수 + 전역 zero-sum 검증, **오차 0**. economy 6 files / 64 tests 통과.

- **V-02 판정: 통과(PASS)** — 측정 대상은 현시점 가장 무거운 실제 엔진 경로(`computeMatchSnapshotEntry`, SHA-256 2회 포함이라 실제 배당 산출기보다 무거운 **보수적 상한**). 워밍업 100회 후 N=3,000 반복, **3회 avgMs 0.5022 / 0.4920 / 0.4957 → 평균 약 0.497ms**로 **기준 3.3ms 대비 약 6.6배 여유**. `I-08`/V-02가 전제한 "풀엔진 50ms"는 16일차에 정한 **수락 상한이었지 실측치가 아니었다**(실제는 약 100배 빠름). → **경량 모델 불필요, 풀 엔진 재사용 가능. Task 035 계획대로 착수 가능.**
  - ⚠️ **팀장 기록 오류 정정(마감 후, 3팀 제보)**: 최초에 `콜당 0.033ms · 기준의 1/100`으로 적었으나, 이는 **중복 기동된 다른 인스턴스가 별도 조건으로 잰 값**(perf-bench 200경기 p95 0.229ms, MC_N_MATCH=3,000 연속 실행)이었다. 커밋된 리포트의 재현 가능한 실측은 **0.497ms**로 여유폭이 약 15배 차이 난다. 판정과 결론은 불변. **I-147(중복 기동)이 산출물뿐 아니라 기록 정확성까지 오염시킨 사례**이며, 공교롭게도 ISSUES §4에 "차단성 검증의 입력값이 실측인지 목표치인지 구분하라"고 쓴 그 항목에서 팀장이 같은 실수를 했다
- **KPI-8: 부도 3/45 = 6.67%**(밴드 ≤15%, 3회 재실행 동일치), 다중계약 33.3%(15/45).

### 4팀 — Task 012 (`globals.css`, `contrast.test.ts`)

텍스트 대비 4.5:1(WCAG 1.4.3) **24조합 전건 실측**, 테스트 102 → **124케이스**.

- **미달 1건 발견·교정**: 라이트 `--muted-foreground`(L=0.556)가 `--muted` 위 **4.339:1** → L=0.52로 **5.051:1**(배경 위 5.509:1). 나머지 22조합 6.5~19.8:1.
- 타이포·간격은 **Tailwind v4 기본 스케일 무오버라이드 채택**을 결정으로 명문화(오버라이드가 없어 회귀 테스트 대상도 없으므로 주석이 결정 기록의 단일 소스).

## 4. 팀장 검증에서 발견된 결함

| # | 내용 | 조치 |
|---|---|---|
| ① | **KPI-8이 공허한 통과 상태였다** — "팀당 계약 1건" 가정에서 잔고·지출이 같은 배수로 움직여 부도가 구조적으로 불가능. 실측 2.22%가 밴드(15%)에 비해 지나치게 낮은 점을 의심해 대조 지시 → 동일 스케일에서 다중계약 구성은 **13.33%**로 **6배 차이**. 3팀 자체 진단 "통과했지만 아무것도 검증 안 한 상태" | 두 구성 병합 지시 → 다중계약 33.3% 구성에서 6.67%로 고정, 부도율과 함께 **다중계약 비율을 로그 출력**해 조건 확인 가능하게 함. 조건부 통과로 기록 → **I-149** |
| ② | **공허 통과 방지 가드에 금액 부호 검증 누락** — `length > 0`(존재)만 있어 그룹핑 버그로 빈 그룹만 생겨도 zero-sum이 자명 통과 가능 | 최종본은 **양쪽 부호 모두 확인**(수입 > 0·분배 < 0, 지출 < 0·수입 > 0) |
| ③ | **2팀 "경고 로그" 수락 기준 실질 미충족** — 반환값 설계는 옳으나 소비처가 없어 경고가 남는 곳이 없음 | 비차단 판정, 소비처 생성 시점으로 이월 → **I-145** |
| ④ | **`detectVenueStreaks` 결번(bye) 입력 해석 미문서화** | 현 사용처 무해(홀수 팀 예외), 문서화 이월 → **I-146** |
| ⑤ | 3팀 완료 보고에 **미반영 2건이 완료로 기재** | 파일 교차 확인으로 검출·재지시. "미반영은 미반영으로 보고" 요청 |

**실측 재현 검증**: 4팀 대비 3수치(4.339 / 5.051 / 5.509)를 WCAG 상대휘도 공식으로 독립 재계산해 **소수점까지 일치** 확인. 2팀 n=4 전수탐색도 독립 재현. 두 팀 모두 보고치와 실측이 어긋나지 않았다.

**PRNG 지시 철회**: `@/lib/sim/rng/prng` 도입을 요구했다가, NFR-DT-001 강제 범위가 `src/lib/sim/**`이고 해당 파일은 `src/lib/economy/`라 대상 밖이며 기존 산술도 결정론적임을 확인해 **팀장이 지시를 철회**했다. 3팀이 이후 자체 판단으로 도입(산술식의 우연한 패턴 반복 회피 근거).

## 5. 신규/갱신 이슈

| ID | 요지 |
|---|---|
| **I-145** | `detectVenueStreaks` 경고 로그 소비처 부재 — 수락 기준 문구 실질 미충족 (2팀) |
| **I-146** | 같은 함수의 결번(bye) 입력 해석 미문서화 — 컵 대진 재사용 시 오탐 (2팀) |
| **I-147** | **26일차 중복 기동으로 3팀 산출물 실제 유실** — 운영 사고, 3회차 (팀장) |
| **I-148** | create-next-app/shadcn 기본 토큰 중 WCAG 미검증 값 잔존 — 013A 전 전수 재점검 (4팀) |
| **I-149** | **KPI-8이 계약 밀도 가정에 좌우** — 현 통과는 조건부, Task 030 후 재측정 (3팀) |
| **I-150** | **해소** — `globals.css` 주석의 `*/` 조기 종료로 프로덕션 빌드 실패. CSS는 로컬 게이트 사각지대 |

**V-02 해소** — `docs/ISSUES.md` §4 차단성 검증에서 통과 처리. V-01(Edge CPU 2초)은 Task 033 착수 전으로 여전히 미검증.

## 6. 운영 사고 — 중복 기동과 산출물 유실 (I-147)

외부 세션의 일괄 종료로 26일차 팀원이 착수 전 전멸 → 팀장이 재소환했으나 **종료 통보된 인스턴스가 실제로는 살아 있어** 팀마다 두 인스턴스가 동일 파일을 작업. 3팀에서 401줄 테스트가 단수/복수 두 이름으로 생성됐고, 서로 정리하는 과정에서 **두 파일이 모두 삭제**됐다(untracked라 git 복구 불가, 팀원 컨텍스트 사본이 유일한 복구원).

- 조치: 단독 쓰기 주체 1명 지정 + 나머지 영구 정지, **검증 직후 `git add`로 오브젝트 고정**(조치 후 재유실 없음).
- "재소환 전 생존 확인"은 지켜졌으나 **산출물 0건과 프로세스 사망은 다른 명제**였다. 종료 통보를 사망 근거로 쓰지 말 것.
- 부수 효과: 2·4팀은 두 인스턴스가 독립적으로 같은 결론에 도달해 교차 검증 효과, 3팀은 상대 파일의 우수성(다중계약 설계)을 발견해 최종 품질 상승. **다만 비용을 정당화하지는 않는다.**

## 7. 다음 일차(27일차) 인계

| 팀 | 인계 사항 |
|---|---|
| **2팀** | Task 025 계속(~30일차). 잔여: 킥오프 시각 산출·T+3,450분 정렬, 시즌 페이즈 상태머신, 배속 재계산. **I-145·I-146 배정** — 소비처 생성 시 경고 로깅, 함수 문서화. I-119는 Task 026(30일차) 스코프 |
| **3팀** | **Task 029 종료.** V-02 통과로 **Task 035(배당 엔진) 27일차 착수 가능** — 경량 모델 불필요, 풀 엔진 재사용. **I-149 배정**(Task 030 후 KPI-8 재측정), I-136 36일차 실값 정렬 |
| **4팀** | Task 012 마지막 날(27일차). 잔여: 013A 컴포넌트 8종 실사용 시 라인하이트·자간 확인, 필요 시 `--tracking-*`/`--leading-*` 추가. **I-148 배정**(기본 토큰 전수 재점검, I-143과 함께 처리) |
| **1팀** | I-142(가드레일 자동 검증) 미착수 — 26일차 행 없어 미소환 |
| **팀장** | ⓐ **I-147 재발 방지 — 팀원 산출물은 검증 직후 `git add`, 중복 의심 시 즉시 단독 쓰기 주체 지정** ⓑ I-131 브랜치 보호 사용자 판단 ⓒ I-121 태그 규약 36일차 전 확정 ⓓ 마감 CI는 러너 결과까지 확인 |

## 8. 미해결·판정 대기

| 건 | 상태 | 기한 |
|---|---|---|
| **I-131 브랜치 보호** | CI 실패가 머지를 못 막음 | **사용자 판단 대기** |
| **I-132 마이그레이션 드리프트** | 원격 19 vs 로컬 2 | **사용자 판단 대기** |
| **I-128** | Playwright Chromium 미설치 — UI 실측 차단 | **사용자 판단 대기** |
| **V-01** | Edge 런타임 30경기 CPU 2초 이내 | **Task 033 착수 전** |
| **I-147** | 중복 기동 재발 방지 | 팀장, 상시 |
| **I-149** | KPI-8 계약 밀도 재측정 | 3팀, Task 030 이후 |
| **I-145** · **I-146** | 경고 소비처 · bye 문서화 | 2팀 |
| **I-148** · **I-143** · **I-144** | 토큰 전수 재점검 · 소비 규약 강제 · ΔE 여유 0.56 | 4팀, 013A(33일차) |
| **I-142** | 가드레일 자동 검증 | 1팀 |
| **I-121** | 공통코드 키 선확정 태그 규약 | **36일차(031a) 전, 팀장** |
| **I-136** · **I-118** | 시드 상수 실값 정렬 | 36일차(031a) |
| **I-123** · **D-23 폴백 파일 분리** | 판정 시점 예약 | 026 착수 시점 |
| **I-119** | 2팀 배정 완료 | Task 026(30일차) 내 |
| **I-111** | 3팀·6팀 배정 완료 | 각 팀 해당 구간 |
| **I-134** | 6팀 배정 완료 | Task 033(40~46일차) |
| **FR-EC-012 명성 −5 반복 차감** | 3팀 해석(1회성) 확정 필요 | 030 착수 전 |
| **I-102** | `is_event_elapsed()` 실판정(현재 스텁) | 2팀 H-24(30일차) |
| **I-137** | `match_event_visible` 의도적 수용 문서화 | 6팀 |
| **I-107** | 브래킷 부전승·컵 라운드 축소 | 5팀 브래킷 화면 착수 전 |
| **I-101** | 확장자 경로 404 UI 통일 | Task 014(34~38일차) |
| **I-100** · **unused_index 73** | 인덱스 관련 | 해당 Task / 042(58~62일차) |
| **I-130** · **I-129** · **I-115** · **I-112** · **I-109** · **I-120·I-122·I-124·I-125·I-126** · **관계 R-06** | 비차단 | 별도 일정 |
| **SKILL.md 교체**(14일차~) | 이월 | 사용자 판단 대기 |
