# 50일차 (2026-09-28, 월)

**참여 팀**: 2팀(시뮬레이션엔진) · 4팀(UI기반i18n) · 5팀(화면배팅UX) · 6팀(DB인프라)
**미참여**: 1팀 · 3팀

**한 줄 요약**: Task 028(시즌 종료 순위 포인트) · 048(`/playoffs` 인덱스) · 018(선수 상세 2/2) 완료.
SP-4 크론×엔진 통합 스모크는 **소비 화면 미존재로 불성립**(6팀 결함 아님, I-256).

---

## 1. 팀별 작업

### 2팀 — Task 028 시즌 종료 순위 포인트

- `src/lib/sim/season/prize.ts` 신설. `resolveLeagueFinishPrizes(entry, table?)`가 리그 전 팀
  (승격/강등 구분 없이 전 순위)에 대해 `{teamId, leagueId, finalRank, award:{amount, reasonCode:'LEAGUE_FINISH'}}`
  반환. **반환 배열 길이가 항상 `league.teamCount`와 일치**하는 것이 "원장 기록 누락 0"의 근거.
- 곡선은 FR-EC-002 확정값 — `P(r) = round(Base + Range × ((N−r)/(N−1))^1.8)`,
  L1 1500+1500 / L2 850+950 / L3 400+600. I-83 패턴대로 `LEAGUE_FINISH_POINT_DEFAULT`
  안전 기본값 사용, `loadConstants()` 직접 호출 없음.
- tier·순위 완전성 위반 시 `RangeError`/`Error`.
- 변경: `src/lib/sim/season/prize.ts`, `prize.test.ts` (둘 다 신규)
- 본인 테스트: `src/lib/sim` 전체 46파일 736건 통과 · typecheck · eslint 클린

### 4팀 — Task 048 `/[lang]/playoffs` 인덱스 신설 (I-223)

- `getLeagues()` × 리그별 `getPlayoffBracket({leagueId})`로 리그 카드 3개(슬롯 수 · 생성 라운드 수 ·
  다음 경기 또는 최종 라운드 결과) + `/cup` 별도 섹션. **`BracketTree` 미사용** — 인덱스는 트리가
  아니라 "리그 3 + 컵 1" 4갈래 대회 선택 화면.
- 승자 판정만 `BracketTree.tsx`의 `resolveBracketWinnerSide()` 재사용(중복 구현 회피).
  요약 계산은 화면 로컬 순수 함수 `summarizePlayoffBracket`로 호출부에서 종료, 카드는 계산값만 수신.
- 미래정보 비노출(C-23) — 스코어는 **종료된 최종 라운드에만** 노출되고, LIVE 경기는 자연히
  "다음 경기" 분기로 들어가 점수를 내지 않는다.
- I-227(mock 리그1 WC 절삭)로 라운드 수가 명세와 달라도 **어댑터 반환값을 정직하게** 렌더.
- 변경: `playoffs/{page,error,loading,not-found}.tsx` 신설, `i18n/messages/{ko,en}/match.ts`
  `playoffsList` 그룹 추가(기존 키 최대 재사용, 신규 중복 키 0)
- **팀장 피드백 반영(재수정 1회)**: `layout.tsx` `NAV_GROUPS`의 `playoffs` `pending` 제거 → 2절 참조
- 본인 테스트: typecheck · lint 0 · dev `/ko|en/playoffs` 200, 카드 3개 + `/cup` 링크 렌더

### 5팀 — Task 018 선수 상세 2/2 (E5~E9) + I-244

- 몸값·계약(E5), 시즌별/통산 스탯(E6, **`평점` 열 = `avgRating`**), 성장 곡선(E7),
  부상 타임라인(E8), 트로피·이적 이력(E9) 배선. lg 2컬럼 레이아웃.
- **PA 원값 비노출** — 스카우트 등급 ★1~5만(FR-PL-004).
- **I-252**(은퇴 선수 표기 규약)는 판정 전이라 49일차와 동일하게 **empty 폴백만** 적용, 별도 UI 없음.
- **자체 회귀 발견·수정**: 신규 lg 2컬럼이 49일차 E4(포지션 리스트)의 md 전환과 겹쳐 글자 단위
  줄바꿈 발생 → md→xl로 완화 후 재확인.
- **I-244 해소**: `matches/[matchId]/timeline.test.ts` 신설(6건) — 정렬 순서·ASSIST 병합·
  같은 분 다중 골 케이스.
- 변경: `players/[playerId]/page.tsx`, `PlayerStatTable.tsx`(신규), `TransferHistoryList.tsx`(신규),
  `i18n/messages/{ko,en}/player.ts`, `matches/[matchId]/timeline.test.ts`(신규)
- 본인 테스트: typecheck · lint 0 · `npm run test` 1718 통과 · dev 실측(ko/en, 1280·375px)

### 6팀 — Task 033 SP-4 스모크 → **불성립 (사실 보고)**

- `/ko/admin/scheduler` 실렌더 결과 본문이 `{"route":"/[lang]/admin/scheduler","lang":"ko"}` JSON뿐 —
  11일차 Task 005 골격 그대로. 실행 이력·중단 구간·밀린 라운드 표시가 없어 **수락 기준 4종 전부 측정 불가**.
- 지시대로 억지 구현하지 않고 사실만 보고. → I-256 등재.
- 크론 인프라 자체(6팀 몫)는 45~46일차 완료분이 그대로 존재함을 확인: `tick_run()` gap-detect
  마이그레이션 `20260721133219` · `cron_run`/`cron_gap` 테이블 · `/api/health` 배선.
- **I-214 준수 확인** — `cron.job` relation 부재(pg_cron 미등록)로 실 스케줄 미점등.
  `cron_run` 21건(SUCCESS 15 · PARTIAL 1 · FAILED 0 · NOOP 5)은 과거 수동 호출 잔재, `cron_gap` 0건.
- 변경 파일: **없음**

---

## 2. 팀장 검증

**전역 게이트** — typecheck 통과 · lint 0 · `npm run test` **127파일 1718건 통과 / 0 실패**.

**수락 기준·규약 직접 확인** (팀 보고 재현이 아니라 팀장 재실행):

| 항목 | 결과 |
|---|---|
| PA 원값 비노출 (5팀 수락 기준) | `players/**` 전체 `potentialAbility` grep **0건** ✅ |
| E6 `평점` 열 실값 | 선수 상세 실렌더에서 `7.30` 확인, ★ 8개 렌더 ✅ |
| sim 순수성 NFR-DT-001 (2팀) | `prize.ts` 위반 0건(주석 언급만) ✅ |
| 곡선 상수 명세 대조 (2팀) | L1 1500+1500 / L2 850+950 / L3 400+600 / EXP 1.8 **일치** ✅ |
| 서브경로 type import 금지 C-5·C-6 | 신규 3경로 전부 0건 ✅ |
| `sm`/`xs` 브레이크포인트 오용 I-184 | 신규 화면 0건 ✅ |
| ko/en 키 대칭 | `match.ts` 165=165 · `player.ts` 185=185 ✅ |
| 신규 라우트 실렌더 | `/ko|en/playoffs` · `/ko|en/players` 전부 200 ✅ |
| I-244 테스트 | 팀장 단독 재실행 6건 통과 ✅ |

**발견 결함 1건 → 4팀 피드백 → 재수정 → 재검증 통과**:

`/playoffs` 화면이 생겼는데 `layout.tsx`의 `NAV_GROUPS`에서 `playoffs`가 여전히 `pending: true`라
**앱 내에서 눌러 들어갈 수 없는 상태**였다 — 49일차 선수 상세와 동일한 "화면은 있으나 도달 불가".
H-28이 `pending` 제거를 62일차로 미룬 근거는 "`layout.tsx`가 4팀 단독 소유라 화면 담당팀이 직접
못 지운다"였는데, **이번 화면은 4팀 본인 산출물이라 그 전제가 성립하지 않는다.** 44일차 `leagues`가
인덱스 신설과 동시에 `pending`을 뗀 선례와 동일 처리하도록 지시.
→ 4팀이 `playoffs`만 제거(+근거 주석), `matches`·`teams`는 유지. 재검증에서 `href="/ko/playoffs"`
활성 링크 렌더 확인, typecheck·lint 재통과.

---

## 3. 이슈

**신규 4건** (`docs/ISSUES.md` 3.3절):

| ID | 요지 | 소유 |
|---|---|---|
| **I-256** | SP-4의 6팀 수락 기준이 일정상 성립 불가 — 소비 화면(`/admin/scheduler`)이 **5팀 Task 021 / 58일차**로 SP-4보다 8일차 뒤 | 팀장/일정 |
| **I-257** | FR-EC-002 순위 포인트가 `economy/salary.ts`와 `sim/season/prize.ts` **두 곳에 독립 구현** | 2·3팀 조율 |
| **I-258** | `season/rebuild.ts`가 `Standing.points`(승점)를 "1위 최종 포인트"(상금 화폐)로 사용 중일 가능성 | 2팀 발견, 소유 판정 필요 |
| **I-259** | 선수 상세 잔여 공백 4건(트로피 현 소속팀만 · `competitionType`/`transferType` 카탈로그 부재 · E6 대표 지표만 · md~lg 티어 생략) | 5팀 |

**해소 1건**: **I-244**(`timeline.test.ts` 신설, 6건 통과)
**부분 해소 1건**: **I-223** 3차 — `/playoffs` 인덱스 신설 + `pending` 제거. 잔여 `pending` **2건**(`matches`·`teams`)

> I-257·I-258은 **둘 다 "실지급 배선 전에 결론"** 조건이라 같은 패스로 처리하는 것이 효율적이다.
> 현재 I-257의 두 구현 모두 **프로덕션 호출자 0건**(테스트 참조만)이라 지금 깨지는 것은 없다.

---

## 4. 다음 일차 인계

1. **I-256 판정이 51일차 6팀 등판 전에 필요**하다. 팀장 예비 견해는 **ⓑ(DB 직접 질의로 수락 기준 재정의)** —
   4종 지표의 원천은 `cron_run`/`cron_gap` 테이블이고 화면은 표시 계층일 뿐이라, 스모크의 본질인
   크론×엔진 결합 검증은 화면 없이도 성립한다. ⓐ(58일차 이후 재배치)는 SP-4를 8일차 지연시킨다.
2. **51일차 6팀**은 48·49일차 인계 그대로 — `database.types.ts` 재생성 + `mapper.ts` 로컬 캐스트 3곳 제거
   + `SupabaseDataSource` 스텁 3종 정리 + **I-243 판정**, **I-246 재시도 정책 확인도 함께 묶기 권장**.
3. **51일차 4팀**은 Task 048 잔여(2/2). 인덱스 본체는 50일차에 끝났으므로 잔여 스코프를 팀장이 먼저
   확정할 것 — 화면이 이미 수락 기준을 충족해 **51일차 몫이 비어 있을 수 있다**.
4. **I-257 + I-258을 한 패스로.** I-257의 세 안(ⓐ import 허용 후 위임 / ⓑ 상수 단일화 + 대조 테스트 /
   ⓒ 역할 분리 명문화) 중 택일이 필요하며, **`sim → economy` import 선례가 저장소 전체 0건**이라
   계층 규약을 새로 여는 판단이다(팀 소유 규칙은 *편집* 금지이지 *import* 금지가 아님을 팀장이 확인).
   FR-EC-002 원문 표의 **±1 불일치 5구간**(L1 rank5·7, L2 rank11·13·16) 확정도 같은 패스에서.
5. **I-259 ⓑ는 I-249와 함께 판정**하면 카탈로그 확장 1회로 끝난다(동일 패턴, 이미 두 건).
6. **5팀 018 후속은 53일차 유지**(팀장 승인 확정). **51일차 종료 시점에 클럽 상세 1/2(F3-o 구단주 카드
   포함)이 미완이면 즉시 보고** → 팀장이 완화 3종 중 택일. 5팀 자체 판단으로 일정·스코프 변경 금지.
7. **I-252**(은퇴 선수 표기 규약)는 여전히 미판정. 50일차도 empty 폴백으로 우회했고, 세 번째 우회가
   나오기 전에 판정하는 것이 좋다.
8. **I-223 종결은 62일차 그대로** — 단 잔여가 `matches`·`teams` 2건으로 줄었고, 두 화면(Task 047·046,
   60·61일차)이 끝나는 즉시 같은 날 `pending`을 제거하면 62일차 소급 자체가 불필요해진다.
   **화면을 만든 팀이 4팀이면 당일 제거가 원칙**(50일차 선례).
9. **I-229 잔여**(3곳 `selectLineup()` 연결 + I-34 LIVE 컷오프 배선)는 여전히 **Task·일차 미배정**.
10. **I-245 · I-240**(일정 드리프트·소급 반영 확인)은 별도 동기화 패스로 이월. I-256이 같은 계열
    (일정표와 실제 순서 불일치)이라 **함께 보는 것이 효율적**이다.

---

## 5. 미해결·판정 대기

- **I-256** SP-4 수락 기준 재정의 — 팀장, 51일차 6팀 등판 전 판정 권장
- **I-257 · I-258** 순위 포인트 중복 구현 · 리빌드 보조금 단위 — 2·3팀 조율, 실지급 배선 전
- **I-259** 선수 상세 잔여 4건 — 5팀, ⓑ는 **I-249와 동시 판정**
- **I-252 · I-251 · I-250 · I-248 · I-247 · I-246 · I-245** 49일차 신규 — 변동 없음
- **I-255** 전역 경기 조회 계약 — 1팀, 034b 이후 배치 재검토
- **I-244** ✅ 해소(50일차) / **I-223** 3차 부분 해소, 종결은 62일차 `pending` 0건 확인 시
- **I-230** 넉아웃 ET 판정 오케스트레이터 부재 — 소유팀 미정, I-214 해제 조건의 일부
- **I-229 잔여**(3곳 `[]`) · **I-241**(MOCK_NOW 고정) — 3팀, Task·일차 배정 필요
- **I-243** 마이그레이션 채번 불일치 — 6팀, 51일차 판정
- **I-240** ROADMAP 일정 드리프트 — 별도 동기화 패스(**I-245 · I-256과 함께**)
- **I-226** 중계 문구 배선(5팀) / **I-237** D1 스코어보드 실시간성(5팀)
- **I-235** 공유 트리 git 조작 지침 명문화 — **48·49·50일차 연속 사고 0건**
- **I-236** `homeModifier` 공식 미확정(2팀) / **I-234(c)** 1팀 확인 대기
- **I-192** 규약 판정 / **I-228 · I-227** I-50 종속 / **I-225** 담당·일차 미배정
- **I-224 · I-215 · I-217** 1팀 소관 / **I-214** 크론 점등 금지(차단성, 50일차 준수 재확인) / **I-218 · I-208** 배정 대기
- **I-232 · I-216 · I-220** 표기·구조 정합 / **I-233** 중립지 구장 / **I-212 · I-209 · I-211 · I-204 · I-205** 비차단
- **I-200~I-203** 프로덕션 빌드 병목 / **I-189~I-191 · I-194** 값·규칙 정합 / **I-193 · I-197** 031b
- **I-195 · I-196**(재실측 11일 연속 미이행) · **I-198 · I-199** 변동 없음
