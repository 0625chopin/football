# 36일차 (2026-09-08 화)

## 1. 참여 팀

**2팀 · 3팀 · 4팀 · 5팀** (1·6팀은 배정 없음).

이 일차는 두 갈래로 진행됐다.
- **팀장 단독 — Task 013C 비주얼 디렉션 개편**(사용자 지시 "화면이 너무 밋밋하다"). 배정에 없던 삽입 작업이며 4·5팀 소유 경로를 넘나들었다. 커밋 `467039c`(디자인) · `bcd1542`(판정 3건).
- **팀 스프린트 — 배정분 4건.** 위 개편이 4·5팀 작업 파일을 먼저 바꿔 놓았으므로, 소환 시 변경 범위를 전달하고 **최신 파일 기준 작업 + 디자인 되돌리기 금지**를 명시했다.

---

## 2. 최종 게이트 (팀장 직접 실행)

| 항목 | 결과 |
|---|---|
| `npm run typecheck` | 통과 |
| `npm run lint` | 통과 (0 errors / 0 warnings) |
| `npm run test` | 통과 — 95 files / **1340 tests** / Type Errors 0 |

> 프로덕션 빌드는 CLAUDE.md대로 검증 수단으로 쓰지 않는다(WSL EPERM).

---

## 3. 팀장 단독 — Task 013C 비주얼 디렉션 "Floodlit"

**원인**: 24~26일차 Task 012가 확정한 것은 시맨틱 5색·브레이크포인트·타이포뿐이었고, **중립색은 create-next-app 기본값(채도 0 순수 회색) + 차트 5종이 명도만 다른 회색**으로 남아 있었다. 34~35일차 첫 실렌더에서 그 상태가 드러난 것이다.

**명제**: "지금 계산되고 있는 가상 리그 세계를 *중계로* 지켜본다" → 화면 재질을 대시보드가 아니라 **중계 화면 + 매치데이 프로그램**으로 잡았다.

**핵심 판단 3**
- 브랜드 브라이트 = **조명 호박색**(hue 68~70). 초록을 쓰지 않는다 — `--promotion`과 겹치면 시맨틱 색의 구분력이 희석된다.
- 중립 = 야간 잔디 잉크(hue 172~180, C 0.006~0.03).
- `--board-*` = **라이트/다크 공통으로 항상 어두운 중계 표면**(경기장은 OS 테마와 무관하게 늘 야간이다).

색상 값은 코드 작성 **전에** `contrast.test.ts`와 같은 수식으로 전수 실측했다. 최소 여유는 다크 `--relegation` 대 배경 3.599:1(하한 3). 시맨틱 5색(ΔE 여유 0.56, I-144)은 **일절 미변경**.

**구조 변경**: 홈 → 라이브 보드 히어로 / `MatchCard` → 세로 스코어보드(+`surface` prop) / 사이드 내비 → 3그룹 + **활성 표시 신설**(종전 없음) / `MobileNav` 신설 / **4상태 60파일 → 공용 껍데기 3종**(`RouteLoading`·`RouteNotFound`·`RouteError`).

**함께 고친 결함 6건**(I-170 로케일 근사 미적용 19곳 / `global-not-found` 폰트 미주입 / `PitchLineup` 색 하드코딩 / 데이터에 브랜드색 사용 / `<main>` 중첩 / 푸터 미고정) — 상세는 커밋 `467039c` 메시지 참조.

> 디자인 근거의 단일 소스는 `src/app/globals.css` 상단 주석이며, 요약은 ROADMAP Task 012 후속 절 · CLAUDE.md 스타일링 절에 있다.

---

## 4. 팀별 산출물

### 4-1. 2팀 — Task 026 승강 경계 동률 TIEBREAK

`playoff-tiebreak.ts`(신규) `detectBoundaryTiebreaks()` / `tiebreak.ts`에 `groupStandingsBeforeSeedDraw()` 추가(기존 export 무변경) / 테스트 9건.

설계 판단 2가지가 규약을 지켰다 — ⓐ **경계 순위값을 리터럴화하지 않고 호출자가 `StandingBoundary[]`로 주입**(NFR-CFG-001) ⓑ **브랜드 ID를 발급하지 않고 `TiebreakFixtureDraft` 초안만 반환**(`berger.ts`의 `BergerFixture`와 동일 패턴, `brand.ts` 규약). 테스트에 `homeTeamId` 입력순서 무관·재현성 검증이 포함된 것도 이 계층에 맞다.

### 4-2. 3팀 — Task 031a 공통코드 시드

`supabase/seed/common-code.sql`(신규, 594행, **DB 미적용**). 38그룹 메타 전량 + 값이 있는 33그룹 155건.

- **35일차 로그의 폴백 경고 7그룹 전량 포함** 확인(팀장 실측).
- **`UI_PARAM`만 `fallback.ts`와 의도적으로 다르게** 했다(5000/3000 vs 30000/15000). 근거가 정확하다 — `fallback.ts` 스스로 30000/15000을 "장애 시 비용 안전망 전용값"이라 밝히고 **"정상값은 6팀이 031a(36일차)를 적재한 뒤 전역 기본값 소스가 공급한다"**고 명시한다. `ODDS_PARAM`은 안전망이 아니라 확정 개선값(I-08/I-167)이라 그대로 재사용 — 이 구분도 맞다.
- 값 없이 구조만 서술된 5그룹은 **억측 없이 그룹 메타만** 등록 → §6 I-187 참조.

### 4-3. 4팀 — Task 014 로케일 전환 컨트롤

`LocaleCompareToggle.tsx`(신규) / `sample/page.tsx` 본문을 `renderShowcaseBody(locale)`로 감쌈 / `sample.ts` 키 2종.

기각 근거 2가지가 좋았다 — ⓐ **헤더 `LocaleSwitcher` 재사용 기각**: 라우트 이동은 서버 재실행 + RSC 왕복이라 "즉시"가 아니다 ⓑ **CSS `hidden` 동시 마운트 기각**: `ShowcaseSection`의 고정 `id`가 앵커 대상이라 두 트리를 동시에 두면 **앵커가 숨겨진 사본을 가리킬 수 있다**. 조건부 마운트를 택했다.

이슈 후보를 올리기 전 `src/types/ops.ts`를 확인해 "번역 비대상 자유 텍스트"임을 근거로 자진 철회한 것도 규약(판정 전 타입 확인)대로다.

### 4-4. 5팀 — Task 015 폴링 + I-182

`api/live/matches/route.ts`·`types.ts`(신규) / `LiveMatchGrid.tsx` fetcher를 `fetch()` 경유로 교체.

Route Handler는 서버에서 `getLiveFixtures`+`getMatchClockContext`만 조회해 원시 데이터를 반환하고, 팀·리그명 매핑과 `computeElapsedMinutes`는 초기 SSR 맵을 재사용해 클라에서 처리(응답 경량화). **1팀 응답 타입 계약이 미확정이라 `LiveMatchesApiResponse`를 임시 정의**하고 파일 헤더에 교체 대상임을 명시했다 — 새 도메인 타입을 만들지 않고 `@/types`·`DataSource.ts`만 재사용한 점이 옳다.

---

## 5. 팀장 검증 (전원 완료 후 단독)

| 대상 | 방법 | 결과 |
|---|---|---|
| 전체 스위트 | `npm run test` | **1340 passed** — 3·4팀이 본 `playoff-tiebreak` 실패는 **2팀 작업 중간 상태**였음이 확정(2팀은 부분 실행 539/539만 보고) |
| 5팀 I-182 ⓐ | `curl /api/live/matches` | **200 / 2475 bytes** |
| 5팀 I-182 ⓑ | 클라 번들 모듈 등록 검사 | 클라 레이어 7개 모듈뿐 — **`bootstrap.ts`·`factory.ts`·`data/mock`·`data/supabase` 전부 0건** |
| 5팀 Task 015 | Playwright 80초 관찰 | 탭 활성 40초 **1건** / 탭 비활성 40초 **0건** → 수락 기준 충족 |
| 4팀 Task 014 | Playwright | **URL 불변 · 문서/RSC 요청 0 · 본문 전환 · 중복 id 없음 · 전환 후 `#domain` 앵커 정상** → "즉시 전환" 충족 |
| 3팀 031a | `fallback.ts` 대조 | `UI_PARAM` 분기 근거 확인, 경고 7그룹 전량 포함 |
| 디자인 회귀 | 유틸리티 렌더 확인 | `board`·`eyebrow`(42)·`scoreboard`(20)·`touchline-on` 전부 유지 — 되돌림 없음 |
| 반응형 | 40조합 자동 판정 | 오버플로 **0건** |

**재작업 지시 없음.** 전 팀 수락 기준 충족.

> ⚠️ **번들 심볼 검사는 raw grep이면 오경보가 난다** — `bootstrapApp`/`getDataSource` 문자열이 dev 번들에 남지만 전부 `polling.ts`·`fetch-result.ts`의 **JSDoc 예제 산문**이다. 5팀이 이 함정을 먼저 짚고 `(app-pages-browser)` 모듈 등록 문자열로 재검증한 것이 정확했다. 팀장도 같은 오탐을 밟았다가 문맥 확인으로 정정.

---

## 6. 이슈

| ID | 상태 | 요약 |
|---|---|---|
| **I-153** | **해소(2026-09-08)** | Task 012 수락 기준 "두 테마 × 두 로케일 깨짐 0" 최초 실측 — 84조합 자동 판정 |
| **I-183** | **해소(2026-09-08)** | 320px 오버플로 536→320 |
| **I-184** | **해소(2026-09-08)** | `sm`=375px 재정의 → **규약 명문화**. 와이어프레임 §5가 이미 "sm은 320과 동일 취급"으로 정의하고 있었다 |
| **I-185** | **해소(2026-09-08)** | WSL watcher 미동작 → `dev` 스크립트에 `WATCHPACK_POLLING` 고정 |
| **I-186** | **부분 해소** | 내비 5개 404 → **비활성 표시**로 확정. 인덱스 화면은 Task 016~021 대기 |
| **I-182** | **해소(2026-09-08)** | 폴링의 Route Handler 우회 — 5팀 조치 + 팀장 실측 검증 |
| **I-187** | **신규 OPEN — 3팀, 37일차 블로커 후보** | **값 없는 4그룹(WEATHER_EFFECT·RATING_WEIGHT·OVR_WEIGHT·MANAGER_MATCHUP)의 값 정의 주체·시점이 사라졌다** — `fallback.ts`가 네 그룹 모두 "실제 구조는 **36일차(031a) 소관**"이라 미뤄 뒀는데, 오늘 행의 스코프는 "05문서 5.12.1 → default_value"이고 05문서에 그 값이 없어 3팀이 다시 비웠다(억측 금지 원칙상 옳은 처리). 3팀 37·38일차 행은 "타입·범위 메타데이터"·"발효 정책"이라 값 정의 자리가 없다. **⚠️ 2팀 37일차 행이 "경기 평점 산출(FR-ST-003)"인데 `RATING_WEIGHT`가 비어 있어 내일 막힐 수 있다** — 하드코딩하면 NFR-CFG-001 위반 |
| **I-188** | **신규 OPEN — 운영 규칙, 즉시 적용** | **병렬 스프린트 중 `git stash` 등 트리 전역 조작 금지** — 4팀이 스코프 확인용으로 `git stash`를 썼다. 공유 작업 트리에서 stash는 **그 시점 모든 팀의 uncommitted 작업을 통째로 치운다**. 즉시 `pop` + `git status` 확인 + 자진 보고로 무손실 종료됐고 팀장도 `git stash list` 비어 있음을 확인했으나, 다른 팀이 같은 순간 파일을 쓰고 있었다면 유실이 났다. 스코프 확인은 `git status`/`git diff --stat`으로 충분하다 |
| **I-189** | 신규 OPEN — 2팀 | **3팀 이상이 동시에 경계 동률**일 때의 다자 결정전 대진 규칙이 요구사항에 없다. 2팀이 억측 대신 **명시적 오류 처리**로 막아 뒀다(옳은 처리). 발생 확률은 낮으나 규칙 결정 필요 |
| **I-190** | 신규 OPEN — 3팀, 031b | `INJURY_PARAM.BASE_TICK_PROB=0` / `RETIREMENT_PARAM.BASE_PROB=0.05`는 05문서에 예시가 없어 `fallback.ts` 잠정값을 물려받았다. 밸런싱 근거 없음 — 031b 이전 확인 필요. `PLAYOFF_PRIZE`도 05문서가 중간 조합을 생략 표기해 2코드만 등록됨 |
| **I-191** | 신규 OPEN — 1팀, 경미 | team-schedule 36일차 행 문구는 "36개 그룹"이나 `catalog.ts`는 이미 **38종**이 단일 소스다(14일차 `NATIONALITY_WEIGHT`, 31일차 `MANAGER_STYLE_XG` 추가분). 시드는 FK 정합을 위해 38종 전량을 넣었다. 표 문구 정정 필요 |

**미착수 이월**: `SiteHeader`/`SideNav`/`SiteFooter` 컴포넌트 분리(23일차부터). 4팀이 **"기계적 이동이라 위험은 낮으나 `layout.tsx`가 오늘 큰 diff를 받아 리뷰 표면이 겹친다"**며 보류를 권고했고 팀장이 동의했다. I-186에 묶여 있다.

---

## 7. 다음 일차 인계

1. **⚠️ I-187이 37일차 2팀을 막을 수 있다** — 2팀 37일차 "경기 평점 산출(FR-ST-003)"이 `RATING_WEIGHT`를 필요로 하는데 비어 있다. **37일차 착수 전에 ⓐ 값을 3팀이 정할지 ⓑ 2팀이 임시 가중치로 진행하고 후속 교체할지 먼저 판정할 것.**
2. **정상 폴링 주기 5초는 아직 안 걸린다** — 현재 `UI_PARAM` 미적재라 `polling.ts` 안전망 30초로 동작한다. **6팀이 3팀 시드(`supabase/seed/common-code.sql`)를 적재해야** 5초가 된다. Task 015 수락 기준 자체는 충족했으나 실운영값은 그 이후다.
3. **1팀 — 라이브 API 응답 타입 계약** 확정 시 `src/app/api/live/matches/types.ts`의 임시 `LiveMatchesApiResponse`를 교체할 것(파일 헤더에 지점 명시됨).
4. **I-188 규칙을 다음 소환 지시문에 포함**할 것(트리 전역 git 조작 금지).
5. **인덱스 화면 5종(Task 016~021)** 채울 때 `NAV_GROUPS`의 `pending` 플래그를 함께 지울 것.
6. **셸 컴포넌트 분리** — 4팀이 다음 회차 권고. `layout.tsx` diff가 가라앉은 뒤 처리.

---

## 8. 미해결·판정 대기

- **I-187 판정**(3팀 값 정의 vs 2팀 임시 진행) — **가장 급함, 37일차 착수 전**
- **I-189** 다자 경계 동률 대진 규칙(요구사항 미기재)
- **I-190** `INJURY_PARAM`/`RETIREMENT_PARAM` 잠정값, `PLAYOFF_PRIZE` 조합 누락
- **I-191** 36 vs 38그룹 표기 불일치(1팀)
- **뉴스 헤드라인이 `/en`에서도 한국어** — mock 생성기(3팀)가 만드는 데이터 자체가 한국어다. i18n 카탈로그 대상이 아니라 데이터 계층 문제. 실데이터 전환 시 재검토
- **`다음 킥오프` 5건이 전부 `오전 12:00`** — mock 킥오프 시각이 동일하다. 화면 결함이 아니라 생성기 문제로 보이며 확인 필요
