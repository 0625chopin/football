# 60일차 (2026-10-12, 월)

**참여 팀**: 4팀(UI기반i18n) · 5팀(화면배팅UX) — 1·2·3·6팀 미배정

**한 줄 요약**: 5팀이 `/[lang]/teams`를 신설해 내비 잔여 3종 중 **팀**이 열렸고, 4팀은 스폰서 「체결
구단주」 열(020 소급)·`proxy.ts` 소유 판정(I-272)·플래그 404화(I-287)를 한 패스로 끝낸 뒤 022 전수
검증에서 기존 결함 1건(I-290)을 찾아냈다.

---

## 1. 팀별 작업

### 4팀 — Task 020 소급 + `proxy.ts` 한 패스 + Task 022 착수

**① `/[lang]/sponsors` 「체결 구단주」 열** (D-33 경로②, D-35, I-239)

- `signedByOwnerId`는 조회 전용이라 역조회 메서드가 없어 `contract.teamId → getClubOwner` 조인으로
  대체. 그 조인이 곧 체결 당사자인 근거(`world.ts`가 계약 생성 시점 구단주를 그대로 기록하고 교체
  로직 없음)를 주석에 남겼다. 이름은 고유명사라 **변수 주입**(D-17), i18n은 2키만 추가.
- 46일차 주석의 "항상 계약 상세 없음" 문단은 48일차 H-27로 이미 해소돼 있어 현재 상태로 고쳐 썼다.

**② `src/proxy.ts` — I-272 소유 판정 + I-287 수정** (59일차 인계대로 한 패스)

- **I-272 판정: 4팀 단독 소유 유지.** 단일 진입점이라 공동 소유는 충돌을 부른다. 대신 "판정 로직
  본체는 각 팀 소유 파일, `proxy.ts`는 import만"을 규약으로 고정하고 파일 상단 주석에 명시.
- **I-287 수정**: `isAdminConsoleEnabled()`(5팀 `console-flag.ts`, 재구현 없이 import)를 인증 검사
  **앞에** 호출해 플래그 비활성 시 403이 아닌 404 반환. `proxy.test.ts` 3케이스 추가(미인증·플래그off,
  유효 쿠키·플래그off, `"1"` 오타값 fail-closed).

**③ Task 022 — 전 화면 가로 스크롤 0 전수 검증**

- 20화면 × 2로케일 × 6브레이크포인트. admin 3종(인증 게이트)·`/matches`(미존재, Task 047) 제외 —
  둘 다 회귀 아님. 320/375/768/1440/1920 **전부 clean**, **1024px `/teams/[teamId]` 단독 오버플로**
  발견 → I-290. 5팀 소유 파일이라 **사실만 보고하고 직접 고치지 않았다**(소유 경로 규약 준수).

**④ (팀장 추가 지시) `NAV_GROUPS`의 `teams` `pending` 제거** — 아래 2절 참조.

### 5팀 — Task 046 `/[lang]/teams` 구단 목록 신설 (I-223)

- `?league=` 순수 GET 폼 → `getStandings({leagueId})`로 팀 id → `getTeamsByIds`로 카드 렌더("리그
  소속 팀 목록" 계약이 없어 순위표 경유 — `players/page.tsx` 50일차와 동일 사정).
- **정렬은 팀명순**(`localeCompare`) — 순위순은 없는 의미가 붙는다는 49일차 선례 준수.
- **집계 열(성적·재정·스쿼드 규모) 0개** — 스코프 상한 준수. 티어 + 스쿼드/순위표 링크만.
- 4상태 3종은 `state/` 껍데기 위임 얇은 래퍼, i18n `team.list.*` 7키 ko·en, 그리드는 `md:`/`lg:`만
  전환점(I-184 준수).
- **(재수정) I-290 해소** — 아래 2절 참조.

---

## 2. 팀장 검증

**전역 게이트**: `npm run typecheck` 클린 · `npm run lint` 0건 · `npm run test` **2076 통과 / 타입에러 0**
(재수정 후 재실행분 기준).

**런타임 실측** (dev 3000, 검증 후 서버 정지·`.playwright-mcp/` 삭제 확인)

| 대상 | 결과 |
|---|---|
| `/ko,en/sponsors` 구단주 열 | **실값 렌더**(Federico Greco·Kwame Owusu·Ulises Garcia), 헤더 「체결 구단주」/「Signing owner」 — D-33 판정 기준 충족 |
| `proxy.ts` 플래그 ON·미인증 `/ko/admin` | **403 유지** = 회귀 없음 |
| `proxy.ts` 플래그 OFF | 404 (테스트 3케이스, fail-closed 포함) |
| `/ko,en/teams` | 24개 카드 팀명순, `teams/[teamId]` 링크 정상, 320·1024px 오버플로 **0** |

**재수정 2건 지시 → 전부 해소**

1. **I-290 (5팀)** — 4팀 보고를 팀장이 **직접 재현**했다(타 팀 회귀 주장 직접 확인 원칙).
   1024px에서 컨테이너 **737px** vs `3fr_2fr` 트랙 합계 **1025px** → 오버플로 264px. `fr` 트랙의 기본
   최소 크기가 `auto`(min-content)라 아이템에 `min-w-0`이 없으면 트랙이 줄지 못하는 것이 원인.
   5팀이 그리드 아이템 2개에 `min-w-0` 부여 → **트랙 737 = 컨테이너 737, 오버플로 264→0**(ko·en 확인).
   `overflow-x-hidden`으로 덮지 않도록 명시 지시했고 준수됐다.
   - **오늘 작업의 회귀가 아니다** — 파일 최종 변경이 52일차인 기존 결함이다. 그럼에도 당일 처리한
     이유는 ⓐ Task 022 수락 기준이 "가로 스크롤 0"이고 ⓑ 오늘 신설한 `/teams`의 **주 이동 목적지**가
     바로 이 화면이라 새 화면이 사용자를 깨진 뷰포트로 곧장 보내기 때문.
2. **`teams` `pending` 플래그 (4팀)** — `/ko/teams`가 200이 된 시점에 `layout.tsx`의 `pending: true`가
   stale이 됐다. 같은 파일 주석의 **44일차(leagues)·50일차(playoffs) 선례가 둘 다 "인덱스가 생긴 그
   일차에 해제"** 였으므로 H-28(62일차 일괄)을 기다리지 않고 당일 제거하도록 지시했다. 방치하면 오늘
   만든 화면이 이틀간 내비에서 잠긴다(49일차 "앱 내 링크 0건" 사고와 동계열).
   검증: 사이드·모바일 **양쪽 내비**에서 「팀」이 `<a href="/ko/teams">`로 렌더, 「경기」는 준비 중 유지.

**부수 전수 조사** — 커스텀 `fr` 그리드 6곳 확인. 홈(`page.tsx:190`)은 `minmax(0,1fr)`로 이미 방어돼
있고, `players/[playerId]:174`·`admin/config:116`·`matches/[matchId]:260`은 같은 무방비 패턴이나
1024px 실측 오버플로 0으로 **잠복 상태**다(I-290 파생 규칙에 기록).

---

## 3. 이슈

- **I-290 해소(60일차)** — `/teams/[teamId]` 1024px 가로 오버플로 264px. 4팀 발견 → 팀장 재현 → 5팀 수정.
  파생 규칙(커스텀 `fr` 그리드는 `minmax(0,Nfr)`로 하한 명시)과 잠복 3곳을 함께 기록.
- **I-291 신규 OPEN** — 위 수정의 대가로 min-content 압력이 `truncate` 스팬으로 이동해, 1024px에서
  우측 레일(271px) 경기 카드의 팀명이 `clientWidth` **40px**까지 절삭된다(1440px에선 절삭 0). 근본
  원인은 코드가 아니라 **스펙 공백** — `docs/wireframe/06-클럽상세.md:111`이 "1024+ 62%/38%"만 규정하고
  우측 열 최소 실폭에서 경기 카드가 성립하는지를 따지지 않았다. 5팀이 ⓐ전환점 `xl` 상향 / ⓑ컨테이너
  쿼리 스택 / ⓒ현행 수용+툴팁 중 판정(ⓒ는 1024=iPad 가로에서 툴팁이 안 떠 단독으로는 부족).
- **I-272 해소(60일차)** — `proxy.ts` 4팀 단독 소유 확정 + import 패턴 규약화.
- **I-287 해소(60일차)** — 플래그 비활성 시 404, fail-closed 포함 3케이스.

---

## 4. 다음 일차 인계

1. **`/matches` 인덱스가 `pending` 마지막 1건입니다** — Task 047(5팀 61일차)로 열립니다. 61일차에
   화면이 서면 `layout.tsx`의 `matches` 플래그도 **그 일차에** 제거하세요(44·50·60일차 선례 3연속).
   그러면 **I-223 종결 조건("`pending` 0건")이 61일차에 충족**되며, H-28의 "62일차 일괄 제거"는
   더 이상 남는 일이 없습니다 — 62일차엔 확인만 하면 됩니다.
2. **I-291은 5팀이 판정해야 진행됩니다.** ⓐ를 고르면 `docs/wireframe/06-클럽상세.md` 3-2절 수정이
   동반되므로 와이어프레임 소유와 조율이 필요합니다. 판정 전까지는 현행(ⓒ 상태) 유지가 맞습니다.
3. **커스텀 `fr` 그리드를 새로 쓸 때는 `grid-cols-[minmax(0,3fr)_minmax(0,2fr)]` 형태로 하한을
   명시하세요**(I-290 파생 규칙). 잠복 3곳(`players/[playerId]:174`·`admin/config:116`·
   `matches/[matchId]:260`)은 지금 오버플로 0이라 손대지 않았습니다 — 그 화면에 콘텐츠가 늘면
   같은 증상이 납니다.
4. **Task 038 잔여는 여전히 3단계**(원격 적용 → `database.types.ts` 재생성 → `mapper.ts` 배팅 매퍼).
   착수 조건 = I-269 절차 정비 + I-283 선행(5팀 몫 완료) + **I-289 격리 실측**. 59일차 인계 그대로 유효하며
   60일차에 진전 없음(6팀 미참여).
5. **W-48(스케줄러 폴링 주기) 판정 여전히 대기** — 58일차 수동 새로고침 갈음 상태 유지.
6. **I-288 수치는 6팀 단독으로 정하지 마세요** — NFR에 어드민 로그인 리밋 규정이 없어 1팀과 NFR 보강 동반.
7. **I-275·I-276·I-282·I-273 한 패스**, **H-16(2팀→3팀 프리시즌)**, **PS-2 수락 판정(3팀 생성기 대기)**,
   **H-20(→4팀 022)·H-18(→5팀 039) 인계 발효** — 그대로 유효.
8. **Task 022는 60일차분만 끝났습니다**(가로 스크롤 축). 접근성·다국어 축은 미착수이며 4팀 잔여 과업입니다.
9. **어드민 계정 상시 존재**(`0625chopin@gmail.com` / `qwer1234`, `role='ADMIN'`) — ⚠️ 평문이므로
   **외부 배포 전 회수**. `ADMIN_CONSOLE_ENABLED=true`는 `.env.local`(gitignore)에만 있어 새 체크아웃·
   CI·배포에서 콘솔 3종이 **의도대로 404**입니다. 이제 `proxy.ts`도 404로 통일됐으므로(I-287)
   **403을 봤다면 그게 이상 신호**입니다.

---

## 5. 미해결·판정 대기

- **I-291** `/teams/[teamId]` 1024px 팀명 40px 절삭 — 5팀 판정 + 와이어프레임 스펙 공백(신규)
- **I-289** I-283 대응 코드 실행 경로 0건 — 5팀, **원격 적용 전 격리 실측 필수**
- **I-288** 어드민 로그인 리미터 300/분 과대 — 6·1팀, NFR 보강 동반
- **I-286** `get_advisors` 사전 존재 2건 — 6팀, **I-263과 한 패스**
- **I-285** `/api/health` 리밋 미적용 — 6·1팀, 의도적 보류로 확정
- **I-283** 감사 SELECT service_role 전용화 — 5팀 몫 완료(59일차), 6팀 원격 적용 대기
- **I-282** `CommonCodeHistory.changedBy` 자리표시자 — 5·6팀, I-276과 한 패스
- **I-280** "36그룹" 문서 표기 vs 실측 38 — 문서, 숫자 재기입 금지
- **I-279** 공통코드 4그룹 코드 0건 — 3팀 Mock 시드, **I-289와 동계열**
- **I-278** 시즌 종료 ≤20초 실측이 축소 스케일 — 2팀, 3팀 생성기 붙는 시점 재측정
- **I-277** 일반 사용자 인증 UI 0건 — 5팀, 039(62일차~)
- **I-276** 감사 로그 `actorId` null — 5·6팀 계약 조율(파괴적 변경), I-282와 동계열
- **I-275** G2·G3 조작 감사 로그 미기록 — 5팀, I-273과 한 패스
- **I-273** `/admin` 배속·정지 + G6 로그 오버레이 휘발 — 5·2·6팀, **스토어 3종**
- **I-269** 원격 마이그레이션 절차 — 6팀, Task 038 잔여 착수 조건. 60일차 원격 DDL 0건(준수)
- **I-268** 수상 계수 미등록 — 3팀, I-265·I-121·I-136과 031b 한 패스(4건)
- **I-267** 지갑 멱등성 키 부재 — 6팀, Task 037 잔여(비차단, 호출자 0건)
- **I-266** 클럽 상세 2/2 데이터 계약 공백 4건 — 5팀 발견, 계약은 1·3팀
- **I-264** `User` 선호 로케일 도메인 필드 부재 — 1팀 배치(비차단)
- **I-263**(17건) · **I-243** 마이그레이션 미문서·채번 — 6팀, **I-286 동반**, 034b 전 필수
- **I-262** OVR 산출식 중복 — 2·3팀, I-257과 한 패스
- **I-261** I-229 라인업↔이벤트 독립 표본 — 3팀, 다음 등판
- **I-260** 창단·재임 시즌 도메인 축 — 3·5팀, 화면 숨김 우회 중
- **I-259** 선수 상세 잔여 4건 — 5팀, ⓑ는 I-249와 동시 판정
- **I-257 · I-258** 순위 포인트 중복·리빌드 보조금 단위 — 2·3팀 조율
- **I-256** SP-4 잔여 2건 — 팀장/6팀
- **I-255** 전역 경기 조회 계약 — 1팀, 034b 이후
- **W-48** 스케줄러 폴링 주기 — 팀장 판정 대기
- **I-252 · I-251 · I-250 · I-248 · I-247 · I-245** 49일차 신규 — 변동 없음
- **I-241**(MOCK_NOW 고정) — 3팀
- **I-223** 종결은 **61일차 `pending` 0건 확인 시**(60일차에 `teams` 해제, `matches` 1건만 잔존)
- **I-236** `homeModifier` 공식(2팀) / **I-214** 크론 점등 금지(차단성, 60일차 준수)
- **I-235** 공유 트리 git 조작 — 48~60일차 연속 사고 0건
- **I-142** `lint-guardrails` flake — 60일차 재발 없음
