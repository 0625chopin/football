# 46일차 작업 로그 (2026-09-22, 화)

> 참여 팀: **2팀(시뮬레이션엔진) · 4팀(UI기반i18n) · 5팀(화면배팅UX) · 6팀(DB인프라)**
> 미참여: 1팀 · 3팀
> 게이트: `npm run typecheck` ✅ / `npm run lint` ✅ / `npm run test` **121 files · 1646 passed** ✅
>
> **오늘의 본체는 코드가 아니라 판정이다** — 5일 연속 6회 재발한 인계물 배선 누락을 **D-33**으로 확정하고, 그 소급 적용으로 **I-221을 당일 종결**했다.

---

## 1. 팀별 작업

### 2팀 — Task 027 · 무승부 → 연장 → 승부차기 연결 통합 검증

- `penalty.ts`(실제 `simulatePenaltyShootout`) ↔ `playoff.ts`(`resolveKnockoutWinnerTeamId`) ↔ `cup.ts`(`resolveCupWinnerSeed`)가 지금까지 **각자 손으로 채운 값으로만 단위검증**됐고 실제 체인으로 이어붙여 검증된 적이 없음을 확인 → 신규 통합 테스트로 연결을 값으로 증명.
- 결정론적 시드 40개 × 확률 프로필 5종(대칭/약우세/극단) = 200회 전건:
  1. 실제 PK 결과 → 공통 승자판정 무예외, 두 함수 승자 일치
  2. 실제 컵 대진(`generateCupRound1`) 3경기 표본 → PK → 승자판정 → 시드 환산 일치
  3. `pkHome === pkAway`(무승부 잔존) **0건**
- 변경: `src/lib/sim/knockout/knockout-resolution.test.ts`(신규 1개, 기존 파일 무수정)
- 잔여: 비중립 홈 계수(`ability/modifiers.ts` `homeModifier`) TODO — 오늘 범위 밖

### 4팀 — Task 020 · 스폰서 현황 + PK 표기 확인 (+ D-33 소급 등록)

- **PK 스코어 분리표기(D-19)는 신규 코드 불필요** — 5팀 `MatchScoreboard`/`BracketTree`가 44·45일차에 이미 구현. 재확인만 수행.
- `/[lang]/sponsors` 신규 구현: 목록(잔고·계약 팀 수·부도 위험 배지 3종) + 계약 상세 표. 4상태 파일 3종 포함.
- **추가 지시분(D-33 소급)**: `/sample`에 4종 등록 — 신규 `MatchOddsPanel`(4상태, StateToggleSlot) + 누적 미등록 `RoundNav`·`SeasonSelect`·`TiebreakNote`(4상태 비대상, `ZoneLegend` 패턴 정적 등록). 커버리지 **19/19(4상태) · 29종 전체 등록**. 5팀 컴포넌트 파일 자체는 미수정.
- 변경: `sponsors/page.tsx`, `i18n/messages/{ko,en}/sponsor.ts`(신규), `i18n/messages/index.ts`, `sample/{page.tsx,component-registry.ts,component-registry.test.ts,StateToggleSlot.tsx}`

### 5팀 — Task 017 · D6(날씨·구장) + D7(배당 패널, 표시 전용)

- D7은 3팀 H-19(`src/lib/odds/display.ts` `OddsDisplayPanel`) 타입을 **그대로 소비**하는 신규 composite `MatchOddsPanel`(4상태) + 기존 `OddsButton`(4팀, disabled 고정) 재사용으로 구성.
- **베팅 버튼 비활성은 `OddsButton`에 `onClick` prop 자체가 없어 물리적으로 강제**된다 — 소비처가 실수로도 인터랙션을 붙일 수 없다. 보조 텍스트(`match.odds.disabledHint`, I-9)도 병기.
- i18n `info`/`weather`/`odds` 그룹 추가. `weather`는 `satisfies Record<WeatherType, string>`로 9종 누락을 컴파일 타임에 차단.
- 변경: `matches/[matchId]/page.tsx`, `components/composite/MatchOddsPanel.tsx`(신규), `i18n/messages/{ko,en}/match.ts`

### 6팀 — Task 033 · `/api/health` + p95 1차 측정

- `/api/health` 신설(NFR-OB-004) — service-role 클라이언트로 `SupabaseDataSource` 재사용, 3팀 `obs/alert.ts`의 `computeSystemHealth`/`CronHeartbeat` 계약을 그대로 배선(**임계값 중복 구현 회피**). 4상태 필드(`schedulerAlive`/`lastCronRun`/`nextKickoffAt`/`backlogFixtureCount`), **항상 200 + `status` 필드로 이상 표현**(핸들러 예외도 200 + `degraded`).
- `cron_run` no-op 기록은 **41일차 `tick_advisory_lock.sql`부터 이미 구현**됨을 코드로 재확인 — 신규 변경 0건(→ I-234).
- **p95 실측: 190.8ms / p99 206.7ms(웜 40회)** — NFR-PF-008(≤300/800ms) 통과. 25일차 이월 항목 해소.
- 변경: `src/app/api/health/route.ts`(신규), `docs/db/46Day-health-p95측정.md`(신규). 타 팀 파일 무손.

---

## 2. 팀장 검증

- **게이트 3종 전부 clean**(팀원 보고와 일치, 리더 세션에서 독립 재실행).
- **2팀 이슈 후보 직접 재현** — `grep`으로 `includeExtraTime` 전 소비처를 열거: 생산 코드는 `odds/runner.ts:148`(`false` 고정)과 `snapshot-pipeline.ts:186`(`index % 4 === 0`, **경기 순번 기반 임의 값**) 2곳뿐이며 **실스코어로 판정하는 경로 0건** 확인 → I-230.
- **4팀 이슈 후보 직접 재현** — `MockDataSource.ts:1047-1052` 무조건 `return []`, `world.ts`의 `SponsorContract` 생성 **0건** 확인 → I-231.
- **I-188 준수 확인** — 5팀의 `i18n/messages/{ko,en}/match.ts` 편집은 위반이 아니다. 해당 파일은 4팀 md §1 각주가 **"구조 4팀 / 값 공동, 콘텐츠 확장은 5팀 기여 몫"**으로 명시한 공동 경로이며, 5팀이 그 각주를 코드 주석에 인용해 두었다. 그 외 교차 편집 0건.
- **D-33 등록형 예외 실효 확인** — 4팀 등록 4종이 레지스트리에 실제 등재됐고(`component-registry.ts:42,63-65`) 5팀 소유 컴포넌트 파일은 무수정.

---

## 3. 이슈

### 판정 — **D-33 확정** (본체)

**소유가 갈린 인계물의 등록·소비 책임 = 생산자 DoD.** 42~46일차 5일 연속 6건 재발(I-206 → I-221 → I-226 → I-229 → I-231 + `MatchOddsPanel` 미등록)에 대한 규약 판정. 단일 소스는 `docs/require/06-prioritization-and-risks.md` **D-33**.

- **핵심**: 생산자 DoD는 "파일 생성"이 아니라 **"소비처에서 실제로 값이 보이는 상태"**까지다.
- **경로 ① 등록형**(`/sample` 등록 · `messages/index.ts` 등록 2종 한정) → **생산자가 직접 편집. I-188의 명시적 예외**. 커밋 메시지에 `(D-33 등록)` 표기.
- **경로 ② 배선형**(어댑터 구현·소비처 개조 등 판단 필요) → 생산자는 **이슈 등재 + 소유팀 통보**까지, 소유팀은 **다음 등판 일차 최우선 배정**(팀장이 소환 지시문에 명시).
- **판정 기준**: 소비처 정상 상태가 실측된 적 없으면 그 Task는 완료가 아니다. **빈 상태 렌더 확인만으로 수락 기준 충족을 선언하지 않는다.**
- **소급**: I-221 잔여 3종 + `MatchOddsPanel` 당일 등록 → **I-221 CLOSED**. I-226·I-229·I-231은 경로 ②로 다음 등판 최우선.

### 신규 등재 (I-230 ~ I-234)

| ID | 요지 | 소유 | 차단성 |
|---|---|---|---|
| **I-230** | 정규 90분/연장 120분 실스코어로 **ET 필요 여부를 판정하는 생산 코드가 0건** — `tick.ts`가 상위 계층에 위임했으나 그 계층이 없다. 넉아웃 경기 오케스트레이터 부재 | 미정(2팀/3팀 경계) | 실엔진 연동 시점에 차단 |
| **I-231** | 스폰서 계약 mock 부재(`getSponsorContracts` → `[]`, `world.ts` 생성 0건) → 계약 상세·부도 배지 정상 상태 무검증. **D-33 적용 첫 신규 건** | 3팀 | 비차단 |
| **I-232** | `SponsorContractStatus` 라벨이 `enums.ts`에 없어 `sponsor.ts` 로컬 배치 — 표기 이원화 | 3팀 이관 검토 | 비차단 |
| **I-233** | 중립지(`isNeutral`) 경기에 **홈팀 구장명을 그대로 표시** — 중립 구장 엔티티가 동결 타입에 없음. 대안 ⓑ(구장 줄 생략)가 타입 무변경으로 즉시 가능 | 미정 | 비차단(오표기) |
| **I-234** | 46일차 일정 문구 "no-op 기록"은 **41일차에 이미 충족된 요구의 재기재**(문구 정정 필요) + `count=exact` 미지원으로 backlog 수가 상한 1000 근사치 + `tick_run` 쪽 cronHeartbeat 배선 미착수 | 6팀 | 비차단 |

---

## 4. 다음 일차 인계

1. **D-33이 발효됐다 — 다음 일차 소환 지시문부터 반영할 것.** ⓐ 신규 컴포넌트/네임스페이스를 만든 팀은 `/sample`·`messages/index.ts` 등록까지 직접 하고 커밋 메시지에 `(D-33 등록)` 표기 ⓑ 배선형은 이슈 등재 + 소유팀 다음 등판 최우선. **7회째가 나오면 그건 재발이 아니라 D-33 위반이다.**
2. **3팀이 D-33 경로 ② 미결 3건(I-229 · I-231 · I-228/I-227)의 소유팀인데 45·46일차 연속 미참여**다. **다음 등판 일차에 I-229·I-231을 최우선 배정할 것** — 둘 다 `src/lib/data/mock/**`·`src/lib/mock/**` 단일 소유라 다른 팀이 대신할 수 없다.
3. **4팀은 I-226(중계 카탈로그 소비처 0건)을 다음 등판 최우선**으로 배정한다. 판정 사항 ⓐ 문장형 캡션 전환 ⓑ 병기 ⓒ 다른 소비처 보류 중 택일 — 와이어프레임 04번 §4 D3 확인 선행.
4. **I-230은 소유팀 배정이 먼저다.** 넉아웃 오케스트레이터를 2팀 엔진 내부에 둘지 3팀 시즌 진행에 둘지 정하지 않으면 착수할 수 없다. **I-214(크론 점등) 해제 조건과 맞물리므로 그 전에 결론 필요.**
5. **Task 017은 D6·D7까지 완료, D2 잔여**(47~48일차). **Task 020은 스폰서까지 완료.** **Task 027은 연결 검증까지 완료**, `homeModifier` TODO 잔여. **Task 033은 3항목 전부 완료**(p95 실측 190.8ms 기록).
6. **I-214 크론 점등 금지 유지** — 해제 조건(2팀 실엔진 연동 + 후처리 배선) 미충족. I-230이 그 조건의 일부임이 오늘 드러났다.
7. **I-225 · I-223 잔여 4종은 여전히 변동 없음** — 담당·일차 미배정. schedule-planner 배정 필요.
8. **I-188 전 팀 준수 계속**(4개 팀 동시 트리, 위반 0건). **단 D-33 등록형 예외 2종은 위반이 아님을 지시문에 함께 명시할 것** — 안 그러면 생산자가 등록을 주저해 D-33이 무력화된다.
9. **I-234에 따라 `docs/team-schedule/06-DB인프라팀.md` 문구 정정 필요** — 이미 끝난 요구가 일정에 남아 있으면 다음 사람이 중복 착수한다.

---

## 5. 미해결·판정 대기

- **I-230** 넉아웃 ET 판정 오케스트레이터 부재 — **소유팀 미정, I-214 해제 조건의 일부(가장 급함)**
- **I-226**(4팀) · **I-229 · I-231**(3팀) — **D-33 경로 ②, 각 팀 다음 등판 최우선 배정 확정**
- **I-192 규약 판정**(경계를 넘는 값에 접점 테스트 의무화) — 39일차부터 8일 이월
- **I-228 · I-227** `floorPow2` 절삭에 따른 컵·플레이오프 대진 축소 — **I-50 종속**, 3팀
- **I-225** 컵 슬롯 창 ↔ 페이즈 전이 접점 / **I-223** 인덱스 4종 무배정 — 담당·일차 미배정(schedule-planner)
- **I-224** stale `.next/types` — 1팀 / **I-215 · I-217** 1팀 소관(테스트 인프라 / 동결 타입 배치)
- **I-214** 크론 점등 금지 — **차단성 있음** / **I-218** `/admin` 선행 배정 / **I-208** 소비처 배선 팀·일차 배정
- **I-232 · I-216 · I-220** 표기·구조 정합(3팀 계열, 묶어 처리 권장) / **I-233** 중립지 구장 표기 — 소유 미정
- **I-234** 일정 문구 정정 · `count=exact` · `tick_run` cronHeartbeat — 6팀
- **I-212**(mock 라이브 점수 + FINISHED 이벤트 로그 — **I-229·I-231과 함께 처리 권장**) / **I-209** 마이그레이션 백필 / **I-211** 시즌 경로 승격 / **I-204** 4강 대진 / **I-205** en 뉴스 한국어 — 전부 비차단
- **I-200 · I-201 · I-202 · I-203** 프로덕션 빌드 불가가 병목이거나 배정 대기(변동 없음)
- **I-189 · I-190 · I-191 · I-194** 값·규칙·표기 정합(변동 없음) / **I-193 · I-197** 031b 밸런싱 루프(66~68일차)
- **I-195** mock 킥오프 시각 / **I-196** 재실측 **7일 연속 미이행** / **I-198** `/sample` 격리 / **I-199** 사용자 조치 대기
