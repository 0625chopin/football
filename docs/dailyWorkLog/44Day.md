# 44일차 작업 로그 (2026-09-18, 금)

**참여 팀**: 2팀(시뮬레이션엔진) · 4팀(UI기반i18n) · 5팀(화면배팅UX) · 6팀(DB인프라)
**미참여**: 1팀(코어품질) · 3팀(데이터밸런싱배당) — 팀 일정표에 44일차 행 없음

**게이트 최종**: `npm run typecheck` 0건 / `npm run lint` 0건 / `npm run test` **1609 passed, 6 skipped, 6 todo** — 회귀 0

> 별건: 본 스프린트 착수 전, 사용자 지시로 리그 목록 화면(`/[lang]/leagues`) 신설과 I-222(RSC 경계 함정) 조치가 선행 커밋으로 반영돼 있다(873c297·68e3fca). 아래 팀별 작업과는 별개 스트림이다.

---

## 1. 팀별 작업

### 2팀 — Task 027 컵 슬롯 삽입

정규시즌 리그1 라운드 6/12/18/24/32/40 직후 **컵 슬롯 6회 삽입**을 순수 계층으로 구현.

- 신규 `src/lib/sim/schedule/cup-slot.ts` — `computeCupSlotWindows` / `insertCupSlotsIntoLeagueKickoffs` / `deriveCupSlotMarkers` / `findCupSlotConflicts` 등
- 신규 `src/lib/sim/schedule/cup-slot.test.ts` — 16건
- 핵심 설계: `shift(t) = t + duration × (마커 중 t 미만인 개수)` 공식이 **모든 리그 킥오프가 슬롯 창 내부에 들어갈 수 없음을 수학적으로 보장**(런타임 보정 불필요, 증명을 파일 헤더에 기재)
- `CUP_PARAM.INSERT_ROUNDS` / `PHASE_DURATION_MIN.CUP_SLOT`은 주입값 — 리터럴 0건. NFR-DT-001 준수(`Math.random`/`Date.now`/`react`/`@supabase` 0건)
- **수락 기준 충족**: 3리그 실규모(24/20/16팀) 검증에서 슬롯 중 리그 킥오프 **0건**

### 4팀 — Task 020 플레이오프 브래킷

`src/app/[lang]/playoffs/[leagueId]/page.tsx` 자리표시자 → 실화면.

- `getLeague` + `getPlayoffBracket` + `getTeamsByIds`로 `Fixture[]`를 라운드별 `BracketTreeData`로 변환해 **기존 `BracketTree`(5팀 013B, composite)에 위임** — 신규 컴포넌트 0종(`/sample` 등록 대상 없음)
- 신규 i18n 키는 `match.playoffs.title` 1개, 나머지는 기존 `league.*` 재사용. 리그명은 고유명사라 변수 주입(D-17)
- **수락 기준 충족**: ko/en 양 로케일에서 리그1(8강→4강→결승 3열) · 리그2(4강→결승 2열) · 리그3(결승 1경기) 전부 실렌더 확인

### 5팀 — Task 017 중계 문구 카탈로그

이벤트 **23종** 중계 문구 템플릿을 번역 카탈로그로 신설.

- `src/i18n/messages/{ko,en}/match.ts`에 `event` 그룹 추가 — 와이어프레임 04번 §4 D3의 지정 프리픽스 `match.event.*` 준수
- 기존 `enums.matchEvent.*`(4팀, 뱃지용 단어 라벨)와 **역할 분리** — 기존 키 삭제·변경 0건(순수 추가라 4팀 소유 경로 협의 조건 충족)
- `{playerName}`/`{teamName}` 등 고유명사는 변수 자리표시자로만 존재, 번역 대상에서 제외(D-17/D-18)
- `satisfies Record<MatchEventType, string>`로 **23종 전량 매핑을 tsc가 강제** — 누락 시 타입 오류
- **수락 기준 충족**: 중계 문구 하드코딩 0

### 6팀 — Task 033 재시도·catch-up

`tick_run()`에 **지수 백오프 3회 재시도**(0.1/0.2/0.4s)와 밀린 라운드 catch-up 판정 추가.

- 신규 `supabase/migrations/20260721120512_tick_run_retry_catchup.sql` (CREATE OR REPLACE — 42/43일차 함수 대체)
- 클레임 실패 시 즉시 FAILED가 아니라 재시도 루프, 소진 시에만 FAILED로 **폴백 경로를 구분 기록**
- `is_catch_up`은 킥오프가 `INTERVAL_MIN`보다 오래 지난 SCHEDULED 존재 여부로 판정 — 기존 상수 0/false에서 **실측 기록으로 전환**
- **수락 기준 충족**: fixture 40건(상한 초과 + 킥오프 2시간 전) 임시 삽입 → `tick_run()` 2회로 전량 FINISHED 완주(1회차 PARTIAL 30/10잔여 `is_catch_up=true`, 2회차 SUCCESS 10/0잔여). 백로그 없는 3회차는 `is_catch_up=false`로 정상 구분. **테스트 데이터 전량 삭제 원복 확인**
- **I-214 크론 점등 금지 유지** — 스케줄 활성화하지 않음

---

## 2. 팀장 검증

전원 완료 후 단독 검증. 재작업 지시 **1건**(2팀), 이슈 등재 **3건**.

### ⚠️ 팀장 판정 오류 1건 — 기록해 둔다

2팀이 "41일차 `knockout/cup.ts` 주석은 슬롯 삽입을 `season/` 소관이라 했는데 44일차 배정은 `schedule/`"라는 문서 모순을 제보했다. 팀장이 `src/lib/sim/season/` 미존재를 근거로 **"팀 일정이 옳고 41일차 주석이 stale"**이라 판정하고 주석 정정을 지시했으나, **이 판정이 틀렸다.**

- **원인**: 팀장이 `ls src/lib/sim/season/`을 실행해 놓고 출력 라벨을 `"schedule/ 에 season 오케스트레이션 존재?"`로 잘못 달아, 반환된 `phase.ts`/`phase.test.ts`를 `schedule/`의 내용으로 착각했다. **자기가 만든 라벨을 근거로 정반대 결론을 냈다.**
- **사실**: `src/lib/sim/season/phase.ts`는 28일차 Task 025 산출물로 실재하며, 34~35줄에 `ENTER_CUP_SLOT: REGULAR→CUP_SLOT` / `EXIT_CUP_SLOT: CUP_SLOT→REGULAR`가 정의돼 있다.
- **결론**: 두 계층은 경쟁이 아니라 **역할 분담**이다 — `season/phase.ts`=이산 페이즈 전이(언제 넘어가는가), `schedule/cup-slot.ts`=연속 타임스탬프 산출(몇 시부터 몇 시까지인가). 41일차 주석은 후자의 부재를 말한 것이지 전자를 부정한 게 아니었다. **2팀 분석이 정확했다.**
- **처리**: 팀장이 오판을 인정·정정하고, 2팀 제안대로 **세 계층을 모두 명시**하는 문구로 확정. `knockout/cup.ts` 71~77줄 갱신(이슈 후보 문구 제거) + `cup-slot.ts` 헤더에 3자 경계 역참조 추가. `npx vitest run src/lib/sim/` 39파일 652건 전부 통과(회귀 0).
- **교훈**: 팀원이 팀장 판정에 이의를 제기했고 **편집 전에 확인을 요청해 잘못된 수정이 실제로 반영되는 것을 막았다.** "지시대로 했습니다"였으면 실재하는 `phase.ts`를 없는 것처럼 지우는 부정확한 주석이 남았을 것이다. 이 제동이 정상 동작이며 앞으로도 권장한다.

### 이슈 후보 3건 — 전부 팀장이 원본으로 직접 재현

| 제보 | 팀장 판정 |
|---|---|
| 4팀: 리그1 WC 라운드 미생성 | **결함 아님** — `progress.ts` 헤더 30~32줄이 "bye 슬롯 미지원, I-50과 동일 유보"로 이미 문서화. 다만 수락 기준 문구와 산출이 다른 상태는 남으므로 **I-227**로 추적 |
| 5팀: `match.event.*` 카탈로그 미소비 | **확인됨** — `EventTimelineItem.tsx:110`이 여전히 `enums.matchEvent.*`만 사용. 하드코딩 0이라 수락 기준엔 지장 없으나 **I-206/I-221과 세 번째 같은 패턴** → **I-226** |
| 2팀: `cup-slot.ts` 소비처 0건 | **정상 범위** — 41일차 "미배선"과 동일, 44일차 수락 기준은 충족. 다만 슬롯 창↔페이즈 전이 접점 부재가 드러나 **I-225**로 등재 |

---

## 3. 이슈

**신규 3건 등재** (`docs/ISSUES.md`)

- **I-225** — 컵 슬롯 창(연속)과 `ENTER/EXIT_CUP_SLOT`(이산)이 서로를 모름. 두 엔진 계층의 결함이 아니라 **오케스트레이션 계층 부재**. 2팀 소관 아님(6팀 크론/5팀 라이브 배선 후보), **담당·일차 미배정**. 비차단
- **I-226** — `match.event.*` 23종 카탈로그 소비처 0건. 만든 팀(5팀)과 소비처 소유 팀(4팀)이 갈리는 **I-206/I-221 계열 3회째 재발**. I-221과 묶어 판정 권장. 비차단
- **I-227** — 리그1 플레이오프 WC 라운드 미생성(수락 기준 문구 ↔ 산출 불일치). 기존 문서화된 스코프 축소이며 **I-50 종속**. 비차단

---

## 4. 다음 일차 인계

1. **I-206 → I-221 → I-226, 같은 형태가 세 번 재발했다.** 매번 "만드는 팀과 등록·소비처 소유 팀이 다르다"는 한 가지 원인이고, 매번 아무도 규칙을 어기지 않았는데 인계물이 떴다. **개별 건을 메우는 방식으로는 네 번째가 온다** — I-221의 DoD 귀속 판정을 더 미루면 안 된다. **가장 급함.**
2. **I-192 규약 판정이 39일차부터 6일 연속 이월.** 오늘은 직접 근거가 추가되진 않았으나 I-225(두 계층이 경계를 넘는 값을 공유하면서 접점 테스트가 없음)가 정확히 같은 구조다.
3. **I-225 담당 배정이 필요하다.** 41일차 "시즌 페이즈 전환 배선" 인계와 같은 계열이므로 묶어서 schedule-planner에 올릴 것. 지금은 `cup-slot.ts` 소비처가 0건이라 비차단이지만, 배선하는 순간 접점이 없으면 어긋난 시각에 페이즈가 전환된다.
4. **I-223 잔여 4종(경기·플레이오프·팀·선수) 인덱스 화면은 여전히 무배정.** 오늘 4팀이 `playoffs/[leagueId]` **상세**를 채웠으나 인덱스와는 무관하다 — `NAV_GROUPS`의 `pending` 4개 유지가 맞다.
5. **I-214 크론 점등 금지 유지.** 6팀이 재시도·catch-up까지 실측 완료했으나 해제 조건은 여전히 **2팀 실엔진 연동 + 후처리 배선**이다.
6. **Task 017은 44일차 행까지 완료, D2·D4~D7 잔여**(45~48일차). **Task 027은 컵 슬롯까지 완료**, 비중립 홈 계수(`ability/modifiers.ts`의 `homeModifier`) TODO 골격이 잔여.
7. **I-188 전 팀 준수 계속**(4개 팀 동시 작업 트리에서 위반 0건 — 5팀이 타 스트림 파일 감지 후 손대지 않고 보고한 사례 포함). **다음 지시문에도 계속 포함할 것.**
8. **I-224 우회법을 알고 있을 것** — 새 라우트 추가 후 typecheck가 `AppRoutes` 오류로 실패하면 `rm -rf .next/types`. 오늘은 신규 라우트가 없어 재발하지 않았다.

---

## 5. 미해결·판정 대기

- **I-221 → I-226** 소유가 갈린 인계물의 등록·소비 책임 DoD 귀속 — **3회 재발, 가장 급함** / 소유·일차 미정
- **I-192 규약 판정**(경계를 넘는 값에 접점 테스트 의무화) — 39일차부터 6일 이월
- **I-225** 컵 슬롯 창 ↔ 페이즈 전이 접점 — 담당·일차 미배정(schedule-planner) / **I-227** WC bye 슬롯 — I-50 종속
- **I-223** 인덱스 4종 무배정(리그만 44일차 해소) / **I-224** stale `.next/types` — 1팀
- **I-214** 크론 점등 금지 — **차단성 있음** / **I-218** `/admin` 선행 배정 필요 / **I-208** 소비처 배선 팀·일차 배정
- **I-215 · I-217** 1팀 소관(테스트 인프라 / 동결 타입 배치) / **I-216** 표 2열 시각 배치 / **I-220** `roundLabel` 구조화 — 3팀
- **I-212**(mock 라이브 점수 + FINISHED 이벤트 로그 부재) / **I-209** 마이그레이션 백필 / **I-211** 시즌 경로 승격 / **I-204** 4강 대진 / **I-205** en 뉴스 한국어 — 전부 비차단
- **I-200 · I-201 · I-202 · I-203** 프로덕션 빌드 불가가 병목이거나 배정 대기(변동 없음)
- **I-189 · I-190 · I-191 · I-194** 값·규칙·표기 정합(변동 없음) / **I-193 · I-197** 031b 밸런싱 루프(66~68일차)
- **I-195** mock 킥오프 시각 / **I-196** 재실측 **5일 연속 미이행** / **I-198** `/sample` 격리 / **I-199** 사용자 조치 대기
