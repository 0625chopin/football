# 40일차 (2026-09-14 월)

**참여 팀**: 2팀(시뮬레이션엔진) · 3팀(데이터밸런싱배당) · 4팀(UI기반i18n) · 5팀(화면배팅UX)
**미참여**: 1팀 · 6팀 (팀 일정표에 40일차 배정 행 없음)

| 팀 | Task | 결과 |
|---|---|---|
| 2 | 027 | 플레이오프 브래킷 생성기 — 완료 |
| 3 | 043 | 6종 핵심 메트릭 수집 계약 — 완료 (+ 팀장 지시 mock 순위표 수정) |
| 4 | 019 | 이적/뉴스 피드 화면 — 완료 |
| 5 | 016 | 존 구분 확인 + B4 타이브레이커 단계 표시 — 완료 (1회 재수정) |

---

## 1. 팀별 작업

### 2팀 — Task 027 플레이오프 브래킷

`src/lib/sim/knockout/` 최초 입주. `playoff.ts` + `playoff.test.ts`(20 케이스) 신규.

- 라운드별 순수 함수로 분리: 리그1 WC→8강→4강→결승, 리그2 준결승→결승, 리그3 결승.
- **경기 수 검증**: 리그1 2+4+2+1 = **9경기**, 리그2 **3경기**, 리그3 **1경기** — FR-LG-011~013 수용 기준과 일치(팀장이 요구사항 원문 대조).
- 라운드 간 체이닝을 **팀 ID가 아니라 원 시드 번호**로 넘긴다(`PlayoffAdvancement.winnerSeed`) — 홈/원정 결정이 순위 기반이라 시드가 필요하고, 팀 ID만 넘기면 다음 라운드에서 역인덱싱이 생긴다.
- 결승만 `isNeutral: true`(수용 기준 ④). `resolveKnockoutWinnerTeamId()`로 정규→연장→승부차기 승자 판정(D-19: PK는 판정에만).
- NFR-DT-001 준수 확인(금지 API 실사용 0건, 주석 언급만), 타입은 `@/types` 배럴만.

### 3팀 — Task 043 메트릭 계약

`src/lib/obs/metrics.ts` + 테스트(9 케이스) 신규.

- NFR-OB-002 원문 6종(시뮬 소요 / 라운드 지연 / 크론 성공률 / 배당 산출 시간 / 정산 시간·실패율 / API 지연)을 **원문 나열 순서 그대로** 고정.
- `createMetricsRecorder()` → 종류별 링버퍼(기본 500), `snapshot()`이 6필드 고정 `MetricsSnapshot` 반환. `count === 0`이면 통계 필드 전부 `null`(빈 상태와 0값 구분).
- `Clock` 주입형(39일차 `logger.ts`와 같은 패턴). 실 계측 호출부 배선은 타 팀 소유 경로라 오늘 스코프 제외, **계약만 고정**.

### 4팀 — Task 019 이적/뉴스 피드

`src/app/[lang]/transfers/page.tsx` 실장 + `i18n/messages/{ko,en}/transfers.ts` 신규.

- `getNewsFeed`로 6종(TRANSFER/LOAN/RETIREMENT/YOUTH_DEBUT/MANAGER_CHANGE/SPONSOR_BANKRUPT) 조회, 기존 `NewsItem`(5팀) 재사용 — **신규 컴포넌트 0종**이라 `/sample` 등록 불필요.
- 체크박스 GET 폼(`name="type"` 다중값) 타입 필터. 전부 해제 시 빈 목록 대신 전체로 되돌린다(`getNewsFeed`가 `types: []`를 "없음"과 "전체"로 구분하지 않아 화면에서 방어).
- 유형 라벨은 `enums.newsFeedItemType.*` 재사용(신규 선언 0). 이적 문구 리터럴 0건 — **수락 기준 충족**. `NAV_GROUPS`의 transfers는 원래 `pending`이 없어 조치 불필요.

### 5팀 — Task 016 존 구분 + B4

- **존 시각 구분은 39일차 산출물로 이미 충족** 확인(▲◆▼ 아이콘 + 시맨틱 색 + 범례 텍스트 3중). 변경 불필요 판단 — 팀장 실렌더로 확인(승격/PO/강등 라벨 렌더됨).
- **B4 타이브레이커 단계 표시 신규 구현**: `tiebreak-note.ts`(순수 함수) + `TiebreakNote.tsx` + ko/en `league.tiebreak.*` 키 + 순위표 페이지 배선.
- 색맹 3종(protanopia/deuteranopia/tritanopia) 대응 근거: 존 구분은 **형태(아이콘)+텍스트 라벨**이 색과 독립적으로 존재해 색 단독 의존 0. B4는 색을 아예 쓰지 않는다(텍스트 + ⓘ).

---

## 2. 팀장 검증

**게이트(팀장 직접 실행)** — `npm run typecheck` 오류 0 / `npm run lint` 오류 0 / `npm run test` **107 파일 · 1469 통과 · 6 todo · Type Errors none**.

- **신규 테스트가 실제로 수집되는지 확인**: 39일차 104파일·1434 → 오늘 107파일·1469. 델타 **+3파일 +35테스트**가 playoff(20)+metrics(9)+tiebreak-note(6)와 정확히 일치. 파일만 늘고 러너가 안 잡는 사례 아님.
- **요구사항 원문 대조**: FR-LG-011~013 경기 수·대진·중립지, NFR-OB-002 6종 나열 순서 — 전건 일치.
- **실렌더 검증**(dev 서버 포트 3040, 기존 3000 미간섭): 라우트 11종 전부 200.
- **타입 재확인**: `Standing.tiebreakApplied`는 `number | null`(`src/types/stat.ts:309`), 엔진 `stageLabel`이 0(승점)→null이라 실제 방출 범위 2~7. 5팀 구현과 일치.
- `RouteLoading`이 `/en`에서 한국어를 내는 것은 **기존 I-170 쿠키 근사의 알려진 한계**(파일 주석에 명시)로, 오늘 산출물 결함 아님을 확인하고 넘어갔다.

### 발견 → 피드백 → 재수정 (2건)

**(1) mock 순위표가 `tiebreakApplied`를 채우지 않아 B4가 실화면에서 죽어 있었다 → 3팀**

5팀의 B4는 단위 테스트로만 통과했고, 실렌더 시 문구가 **0건**이었다. 원인은 `src/lib/mock/fixtures/schedule.ts`가 `tiebreakApplied: null`을 무조건 박고, 순위 정렬도 `points→gd→gf` 3단계를 **mock 안에서 독자 구현**한 것(FR-LG-005 7단계 규칙의 축소 복제 = 표류 위험).

→ 3팀이 `resolveStandings()`(2팀 `standing/tiebreak.ts`, **import만·미편집**) 위임으로 교체. 7단계 계산에 필요한 `seasonSeed`를 호출부 4곳에 스레딩. 세 리그 전부에서 실제 동률 발생 확인(시드 조정 없음).

**(2) B4 문구에 "5위는 골득실로 순위가 갈렸습니다" 같은 단독 순위가 나왔다 → 5팀**

(1) 수정 후 B4가 처음 렌더되며 드러났다. 11줄 중 **4줄이 순위 1개짜리** — 갈리려면 상대가 있어야 하므로 말이 안 되고, 5팀 자신의 `TiebreakNoteGroup.ranks` 주석("2개 이상") 계약도 위반. 원인은 그룹화 키를 `points`+`tiebreakApplied` **양쪽 일치**로 잡아, 같은 승점 블록이 여러 단계로 갈리면 잔여가 1개만 남는 것.

→ 5팀이 2단 구조로 재작성: 1차로 `points` 동일 블록(2명 이상), 2차로 그 안의 단계별 하위구간. `"5·6·7·8위 동률 — 5위는 골득실로, 6·7위는 페어플레이로, 8위는 골득실로 순위가 갈렸습니다"` 형태. **단독 문장이 구조적으로 불가능**해졌다.

→ 팀장 재검증: 실렌더 11줄 → **7줄, 단독 0건**. `/en`도 번역 렌더 확인(`Ranks 15·16·17 were tied on points — …`).

### 검증 중 오탐 1건 (기록)

`/en/transfers?type=LOAN`이 빈 결과를 내 4팀 필터 결함으로 의심했으나, **3팀이 같은 시각 `schedule.ts`를 수정 중이라 `MockDataSource` 생성자가 던지던 일시 상태**였다(`ReferenceError: standings is not defined`). 3팀 완료 후 재측정하니 13건 → LOAN 5건으로 정상. **병렬 작업 중 실렌더 검증은 타 팀 편집 상태에 오염될 수 있다** — 이상 발견 시 dev 로그 스택트레이스로 소유 파일부터 확인할 것.

---

## 3. 이슈

**신규 등재 2건** (`docs/ISSUES.md`):

- **I-204** — 리그1 플레이오프 **4강 대진이 FR-LG-011에 미명세**. 2팀이 표준 브래킷 반대편 배치(1·2위 쪽 분리)로 추론 채택하고 근거를 파일 헤더에 기재. 수용 기준(총 9경기)은 어느 배치든 충족돼 **테스트로 잡히지 않는 공백**이다. 기획 확정 필요, 비차단.
- **I-205** — **`/en/transfers`에서 뉴스 headline·body가 한국어**. 39일차 인계에 이미 있던 사안이나 오늘 전용 화면이 생기며 사용자 노출 경로가 확정됐다. 화면 결함 아님(배지·필터는 정상 번역, 하드코딩 0) — `NewsFeedItem.headline`이 데이터 계층 조립 문자열이라는 계약 자체의 문제. 타입 동결·조사 결합 때문에 실데이터 전환과 묶어 판단. mock 한정이라 비차단.

**I-202 관련 판정**: 신규 이슈 불필요. `config/fallback.ts`가 이미 자체 주석으로 "42일차 `alert.ts`에서 카운터로 대체" 계획을 명시 — 계획대로면 42일차에 첫 소비처가 생긴다. `data/polling.ts`(1팀)·`sim/match/gk-fallback.ts`(2팀)의 `console` 직접 호출은 타 팀 소유라 3팀이 판단만 전달, 미수정.

---

## 4. 다음 일차 인계

1. **B4 노이즈 스코프 판단이 5팀 미결로 남았다** — 병합으로 11→7줄이 됐으나 24팀 리그에선 여전히 잦다. 전량 나열이 와이어프레임 §4 B4 의도인지, 존 경계 인접 블록 우선 노출로 좁힐지 **판단만 회신된 상태**(구현 보류). Task 016 잔여와 함께 처리할 것.
2. **Task 016 잔여 스코프** — B1-t 탭 · B4 두 번째 줄(TIEBREAK Fixture "단판 플레이오프 예정" 안내, **Mock에 재경기 생성기가 없어 오늘 제외**) · B5 리그3 리빌드 제재 · 시즌 선택기. **완전 종료는 이것들이 끝난 뒤**.
3. **2팀 `playoff.ts` 소비처가 아직 없다** — 브래킷 생성 함수만 있고 시즌 페이즈 전환에서 호출하는 배선이 미배정. `RATING_WEIGHT` 소비 배선(37일차부터 이월)과 함께 지정할 것.
4. **3팀 `metrics.ts` 계측 호출부 배선 미배정** — 계약만 고정된 상태. 소비처는 2팀(sim 소요)·6팀(크론·정산)·라우트(API 지연)로 갈리므로 **일차·팀 지정이 필요**하다. `/admin` 대시보드(5팀)도 아직 없다.
5. **H-15 소비가 아직 시작되지 않았다** — 39일차 인계 1번(6팀 크론 033·3팀 정산)이 40일차에 6팀 배정 행이 없어 미착수. **SP-3 계약서에 대한 6팀 관점 검토도 여전히 미이행**(39일차 인계 2번 이월).
6. **I-201 방침 결정이 계속 미뤄지고 있다** — 오늘 Task 019가 `notFound()` 사용처를 늘렸다. 프로덕션 재현 확인이 선행이나 **프로덕션 빌드 불가가 병목**(I-200과 동일).
7. **인덱스 화면 5종(Task 016~021)** 채울 때 `NAV_GROUPS`의 `pending` 플래그 함께 제거. `/ko/leagues`는 여전히 404(상세 `[leagueId]`만 존재).
8. **정상 폴링 5초 미적용** — `UI_PARAM` 미적재로 안전망 30초 동작 중. 6팀의 3팀 시드 적재가 선행 조건(36일차부터 이월).
9. **1팀 — 라이브 API 응답 타입 계약** 확정 시 `api/live/matches/types.ts`의 임시 `LiveMatchesApiResponse` 교체.
10. **셸 컴포넌트 분리**(I-186) 36일차부터 미이행. **I-188**(트리 전역 git 조작 금지)은 소환 지시문에 계속 포함할 것 — 오늘도 4팀 전원 준수.
11. **`config/fallback` 경고가 dev 로그를 도배한다** — `MATCH_POINTS`·`SQUAD_PARAM` 등 공통코드 미등록분이 요청마다 반복 출력돼 실제 오류를 가린다. 6팀 시드 적재(8번)로 해소될 사안이나, 그 전까지 로그 판독을 방해한다.

---

## 5. 미해결·판정 대기

- **I-192 규약 판정**(경계를 넘는 값 공유에 접점 테스트 의무화 여부) — 39일차에 "주석·인계 의무화"로 좁히는 선택지가 제시된 상태. **가장 급함**
- **오늘의 교훈이 I-192와 같은 계열이다** — B4는 단위 테스트를 전건 통과했는데 실화면에서는 아무것도 렌더되지 않았다. 원인이 **다른 팀 소유의 mock 데이터**에 있었기 때문이다. 「본인 범위만 테스트」 규칙의 사각지대이며, **화면 산출물은 실렌더 확인이 팀장 검증에 반드시 포함돼야 한다**는 근거가 하나 더 쌓였다.
- **I-204** 4강 대진 미명세 / **I-205** en 뉴스 한국어 — 둘 다 오늘 신규, 비차단
- **I-201** `notFound()` 200 — 프로덕션 재현 여부 확인 후 방침 / **I-202** 로거 배선 팀 배정(42일차 자동 해소 가능성) / **I-203** 스켈레톤 시프트 실측
- **I-200** ko CLS — 프로덕션 검증 환경 확보 시 재실측
- **I-194** `fairPlayScore` 산식 부재 / **I-189** 다자 경계 동률 대진 규칙 / **I-190** 잠정값·`PLAYOFF_PRIZE` 누락 / **I-191** 36 vs 38그룹 표기
- **I-193 · I-197** 031b 밸런싱 루프(66~68일차) 대상 / **I-195** mock 킥오프 시각 분산 / **I-196** 40일차 재실측 **— 오늘 미이행, 이월** / **I-198** `/sample` 조회 단계 격리 / **I-199** 사용자 조치 대기
