# 19일차 (2026-08-14, 금)

## 1. 참여 팀

| 팀 | Task | 요약 |
|---|---|---|
| **1팀** 코어·품질 | 010 (마지막 날) | I-113 조율, PR 체크리스트·ISSUES 갱신 규약, H-06 5팀 인계 |
| **2팀** 시뮬레이션 엔진 | 024 | 포지션 숙련도 5단계 + 인접 그래프 BFS 미보유 페널티 |
| **3팀** 데이터·밸런싱·배당 | 007 (종료) | H-07 배선(mock 배럴 + `registerDataSource`), 종료 스위트, I-114 해소 |
| **4팀** UI기반·i18n | 011 | 열거형 표시명 카탈로그 골격 `enums.ts` (ko/en) |
| **6팀** DB·인프라 | — (대기) | 034a는 3팀 H-07 대기 — V-01 사전 준비 체크리스트만 |

**5팀 화면·배팅 UX는 미참여**(팀 md에 19일차 행 없음).

## 2. 최종 게이트

| 항목 | 결과 |
|---|---|
| **`npm run gate`** | **exit 0 — `[gate] all checks passed`** (aggregate lines 98.28% / branches 92.49%, perFile 전 파일 통과). **최초 실행은 exit 1이었고 §4-A 참조** |
| `npx tsc --noEmit` | 오류 **0** |
| `npm run test` | **47 파일 / 624 테스트 통과** (6 파일·5 테스트 skip, 6 todo). typecheck 오류 0 |
| `npm run lint` | **0 오류 / 111 경고** — 전량 `src/app/**` D-18 (§4 판정 참조) |
| 금지 패턴 재현 | `src/lib/sim/ability/position.ts`에 `Math.random()`+`Date.now()` 삽입 → `no-restricted-properties` 오류 2건 검출, 파일 원복 확인 |

## 3. 팀별 산출물

### 1팀 — Task 010 마감
- `src/lib/data/bootstrap.test.ts` — mock 배럴 **존재 여부를 런타임 검사**해 `describe.runIf`/`skipIf`로 "배럴 이전/이후" 두 세계관을 모두 검증하도록 재작성. 순서 고정 없이 3팀이 언제 배럴을 얹어도 되게 함 (I-113 해소).
- `.github/pull_request_template.md` (신규) — D-16 / Next.js 16 문서 참조 경로 / 신규 의존성 근거 3항목 + 3단 게이트 + 이슈 연동 + 소유 경로 확인.
- `docs/ISSUES.md` §6 — 상태 표기 어휘, `R-*` 인용 규칙(I-109), PR 연동 규약 추가. I-36 해소, **I-116 신규**.
- `eslint.config.mjs` — `no-unused-vars`에 `^_` 접두사 관례 인식 추가(134→111 경고). **⚑ H-06 5팀 인계 완료.**

### 2팀 — Task 024 포지션 계수
- `src/lib/sim/ability/position.ts` (신규) + `position.test.ts`(60건) — 11노드/15엣지 인접 그래프, 모듈 로드 시 전 쌍 BFS 테이블 사전 계산. 보유 숙련도 5단계(1.00/0.95/0.88/0.75/0.60), 미보유 `max(0.45, 0.88 − 0.11×dist)`, **GK 교차 0.35를 보유 단계표보다 우선** 적용. `modifiers.ts`의 스텁은 제거·이관.
- **파일 분리 판단(인계 사항)**: **분리 확정**. 근거 ① 그래프+BFS+3단 분기로 단일 수식 계수들과 복잡도 층위가 다름 ② 팀 일정표 산출물 필드가 이미 `position.ts` 지정 ③ `match/gk-fallback.ts`(14일차) 선례. GK 교차 배율은 재선언 없이 `gk-fallback.ts` 상수를 import. **20일차 날씨·감독은 단일 매트릭스 룩업이라 이 판단이 자동 승계되지 않음 — 그날 별도 판단.**

### 3팀 — Task 007 종료 (⚑ H-07 인계)
- `src/lib/data/mock/index.ts`·`index.test.ts` (신규) — `registerDataSource('mock', () => new MockDataSource())`. **지연 생성(프로바이더 함수)** 채택으로 부트스트랩 시점에 무거운 월드 생성이 돌지 않게 함. Task 007 종료 스위트(시드 재현성, 스쿼드 불변식, 등번호 중복 0, 이름 풀 매칭, 정적 JSON 0건) 동반.
- **I-114 해소** — Mock 기준 시각 3종(world 08-10 / progress 08-11 / MockDataSource 08-13, 최대 3일 편차)을 `world.ts`의 `MOCK_EPOCH_NOW` 단일 앵커로 통일(`WORLD_CREATED_AT`은 앵커−40일 재계산, MockDataSource는 `world.createdAt` 재사용).

### 4팀 — Task 011 enums 골격
- `src/i18n/messages/{ko,en}/enums.ts` (신규) — H-10 목록 전량(포지션 11·이벤트 23·부상 4·전술 6·페이즈 6·수상 12·마켓 상태 4)을 `EnumTranslationCatalog<T>`로 감싸 **enum 전 멤버 매핑을 tsc가 강제**. 값은 enum 리터럴 echo 자리표시자 — **3팀이 23일차 이후 실값 기입**. `../index.ts` 통합 `messages`에는 미합류(Provider 실배선 22일차 이후).

### 6팀 — 대기 일차
- `docs/db/19Day-V01사전준비-034a체크리스트.md` (신규) — V-01 실측(26~37일차) 사전 준비 + 034a 착수 체크리스트. 마이그레이션·스키마 변경 **없음**. `DataSource.ts`(H-02) 62메서드 수신 완료, `supabase/mapper.ts`는 034a에서 재사용 예정. (추가로 §4-A 조치 수행)

## 4. 팀장 검증에서 발견된 결함

### 4-A. **실결함 1건 — 3단 머지 게이트가 마감 검증에서 돌지 않고 있었다 (I-117)**

**19일차 산출물의 결함이 아니라 16~18일차에 누적된 미검출 건이며, 근본 원인은 팀장의 검증 절차다.**

- **발견**: 로그 작성 후 커밋 직전 `npm run gate`를 돌리자 **exit 1**, perFile 커버리지 임계 위반 **ERROR 6건**.
- **원인 규명(직접 재현)**: 18일차 커밋(`aa4c226`)을 **별도 worktree에 체크아웃해 `npx vitest run --coverage`를 돌린 결과 동일 ERROR 6건이 그대로 재현**됐다. 즉 오늘 산출물이 만든 것이 아니다. 15일차에 `npm run gate`(`tsc → lint → test:coverage`)가 도입됐으나 **일차 마감 검증은 계속 `npm run test`로만 판정**해 왔고, `test`에는 `--coverage`가 없어 **임계가 한 번도 평가되지 않았다**. 16·17·18일차 로그의 "최종 게이트" 절이 전부 `npm run test` 기준이다.
- **조치(소유 팀별 분담, 회피성 조치 불허 명시)**:

| 파일 | 최초 | 조치 후 | 팀 |
|---|---|---|---|
| `data/mock/MockDataSource.ts` | lines 77.59 / br 45.65 | **98.45 / 89.23** | 3팀 |
| `mock/fixtures/screens.ts` | br 54.54 | **100 / 100** | 3팀 |
| `data/supabase/mapper.ts` | lines 0 / br 0 | **100 / 100** | 6팀 |
| `data/database.types.ts` | lines 0 | `coverage.exclude` 영구 제외 | 1팀 |

- 신규 테스트 **54건**(570→624). `database.types.ts`는 MCP `generate_typescript_types` 생성물이라 실행 로직이 없어 제외가 맞고, 15일차 `polling.ts`(재포함 조건부)와 성격이 다름을 `vitest.config.ts` 주석에 구분 기재했다.
- **팀장 재감사**: 3팀이 쓴 `/* v8 ignore */` **8곳을 전수 확인** — 전부 `throw` 방어 가드와 `?? null` 폴백이며 "어느 테스트가 고정한 불변식 때문에 도달 불가능한지"를 지목한 근거 주석이 붙어 있다. **실로직을 숨긴 회피가 아니고**, 커버리지 상승의 실체는 신규 테스트다. 임계값 인하·실로직 파일 제외는 없었다. **승인.**
- **재발 방지**: `.github/pull_request_template.md`의 3단 게이트 절을 `tsc`/`lint`/`test` 개별 나열에서 **`npm run gate` 단일 체크박스**로 갱신(1팀). **일차 마감 검증도 `npm run test`가 아니라 `npm run gate`로 고정한다** — 이 로그의 §2 최상단 행이 그 첫 적용이다.

### 4-B. 팀 산출물 자체의 결함 — **0건**

팀 보고를 그대로 수용하지 않고 재현한 항목:

1. **금지 패턴 실패 재현** — 보고에 의존하지 않고 `position.ts`에 직접 위반 코드를 심어 검출 확인 후 원복.
2. **`eslint.config.mjs` 변경의 성격 검토** — "수락 기준을 맞추려는 규칙 완화"인지 diff를 직접 확인. 접두사 없는 실제 미사용 변수는 계속 경고되므로 **완화가 아닌 정합**으로 판정.
3. **경고 111건 파일별 분해** — 전량 `src/app/**` D-18 항목, 4팀 신규 `enums.ts`는 경고 0.
4. **C-6 위반 검색** 0건 / **소유 경로 대조** — 6팀 산출물이 `docs/db/**` 내에 있음 확인.

### 판정 조정 — 1팀 수락 기준 "`npm run lint` 경고 0"

문면상 **미충족**(111 경고 잔존)이나, 잔존 경고 전량이 **4·5팀 소유 `src/app/**`의 D-18 하드코딩 UI 텍스트**이고 `src/i18n` 배선(Task 011, 22일차)에 구조적으로 종속된다. 1팀 통제 범위(1팀 소유 경로 + 오늘 신규 파일) 내 경고는 0이며, 오늘 새로 늘어난 경고도 0(134→111 감소)이다. → **조건부 충족**으로 판정하고 **22일차 재판정** 항목으로 이월한다. 1팀이 PR 템플릿의 3단 게이트를 "경고 0"이 아닌 "이 PR로 새로 늘어난 경고 없음"으로 정의한 것도 같은 이유이며 타당하다.

## 5. 신규/갱신 이슈

| ID | 상태 |
|---|---|
| **I-113** | **해소** — `bootstrap.test.ts` 조건부 분기 재작성, 3팀 배선 후 정상 전환 확인 |
| **I-114** | **해소** — Mock 기준 시각 `MOCK_EPOCH_NOW` 단일 앵커 통일 |
| **I-36** | **해소** — H-06 ESLint 가드레일 반영 + 19일차 재현 검증으로 종결, 5팀 인계 |
| **I-116** | **신규(정보성, 해소)** — Task 010 수락 기준과 D-18 규칙의 문면 충돌 기록. 20일차 이후 H-06 인수 팀이 잔존 경고를 "새 경고"로 오인하지 않도록 남김 |
| **I-115** | 미착수 — `check-literals.mjs` allowlist 보강, 급하지 않아 1팀이 보류 |
| **I-117** | **신규·해소(19일차)** — 3단 머지 게이트가 일차 마감 검증에 연결되지 않아 16~18일차 perFile 커버리지 위반이 누적·미검출. §4-A 참조. 1·3·6팀 조치 완료, 재발 방지로 마감 게이트를 `npm run gate`로 고정 |

## 6. 다음 일차(20일차) 인계

| 팀 | 인계 사항 |
|---|---|
| **1팀** | Task 010 **종료**. H-06 가드레일은 5팀 인계 완료 — 20일차부터 5팀이 인수. I-115(allowlist 보강) 미착수분 잔존 |
| **2팀** | Task 024 계속 — 남은 계수 **날씨·감독**. 포지션은 분리로 결론났으나 **두 계수는 각각 단일 매트릭스 룩업이라 판단이 자동 승계되지 않음 — `modifiers.ts` 잔류 여부를 그날 별도 판단** |
| **3팀** | Task 007 **종료**, H-07 배선 완료 — **4·5·6팀이 20일차부터 소비 개시 가능**. `enums.ts` 실값 기입은 23일차 이후 몫 |
| **4팀** | Task 011 계속(22일차까지) — 날짜·숫자 서식, 로케일 스위처. `enums.ts`의 `../index.ts` 합류와 Provider 실배선은 22일차. **D-18 경고 111건 해소가 22일차 수락 판정 대상** |
| **5팀** | **H-06 ESLint 가드레일 인수**(1팀→5팀). 잔존 111 경고는 D-18 기지 상태이며 새 경고가 아님(I-116) |
| **6팀** | 3팀 H-07이 종료됐으므로 **034a 착수 가능**. `docs/db/19Day-V01사전준비-034a체크리스트.md` 기준으로 진행, `supabase/mapper.ts` 재사용 |
| **팀장** | ⓐ 22일차에 1팀 Task 010 lint 수락 기준 **재판정** 필요(§4-B 판정 조정) ⓑ **일차 마감 검증은 `npm run test`가 아니라 `npm run gate`로 한다**(I-117) — `test`에는 `--coverage`가 없어 커버리지 임계가 평가되지 않는다 ⓒ 이월 결함 의심 시 **HEAD를 별도 worktree에 체크아웃해 재현**하면 "오늘 산출물 탓인지"를 안전하게 가릴 수 있다(작업 트리 stash 불필요) |

## 7. 미해결·판정 대기

| 건 | 상태 | 기한 |
|---|---|---|
| **1팀 lint 경고 0 수락 기준** | 조건부 충족 — D-18 111건이 Task 011에 종속 | **22일차 재판정(팀장)** |
| **I-111** | 미커버 화면 10종 중 6종이 소관 미배정 | 5팀 착수 역산, 팀장 배정 |
| **I-115** | `check-literals.mjs` allowlist | 1팀, 급하지 않음 |
| **날씨·감독 계수 파일 분리** | 포지션은 분리 확정, 두 계수는 미판단 | **20일차 2팀** |
| **I-107** | 브래킷 부전승·컵 라운드 축소 | 5팀 브래킷 화면 착수 전 |
| **I-102** | `is_event_elapsed()` 실판정 로직 (현재 `select true` 스텁) | 2팀 H-24(30일차) |
| **I-101** | 확장자 경로 404 UI 통일 여부 | Task 014(34~38일차) |
| **I-100** | `bet` 계열 인덱스 2건 | 해당 테이블 생성 Task |
| **unused_index 73** | 회수 검토 이관 | Task 042(58~62일차) |
| **I-112** 승인됨(인증 도입 시 재검토) · **I-109** 낮은 우선순위 · **관계 R-06** 삭제정책 미확정 | 비차단 | 별도 일정 |
| **SKILL.md 교체**(14일차~) · **Playwright 콘솔 스모크**(13일차~, Chrome 필요) | 이월 | 사용자 판단 대기 |
