# 49일차 (2026-09-25, 금)

**참여 팀**: 1 코어품질 · 2 시뮬레이션엔진 · 3 데이터밸런싱배당 · 5 화면배팅UX · 6 DB인프라
**미참여**: 4 UI기반i18n (해당 일차 배정 없음)

---

## 1. 팀별 작업

### 1팀 코어품질 — 리뷰 게이트 재개 + I-242

- **I-242(typecheck 게이트 오탐) 해소.** `scripts/typecheck.mjs` 수정.
  - 원안(ⓑ `.next/dev/types` 삭제 후 즉시 재시도)은 **실측에서 기각**했다 — 삭제 직후 재검사가 `PageProps`/`LayoutProps` 등 전역 타입 부재로 무관 파일까지 TS2304/TS2740 오탐을 번지게 함을 직접 확인.
  - 최종안: **삭제 후 이 실행에서는 재시도하지 않고 exit 2로 분리해 재생성을 유도**한다.
  - 검증: `.next/dev/types/routes.d.ts`를 96줄 유사 truncate로 인위 재현 → 경고·삭제·exit 2 확인 → 원복 후 재확인까지 2회 왕복 실측.
- **Task 017 완료 판정(리뷰 게이트 9개 조건, 48일차 이월분) — 8/9 통과.**
  - 미충족은 조건 3(신규 코드 단위 테스트) 1건뿐: `matches/[matchId]/timeline.ts`의 `buildTimelineRows`(정렬 + `ASSIST`→`GOAL` 병합) 전용 테스트 0건.
  - **판정: Task 017은 완료 인정**(기능은 48일차 Playwright E2E로 행위 검증 완료), 결함만 **I-244**로 등재해 5팀 재요청.

### 2팀 시뮬레이션엔진 — Task 028 리빌드 제재

- FR-LG-007 리그3 15~16위 리빌드 제재를 계산하는 **순수 함수 `resolveRebuildSanctions()`** 구현.
  - 페널티 3종(②협상 명성 −5 1회 · ③영구 명성 −3 · ①FR-EC-002 표는 표시만) + 구제 2종(④보조금 = 리그3 1위 포인트 × 40% · ⑤유소년 +10%p 1시즌).
  - **강등/리그 탈락 로직을 아예 두지 않아 "최하위 강등 0건"이 타입상 자명하게 성립**한다.
  - 대상 슬롯 수(2)는 `League`에도 `SANCTION_PARAM`에도 없어 **안전 기본값 없이 필수 파라미터**로 받았다(I-83·`tier-b-resim.ts` 선례).
  - `SANCTION_PARAM` 4종은 I-83 패턴대로 파라미터 주입 + `REBUILD_SANCTION_PARAM_DEFAULT`(3/5/0.4/0.1) 안전 기본값. 3팀 `config/fallback.ts` 카탈로그값과 **대조만 하고 import는 하지 않았다**.

### 3팀 데이터밸런싱배당 — 49일차 행 실체는 이미 완료분

- 착수 전 확인 결과 행 ⓓⓔ(SponsorContract 생성 · ACTIVE ≤ 3 · `signedByOwnerId` · 구단주 축 반영)는 **48일차 `d06c213`에서 이미 완료**돼 있었고 **I-231도 그 시점에 실질 종결**이었다.
- 유일한 미반영 델타인 **부도 위험 배지 점등 구성**만 신규 반영: `generateSponsors()`에서 첫 스폰서(`i===0`)의 balance를 **RNG 추가 소비 없이 순수 후처리로 음수화**(`rawBalance*0.05`, 최소 200), `bankruptAtSeason`은 null 유지해 "위험"과 "확정 부도"를 구분.
- 스케줄 드리프트를 **I-245**로 등재.

### 5팀 화면배팅UX — Task 018 선수 상세 1/2

- `/players/[playerId]`에 E1(프로필 헤더) · **E1-r 지표 스트립(D-34)** · E2(능력치 레이더 + 34속성 아코디언) · E3(컨디션·피로 + 가용성 배지) · E4(포지션 맵 + 11군 숙련도 리스트) 구현.
- **기존 컴포넌트만 재사용**(AbilityRadar/ConditionGauge/PositionMap/PlayerAvatar/TeamBadge/StatBar) — **신규 컴포넌트 0종**이라 `/sample` 등록(4팀 소유) 필요 없음.
- **평점 4지표 실값 실측**(D-33 판정 기준 충족): 출전 6 · 최근 평점 6.3 · 시즌 평균 9.90 · 리그 벤치마크 6.04. **선수 평균과 리그 벤치마크는 색이 아니라 라벨로 구분**(NFR-A11Y-002).
- `FINISHED` 재필터 없음(데이터 계약 신뢰, 와이어프레임 05 W-34/S-4). 표본(60인) 밖 선수는 4지표가 `—`로 정상 empty 폴백 — `MockDataSource.ts` 헤더에 문서화된 기존 제약.

### 6팀 DB인프라 — Task 033 통합 테스트

- **코드·마이그레이션 변경 0건.** DML 테스트 데이터로만 검증하고 종료 후 전량 삭제(잔존 0).
- **30분 중단 catch-up 완주**: 마지막 SUCCESS를 30분 전으로 세팅 → 1회차 `gap_detected=true`/`gap_minutes=30`, 35건 중 상한 30건 처리(PARTIAL, remaining 5) → 2회차 즉시 재호출로 나머지 5건 완주(SUCCESS, remaining 0).
- **동시 호출 부하(리스크 R-08)**: 배포된 Edge Function에 실제 HTTP 10건 동시 요청(20건 fixture) — 1건만 락 획득해 20건 전량 처리, 7건 NOOP(락 경합, 정상), 1건은 지연 락 획득 후 처리분 0. **중복 처리 0건.**
- **I-214(크론 점등 금지) 준수** — pg_cron 스케줄 활성화 없이 수동 호출로만 검증.

## 1-1. 마감 커밋 후 후속 수정 — 선수 상세 진입 동선 (팀장 단독, 사용자 지시)

스프린트 마감 커밋(`fa23dd4`) 직후 사용자가 **"선수 카드를 어떻게 보느냐"**고 물었다. 확인해 보니 5팀이 49일차에 만든 `/players/[playerId]`는 **정상 동작하지만 앱 안에서 도달할 방법이 하나도 없었다** — 전 화면을 grep해도 `players/`로 가는 링크가 0건이고, 사이드 내비의 "선수"는 I-186 이래 `pending: true`(비활성)였다. URL을 직접 쳐야만 볼 수 있는 상태였다. 이어진 사용자 지시로 진입 동선을 두 단계로 열었다.

**(1) 기존 화면에서 선수명 → 상세 링크** (`stats`·`awards`, 4팀 소유 경로)

- `/stats` 랭킹 표의 "선수" 열, `/awards`의 시즌별 수상 "대상" 열(수상자가 선수인 행) + 통산 다관왕 랭킹 선수 부문 — 총 3곳.
- **행 전체 링크(48일차 `7bd11c6` 패턴)를 쓰지 않았다.** 이 표들은 한 행에 선수·팀·리그가 함께 있어 행 단위 링크는 목적지가 모호해진다. 이름 텍스트만 링크로 만들고 `focus-visible` 링을 붙였다.
- **`getPlayerProfile`이 `null`을 준 선수는 링크로 감싸지 않는다** — 상세가 `notFound()`로 떨어지는 죽은 링크이기 때문이다. `awards`는 이를 위해 `resolve*Name`(문자열 반환) 2종을 `render*`(`ReactNode` 반환)로 바꿨다.
- **베스트11 피치 뷰의 이름은 링크가 아니다.** 이름을 그리는 주체가 `PitchLineup`(**5팀 소유 `composite/**`**)이고 `PitchLineupPlayer` 계약에 링크 자리가 없다. 남의 경로를 조용히 고치지 않는 규약대로 **판단만 남기고 손대지 않았다** → I-253.

**(2) 사이드 내비 "선수" 활성화 = 인덱스 화면 신설** (I-223 두 번째 부분 해소)

- 플래그만 떼면 404다. **44일차 `leagues/page.tsx` 선례를 그대로 따라** `src/app/[lang]/players/page.tsx` + 4상태 래퍼 3종을 신설하고 `NAV_GROUPS`의 `players` `pending`을 제거했다.
- **전 선수를 나열하는 조회 계약이 `DataSource`에 없다**(선수 다수 조회는 `getTeamSquad`·`getPlayerStatRanking` 두 갈래뿐, 이름 검색 없음). 그래서 **리그 → 구단 → 스쿼드**로 좁히는 GET 폼 필터 화면으로 만들었다(`/stats` 패턴, 클라이언트 컴포넌트 0). 리그별 팀 목록도 전용 계약이 없어 `getStandings({leagueId})`의 `teamId`에서 얻되, **정렬은 순위가 아니라 팀명순** — 선수를 찾는 화면에 순위 순서를 쓰면 없는 의미가 붙는다.
- 리그를 바꾸면 이전 `team` 값이 그 리그에 없을 수 있어, 유효하지 않은 `team`은 무시하고 그 리그의 첫 팀으로 되돌린다(빈 표 대신 항상 무언가를 보여준다).
- **표시 열은 이름·포지션·나이 3개뿐이다.** 와이어프레임이 없는 화면이라 지표를 늘리면 "무슨 기준의 목록인가"가 임의로 정해진다. 스카우트 등급은 상세에서 툴팁("정확한 값은 공개되지 않습니다")과 한 쌍으로만 노출되던 값이라 목록에 맨몸으로 세우지 않았고, 국적은 `enums` 카탈로그가 없어(**I-248**) 번역키를 경유할 수 없어 제외했다.
- i18n은 16일차 골격의 미사용 키 `player.list.title`을 살리고 `description`/`leagueLabel`/`teamLabel`/`apply`/`caption`/`*Header` 등 9종만 ko·en에 추가. 나이 값은 `profile.ageFormat`, 포지션은 `enums.position.*` 재사용.

**검증**: typecheck·lint·`npm run test`(1698 passed) 통과. dev 실렌더로 `/ko`·`/en` 양쪽 200, 내비 `/ko/players` 링크 점등, 인덱스 표의 선수 링크 → 상세 200, 리그 전환·잘못된 `team` 파라미터 폴백까지 확인.

**⚠️ 게이트에서 걸린 것 하나 — stale `.next/types/`**: 새 라우트가 `Type '"/[lang]/players"' does not satisfy AppRoutes`로 계속 실패했다. 원인은 **dev 서버가 `.next/dev/types/`만 갱신하고 `.next/types/`(별개 산출물)는 갱신하지 않는 것** — `tsconfig.json`이 둘 다 include하므로 오래된 쪽이 새 라우트를 모른 채 판정에 끼어든다. 49일차 I-242 조치는 `.next/dev/types` 계열만 다루므로 이 경로는 여전히 사각이다. 스크래치패드로 옮겨 해소했고(gitignore 대상·재생성 산출물), **재발 가능성이 있어 I-254로 등재**했다.

**교훈**: **"화면을 만들었다"와 "사용자가 그 화면에 갈 수 있다"는 다른 판정이다.** 49일차 5팀 검증은 화면 자체를 실렌더로 확인했고 전부 정상이었지만, 진입 동선이 0건이라는 사실은 그 검증 범위 밖이었다. 상세 화면을 신설하는 Task는 **"어디서 눌러 들어오는가"를 수락 기준에 포함**해야 같은 누락이 반복되지 않는다(`matches/[matchId]`는 42일차에 일정표 링크가 함께 들어가 이 문제가 없었다 — 우연히 갈렸다).

---

## 2. 팀장 검증

**전원 개발 완료 후 단독 검증. `npm run gate` 최종 통과** (커버리지 Statements 95.46% / Branches 87.21% / Functions 97.75% / Lines 95.62%).

### 2-1. 피드백 → 재수정 (1건)

- **2팀 `rebuild.test.ts` TS1117** — 2팀 자체 보고는 "타입 오류 0건"이었으나 팀장이 `npx tsc --noEmit`로 40행 중복 키를 직접 재현. **vitest는 esbuild 트랜스폼만 하므로 테스트 통과가 타입 검증을 대신하지 못한다**는 점을 명시해 피드백.
  - 2팀 재수정: 31행 중복 `id: LEAGUE3_ID` 제거. **덮어쓰기로 인한 오탐이 아님도 함께 확인**(뒤 프로퍼티가 앞을 덮으므로 40행 값이 항상 적용되던 값 = 단언은 의도대로 동작 중이었음). typecheck 0건 / vitest 35건 통과로 재보고.

### 2-2. 팀장 직접 확인 (2건, 모두 정상 판정)

- **`rebuild.ts`가 커버리지 표에 안 보임** → json-summary로 직접 확인한 결과 **lines 100% / branches 100%**. text 리포터가 완전 커버 파일을 생략한 것으로, 누락이 아니다(`phase.ts`도 동일).
- **"시즌 평균 9.90 vs 리그 평균 6.04"가 모순돼 보임** → `src/lib/mock/progress.ts:501`이 **1.0~10.0 균등 분포**(D-34 결정① 그대로)라 정상 Mock 값. **5팀 배선 버그 아님.** 분포 현실성 문제로 **I-247** 등재.

### 2-3. 규약 점검 (5팀 신규 화면)

`sm:`/`xs:` 레이아웃 전환 0건(I-184) · 데이터 시각화에 `--primary` 오용 0건 · i18n 키 경유 · PA 비노출(P-2 주석 명시) · 아이콘 `aria-hidden` 병기 — **위반 0건**.

### 2-4. 위생

검증 부산물 잔존 0건(5팀 임시 `__tmp_debug.test.ts` 정리 확인, 팀장 프로브 파일도 스크래치패드에서 삭제). 공유 트리 `git stash`/`reset` 사고 0건 — 48일차에 이어 **전 팀 지시문에 검증 스코프를 명시한 조치가 2일 연속 유효**(I-235).

---

## 3. 이슈

**신규 11건** (I-244는 1팀 등재, I-245~I-252는 팀장 등재. **I-253·I-254는 마감 커밋 후 후속 작업(1-1절)에서 팀장 등재**).

| ID | 소유 | 요지 |
|---|---|---|
| I-244 | 5팀 | `buildTimelineRows` 단위 테스트 0건 — `timeline.test.ts` 신설 요청(Task 017 판정 잔여) |
| I-245 | 팀장/스케줄 | 3팀 49일차 행이 48일차 완료분과 중복 — 소급분 역반영 누락(I-240 계열) |
| I-246 | 6팀 | 부하 시 "JWT issued at future" 500 간헐 발생 — 재시도 정책 확인 필요 |
| I-247 | 3팀 | `avgRating` 균등 분포가 비현실적 — 031b에서 분포 조정 판정 |
| I-248 | 3팀 | `nationality`/`PreferredFoot` 표시명 카탈로그 미이행 → E1 표기 생략 중 |
| I-249 | 3·4팀 | 34속성 라벨 카탈로그 소유 위치 미정 — 화면 로컬 채움 상태 |
| I-250 | 4팀 | `PositionMap` 단일 포지션만 지원 — 11군 오버레이 불가(리스트로 대체) |
| I-251 | 판정 대기 | dev에서 `notFound()`가 HTTP 200 — 기존 증상, prod 대조 불가(I-200~I-203) |
| I-252 | 판정 대기 | W-28 은퇴 선수 표기 규약 미확정 — 50일차 E5 착수 전 판정 권장 |
| I-253 | 5팀 | `PitchLineup`이 선수명 링크 불가 — `PitchLineupPlayer`에 `href` 추가 판정(1-1절) |
| I-254 | 1팀 | stale `.next/types/`가 신규 라우트 typecheck를 오탐 — I-242 조치의 사각(1-1절) |

**해소 1건**: **I-242**(게이트 오탐 모드) — 1팀 조치 완료. 48일차 인계의 최우선 항목이었고, 이로써 **검증 신뢰성이 회복**됐다. 다만 같은 날 마감 후 작업에서 **`.next/types/`(dev 계열이 아닌 쪽) 오탐이 새로 드러나** I-254로 남았다.

**부분 해소 1건**: **I-223**(인덱스 화면 무배정) — 선수 1종 신설로 잔여 4종 → **3종**(경기·플레이오프·팀). 1-1절 참조.

---

## 4. 다음 일차 인계

1. **I-242가 해소돼 게이트 판정을 다시 신뢰할 수 있다.** 다만 조치안이 "삭제 후 exit 2"라 dev 서버가 떠 있을 때 **1회 실패 후 재실행이 필요할 수 있다** — 실패 메시지에 재생성 유도 문구가 있으니 그대로 따르면 된다.
2. **I-244(5팀 `timeline.test.ts` 신설)는 Task 017 판정의 잔여 결함**이다. 5팀 다음 등판에서 처리하되, **Task 017 자체는 이미 완료 인정됐으므로 일정 재개방이 아니다**(공수 소).
3. **50일차 5팀은 선수 상세 2/2**(E5 커리어 이력·타임라인). 착수 전 **I-252(W-28 은퇴 선수 표기 규약)** 판정이 있으면 좋다 — 없으면 49일차와 동일하게 empty 폴백만 적용하고 별도 UI를 만들지 않는다.
4. **I-248·I-249(3팀 카탈로그)는 함께 판정**하는 것이 효율적이다. I-248이 채워지면 5팀이 E1에 국적·주발을 추가 배선하면 된다.
5. **I-245 조치** — 일차 배정 전 "직전 일차 소급분이 team-schedule에 반영됐는지" 확인 단계를 마감 절차에 넣을지 판정. **I-240과 함께 별도 동기화 패스로 처리**한다.
6. **51일차 6팀** — `database.types.ts` 재생성 + `mapper.ts` 로컬 캐스트 3곳 제거 + `SupabaseDataSource` 스텁 3종 정리 + **I-243 판정**을 함께(48일차 인계 유지). **I-246 재시도 정책 확인도 이때 묶는 것을 권장.**
7. **5팀 018 후속은 53일차 유지가 팀장 승인 확정**이며, **51일차 종료 시점에 클럽 상세 1/2(F3-o 구단주 카드 포함)이 미완이면 즉시 보고** → 팀장이 완화 3종 중 택일. 5팀 자체 판단으로 일정·스코프를 바꾸지 않는다.
8. **I-229 잔여**(3곳 `selectLineup()` 연결 + I-34 LIVE 컷오프 배선)는 여전히 **Task·일차 미배정**. 3팀이 판정만 남긴 상태 그대로 이월.
9. **Task 030(3팀, 56일차~) 9단계 스폰서 협상은 구단주 경유**(D-35). 48일차 `sponsor.ts` 구단주 계수는 중립값 처리라 **031b 실값 정렬 대상**이며, **I-247(avgRating 분포)도 같은 패스에서 함께 본다.**
10. **4팀은 49일차도 미참여** — `/sponsors` 구단주 열 소급은 60일차 배정. **I-250(`PositionMap` 확장 여부)이 4팀 다음 등판의 판정 대상**으로 추가됐다.

---

## 5. 미해결·판정 대기

- **I-244** `timeline.test.ts` 신설 — 5팀 다음 등판(Task 017 판정 잔여, 비차단)
- **I-254 · I-253** 마감 후 후속 작업 신규 — 1-1절 참조 (I-254는 신규 라우트 추가 때마다 재발)
- **I-252 · I-251 · I-250 · I-249 · I-248 · I-247 · I-246 · I-245** 49일차 신규 — 위 표 참조
- **I-230** 넉아웃 ET 판정 오케스트레이터 부재 — 소유팀 미정, I-214 해제 조건의 일부
- **I-229 잔여**(3곳 `[]`) · **I-241**(MOCK_NOW 고정) — 3팀, Task·일차 배정 필요
- **I-243** 마이그레이션 채번 불일치 — 6팀, 51일차 판정
- **I-240** ROADMAP 일정 드리프트 — 별도 동기화 패스(**I-245와 함께**)
- **I-226** 중계 문구 배선(5팀 다음 등판) / **I-237** D1 스코어보드 실시간성(5팀)
- **I-235** 공유 트리 git 조작 — 지침 명문화 여부 판정 필요(**48·49일차 연속 사고 0건**)
- **I-236** `homeModifier` 공식 미확정(2팀) / **I-234(c)** 1팀 확인 대기
- **I-192** 규약 판정 / **I-228·I-227** I-50 종속 / **I-225** 담당·일차 미배정 / **I-223** 잔여 3종 미배정(2회 연속 사용자 질의로 열림)
- **I-224 · I-215 · I-217** 1팀 소관 / **I-214** 크론 점등 금지(차단성) / **I-218 · I-208** 배정 대기
- **I-232 · I-216 · I-220** 표기·구조 정합 / **I-233** 중립지 구장 / **I-212 · I-209 · I-211 · I-204 · I-205** 비차단
- **I-200~I-203** 프로덕션 빌드 병목 / **I-189~I-191 · I-194** 값·규칙 정합 / **I-193 · I-197** 031b
- **I-195 · I-196**(재실측 10일 연속 미이행) · **I-198 · I-199** 변동 없음
